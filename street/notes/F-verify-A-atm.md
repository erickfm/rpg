# F verifying A's ATM row — reached at last, and it holds

I handed this back twice tonight as unverifiable: *"no spot registered under
atm/cash/withdraw/bank"*, and later as one of five rows where **nothing
publishes where to stand to SEE a thing.** Both times I was wrong in the same
way — I searched for a *label* and gave up when the label did not exist, when
the object was **tagged** all along.

## How it was actually found

    16 objects carrying userData.atmPart  ->  centroid (-7.1, 1.3, 7.8)
    viewOf(centroid, { minR: 2, maxR: 14, maxPitch: 0.5 })
      ->  x -5.11  z 7.76  dist 2  pitch -0.16

Tag to find it, `viewof` to look at it. Neither step needed a station and
neither needed anyone to tell me anything.

## Verdict: holds

`shots/f-verify-atm.png`. **Two ATMs recessed into the FIRST FEDERAL wall** —
green CRT screens with a cursor line, keypads, card slots, cash trays, a stone
surround, and `[E] FIRST FEDERAL — use the machine` up when you stand there.
They read as period cash machines immediately.

The user's words on this row were *"i like the atm, maybe add an..."* — the
truncation means I cannot see what they wanted added, so **I am verifying that
the ATM is good, not that the addition is done.** Whoever holds the full text
should check that half.

## What this retracts

**Two of my five "unverifiable for want of a station" reports were wrong.** The
ATM was tagged and reachable the whole time. That leaves three genuinely
unindexed: B's alley, B's phone box, and C's lot office — and I should re-check
those against the tag census too before anyone acts on my complaint, because
`payphone: 8` and `alley: 4` and `alley2: 79` are all in it.

I made a general argument — *"anything you look up at or across a street at has
no coordinate"* — from cases that mostly did have one. The argument was tidy and
the evidence for it was thin, which is the same fault as a 46-character
CONFIRMED.
