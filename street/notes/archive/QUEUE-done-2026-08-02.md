| 105 | DONE w56 — DESK PREMISE FALSE: doormatch12 did not compare position — it compared NOTHING (no assertion, no process.exit; 'MEASURED 12 OF 12' was a count of rows printed). Rewritten to ask the user's actual question and exit 1/0/2, both branches proven; RED on 5 of 12 and must stay RED. JAIL FIXED: jail.ts hoists jailLeafTex() (memoised, so zero new Math.random draws and both faces are the SAME texture), int-jail.ts hides the kit leaf and hangs leafPair's two + handles. CLOCK: was not misparented — it sat at lx 0 = door.at, spanning y 2.31-2.79 inside a 0-3.06 opening; moved 2.2 m along the wall (cannot go above a 3.06 head in a 3.3 m room). BANK verified by eye: MATCHES. CHURCH verified by eye: BROKEN, and NOT FIXED — ct/int-church.ts is not named by this item (item 145 names it). Its room passes no 'building' to buildRoom so it never reaches doorLeafFor at all; fix is 3 steps in notes/w56-door-faces.md §5a. Also flagged: burger/diner/tax/thrift on the same generic leaf; interiors-walk.mjs cannot run on a built bundle; which-world.mjs prints WRONG WORLD and exits 0. Verified on the BUILT bundle, port 4184: bugsweep 0 STATION MISS, w15-jail-walk all legs passed, tsc clean. fp deliberately not used (adds geometry, GOTCHAS 75). | scripts/doormatch12.mjs FIRST, then ct/int-jail.ts + ct/jail.ts | **⚡ DECISIVE NEW EVIDENCE, 2026-08-02 — the user, on the CHURCH: *"inside door of the church is still mismatched from the doors outside."* SECOND report on that door, THIRD on this class.** **THE DESK FOUND WHY THE EARLIER FIX DID NOT TAKE, and it names the bug in the check:** commit `db8322269` *"Church door: one CHURCH_FACE constant instead of five agreeing literals"* hoisted a shared constant so five literals agreed about **WHERE THE DOOR IS**. It never touched **what the leaf LOOKS LIKE**. And `scripts/doormatch12.mjs` references the church **five times**, so the check covers this door and passes it. **THEREFORE: the check compares POSITION and reports a match, while the user is judging APPEARANCE.** That is the entire class, and it explains all three of his reports at once. **Start there and prove it before touching any door.** **Original report: *"jail interior front door also looks bad and doesnt match outside."* THIS IS THE THIRD BUILDING HE HAS REPORTED FOR THIS.** Before it: *"door of the bank doesnt match the inner door of the bank"* and *"inner door of the church does not match outer doors"*. **START WITH THE CHECK, NOT THE DOOR.** There is a `scripts/doormatch12.mjs` and a CONFIRMED ledger row asserting exteriors match interiors **12 of 12** — and the user has now found three mismatches by eye. **Either the check compares the wrong thing, or it compares nothing.** Find out which and say so; that is worth more than this one door, because it is why the class keeps reaching him. Six checks were found last night reporting failure as success, and one compared the world against its own retyped copy of itself. **Then the door itself:** in the frame the interior leaf is a **flat slab** — no panelling, no reveal, a single tiny handle — against an exterior door that has real panel structure. A builder previously fixed the kits default leaf to read `LEAF.frame.colour`/`glazing` and noted **the jail specifically was wrong before that**, and the leaf COUNT (1 vs 2) was **reverted** in the same work after it doubled the bank/casino/library doors — so this pair may be a leftover of that revert. **Also in the frame: a clock appears to hang ON the door.** Check whether it is wall-mounted and merely reads that way from this angle, or is genuinely parented wrong. DONE WHEN: the check is fixed or retired with the reason stated, the jails two faces match, the bank and church pairs are re-verified by eye rather than by the check, and the clock is where it belongs. |
| 123 | DONE w57 — Item 123 DONE. The real 12-key pad on the ATM is now the PIN pad and the drawn-on one is retired. ROOT CAUSE: the pad's grid was twelve literals inside a closure in ct/bank.ts, so ct/atm.ts could not hit-test the keys the player can see. NEW ct/atm-face.ts is the third module the item asked for (imports neither, so no cycle, so GOTCHAS 28 cannot bite): it owns the fascia's eight metrics HOISTED out of bank.ts, the key grid as panel fractions, and the raycast delegation. bank.ts paints from padCells() and now prints DIGITS on the keys at 640 px/m on that panel alone (at the fascia's 160 a key is 12x4 texels and a number will not fit). The pad is live on EVERY screen: 1-8 pick the soft-key rows the way a real numeric pad does. ALSO FIXED, nothing was watching it: the typed pitch asked for 0.152 m of shelf on a 0.1442 m panel, so the CLR/0/ENT row ran off the bottom edge and has been CLIPPED in every frame of this machine; GAP_Y is derived now. PROOF, all on the BUILT bundle: scripts/probes/w57-pad-walk.mjs drives a REAL mouse at points projected from each key's own place on the mesh, 23/23 green, exits non-zero, and goes 12-red under a mutation. It caught a bug in my own change (raising the pickable flag in openAtm leaked it whenever hud.ts DECLINED the open; moved to onOpen). fp is VALID here (no geometry added or removed): textures 2 of 1458 differ and they are the two keypad panels, structure 2 of 8415 and same geometry, tints IDENTICAL, places 4 pigeons at 1 cm. bugsweep 0 STATION MISS. health OK. check-seethrough clean. K-no-panel-traps all good. Ports 4185 dev + 4191 preview, both were 000 first, both shut down. OUT OF BOUNDS I DID NOT TAKE: (1) this belongs in ct/hud.ts as a second pickable in ScreenSurface plus one line in crosstown.ts pick - w58 holds hud.ts on item 143, so the machine extends its own reach instead and the hoist is queued; it would give item 100's slots and the library PC the same reach for free. (2) THE CAPTION SITS ON THE BOTTOM KEY ROW - hud.ts puts a diegetic caption at bottom:7%, right across CLR/0/ENT; the keys are still clickable through it (proven) but not readable. w41 reservation 4, now load-bearing. FOUND RED ON MAINLINE, NOT MINE: scripts/K-atm-walk.mjs (registered, checks.mjs:838) asserts screen==='thanks' after TAKE CARD, which commit 1ab300666 changed AT THE USER'S REQUEST and never updated - identical single failure on the parent build, proven by rebuilding HEAD~1. And spots-walk fails 35 including two ATM lines ('NOT ON ITS DOOR'), byte-identical on the parent - its premise does not fit a machine in a wall. ownership.sh still calls bank.ts 'A's' - CLAUDE.md demotes OWNERSHIP.md and the item names the file. Note: notes/w57-atm-keypad.md | ct/bank.ts (the 12-key pad layout) + ct/atm.ts | **THE USER NOTICED, 2026-08-02: *"for the atm why do we not use the number button at the bottom?"* — promoted to the top on his eye.** **The real 12-key pad on the ATM is not clickable** — its layout lives as literals inside a `ct/bank.ts` closure, so the diegetic work could not hit-test it and drew a PIN pad on the CRT instead. That is a workaround and the builder said so. **The honest fix is to publish the pads layout** so the panel can hit-test the keys the player can actually see. **THE OBVIOUS ROUTE IS A TRAP AND IT IS DOCUMENTED:** `ct/bank.ts:8` imports `openAtm` from `ct/atm.ts`, so having `atm.ts` import from `bank.ts` closes a **cycle** — and GOTCHAS §28 records that this silently drops the module **from the built bundle only**. Dev would look perfect and the ATM would not exist in the artifact. **A third module is the way.** DONE WHEN: clicking the machines real keys works, the drawn-on PIN pad is retired, and the ATM still exists in a BUILT bundle (check it, do not assume). |
| 141 | DONE w53 — Region cull: street geometry (x<100) is not drawn while the player is east of it. 301 facing the window 4,012 -> 182 GPU draw calls/frame, the landing 3,987 -> 108, the street unchanged. THE ITEM'S RISK IS FALSE — 301's window does not show the street, it shows the closed brick light well the user asked for (ct/apartment.ts:1751), so all 2,691 street meshes it was drawing were behind a wall 1.2m from the glass; no portal test was needed. Proof: fp textures+structure byte-identical to parent; w53-ab.mjs 224 samples over all 14 rooms pass (worst 11px vs 40px floor) with its --mutate run red; boundary WALKED - furthest east on foot is x 23, so x100 has no seam; bugsweep 0 STATION MISS; check-seethrough pass; verified on the built bundle. Ports 4183/4184, both shut down. Note: notes/w53-region-cull.md | crosstown.ts / the room registry — the exterior draws through interior windows | **The user, and this detail identifies it: *"facing the window in my room makes the game feel slow. like my mouse moving across the screen feels like it drags."*** **DESK MEASUREMENT: there is NO interior/exterior culling anywhere in this project.** Nothing is ever hidden for being indoors; the only culling is three.jss default per-object frustum test. **So facing the window puts the entire exterior inside the frustum and it draws through the aperture** — every building, every citizen, the rain, the lot, the whole street. Face away and the exterior falls outside the frustum and costs nothing. **This explains every version of his report**, including *"looking into my room from the hall"* (the window is in view from there) and why the street itself is fine (nothing else is behind you). **⚠ ITEM 128 WAS REAL BUT WAS NOT THIS.** It removed 7,832 raycast tests per frame in 301 and that was worth doing, but he reported the drag AFTER it landed. Do not assume this is the same bug and do not re-do that work. **THE FIX IS A CULLING STRATEGY, AND THE RISK IS THAT IT IS VISIBLE.** The player can see the street through his window — that view is part of the world and **must not simply vanish**; a room whose window goes black is worse than a slow one. Options, yours to choose and defend: hide only what the windows aperture cannot possibly show (a portal test against the window rect); keep near exterior and drop distant blocks; or drop the expensive populations (citizens, rain, traffic) while keeping the static skyline. **Measure first with a COUNT, not a timing** — headless frame times do not transfer to his machine, but a draw-call or visible-mesh count does, and that is how item 128 was proved. **Report the count facing the window, facing away, and on the street.** DONE WHEN: the count facing the window is a fraction of what it is now, **the view through the window is still recognisably the street** (frames, from his own standing position), nothing pops as you turn, and `bugsweep` is clean. |
| 140 | DONE w54 — HANDED BACK, not landed. ROOT CAUSE: item 85 split the near tier and inserted bestLooked BETWEEN the halves, so a spot you are STANDING AT loses the prompt to anything within reach (6 m) as your crosshair crosses it — END TWO only ever needed bestNearLooked above bestNearOnly. Measured by count (changes to #ct-prompt per standing 360 turn, flat 301): mainline 3.26 mean / worst 6 / 14 of 19 cells at 3+; candidate 1.42 / 3 / 2 of 19, and both survivors touch nothing so aim SHOULD decide. DESK'S PRESCRIPTION IS WRONG: not hysteresis — bands are 30 deg wide with ZERO A->B->A reversals; a dead-band would add lag to a wrong answer. Also tried and DISCARDED a 'behind you' tier: measured WORSE (2.74) because it adds a +-90 boundary crossed twice per turn. PROVED no static key over distance-and-angle can satisfy both ends (the two poses are 0.50/2.13 vs 0.59/1.80 at offAxis ~0). NOT LANDED because w40-bed-vs-door goes red on ONE assertion — not the band walk carrying the user's END TWO quote (green at every stride) but the later firing step, whose station is 0.50 m PAST the bed on the far side, 1.80 m from the door, though it logs itself 'the middle of the band' (w54-firing-station.mjs). Fixing that check's navigation would make it test what it claims AND make the patch green, but it is a file this item does not name (BRIEF §9) and it is the check judging my own work. Patch: notes/w54-item140-candidate.patch (git apply). Blast radius 3.28% of 53952 poses change prompt text, 0 lost an offer. Port 4185. bugsweep 0 STATION MISS. Note: notes/w54-doorway-turn.md | fp.ts pickSpot tiers at the 301 doorway — REPRODUCE BY TURNING, not walking | **The user, third report on this and each one sharpened it: *"still feels weird to look around at the door frame to my room. idk what changed but it seems like a recent change. not on click behavior or anything. its just from looking around."*** **LEADING SUSPECT — item 85, which landed today.** Before it, the nearest spot won **outright**, so the prompt was **stable while you turned**. It now has three tiers and **turning changes which tier wins** — that is the fix working, but **301s doorway is the worst place in the world for it**: the bed seat, the door spot and the sleep spot all overlap there. w40 measured them 1.27 m apart with aim-free touch circles of 0.85 m and 1.10 m, and found a third spot (`sleep until morning`) winning at **174 degrees off-axis** before the fix. **RULED OUT, do not re-investigate:** the `canSee` memoisation (item 128) is **yaw-independent and deliberately not keyed on yaw**, so turning cannot change its answers. The desk checked. **REPRODUCE BY TURNING ON THE SPOT — not by walking, not by warping.** Stand at his doorway, sweep the yaw slowly through 360 degrees, and record the prompt string every few degrees. **The instrument for this exists: `scripts/approach-band.mjs` reads `#ct-prompt`s literal text every rendered frame** — adapt it to sweep yaw at a fixed position rather than walk a line. **What to look for:** the prompt flickering between two spots within a few degrees, or a band where it changes and changes back. **A prompt that swaps three times while you turn once is the complaint**, even if every individual answer is defensible. **⚠ DO NOT re-open item 85 by restoring near-over-looked** — that returns his *"i dont want sit on bed and watch tv to be the main option if im facing the door"*. The fix is hysteresis or a dead-band on tier changes, not a different winner. **`scripts/w40-bed-vs-door.mjs` must stay green.** DONE WHEN: the prompt is stable through a slow 360-degree sweep at his doorway, `w40-bed-vs-door.mjs` is still green, and you have the yaw-vs-prompt table before and after. |
| 132 | DONE w51 — Blade REMOVED — from his station it only ever showed its 0.34 m edge-on cabinet down the left third, halving the parapet run and hiding the west chevron; answered with a runtime-hidden frame before deleting. The item's stated cost is FALSE: the blade topped out at 21.4 m and the rooftop board at 26.0 m, so the skyline mark never was the blade's and did not move. Its ground spill and the east riser inset (which existed only to dodge it) went with it; item 121 on ORPHEUS is unaffected. Bulbs: the chase was one behaviour forever (every 3rd socket, 6 Hz) and is now a 13.2 s PROGRAM on the same clock and materials — comet/alt/flash/reverse/hold, PHASES 3->6 because alt needs an even count, COMET 2of6 to keep the same duty. The dark sky shape is the ROOFTOP BOARD's cabinet (0.5x6.6x7.2 at y19.4-26), edge-on with near-black supports set 2.4-4.8 m behind its leading edge — identified, not fixed, queued in the note. Proof: w51-chase-program.mjs PASS exit 0, mutation-tested to exit 1; G-vice-walk 18/18 WALKED; bugsweep 0 STATION MISS; health OK. | ct/vice.ts — the SEVENS vertical blade, and the bulb chase | **The user, on the now-legible facade: *"casino sign still a lil janky. maybe we get rid of the one on the side here? add more flair to the bulbs themselves instead?"*** He means the **vertical blade** — SEVENS set vertically down a tall thin cabinet. **Two parts, and the second is the real ask.** (1) **Remove the blade** — he phrased it as a question (*"maybe we get rid of"*), so **look at the frontage without it before committing**, and say what it costs: the blade is the tallest thing on the building and reads from down the street, so removing it may leave the frontage flat from a distance even while improving it up close. **If removing it hurts the long view, say so and propose what replaces that height** rather than silently keeping it. (2) **More flair in the bulbs** — this is the part he actually wants. Currently the chase is even dots at even spacing. Vegas bulbs **do things**: they chase in sequence, they alternate, they run around a border in one direction, they blink in pairs. **`ct/vice.ts` already animates** (the marquee copy changes), so there is a clock to hang this on — find it rather than adding a second timing mechanism. **The world is unlit 8 px/m: a bulb is a texel or two, so the flair lives in TIMING and PATTERN, not in detail per bulb.** **RELEVANT HISTORY — read `notes/w46-sevens-facade.md` first.** Item 97 gave that blade a lit leading edge, because its art was on the ±x faces and from the road you saw an unlit slab — that was the *"black bar"* of his previous complaint, and TWO earlier fixes had moved the blade when the problem was which face you see. **Also: item 121 is queued to give HOTEL ORPHEUS the same leading-edge fix. If the SEVENS blade is removed, 121 needs re-deciding — do not cancel it, ORPHEUS is a different building and he has not commented on it.** **One more thing in the frame:** a dark angular shape sits above the facade against the sky, apparently unattached. Identify it and say whether it is the blade top, the rooftop board, or something else. DONE WHEN: the blade question is answered with a frame either way, the bulbs do something a 1997 casinos bulbs would do, the dark shape is identified, and you have night frames from his own viewing position. |
| 128 | DONE w52 (claim reaped mid-flight; desk marked) — ROOT CAUSE: `canSee` ended in `seeRay.intersectObject(scene, true)` — a raycast against the ENTIRE WORLD, once per candidate spot, PER FRAME. Flat 301 is the densest room for [E] spots so it paid the whole bill and the street paid none. Counted, not timed: 301 7,832 tests/frame -> 3,970; the landing 15,664 -> 5,221; street 0 -> 0. Memoised against position (re-taken on 0.15 m, a storey change, or 0.10 s), so cost is bounded PER SECOND not per frame. Leak set byte-identical to mainline, seats-walk 103/219 matching w40 exactly, bugsweep 0 STATION MISS. |
| 119 | DONE w48 — 119: the rails NEVER overlapped the glass (all four abut it exactly) — the top was lost to PARALLAX, since ads only play to a seated eye fixed 0.538m ABOVE screen centre, so a 0.06m-proud surround ate 2.5 of 48 canvas rows off the top. Both of the item's hypotheses are false. Fixed BOTH halves: RAIL_D 0.06->0.04 (the authorised 'little bit'; band 2.5->1.6 rows) AND a declared title-safe area (TV_SAFE_T=3) enforced in tvFit/tvAt, so backgrounds still bleed but no spot can paint ink into the band. Shrinking alone could never work — 0.03 still eats 1.19 rows. All 27 spots checked and photographed; before/after shows MEGA HITS 97 going from half-letters to whole and the slate border from three-sided to four. New slow check w48-tv-title-safe (registered in checks.mjs) measures the band from the real seated eye AND requires minRow>=safe.t for all 27; --selftest turns both halves red. fpdiff: textures IDENTICAL, exactly 4 structural diffs (the rails). bugsweep 0 STATION MISS. Used port 4192 — checks.mjs REFUSES 4190 as a Chrome blocked port though playwright opens it fine. Edited scripts/checks.mjs (one row, to register) beyond the named file. NOT fixed: TV_FRONT:2515 is dead AND now stale by 8mm — one-line delete, worth queueing. | ct/apartment.ts (the TV bezel, ~2493-2540, and the ad canvas) | **The user: *"make sure the top of the ad isnt getting cut off by the tv. we can reduce the bezel a little bit."*** **THIS IS THE THIRD PASS ON THIS BEZEL AND THE FILE ALREADY STATES THE RULE IT IS BREAKING.** `:2494` records the original request — *"give the tv a bezel"* — and `:2502` says in as many words: **"the bezel must frame the glow, not swallow it."** `:2507` is a further round on its colour (*"black plastic is not black"*), and `:2535` describes the surround as **four rails around an open aperture**. So the intent is documented and the execution has drifted past it. **Establish WHICH it is before changing anything** — they need different fixes and only one is the bezel: (a) the bezel rails genuinely overlap the screen aperture, or (b) the ad canvas is drawing content above the safe area and the bezel is innocent. **Broadcast has a name for this — title-safe area — and it is the right frame for the fix.** If the ads are painting to the very top edge, shrinking the bezel only buys margin until someone writes a taller headline. Consider giving the ad painter a stated safe inset that all 20 spots respect, so a new spot cannot reintroduce this. **He has authorised reducing the bezel** — *"we can reduce the bezel a little bit"* — so that is permitted, but it is a ceiling on how much, not the whole answer. DONE WHEN: no spot is clipped at the top (check ALL of them, not one — they have different formats and heights), the bezel still frames the glow rather than swallowing it, and a deliberately over-tall headline is contained rather than clipped. |
| 114 | DONE w49 — PREMISE WRONG: dimWorld has NOT skipped transparent:true for some time - props.ts:414 isGlass is now blending===AdditiveBlending only, and my sweep reports the offending plane as 'graded', so the dimmer WAS touching it. The lead came from stale comments (lot.ts:159 and :322-333, street.ts:371, vice.ts:27, weeds.ts:22) that still assert the old rule; one of them aimed this item at the wrong mechanism. props.ts needed NO change. REAL CAUSE, and it is much plainer: the user's 'shadow fence' is the jail yard fence, ct/jail.ts:841 - a 14x2.4m PlaneGeometry, color 0x2a2c2e, transparent, opacity 0.75, and NO MAP. Its own posts are commented 'a touch taller than the mesh they carry' and the mesh was never drawn. Standing in the yard it is the ONLY untextured surface in frame among grained brick, banded stone and jointed paving, so the eye reads it as a shadow, not a fence. FIX: the cure lot.ts:311-333 already proved - alphaTest WITHOUT transparent:true, so the cut-out discards rather than blends. That also removes it from the translucent population entirely (624->623 transparent meshes, 312->311 planes); there is now NO standing translucent plane on the jail site. Added the top/bottom rails lot.ts:606-610 argues for (a chain-link fence is read from its framework, not its mesh). Repeat DERIVED from the panel's own W and FENCE_H at 0.3m pitch (BRIEF 7b); art COPIED from lot.ts with in-code line citations because linkPanel is a local, not an export. FENCE_X, FENCE_H and the collider unchanged. CENSUS (the item's real ask): 624 transparent meshes, 312 planes, 133 standing - only ONE was a defect. The rest are lit-window overlays, additive lamp glow/pool (correctly excluded), and window glass, ALL of which is at |x|>100 i.e. indoors, from interior.ts:924 + int-bank.ts:983 + apartment.ts:1710, each built with sill/mullions/transom and reading as glazing (checked bug-burger-wide.png). Ungraded because interiors keep their own light (props.ts:937), not because transparent - consistent. The flat op=0.26 strips are lot.ts:581 painted bay lines, PlaneGeometry(0.09,5.0), the same false '12 flat slabs' BRIEF 7 warns about. VERIFIED: WALKED it on the BUILT bundle - stops 0.47m short, reverses 5.01m (not wedged), yard still crosses; the check exits non-zero and fails in BOTH directions so it cannot pass vacuously. bugsweep 0 STATION MISS, 0 COVERAGE, 96 shots, no new console errors. tsc + build clean. fp deliberately NOT used - this adds geometry and GOTCHAS 75 says it would report a catastrophe that is not there. Ports 4193/4194, both proved free first and both shut down. OUT OF BOUNDS - PLEASE CHECK: the entire fix is in ct/jail.ts, which item 114 does NOT name (it named props.ts, from the false premise). No builder holds jail.ts; w48 holds apartment.ts via item 119 so I did not touch its pane. QUEUE THESE: (1) fix the 5 stale dimWorld comments - they will misroute another item; (2) hoist one shared linkPanel to paint.ts, there are now two chain-link implementations; (3) 76 undeclared texture densities world-wide (BRIEF 7b), which my sweep does not cover. Note at notes/w49-shadow-fence.md | a world-wide translucent-plane sweep — props.ts dimWorld + the 68 transparent materials | **The user, FIFTH report of this class: *"shadow fence still here. shadow geometry in general needs to be removed."*** **WHY IT IS STILL THERE, and this is the point of the item:** the previous fix (`2d3eba3f7`) was scoped to *"4 real **ground-quad** defects"* and sized the rest correctly. **The plane in this frame is VERTICAL** — a translucent sheet standing across the pavement — so that audit never looked at it. His complaint has never been about ground quads specifically; it has been about **pale translucent planes anywhere**, and each pass has fixed one subset. **Stop fixing subsets.** **STRONG LEAD — verify before building on it:** `ct/lot.ts:159` records that `props.ts`s `dimWorld` **skips any material with `transparent: true`**. So a translucent plane does **not** darken with the rest of the world: at dusk everything else drops and it stays bright, which is exactly how a faint sheet becomes a glowing ghost fence. There are **68** `transparent: true` materials under `ct/`. That may be the whole explanation, or only part — measure it. **SWEEP EVERY TRANSLUCENT PLANE IN THE WORLD, not the ground ones.** For each, report: what it is, whether anything still needs it, and whether it is skipped by the dimmer. **Expect most to be vestigial** — this world is unlit `MeshBasicMaterial`, so a "shadow" is a painted decal, and a translucent sheet standing in the air is almost certainly left over from an approach that was abandoned. **Delete what nothing needs**; the user has asked five times and *"shadow geometry in general needs to be removed"* is about as clear an instruction as he has given. **Related: item 95 is the lighting refactor and it will touch the dimmer — read that row and coordinate rather than collide.** DONE WHEN: no translucent plane stands in the world where the user can see it, every one that remains is justified in one line, the dimmer treats it consistently with everything else, and you have walked his exact viewpoint plus four others at dusk. |
| 112 | DONE w50 — fp.ts: stepping off a surface is now a FALL, not the floor moving. ROOT CAUSE: airY is height ABOVE THE GROUND and world Y is groundY+airY, so clearing a car's edge took groundY 1.415->0.000 in ONE frame and carried the camera along - the player was never falling. Measured before: one frame swallowed 0.514m of a 0.590m descent (87%) against a 0.035m gravity budget. FIX: add the lost floor to airY, so world Y is unchanged that frame and the existing integrator brings it down at the same 14 m/s^2. The mid-air second jump (camera rose 0.310m) fell out of the SAME line with no extra change - the gate is airY===0&&vy===0, which a step-off used to leave true. Desk's diagnosis VERIFIED CORRECT before building on it. Deliberately restricted: drops only (a rise is a landing, which already works), off collider TOPS only (so the kerb/stairs/storeys are untouched bit-for-bit - heldByTop is false for terrain), and only if you WALKED there (bound derived from this.run*dt, not typed, so a warp off a roof gets no phantom fall). PROOF: npm run stepoff - 6 cases, all WALKED not warped, green on the BUILT bundle; PROVED IT CAN FAIL by reverting fp.ts (exit 1, 5 of 6 fail, worst 1.273m in a 22ms frame vs 0.163m bound); the kerb case passes in BOTH worlds on purpose, as a change detector. Regressions green: w21-roof-climb, w29-sedan-climb, jump-walk (stairs+storeys), 17/17 vitest, bugsweep 0 STATION MISS. fp valid here (zero geometry added): textures/structure/tints IDENTICAL, places differs by 2 pigeons <5cm which fpdiff itself calls DRIFT. FOUND 2 INSTRUMENT FAULTS, not world bugs: my kerb case first measured HEAD BOB (bob=0.035, 0.07 peak-to-peak) and falsely reported the kerb changed; and the strafe onto the bed rail ignored facing (w21-roof-climb:159 has the right formula). NOT FIXED, please queue: (1) LANDING pops the camera up before settling - the mirror of this bug, pre-existing, one line next to mine but it re-times every climb so it wants its own item; (2) terrain drops are still instant at ANY height - groundAt finds 0.237m and 0.99m steps that still warp you down, but the item required the kerb unchanged so I did not touch terrain - user call; (3) scripts/interiors-walk.mjs imports src .ts so it CANNOT run against a built bundle (dev-only, undocumented). Ports 4187 dev + 4188 preview, both proved free first (4186 was taken); both shut down. Handoff: notes/w50-stepoff-fall.md | fp.ts — airY / groundY, the step-off fall | **The user: *"when i jump off of stuff i teleport straight down. please fix this."* A regression from the collider-height work — before it nothing could be stood on, so nothing could be stepped off.** **CAUSE LOCATED, VERIFY IT BEFORE BUILDING ON IT:** `fp.ts:553` is `this.airY = Math.max(0, this.airY + this.vy * dt)`, and `airY` is **height above the ground**, with world Y computed as `groundY(x, z) + airY`. Standing on the pickups cab roof, `groundY` returns **1.415** (via `standTop`) and `airY` is **0**. Walk off the edge and `groundY` returns the street — **0.000** — in that same frame, so world Y goes 1.415 to 0.000 instantly. **There is no fall to have: the player is not falling, the floor moved and took him with it.** The fix is that leaving a surface must convert the height you had into a real fall — keep world Y and let gravity bring it down — rather than re-basing to the new ground. **A SECOND EFFECT, IN THE SAME LINES, WORTH CHECKING WHILE YOU ARE THERE:** `:549` gates jumping on `airY === 0 && vy === 0`, which is **true the instant you step off** a raised surface, so stepping off a car hands you a fresh jump in mid-air. He has not reported it, but it is the same root. **`fp.ts` IS THE HIGHEST-RISK FILE IN THE PROJECT AND THE 2 m SIDEWALK LANE IS SACRED.** Stage it, commit each stage, and prove the ordinary world is unchanged with `fp before`/`after`/`fpdiff`. **Check what else reads `airY` or assumes `ground + airY`** before changing its meaning — the kerb, the stairs and the storey picker all deal in ground heights. **Other builders may hold `fp.ts`** (items 85 and 98 are both against `pickSpot`) — run `./scripts/claim.sh --stale` and coordinate or wait. DONE WHEN: stepping off the pickup bed, its cab roof and the sedans boot lid all produce a real fall at the same gravity as a jump; you cannot jump again in mid-air; walking off a kerb still feels as it does now; and you have WALKED all of it rather than warped. |
| 97 | DONE w46 — SEVENS read EVEN because the cap height came from the panel HEIGHT (112 texels of ink into a 92-texel canvas); fitTube now derives size from the width available and throws below a legibility floor. DEFECT 2 AS FILED IS WRONG: the marquee line is NOT clipped (72.2 texels in 96, 12 clear each end) — it was under-resolved, so the fascia doubled to 192x52 (exactly 2x, still hardLayer, not soft). The black bar IS the blade cabinet: its art is on the +/-x faces so from the road you only see 0.34x15.8m of unlit boardM — both earlier fixes MOVED the blade, which could never help; leading edge now carries the chase + a tube, and its rooftop twin had the same dead edge plus a dead 0.5x7.2m soffit. Vegas: CASINO over a framed socket-edged name board over a 4x bigger 777 with the blade's own chevron in the wings; the three risers that ran through the middle of the name now pair at the party edges and break at the board. Port 4180 dev / 4181 built. 0 STATION MISS, health OK, positive-controlled clipping check passes on the BUILT bundle. NOT FIXED, please queue: npm run fpdiff over-reports catastrophically (849/1461 textures) on any change that adds a texture, because scenedump seeds ONE global Math.random for the whole world's grain — grain-pinned, the real diff is 2 replaced + 7 added, all mine; and HOTEL ORPHEUS in the same file has the identical dead blade edge, left alone as out of scope. | ct/vice.ts (the SEVENS facade, marquee, blade sign) | **The user, with a screenshot: *"sevens casino front looks so messed up. take influence from vegas thanks."*** **THIS IS HIS THIRD COMPLAINT ABOUT THIS FACADE** — `ct/vice.ts:117` records *"the blur the user reported on the marquee"* and `:141` records *"the LEFT leaf is reversed"* on the SEVENS entrance. Read both notes before touching anything; two previous fixes here did not settle it. **THREE CONCRETE, VERIFIABLE DEFECTS IN THE FRAME — fix these regardless of taste:** (1) **`SEVENS` is CLIPPED AT BOTH ENDS.** It reads `EVEN` — the word overflows its panel and is cut by the band edges. The facade is the buildings name and it currently cannot be read. (2) **The marquees second line is clipped**: `$1 BLACKJA` where it should say BLACKJACK. (3) **A black vertical bar floats over the left edge** of the facade, unattached to anything. **THEN THE DIRECTION: Vegas.** What he is reacting to, beyond the clipping, is that the composition is mostly **empty maroon field** — `777` sits small and lost in the middle of it, and the sign furniture does not fill the frontage the way a real casino front does. Vegas means **density and hierarchy**: a dominant name, chase bulbs that read as a path rather than scattered dots, stacked copy, and no dead wall. **The world is unlit 8 px/m and the whole facade is texel work** — this is a painting job in the existing signage hand (the file says there is ONE signage hand for both buildings of the pair), not a modelling job, and it must not go soft: `:132` records that fixing alpha alone took a marquee line from 262 distinct colours to 45. DONE WHEN: SEVENS reads in full, the marquee copy is not clipped, the floating bar is gone or attached, the frontage reads as a casino at night from the users own viewing position, and you have the before/after night frames and are willing to defend them. |
| 95 | DONE w45 — PREMISE WRONG: there is ONE registry (props.lit), not four - sidestreet/traffic/crowd each declare a FIELD named lit and crosstown passes props.lit into all three; vice deliberately opts out; lampHeads is in props.ts not sidestreet.ts. Real cause, measured at the (4.1,-23) lamp: 3 materials held up vs 38 at the night floor, and the 3 are 0.1m posts. Everything was REGISTERED and then REFUSED - (a) the span taper zeroes sizeW past 12 m and every surface a lamp stands on is longer than that, so no ground could ever pool, and (b) the road/sidewalk/kerb are in wetMats, which BOTH grading loops skip, so the ground was never in the registry at all. The visible road pool is a painted additive quad, not light, and there is one on the roadway and none on the pavement - which is the user's sentence exactly. FIX: moved the same falloff (same LAMP_R/CORE/smoothstep/WARM) from per-material to PER FRAGMENT via onBeforeCompile, so a 92 m ribbon holds a gradient; retired sizeW and both centroid tests; height is now a per-fragment 2.2-4.5 m fade. NOT real lights - no normals, no diffuse, no light objects, so the brown-slab failure cannot recur. POOL_GAIN 12->6.5 and road decal 0.72->0.22 because at 12 the asphalt went flat grey (judged by eye, not by number). Published scene.userData.addLit for objects built after buildProps, which were permanently unlit (a carVariant car was 0/33 patched, now 27/33). fp: textures+structure+tints IDENTICAL, 4 places differ (pigeons). bugsweep 0 STATION MISS. Verified on the BUILT bundle. Ports 4189/4193, both shut down. OUT OF BOUNDS - PLEASE CHECK: the whole fix is in ct/props.ts, which item 95 does NOT name (it named sidestreet/traffic/crowd/vice/crosstown, all from the wrong diagnosis). No queue item names props.ts so no builder conflict, and I did NOT touch crosstown.ts because item 86/w41 holds it. NOT FIXED: (1) no parked car is under a lamp - nearest is 6.97 m against a 7 m reach - so the screenshot car was dark partly for a PLACEMENT reason; a car I placed under a lamp reads 1.82x. Parking is crosstown.ts, w41's. (2) night frame time +50% (53->79 ms), cut down from +160%; hybrid CPU-for-small-objects is the lever if too dear. Note at notes/w45-lamplight.md | the lamplight architecture — ct/sidestreet.ts, ct/traffic.ts, ct/crowd.ts, ct/vice.ts, crosstown.ts | **THE USER, WITH A SCREENSHOT: *"lighting needs a full refactor. it isnt consistent anywhere. this is a prime example. the lighting only affects the street but not the sidewalk. it doesnt affect the car at all."* He is right, and the cause is architectural.** MEASURED BY THE DESK, verify it yourself before building on it: the world is **unlit** — 671 `MeshBasicMaterial` uses and no real lights — so lamplight is faked by **tinting materials that OPT IN**. And there is **no single registry**: `ct/sidestreet.ts:57` keeps `lampHeads` *private to it* by its own comment, `ct/traffic.ts:133` has another, `ct/crowd.ts:28` a third, `ct/vice.ts:45` exposes `lit()`. **Anything that never registered is never lit** — which is precisely the screenshot: the road registered, the sidewalk and the parked car did not. Only **20 `noLight` opt-outs** exist against 671 materials, so opt-out is vestigial and opt-in is the real rule. **THIS IS THE LARGEST ITEM ON THE BOARD AND THE MOST VISIBLE. Stage it and commit each stage; a half-lit world is worse than an unlit one.** Suggested shape, yours to reject with reasons: (1) **one registry**, in the world rather than per-module, that any surface can be handed to; (2) make the pools **spatial** — a surface is lit because it is under a lamp, not because a module remembered to sign up; (3) migrate the modules that already do it, and **prove nothing dims that should not**; (4) only then extend to what was never lit. **Do not convert the world to real Three.js lights without saying so first** — unlit `MeshBasicMaterial` is the projects entire look and 8 px/m pixel art does not survive being shaded. If your measurement says that is the only correct answer, **stop and report it** rather than building it. DONE WHEN: a lamp lights the road, the sidewalk and a parked car under it consistently; nothing that was correctly dark becomes lit; `fp` before/after shows textures and structure unchanged where they should be; and you have night frames from the players own standing position that you are willing to defend. |
| 90 | DONE w43 — Items 90 AND 89 together (one geometry). The corner: the four edging strips each ran the FULL length of their leg while the legs stopped CHAM short, so all four overran into the turn and crossed each other -- he photographed a grey X on the path, and no work on the corner PATCH would have touched it. Also the turn was a separate quad 0.6 m too long and PATH_W/cos45 = 2.12 m wide where it crossed 1.5 m legs. Loop is now ONE octagonal ring; surface, edging and hoop rail all derive from it, so nothing can fail to mitre (width 1.5 m +/- 2e-15 through every turn, was 2.12). Surface, his 2nd rejection: it read as poured aggregate because it was isotropic 1-texel pepper at even density, identical shoulder to shoulder and end to end -- nothing said people walk there (and slabTex grain 0.18 was over its own 0.14 pebble threshold, so it was laying 2 px stones at full contrast). Answered with a CROSS-SECTION in vertex colour, not new noise: a pale spine that WANDERS with arc length (the direction of travel, and it carries through the turns) and shoulders that go olive into the turf. Edging is flat rather than a 70 mm ROAD kerb and now goes through wet(), which is why it photographed as near-white rails beside a dark path. Proof: walked the leg and the chamfer (5.05 m, 0.000 m off centreline), 338 floor samples level, built bundle same, bugsweep 0 STATION MISS. NOT MINE, FOR w44/item 88: four benches stand 0.160 m INSIDE the park path (list in notes/w43-park-path.md) -- pre-existing, item 88 only recorded the sidewalk case. INSTRUMENT FINDING: fpdiff's 671 texture / 1741 structure diffs are meaningless here -- a control adding ONE Math.random() call and nothing else reproduces the same 671. places is the only field with signal, and it says nothing outside the park moved. Also claim.sh --help claims an item. | ct/park.ts:307-328 (the path corners) | **The user, with a screenshot: *"look at this path corner it looks so messed up."*** Third park complaint in one sitting, and this one is **not** the surface — the corners are drawn separately from the legs. `ct/park.ts:311` says each corner is drawn **0.3 m long** and `:323-328` builds it as a patch rotated by `atan2(sx * CHAM, sz * CHAM)`. In the frame it reads as a **diamond of bare earth sitting proud of the two legs it joins**, with the grey kerb rails crossing it at angles that do not meet. **Look at the screenshot yourself and say what is wrong before changing anything** — candidate reads, none of them the desks verdict: the rotated patch is mitred at the wrong angle so it does not tile with either leg; the kerb rails are drawn per-leg and simply overrun into the corner rather than mitring; the corner is a different width from the legs it joins (the file notes legs at 2.0/2.5 and corners at 1.25). **Related but separate: items 88 (bench too close) and 89 (the surface reads wrong, his SECOND rejection of it). Read those rows before starting — if one fix serves all three, say so and take them together rather than fighting the same geometry twice.** DONE WHEN: a corner reads as the same path turning, the kerbs mitre rather than overrun, and you have stood at the corner at player height and are willing to defend the frame. |
| 86 | DONE w41 — Diegetic screen framework + ATM as first tenant, staged in 4 commits. PanelSpec.surface hangs a panel's own canvas on a mesh: [E] eases you to a standing pose derived from the mesh's world normal (ct/bank.ts untouched, mesh found by its own userData tag), the UI paints onto the machine's raked screen, the mouse raycasts to UV and is hit-tested in the machine's OWN canvas pixels, and a pixel-drawn Win98 arrow/hand replaces the OS cursor. ROW'S CAUSE WRONG IN ONE PLACE: the raked screen is ct/bank.ts's, not ct/atm.ts's — atm.ts only ever drew a DOM panel. Three real bugs: cam.fov had two owners (lock vs scroll-zoom smoother, meeting at a stable 66deg); main.ts:30 re-takes pointer lock on CLICK which was not in BLOCKED, so the first click froze clientX/Y and every click after it missed; an untouched canvas renders flat black on a mesh. Escape proven from ALL TEN screens with movement restored, on the built bundle. fpdiff textures/structure/tints IDENTICAL, bugsweep 0 STATION MISS, health OK. NOT FIXED, needs ct/bank.ts (not my item): the fascia re-arranges layout on [E] (idle texture has a card slot where the live one has soft keys), and the real 12-key pad is not clickable. ALSO: bank.ts invites atm.ts to import ATM_PALETTE, but that closes an import cycle (bank.ts:8 imports openAtm) and GOTCHAS 28 would silently drop the module from the BUILT BUNDLE ONLY — needs a third module. Port 4187/4199. Note at notes/w41-diegetic-screens.md, with a seam guide for item 100 (slots). | ct/atm.ts + ct/hud.ts panel framework + crosstown.ts (camera lock) | **THE USER, WITH A SCREENSHOT: *"this doesnt look integrated. i want when i hit e here to adjust my position and perspective and lock it to be looking at the atm and for the screen on the literal atm be the overlay that i can use my mouse to click through. the mouse cursor should be like a lil hand almost like win98 cursor"*.** This is the concrete form of his standing design law: *"i never want there to be menus popping up unless they are embedded to look as if they are in the actual game."* Item 0c removed the panels chrome; **this removes the panel.** WHAT EXISTS TODAY, measured: the panel framework in `ct/hud.ts` is **DOM, screen-space** (`position:fixed`, z-index, `pointer-events:none`) painted over a dimmed world — that is what the screenshot shows. `ct/atm.ts` already draws a real raked screen on the machine in the world. There is **no** raycasting, no UV picking, and the ATM has **no seat or pose** to lock a camera to. **FOUR PARTS. STAGE THEM AND COMMIT EACH — do not attempt it in one pass.** (1) **A pose to lock to.** `[E]` walks/eases the player to a standing pose facing the machine and locks look — the seat system (`ctx.seat`, used by slots/blackjack) is the prior art for locking the camera, but this is standing, not sitting. Escape must release it, and **a view you cannot leave is the worst bug this project ships** (BUILDER-BRIEF §11). (2) **The UI paints onto the machine.** The ATMs existing screen texture becomes the surface the interface is drawn to, instead of a DOM panel. (3) **The mouse reaches it.** Raycast the pointer against the screen mesh, map the hit to texture UV, hit-test against the ATMs own button layout. (4) **A Win98-style hand cursor**, pixel-drawn, shown only while the machine is focused. **DONE WHEN:** pressing `[E]` at the ATM locks you facing it with no DOM panel visible; the interface is legibly ON the machine in the world; a mouse click on a button in the world operates it; the hand cursor appears while focused and reverts on release; Escape always releases and gives back movement, proven from every screen the ATM has. **If a stage proves the approach wrong, say so and stop — a report beats a half-converted world.** This is the TEMPLATE: slots, blackjack and the library PC follow once it works, so build it to be reused rather than special-cased to the ATM. |
| 85 | DONE w40 — pickSpot: near tier gets a facing gate (3 tiers: standing-in-it/touching-and-aimed-at, then aimed-at, then touching-but-aimed-away). ROOT CAUSE: the 301 bed seat and door spot are 1.27 m apart while their aim-free touch circles reach 0.85 and 1.10 m, so they overlap over most of the floor between them and the near tier ranked by DISTANCE ALONE - the bed just won. Gate is the existing lookTolerance cone, not a new constant; tier 1 keyed on RADIUS (the player's own capsule) is what holds w9's repro and seats-walk at the other end. Walked it: 301 cells offering the bed while facing the door 10/19 -> 3/19 (the 3 are where the bed's centre is inside the player's own body). New check scripts/w40-bed-vs-door.mjs walks the band both ways + both ends; 2 canfail cases, one per end, 2/2 CAUGHT with a green run watched first. fpdiff textures/structure/tints IDENTICAL, bugsweep 0 STATION MISS. FOUND, NOT FIXED: seats-walk 115->103 and the 12 are worth a user call (5 seat you at a booth ~2.9 m away that you were aimed at); w9's note justifies the near tier with a No. 227 competitor that measurement shows does not exist. Port 4188. | fp.ts pickSpot (lookTolerance / bestNear vs bestLooked) | **The user, 2026-08-02: *"i dont want sit on bed and watch tv to be the main option if im facing the door to leave"*.** Standing near the bed in flat 301 and facing the door, the `[E]` prompt still offers the bed seat instead of the door. **HISTORY YOU NEED, because this is a knob with a user complaint at BOTH ends and swinging it back is not a fix.** `fa5c32e01` (item 0b) split `pickSpot` into two tiers — `bestNear` and `bestLooked` — with **near winning outright**, and it did that to satisfy the users EARLIER complaint: *"i dont want to be so far from the bed and the option is still to sit on the bed and watch tv"*. Before that change the comment claimed near-priority and the code did not do it. So: **do not simply restore looked-over-near**, or the desk will get the first complaint back within a day. The shape that satisfies both is something like: near wins **unless** the player is clearly aimed at another candidate — i.e. the near tier needs a facing gate of its own, not the removal of the tier. `lookTolerance(r, d)` at `fp.ts:683` already scales the cone by radius and distance and is the natural place to look. **WALK IT in flat 301: stand where the user stands, face the door, read the prompt; then face the bed and read it again. Both must be right.** DONE WHEN: facing the door offers the door, facing the bed offers the bed, the earlier far-from-the-bed case is still fixed (prove it, do not assume), and a check goes red if either end regresses. |
| 91 | DONE w44 — Item 91: the desk's premise was wrong both ways — the bracket was NOT missing, undersized, or behind the wall plane. It was drawn (0.34x0.10x0.26), in front, with the figure resting on it to 0.000 m, and attached to NOTHING: 0.78 m clear of the west wall at x -6.5. That is the float. Rebuilt the shrine off the wall plane (WALL = -hw) with a 3-stage corbel whose underside reads, and PAINTED the figure — cream robe, blue mantle, face, joined hands, halo — because the bigger failure was that he did not recognise it as a statue at all; it was a grey cylinder and a ball against grey plaster. Walked in and up the aisle for both frames; verified on the BUILT bundle; bugsweep 0 STATION MISS. NOTE FOR THE DESK: the fp 'textures' hash CANNOT match across any change that adds or removes a mesh — three's generateUUID() draws 4 Math.random() per object and scenedump seeds Math.random globally. See notes/w44-church-statue.md. | ct/int-church.ts:651,683 (the votive statue and its bracket) | **The user, with a screenshot: *"what is this floating thing in the church?"*** It is the small painted statue that is meant to stand **on a bracket** above the votive stand in the north aisle — `int-church.ts:651` describes *"a small painted statue on a bracket"* and `:683` draws it. **The statue reads; the bracket does not.** In the frame a pale rounded form hangs against the plaster with a thin sill under it and nothing connecting the two, and the user did not recognise it as a statue at all — which is the real failure. **Two things to settle, and say which you found:** whether the bracket is drawn but too small/too flush to read at player height and distance, or whether it is being drawn behind the wall plane. The world is unlit and 8 px/m, so a bracket that is a plausible size in metres can still vanish — it may need to be read as a silhouette rather than modelled honestly. DONE WHEN: from the players own standing position it reads as a statue standing on something, and you have the before/after frames from that exact spot to show for it. |
| 149 | DONE w58 — K-atm-walk now asserts what TAKE CARD actually does (panel closes, screen back to 'idle'), not the retired 'thanks' screen. ROW HALF WRONG — and it is the headline half: it says the check 'prints 1 FAILED and exits 0', making it the EIGHTH member of that family. IT DOES NOT. :205 is already process.exit(fails.length ? 1 : 0) and a plain run MEASURED exit=1 while failing; the only exit-0 path is --selftest, where exiting 0 because the mutation was caught is correct. There is no eighth member here — the desk's count should not grow on this row. Also fixed a guard that WOULD have slept: the 'ESC closes it' test ran against a panel TAKE CARD had already closed, so it passed without exercising ESC — it now reopens, waits out the 500ms DISMISS_LOCKOUT, and asserts the machine is up first. Assertion was not weakened: one stale fact became two live ones. Proved red by BREAKING THE WORLD (reverting atm.ts's TAKE CARD to 'thanks' -> 2 FAILED, exit=1), restored after; --selftest still 'caught it'; 29 OK exit=0 on the BUILT bundle, port 4192. NOT FIXED: the 'thanks' screen is now dead code in ct/atm.ts (Screen type, onKey branch) — the row says item 144 owns that and 149 does not name atm.ts, so I left it. Note at notes/w58-katm-walk-stale.md | scripts/K-atm-walk.mjs — STALE against the desk own change, and silently failing | **THE DESK BROKE THIS AND THE CHECK HID IT.** `scripts/K-atm-walk.mjs:157` expects the ATM to show `thanks` after TAKE CARD. Commit `1ab300666` — **the desks own one-line change, on the users instruction *"take card from atm should immediately get us out of the menu"*** — made TAKE CARD close the view outright, so that screen no longer exists. **The check now prints `1 FAILED` and exits 0**, making it the **eighth** member of the family found today. So the desk shipped a behaviour change, its check noticed, and the suite reported success. **Fix both halves:** update the expectation to the new behaviour (the view closes; there is no farewell screen), AND give it a non-zero exit on failure. **Do not weaken the assertion to make it pass** — that is the documented trap this project has paid for repeatedly. **Related: item 144 removes the now-dead `thanks` screen.** DONE WHEN: the check asserts what TAKE CARD actually does, exits non-zero when that is broken (proved by breaking it), and the walk is green on the built bundle. |
| 147 | DONE w57 — Item 147 DONE. ROOT CAUSE: buildRoom reaches a DoorDecl only through a building NAME, it has two sources for one (spec.building, the frontage), and a room on a CUT FACE publishes no frontage - so a chamfer room that omits building resolves null, looks up nothing, and every reader takes its ?? branch. The desk's premise was RIGHT and w56's 3-step repair held up in every particular. (1) MECHANISM, ct/interior.ts: buildRoom now warns on the kit's OWN [interior:<id>] channel when no building name resolves - a channel already wired to two registered checks (interiors-walk.mjs:284, G-rooms-walk.mjs:210 both collect and fail on that prefix), so a room that loses its door now FAILS A CHECK rather than merely looking wrong. Not a throw: a room with genuinely no declaration is legal. (2) CHURCH, ct/int-church.ts: building: 'ST BRIGID', a real leaf on DOOR, width:1.4 REMOVED from the spec's door (spec.door.width BEATS the declaration by design, so leaving it would have made the fix look like it did nothing), and the jail's recipe - hide the kit's 32x64 leaf, hang the room's own pair via leafPair, because a DoorLeaf cannot carry a leaf COUNT. LEAF SIZE MEASURED, NOT RETYPED: I ran civic.ts's archHW by hand first and got the height 0.1 m wrong; scripts/probes/w57-church-leaf.mjs reads the leaf timber off the west front's own canvas and converts with the ppm masonry() stamped on it - 2.750 m across both leaves, 3.750 m tall. That probe was wrong twice as an INSTRUMENT before it was right (filtered canvases at >200px when the whole 13 m church front is 104px; then matched the colour on six other facades' drainpipes) and both are written into it. PROOF: doormatch12 5-of-12 red -> 4-of-12, church now 'its own door' (2x28x76), other four rows BYTE-IDENTICAL, nothing loosened. scripts/probes/w57-church-walk.mjs WALKS the widened opening (1.4x2.15 -> 2.75x3.75 with two meshes in it): 0.20 m from the wall plane in the opening, wall stops me 2.91 m further out at the jamb, all three directions move from inside the doorway, [E] in and out on foot - green on dev AND the built bundle, exits non-zero. Deleting the one building: line makes the warning fire and the walk go red. bugsweep 0 STATION MISS on the built bundle, health OK, spots-walk 35 failures = the same 35 as mainline. fp NOT used, deliberately - this adds meshes, GOTCHAS 75. Frames from the player's own position: shots/w56/church-{inside,outside}.png - two tall boarded timber leaves, same timber, straps at the same relative heights, brass rings, inside and out. OUT OF BOUNDS, NOT TAKEN, AND IT IS WHY G-rooms-walk NOW READS 113/114: the HOTEL is the same bug still open. ct/int-hotel.ts declares clearW 2.2 / h 2.6 / leaves 2 and BUILDS ITS OWN STONE CASE AT EXACTLY THOSE NUMBERS, while its spec omits building - so the kit cuts its 1.15x2.15 fallback hole behind it. shots/w57/hotel-inside.png shows 2.2 m of doorcase over a 1.15 m opening. Same one-line fix, and the lobby window at [1.5,5.1] still clears a centred 2.2 m door by 0.4 m. ct/int-hotel.ts is item 96's file so I stopped and reported - the red is this item WORKING, not a regression; before today nothing could see it. ALSO NOT FIXED: ct/civic.ts's church painter should read doorLeafFor('ST BRIGID') instead of its own literals (hoist a churchLeafTex() like ct/jail.ts did); doorWidthFor() in ct/doors.ts has NO callers in src/ while the deprecated DoorDecl.width still OUTRANKS leaf.clearW via spec.door.width - the next version of this same trap. Item 145 (ct/int-church.ts) was TODO and unclaimed on --stale, so nothing collided. Ports 4185 + 4191, both 000 first, both shut down. Note: notes/w57-church-door.md | ct/interior.ts buildRoom — a chamfer room passing no `building` silently loses its door declaration | **STRUCTURAL BUG BEHIND THE USERS MOST-REPEATED COMPLAINT — found by the builder that repaired the door-match check.** The churchs interior door **never reaches `doorLeafFor` at all**: its `buildRoom` spec passes **no `building`**, and a chamfer room publishes no frontage, so **its own `DoorDecl` is never consulted** and it falls to the `?? 0x3a2c22` default branch. Result, which the user has now reported **twice**: outside is a **5.5 m pointed arch with two timber leaves**; inside is a **1.4 m brown domestic door with a grey pane**. **THE SILENCE IS THE BUG.** A room that declares a door and is then never asked for it should not build a generic leaf and say nothing — it should fail loudly, or the kit should refuse to guess. **Fix the mechanism first, then the church.** The 3-step repair is written up in `notes/w56-door-faces.md` §5a — read it. **⚠ `ct/int-church.ts` is named by item 145 (church darkness) — check `./scripts/claim.sh --stale` and coordinate rather than collide.** **The repaired `doormatch12.mjs` is RED on 5 of 12 rooms and MUST STAY RED until they are genuinely fixed** — `burger`, `diner`, `tax` and `thrift` are on the same generic leaf as the church. DONE WHEN: a room whose door declaration is ignored cannot do so silently, the churchs two faces match from the players own position, and `doormatch12.mjs` is green for church without its other rows being loosened. |
| 104 | DONE w59 — Item 104: the jail leaves were BURIED IN THE RECESS-BACK STONE with their front face coplanar to Δ0.0000 m — z-fighting, not glazing. Desk's lead WITHDRAWN: interior.ts:924 opacity 0.55 is a room WINDOW, and flat() is MeshBasicMaterial({map}) with transparent=false opacity=[1] measured on both leaves, so transparency was never the mechanism. One fault explains all three symptoms incl. the 'diagonal hatching' (tear between dithered canvases) and the 'misaligned leaves' (identical geometry, tie breaking differently). Assembly moved forward one leaf thickness, all parts now derived from LEAF_FRONT. Probe RED before (exit 1) / green after; bugsweep 0 STATION MISS; typecheck clean; WALKED in and entered. Port 4187. NOT FIXED, not my file: ct/int-jail.ts's interior leaves stand OPEN (canted ~31deg, bbox 1.00x3.00x0.61) while the street pair is shut — desk should decide. Also could NOT frame the interior face (camera landed inside a wall), so it is unmeasured, not cleared. ct/doors.ts and ct/interior.ts were named by the item but needed no change. | ct/jail.ts door + ct/doors.ts + the shared kit leaf in ct/interior.ts | **⚡ SECOND REPORT, 2026-08-02, IN DAYLIGHT: *"jail door is still messed up."* The lit frame shows what the dark one hid — THE DOOR IS SEE-THROUGH. Masonry coursing and the stone reveal are visible THROUGH the leaves.** **⛔ THE DESKS FIRST LEAD (stretched texture density) IS WITHDRAWN — do not chase it.** **NEW LEAD, verify before building on it:** `ct/interior.ts:924` builds a glazing material at **`opacity: 0.55`**. `ct/int-jail.ts:65` declares `frame: { colour: 0x3a3c40, material: "steel" }, glazing: "none"`, and `:59` says that declaration exists precisely so *"the kit would [not] give the room a 1.10 m"* glazed leaf. Meanwhile `ct/jail.ts:510` only **comments** *"Steel, two leaves, no glazing. `ct/int-jail.ts` declares the same leaf"* — it points at the interior file for the declaration. **SO THE HYPOTHESIS IS: the EXTERIOR leaf never reads that declaration, and is glazed at 0.55 while the interior is solid steel.** That would be this bug AND the inside/outside mismatch of item 105 in one — **read item 105 before starting; if one fix serves both, take them together and say so.** **Establish which file actually builds the leaf you are looking at**, and whether a declaration in an `int-*.ts` file can reach an exterior door at all. If it cannot, that is the real defect and it is structural. **Original: *"front door of jail graphics are messed up."*** In the frame the door carries **diagonal hatching that does not match its own panelling**, and the left and right leaves **do not align** with each other. **STRONG LEAD, NOT A VERDICT — verify it first:** `d3770c506` landed last night and *"size[d] every masonry faces texture to the face it is on"*, after a builder found a texture painted for 4 m stretched across a 14 m wall at 4.57 px/m against a declared 16. **A stretched or wrongly-rotated texture is exactly what diagonal hatching over a rectilinear panel looks like.** The jail door is a **different system** from that fix — `ct/jail.ts` publishes it as a world point and outward normal for `ct/doors.ts`, and the leaf itself comes from the shared kit — so it may simply not have been reached. Two other things worth checking in the same pass: a builder fixed the kits default leaf to read `LEAF.frame.colour`/`glazing` and noted the jail specifically was wrong before that; and the **leaf COUNT (1 vs 2) was reverted** in that work after it doubled the bank/casino/library doors, so a mismatched pair here may be a leftover of that revert. **The frame is also very dark — that is item 95 (the lighting refactor), not this. Do not fix darkness here.** DONE WHEN: the door reads as a door from the street at the users own approach, both leaves match, the texture density on it is derived from the face it is on rather than typed, and you have the before/after from that spot. |
| 108 | DONE w60 — Mug fixed, but NOT for the stated cause: the handle's hole axis was PARALLEL to the direction it stuck out (measured dot = 1.0000, a real handle is 0), so it was a detached hoop with a 9 mm air gap, not a handle. The item said the rotation was right and not to chase it - the rotation IS right, the OFFSET disagreed with it; moved it to +z so the loop hangs across the sightline against the dark glass. Shared material split (own tone 0xa79f8f), ring now passes 14 mm through the cup wall with 30 mm of open hole, dark disc above the top cap because he looks DOWN at it 22 deg and a flat cap read as a peg. TWO ITEM CLAIMS ARE FALSE: it does NOT sit proud of the sill (0.0000 m on the sill top, 0.072 m inside the room edge, unchanged before and after), and it is NOT three texels wide - it is 20x25 px from the room's own SPAWN, which is why a real handle was worth building. Also found 3 instrument faults, incl. a probe that filed a fully BLACK frame from a 600 ms settle. 0 STATION MISS. Port 4184. notes/w60-mug.md | the mug on the flat 301 windowsill — ct/apartment.ts | **⚡ SECOND REPORT, 2026-08-02 (close-up): *"the mug is messed up."* Promoted, and the desk found the cause — verify then fix.** **`ct/apartment.ts:1906-1912`: the body (`CylinderGeometry(0.038, 0.034, 0.095, 8)`) and the handle (`TorusGeometry(0.026, 0.008, 4, 8)`) SHARE ONE MATERIAL — `mugM`, `0xd8d2c4`.** Identical colour, so there is nothing to separate handle from body and the pair reads as a single lump. **The handle also has FOUR radial segments, which is a square ring, not a curve.** It IS rotated correctly (`rotation.y = Math.PI / 2`), so orientation is not the fault — do not chase that. **The fix is contrast and silhouette, not detail:** at this size the handle needs its own tone (a shadow side, or a darker material) so the eye can find the hole, and enough segments that the ring reads as round. **Original: *"mug looks messed up."*** The white mug on the windowsill in flat 301. In the frame it reads as **an undifferentiated blob**: the handle does not separate from the body at the distance the player actually stands, and the whole thing sits **proud of the sill edge** rather than on it. **The world is unlit 8 px/m — at that scale a mug is about three texels wide, so a handle modelled honestly will not read.** The question to answer is whether it should be **silhouetted** (handle as a notch of background colour rather than a modelled loop) or simply be a smaller, plainer vessel that reads cleanly. **Look at it from where the player stands, not from a close orbit** — this is a small prop and the only test that matters is whether it reads at standing distance and height. Check its footing on the sill in the same pass. DONE WHEN: it reads as a mug from the players own standing position, it sits ON the sill, and you have the before/after from that spot. |
| 100 | DONE w55 — SEVENS is diegetic: PanelSpec.surface called, not rebuilt — ct/hud.ts and crosstown.ts untouched, whole diff is ct/slots.ts + 4 probes. The ATM's answer did NOT transfer: the cabinet is one BoxGeometry(0.6,1.45,0.6) with an ARRAY OF SIX materials and no screen mesh, and hud.ts:899 would throw on it (.color.getHex() on an array) — so slots.ts supplies its own single-material plane, parented to the cabinet, found by a ray from the stool and sized from its bounding box (int-casino.ts not imported, no constants copied). Face re-cut 320x256 landscape -> 320x483 portrait to the cabinet's own aspect, 533 px/m square texels; adds a bill acceptor because the mouse had no way to insert. Clicks route through the same onKey as the keys, so SPACE/B/V/M/I/C all survive. Escape proven from all 6 states with feet actually moving; no DOM panel, no dimmed backdrop. fp: 1018 textures differ, ISOLATED to the three import alone — with it removed and everything else in place, textures/structure/tints IDENTICAL, so the face and surface move nothing. Built bundle green: mouse walk, escape-every-state, L-slots-inworld, L-slots-glass/rtp/feel, K-no-panel-traps, health, bugsweep 96 shots 0 STATION MISS. FOUND NOT FIXED: (1) ct/hud.ts throws on a multi-material mesh instead of degrading — next caller will hit it; (2) int-casino.ts:739 puts the whole cabinet-front texture on the 0.56x0.30 topper box, so every machine wears a tiny second slot machine; (3) live face vs baked face disagree (w41 finding 1, one room over); (4) scripts/K-atm-walk.mjs:157 is stale vs commit 1ab300666 (expects 'thanks' after TAKE CARD) AND prints FAILED while exiting 0. Handoff: notes/w55-slots-diegetic.md. Ports 4183 dev / 4194 preview, both shut down. | ct/slots.ts — **ITEM 86 HAS LANDED. THE GATE IS OPEN. START THIS.** | **The user, with a screenshot: *"slots similarly need to be embedded into the game like i mentioned with the atm. fixed perspective. embedded interactable overlay to make it look realistic and immersion forward."*** **⚡ THE USER ASKED ABOUT THIS DIRECTLY, 2026-08-02: *"slots overlay is not embedded like atm? is it still in flight? its been a while"* — it was NOT in flight; the desk left it ranked 21st after its blocker cleared. It is now top.** **THE FRAMEWORK EXISTS AND IS PROVEN:** `PanelSpec.surface` hangs a panel's own canvas on a named mesh; `crosstown.ts` owns the focus controller; the ATM is the worked example. Read `notes/w41-diegetic-screens.md` — **it carries a seam guide written specifically for this item**, and its author confirmed no re-scope is needed: `hot`/`click` are in **canvas-pixel space**, so a machine hit-tests the layout it drew, and `PanelSpec.key` is untouched so SPACE/B/M/I/C survive. **THIS IS NOT A SEPARATE FEATURE — IT IS THE SECOND APPLICATION OF ITEM 86.** Item 86 (the ATM) is building the mechanism and is explicitly scoped to put it **in the framework, not in `ct/atm.ts`**, precisely because the user said this would be general. **DO NOT START THIS UNTIL 86 HAS LANDED**, and when you do, **if you find yourself writing new mechanism rather than calling 86s, stop — that means 86 was not made reusable and the right move is to say so rather than build a second copy.** The desk has already caused two duplicate-work incidents this session by splitting one job across two rows; do not make it three. **What the slot machine needs, in the users terms:** `[E]` eases the player to a fixed standing pose facing the machine and locks the view; the interface is drawn on the machines **own screen and glass in the world**, not as a DOM panel over a dimmed room; the mouse operates the machines real buttons — `BET ONE`, `MAX BET`, `SPIN`, `CASH OUT` are already drawn as buttons and should be clickable **where they are**; the Win98 hand cursor appears while focused. **The current keyboard shortcuts (SPACE spin, B bet, M max, I insert, C cash out) should keep working** — this is about not forcing a menu, not about removing the keys. **Escape must release from every screen, and releasing must give movement back** (BUILDER-BRIEF §11 — the user has been trapped twice). DONE WHEN: no DOM panel appears; the interface reads as being on the machine; a mouse click operates a real button; the keys still work; Escape always releases; and the mechanism used is **86s, called — not reimplemented**. |
| 143 | DONE w58 — [E] now closes every machine view from ct/hud.ts's shared gate — ATM, slots, blackjack, loan desk, letters and pockets all got it without being edited; Escape kept as the net. Latched on the PHYSICAL key both ways (the [E] that opens cannot close, the [E] that closes cannot re-open) — the real bug was releaseHeld()'s SYNTHETIC keyup disarming the open-side latch microseconds after it armed, so only a trusted release disarms now. ROW'S PREMISE WRONG IN ONE PLACE: the item says the library PC must get [E] from the same place, but library-pc.ts:377 is a free-text search field — a global [E] makes the letter 'e' untypable and ejects the player mid-word (Emma, Frankenstein unsearchable). Added PanelSpec.typing opt-out; NEEDED ONE DECLARATION LINE IN ct/library-pc.ts, WHICH THIS ITEM DOES NOT NAME (queue checked first: nobody held it). Caption now reads '[E] leave' not a bare 'E' — changed after LOOKING: slots read '...C cash out . E' where every sibling token is key+verb. Proved on the BUILT bundle, port 4192: ATM from every reachable screen, slots/blackjack sat on for real, key leaned on in both directions, feet move after every close; mutation-tested 3 ways. bugsweep 0 STATION MISS, health OK, tsc clean. NOT FIXED: ATM 'thanks' screen is now unreachable dead code; probes that shoot right after waitForFunction(__ct) photograph a BLACK world because __ct publishes before geometry finishes — cost a bisect, wants a GOTCHAS entry. Note at notes/w58-e-closes-machines.md | ct/hud.ts panel framework — E closes a machine view, not just Escape | **The user: *"instead of getting out of the atm view or the slots or literally whatever. instread of using esc for that lets make it e."*** **`[E]` opens a machine; `[E]` should close it.** He named ATM, slots and *"literally whatever"*, so **this belongs in the panel framework, not in each machine** — ATM, slots, blackjack and the library PC must all get it from one place. **THE DESK RECOMMENDS KEEPING ESCAPE AS WELL, and this is the desks judgement rather than his instruction — he can overrule it.** Reasons: Escape is the universal expectation for "get me out"; **a view you cannot leave is the worst bug this project ships** (BUILDER-BRIEF §11) and the user has been trapped **twice**, once saying *"no im telling you i cant get up anything i do once i sit down"*; and a second way out costs nothing. **Make `[E]` the one he is told about; keep Escape as the silent net.** **⚠ THE REAL RISK IS TOGGLE BOUNCE.** `[E]` is edge-read once per rendered frame, so the SAME keypress that closes the panel can immediately re-open it — and this project has the scar: a *"stranded forceUp flag that ate a sit after Escape stood you up"*. **Latch it**: the key must be released before it can act again, and the spot must not re-offer on the frame it closed. **A held keypress is how the checks drive `[E]`** (`down` → 90 ms → `up`), so a naive edge test will pass a check and fail a human. **Prove BOTH ways out from EVERY screen each machine has** — the ATM has ten. Escape from each, `[E]` from each, and after each one confirm your feet actually move. DONE WHEN: `[E]` closes every machine view from every screen, Escape still does too, no keypress ever re-opens what it just closed, and movement is returned in all cases. |
| 136 | DONE w58 — The rooftop board's returns, soffit and cap are now painted metal instead of board black — ct/vice.ts, SAME BoxGeometry with its six face groups given three materials, so no geometry is added or moved and the silhouette he framed is unchanged. WHY IT READ BADLY: from his station the board shows the street only its narrow returns and its soffit, and in boardM (#24222a) those are a black wedge against a pale sky — at noon, with every socket unlit, nothing says it is a sign. THE ROW IS RIGHT THAT IT IS WORSE BY DAY AND THAT IS WHY THREE ROUNDS MISSED IT: bulbs are the night half and w51 already did the night half; no socket density changes a noon frame. So this is a cross-section fix, not a noise fix. THE SOFFIT WENT IN BECAUSE OF THE AFTER-FRAME: my first cut repainted only the front return and moved almost nothing — his eye is 5.3 m out and ~18 m BELOW the board, so the 0.5x7.2 m underside is most of what he sees and the front return is the sliver; that frame is kept as board-day-after.png. Trim values DERIVED from the sign's own gold #e8c25a (45% returns, 58% cap), no new colour. MY VERDICT: day is a real improvement — it now reads as a sign cabinet in the marquee's gold family rather than a blade stuck in the roof; night unhurt. HONEST LIMIT: it is still a sign showing the street its edge, and the only thing that would fully fix that is TURNING it, which would put a second SEVENS above the marquee's — the exact duplicate-name fault w51 removed the blade for. I did not turn it. W51'S REMAINING ADVICE IS PARTLY STALE: it asks for the uprights to be lit 'in steel rather than #09080a' but vice.ts:1596-1602 already builds them in steel — re-measure before re-queueing. fp NOT usable (2 materials added, GOTCHAS 75) and not used. bugsweep 0 STATION MISS, health OK, tsc clean, built bundle, port 4192. Note at notes/w58-rooftop-board-edge.md | ct/vice.ts — the rooftop board cabinet seen edge-on (the dark sky shape) | **The dark angular shape against the sky, identified but not fixed.** The user asked what it was in his casino frame; the builder that removed the blade found it: **the rooftop boards own cabinet**, `BoxGeometry(0.5, 6.6, 7.2)` at y 19.4-26, seen **edge-on**, with near-black supports set 2.4-4.8 m *behind* its leading edge and occluded by its own mass. **Same fault class as the blade the user just had removed** — an object whose art is on its faces, showing the street its unlit edge — and that has now appeared **three times** on this one building (the blade twice, this once). Not fixed because it is a geometry change on an object item 132 did not name. **It is WORSE BY DAY than by night**, so judge it in daylight, not only in his night frame. **Read `notes/w51-sevens-blade-and-bulbs.md` and `notes/w46-sevens-facade.md` first** — between them they hold the two prior rounds on exactly this. DONE WHEN: the board shows the street something deliberate from the users own station, checked in DAYLIGHT and at night, with before/after frames. |
