| 105 | DONE w56 — DESK PREMISE FALSE: doormatch12 did not compare position — it compared NOTHING (no assertion, no process.exit; 'MEASURED 12 OF 12' was a count of rows printed). Rewritten to ask the user's actual question and exit 1/0/2, both branches proven; RED on 5 of 12 and must stay RED. JAIL FIXED: jail.ts hoists jailLeafTex() (memoised, so zero new Math.random draws and both faces are the SAME texture), int-jail.ts hides the kit leaf and hangs leafPair's two + handles. CLOCK: was not misparented — it sat at lx 0 = door.at, spanning y 2.31-2.79 inside a 0-3.06 opening; moved 2.2 m along the wall (cannot go above a 3.06 head in a 3.3 m room). BANK verified by eye: MATCHES. CHURCH verified by eye: BROKEN, and NOT FIXED — ct/int-church.ts is not named by this item (item 145 names it). Its room passes no 'building' to buildRoom so it never reaches doorLeafFor at all; fix is 3 steps in notes/w56-door-faces.md §5a. Also flagged: burger/diner/tax/thrift on the same generic leaf; interiors-walk.mjs cannot run on a built bundle; which-world.mjs prints WRONG WORLD and exits 0. Verified on the BUILT bundle, port 4184: bugsweep 0 STATION MISS, w15-jail-walk all legs passed, tsc clean. fp deliberately not used (adds geometry, GOTCHAS 75). | scripts/doormatch12.mjs FIRST, then ct/int-jail.ts + ct/jail.ts | **⚡ DECISIVE NEW EVIDENCE, 2026-08-02 — the user, on the CHURCH: *"inside door of the church is still mismatched from the doors outside."* SECOND report on that door, THIRD on this class.** **THE DESK FOUND WHY THE EARLIER FIX DID NOT TAKE, and it names the bug in the check:** commit `db8322269` *"Church door: one CHURCH_FACE constant instead of five agreeing literals"* hoisted a shared constant so five literals agreed about **WHERE THE DOOR IS**. It never touched **what the leaf LOOKS LIKE**. And `scripts/doormatch12.mjs` references the church **five times**, so the check covers this door and passes it. **THEREFORE: the check compares POSITION and reports a match, while the user is judging APPEARANCE.** That is the entire class, and it explains all three of his reports at once. **Start there and prove it before touching any door.** **Original report: *"jail interior front door also looks bad and doesnt match outside."* THIS IS THE THIRD BUILDING HE HAS REPORTED FOR THIS.** Before it: *"door of the bank doesnt match the inner door of the bank"* and *"inner door of the church does not match outer doors"*. **START WITH THE CHECK, NOT THE DOOR.** There is a `scripts/doormatch12.mjs` and a CONFIRMED ledger row asserting exteriors match interiors **12 of 12** — and the user has now found three mismatches by eye. **Either the check compares the wrong thing, or it compares nothing.** Find out which and say so; that is worth more than this one door, because it is why the class keeps reaching him. Six checks were found last night reporting failure as success, and one compared the world against its own retyped copy of itself. **Then the door itself:** in the frame the interior leaf is a **flat slab** — no panelling, no reveal, a single tiny handle — against an exterior door that has real panel structure. A builder previously fixed the kits default leaf to read `LEAF.frame.colour`/`glazing` and noted **the jail specifically was wrong before that**, and the leaf COUNT (1 vs 2) was **reverted** in the same work after it doubled the bank/casino/library doors — so this pair may be a leftover of that revert. **Also in the frame: a clock appears to hang ON the door.** Check whether it is wall-mounted and merely reads that way from this angle, or is genuinely parented wrong. DONE WHEN: the check is fixed or retired with the reason stated, the jails two faces match, the bank and church pairs are re-verified by eye rather than by the check, and the clock is where it belongs. |
| 123 | DONE w57 — Item 123 DONE. The real 12-key pad on the ATM is now the PIN pad and the drawn-on one is retired. ROOT CAUSE: the pad's grid was twelve literals inside a closure in ct/bank.ts, so ct/atm.ts could not hit-test the keys the player can see. NEW ct/atm-face.ts is the third module the item asked for (imports neither, so no cycle, so GOTCHAS 28 cannot bite): it owns the fascia's eight metrics HOISTED out of bank.ts, the key grid as panel fractions, and the raycast delegation. bank.ts paints from padCells() and now prints DIGITS on the keys at 640 px/m on that panel alone (at the fascia's 160 a key is 12x4 texels and a number will not fit). The pad is live on EVERY screen: 1-8 pick the soft-key rows the way a real numeric pad does. ALSO FIXED, nothing was watching it: the typed pitch asked for 0.152 m of shelf on a 0.1442 m panel, so the CLR/0/ENT row ran off the bottom edge and has been CLIPPED in every frame of this machine; GAP_Y is derived now. PROOF, all on the BUILT bundle: scripts/probes/w57-pad-walk.mjs drives a REAL mouse at points projected from each key's own place on the mesh, 23/23 green, exits non-zero, and goes 12-red under a mutation. It caught a bug in my own change (raising the pickable flag in openAtm leaked it whenever hud.ts DECLINED the open; moved to onOpen). fp is VALID here (no geometry added or removed): textures 2 of 1458 differ and they are the two keypad panels, structure 2 of 8415 and same geometry, tints IDENTICAL, places 4 pigeons at 1 cm. bugsweep 0 STATION MISS. health OK. check-seethrough clean. K-no-panel-traps all good. Ports 4185 dev + 4191 preview, both were 000 first, both shut down. OUT OF BOUNDS I DID NOT TAKE: (1) this belongs in ct/hud.ts as a second pickable in ScreenSurface plus one line in crosstown.ts pick - w58 holds hud.ts on item 143, so the machine extends its own reach instead and the hoist is queued; it would give item 100's slots and the library PC the same reach for free. (2) THE CAPTION SITS ON THE BOTTOM KEY ROW - hud.ts puts a diegetic caption at bottom:7%, right across CLR/0/ENT; the keys are still clickable through it (proven) but not readable. w41 reservation 4, now load-bearing. FOUND RED ON MAINLINE, NOT MINE: scripts/K-atm-walk.mjs (registered, checks.mjs:838) asserts screen==='thanks' after TAKE CARD, which commit 1ab300666 changed AT THE USER'S REQUEST and never updated - identical single failure on the parent build, proven by rebuilding HEAD~1. And spots-walk fails 35 including two ATM lines ('NOT ON ITS DOOR'), byte-identical on the parent - its premise does not fit a machine in a wall. ownership.sh still calls bank.ts 'A's' - CLAUDE.md demotes OWNERSHIP.md and the item names the file. Note: notes/w57-atm-keypad.md | ct/bank.ts (the 12-key pad layout) + ct/atm.ts | **THE USER NOTICED, 2026-08-02: *"for the atm why do we not use the number button at the bottom?"* — promoted to the top on his eye.** **The real 12-key pad on the ATM is not clickable** — its layout lives as literals inside a `ct/bank.ts` closure, so the diegetic work could not hit-test it and drew a PIN pad on the CRT instead. That is a workaround and the builder said so. **The honest fix is to publish the pads layout** so the panel can hit-test the keys the player can actually see. **THE OBVIOUS ROUTE IS A TRAP AND IT IS DOCUMENTED:** `ct/bank.ts:8` imports `openAtm` from `ct/atm.ts`, so having `atm.ts` import from `bank.ts` closes a **cycle** — and GOTCHAS §28 records that this silently drops the module **from the built bundle only**. Dev would look perfect and the ATM would not exist in the artifact. **A third module is the way.** DONE WHEN: clicking the machines real keys works, the drawn-on PIN pad is retired, and the ATM still exists in a BUILT bundle (check it, do not assume). |
| 141 | DONE w53 — Region cull: street geometry (x<100) is not drawn while the player is east of it. 301 facing the window 4,012 -> 182 GPU draw calls/frame, the landing 3,987 -> 108, the street unchanged. THE ITEM'S RISK IS FALSE — 301's window does not show the street, it shows the closed brick light well the user asked for (ct/apartment.ts:1751), so all 2,691 street meshes it was drawing were behind a wall 1.2m from the glass; no portal test was needed. Proof: fp textures+structure byte-identical to parent; w53-ab.mjs 224 samples over all 14 rooms pass (worst 11px vs 40px floor) with its --mutate run red; boundary WALKED - furthest east on foot is x 23, so x100 has no seam; bugsweep 0 STATION MISS; check-seethrough pass; verified on the built bundle. Ports 4183/4184, both shut down. Note: notes/w53-region-cull.md | crosstown.ts / the room registry — the exterior draws through interior windows | **The user, and this detail identifies it: *"facing the window in my room makes the game feel slow. like my mouse moving across the screen feels like it drags."*** **DESK MEASUREMENT: there is NO interior/exterior culling anywhere in this project.** Nothing is ever hidden for being indoors; the only culling is three.jss default per-object frustum test. **So facing the window puts the entire exterior inside the frustum and it draws through the aperture** — every building, every citizen, the rain, the lot, the whole street. Face away and the exterior falls outside the frustum and costs nothing. **This explains every version of his report**, including *"looking into my room from the hall"* (the window is in view from there) and why the street itself is fine (nothing else is behind you). **⚠ ITEM 128 WAS REAL BUT WAS NOT THIS.** It removed 7,832 raycast tests per frame in 301 and that was worth doing, but he reported the drag AFTER it landed. Do not assume this is the same bug and do not re-do that work. **THE FIX IS A CULLING STRATEGY, AND THE RISK IS THAT IT IS VISIBLE.** The player can see the street through his window — that view is part of the world and **must not simply vanish**; a room whose window goes black is worse than a slow one. Options, yours to choose and defend: hide only what the windows aperture cannot possibly show (a portal test against the window rect); keep near exterior and drop distant blocks; or drop the expensive populations (citizens, rain, traffic) while keeping the static skyline. **Measure first with a COUNT, not a timing** — headless frame times do not transfer to his machine, but a draw-call or visible-mesh count does, and that is how item 128 was proved. **Report the count facing the window, facing away, and on the street.** DONE WHEN: the count facing the window is a fraction of what it is now, **the view through the window is still recognisably the street** (frames, from his own standing position), nothing pops as you turn, and `bugsweep` is clean. |
| 140 | DONE w54 — HANDED BACK, not landed. ROOT CAUSE: item 85 split the near tier and inserted bestLooked BETWEEN the halves, so a spot you are STANDING AT loses the prompt to anything within reach (6 m) as your crosshair crosses it — END TWO only ever needed bestNearLooked above bestNearOnly. Measured by count (changes to #ct-prompt per standing 360 turn, flat 301): mainline 3.26 mean / worst 6 / 14 of 19 cells at 3+; candidate 1.42 / 3 / 2 of 19, and both survivors touch nothing so aim SHOULD decide. DESK'S PRESCRIPTION IS WRONG: not hysteresis — bands are 30 deg wide with ZERO A->B->A reversals; a dead-band would add lag to a wrong answer. Also tried and DISCARDED a 'behind you' tier: measured WORSE (2.74) because it adds a +-90 boundary crossed twice per turn. PROVED no static key over distance-and-angle can satisfy both ends (the two poses are 0.50/2.13 vs 0.59/1.80 at offAxis ~0). NOT LANDED because w40-bed-vs-door goes red on ONE assertion — not the band walk carrying the user's END TWO quote (green at every stride) but the later firing step, whose station is 0.50 m PAST the bed on the far side, 1.80 m from the door, though it logs itself 'the middle of the band' (w54-firing-station.mjs). Fixing that check's navigation would make it test what it claims AND make the patch green, but it is a file this item does not name (BRIEF §9) and it is the check judging my own work. Patch: notes/w54-item140-candidate.patch (git apply). Blast radius 3.28% of 53952 poses change prompt text, 0 lost an offer. Port 4185. bugsweep 0 STATION MISS. Note: notes/w54-doorway-turn.md | fp.ts pickSpot tiers at the 301 doorway — REPRODUCE BY TURNING, not walking | **The user, third report on this and each one sharpened it: *"still feels weird to look around at the door frame to my room. idk what changed but it seems like a recent change. not on click behavior or anything. its just from looking around."*** **LEADING SUSPECT — item 85, which landed today.** Before it, the nearest spot won **outright**, so the prompt was **stable while you turned**. It now has three tiers and **turning changes which tier wins** — that is the fix working, but **301s doorway is the worst place in the world for it**: the bed seat, the door spot and the sleep spot all overlap there. w40 measured them 1.27 m apart with aim-free touch circles of 0.85 m and 1.10 m, and found a third spot (`sleep until morning`) winning at **174 degrees off-axis** before the fix. **RULED OUT, do not re-investigate:** the `canSee` memoisation (item 128) is **yaw-independent and deliberately not keyed on yaw**, so turning cannot change its answers. The desk checked. **REPRODUCE BY TURNING ON THE SPOT — not by walking, not by warping.** Stand at his doorway, sweep the yaw slowly through 360 degrees, and record the prompt string every few degrees. **The instrument for this exists: `scripts/approach-band.mjs` reads `#ct-prompt`s literal text every rendered frame** — adapt it to sweep yaw at a fixed position rather than walk a line. **What to look for:** the prompt flickering between two spots within a few degrees, or a band where it changes and changes back. **A prompt that swaps three times while you turn once is the complaint**, even if every individual answer is defensible. **⚠ DO NOT re-open item 85 by restoring near-over-looked** — that returns his *"i dont want sit on bed and watch tv to be the main option if im facing the door"*. The fix is hysteresis or a dead-band on tier changes, not a different winner. **`scripts/w40-bed-vs-door.mjs` must stay green.** DONE WHEN: the prompt is stable through a slow 360-degree sweep at his doorway, `w40-bed-vs-door.mjs` is still green, and you have the yaw-vs-prompt table before and after. |
| 132 | DONE w51 — Blade REMOVED — from his station it only ever showed its 0.34 m edge-on cabinet down the left third, halving the parapet run and hiding the west chevron; answered with a runtime-hidden frame before deleting. The item's stated cost is FALSE: the blade topped out at 21.4 m and the rooftop board at 26.0 m, so the skyline mark never was the blade's and did not move. Its ground spill and the east riser inset (which existed only to dodge it) went with it; item 121 on ORPHEUS is unaffected. Bulbs: the chase was one behaviour forever (every 3rd socket, 6 Hz) and is now a 13.2 s PROGRAM on the same clock and materials — comet/alt/flash/reverse/hold, PHASES 3->6 because alt needs an even count, COMET 2of6 to keep the same duty. The dark sky shape is the ROOFTOP BOARD's cabinet (0.5x6.6x7.2 at y19.4-26), edge-on with near-black supports set 2.4-4.8 m behind its leading edge — identified, not fixed, queued in the note. Proof: w51-chase-program.mjs PASS exit 0, mutation-tested to exit 1; G-vice-walk 18/18 WALKED; bugsweep 0 STATION MISS; health OK. | ct/vice.ts — the SEVENS vertical blade, and the bulb chase | **The user, on the now-legible facade: *"casino sign still a lil janky. maybe we get rid of the one on the side here? add more flair to the bulbs themselves instead?"*** He means the **vertical blade** — SEVENS set vertically down a tall thin cabinet. **Two parts, and the second is the real ask.** (1) **Remove the blade** — he phrased it as a question (*"maybe we get rid of"*), so **look at the frontage without it before committing**, and say what it costs: the blade is the tallest thing on the building and reads from down the street, so removing it may leave the frontage flat from a distance even while improving it up close. **If removing it hurts the long view, say so and propose what replaces that height** rather than silently keeping it. (2) **More flair in the bulbs** — this is the part he actually wants. Currently the chase is even dots at even spacing. Vegas bulbs **do things**: they chase in sequence, they alternate, they run around a border in one direction, they blink in pairs. **`ct/vice.ts` already animates** (the marquee copy changes), so there is a clock to hang this on — find it rather than adding a second timing mechanism. **The world is unlit 8 px/m: a bulb is a texel or two, so the flair lives in TIMING and PATTERN, not in detail per bulb.** **RELEVANT HISTORY — read `notes/w46-sevens-facade.md` first.** Item 97 gave that blade a lit leading edge, because its art was on the ±x faces and from the road you saw an unlit slab — that was the *"black bar"* of his previous complaint, and TWO earlier fixes had moved the blade when the problem was which face you see. **Also: item 121 is queued to give HOTEL ORPHEUS the same leading-edge fix. If the SEVENS blade is removed, 121 needs re-deciding — do not cancel it, ORPHEUS is a different building and he has not commented on it.** **One more thing in the frame:** a dark angular shape sits above the facade against the sky, apparently unattached. Identify it and say whether it is the blade top, the rooftop board, or something else. DONE WHEN: the blade question is answered with a frame either way, the bulbs do something a 1997 casinos bulbs would do, the dark shape is identified, and you have night frames from his own viewing position. |
| 128 | DONE w52 (claim reaped mid-flight; desk marked) — ROOT CAUSE: `canSee` ended in `seeRay.intersectObject(scene, true)` — a raycast against the ENTIRE WORLD, once per candidate spot, PER FRAME. Flat 301 is the densest room for [E] spots so it paid the whole bill and the street paid none. Counted, not timed: 301 7,832 tests/frame -> 3,970; the landing 15,664 -> 5,221; street 0 -> 0. Memoised against position (re-taken on 0.15 m, a storey change, or 0.10 s), so cost is bounded PER SECOND not per frame. Leak set byte-identical to mainline, seats-walk 103/219 matching w40 exactly, bugsweep 0 STATION MISS. |
| 119 | DONE w48 — 119: the rails NEVER overlapped the glass (all four abut it exactly) — the top was lost to PARALLAX, since ads only play to a seated eye fixed 0.538m ABOVE screen centre, so a 0.06m-proud surround ate 2.5 of 48 canvas rows off the top. Both of the item's hypotheses are false. Fixed BOTH halves: RAIL_D 0.06->0.04 (the authorised 'little bit'; band 2.5->1.6 rows) AND a declared title-safe area (TV_SAFE_T=3) enforced in tvFit/tvAt, so backgrounds still bleed but no spot can paint ink into the band. Shrinking alone could never work — 0.03 still eats 1.19 rows. All 27 spots checked and photographed; before/after shows MEGA HITS 97 going from half-letters to whole and the slate border from three-sided to four. New slow check w48-tv-title-safe (registered in checks.mjs) measures the band from the real seated eye AND requires minRow>=safe.t for all 27; --selftest turns both halves red. fpdiff: textures IDENTICAL, exactly 4 structural diffs (the rails). bugsweep 0 STATION MISS. Used port 4192 — checks.mjs REFUSES 4190 as a Chrome blocked port though playwright opens it fine. Edited scripts/checks.mjs (one row, to register) beyond the named file. NOT fixed: TV_FRONT:2515 is dead AND now stale by 8mm — one-line delete, worth queueing. | ct/apartment.ts (the TV bezel, ~2493-2540, and the ad canvas) | **The user: *"make sure the top of the ad isnt getting cut off by the tv. we can reduce the bezel a little bit."*** **THIS IS THE THIRD PASS ON THIS BEZEL AND THE FILE ALREADY STATES THE RULE IT IS BREAKING.** `:2494` records the original request — *"give the tv a bezel"* — and `:2502` says in as many words: **"the bezel must frame the glow, not swallow it."** `:2507` is a further round on its colour (*"black plastic is not black"*), and `:2535` describes the surround as **four rails around an open aperture**. So the intent is documented and the execution has drifted past it. **Establish WHICH it is before changing anything** — they need different fixes and only one is the bezel: (a) the bezel rails genuinely overlap the screen aperture, or (b) the ad canvas is drawing content above the safe area and the bezel is innocent. **Broadcast has a name for this — title-safe area — and it is the right frame for the fix.** If the ads are painting to the very top edge, shrinking the bezel only buys margin until someone writes a taller headline. Consider giving the ad painter a stated safe inset that all 20 spots respect, so a new spot cannot reintroduce this. **He has authorised reducing the bezel** — *"we can reduce the bezel a little bit"* — so that is permitted, but it is a ceiling on how much, not the whole answer. DONE WHEN: no spot is clipped at the top (check ALL of them, not one — they have different formats and heights), the bezel still frames the glow rather than swallowing it, and a deliberately over-tall headline is contained rather than clipped. |
| 114 | DONE w49 — PREMISE WRONG: dimWorld has NOT skipped transparent:true for some time - props.ts:414 isGlass is now blending===AdditiveBlending only, and my sweep reports the offending plane as 'graded', so the dimmer WAS touching it. The lead came from stale comments (lot.ts:159 and :322-333, street.ts:371, vice.ts:27, weeds.ts:22) that still assert the old rule; one of them aimed this item at the wrong mechanism. props.ts needed NO change. REAL CAUSE, and it is much plainer: the user's 'shadow fence' is the jail yard fence, ct/jail.ts:841 - a 14x2.4m PlaneGeometry, color 0x2a2c2e, transparent, opacity 0.75, and NO MAP. Its own posts are commented 'a touch taller than the mesh they carry' and the mesh was never drawn. Standing in the yard it is the ONLY untextured surface in frame among grained brick, banded stone and jointed paving, so the eye reads it as a shadow, not a fence. FIX: the cure lot.ts:311-333 already proved - alphaTest WITHOUT transparent:true, so the cut-out discards rather than blends. That also removes it from the translucent population entirely (624->623 transparent meshes, 312->311 planes); there is now NO standing translucent plane on the jail site. Added the top/bottom rails lot.ts:606-610 argues for (a chain-link fence is read from its framework, not its mesh). Repeat DERIVED from the panel's own W and FENCE_H at 0.3m pitch (BRIEF 7b); art COPIED from lot.ts with in-code line citations because linkPanel is a local, not an export. FENCE_X, FENCE_H and the collider unchanged. CENSUS (the item's real ask): 624 transparent meshes, 312 planes, 133 standing - only ONE was a defect. The rest are lit-window overlays, additive lamp glow/pool (correctly excluded), and window glass, ALL of which is at |x|>100 i.e. indoors, from interior.ts:924 + int-bank.ts:983 + apartment.ts:1710, each built with sill/mullions/transom and reading as glazing (checked bug-burger-wide.png). Ungraded because interiors keep their own light (props.ts:937), not because transparent - consistent. The flat op=0.26 strips are lot.ts:581 painted bay lines, PlaneGeometry(0.09,5.0), the same false '12 flat slabs' BRIEF 7 warns about. VERIFIED: WALKED it on the BUILT bundle - stops 0.47m short, reverses 5.01m (not wedged), yard still crosses; the check exits non-zero and fails in BOTH directions so it cannot pass vacuously. bugsweep 0 STATION MISS, 0 COVERAGE, 96 shots, no new console errors. tsc + build clean. fp deliberately NOT used - this adds geometry and GOTCHAS 75 says it would report a catastrophe that is not there. Ports 4193/4194, both proved free first and both shut down. OUT OF BOUNDS - PLEASE CHECK: the entire fix is in ct/jail.ts, which item 114 does NOT name (it named props.ts, from the false premise). No builder holds jail.ts; w48 holds apartment.ts via item 119 so I did not touch its pane. QUEUE THESE: (1) fix the 5 stale dimWorld comments - they will misroute another item; (2) hoist one shared linkPanel to paint.ts, there are now two chain-link implementations; (3) 76 undeclared texture densities world-wide (BRIEF 7b), which my sweep does not cover. Note at notes/w49-shadow-fence.md | a world-wide translucent-plane sweep — props.ts dimWorld + the 68 transparent materials | **The user, FIFTH report of this class: *"shadow fence still here. shadow geometry in general needs to be removed."*** **WHY IT IS STILL THERE, and this is the point of the item:** the previous fix (`2d3eba3f7`) was scoped to *"4 real **ground-quad** defects"* and sized the rest correctly. **The plane in this frame is VERTICAL** — a translucent sheet standing across the pavement — so that audit never looked at it. His complaint has never been about ground quads specifically; it has been about **pale translucent planes anywhere**, and each pass has fixed one subset. **Stop fixing subsets.** **STRONG LEAD — verify before building on it:** `ct/lot.ts:159` records that `props.ts`s `dimWorld` **skips any material with `transparent: true`**. So a translucent plane does **not** darken with the rest of the world: at dusk everything else drops and it stays bright, which is exactly how a faint sheet becomes a glowing ghost fence. There are **68** `transparent: true` materials under `ct/`. That may be the whole explanation, or only part — measure it. **SWEEP EVERY TRANSLUCENT PLANE IN THE WORLD, not the ground ones.** For each, report: what it is, whether anything still needs it, and whether it is skipped by the dimmer. **Expect most to be vestigial** — this world is unlit `MeshBasicMaterial`, so a "shadow" is a painted decal, and a translucent sheet standing in the air is almost certainly left over from an approach that was abandoned. **Delete what nothing needs**; the user has asked five times and *"shadow geometry in general needs to be removed"* is about as clear an instruction as he has given. **Related: item 95 is the lighting refactor and it will touch the dimmer — read that row and coordinate rather than collide.** DONE WHEN: no translucent plane stands in the world where the user can see it, every one that remains is justified in one line, the dimmer treats it consistently with everything else, and you have walked his exact viewpoint plus four others at dusk. |
| 112 | DONE w50 — fp.ts: stepping off a surface is now a FALL, not the floor moving. ROOT CAUSE: airY is height ABOVE THE GROUND and world Y is groundY+airY, so clearing a car's edge took groundY 1.415->0.000 in ONE frame and carried the camera along - the player was never falling. Measured before: one frame swallowed 0.514m of a 0.590m descent (87%) against a 0.035m gravity budget. FIX: add the lost floor to airY, so world Y is unchanged that frame and the existing integrator brings it down at the same 14 m/s^2. The mid-air second jump (camera rose 0.310m) fell out of the SAME line with no extra change - the gate is airY===0&&vy===0, which a step-off used to leave true. Desk's diagnosis VERIFIED CORRECT before building on it. Deliberately restricted: drops only (a rise is a landing, which already works), off collider TOPS only (so the kerb/stairs/storeys are untouched bit-for-bit - heldByTop is false for terrain), and only if you WALKED there (bound derived from this.run*dt, not typed, so a warp off a roof gets no phantom fall). PROOF: npm run stepoff - 6 cases, all WALKED not warped, green on the BUILT bundle; PROVED IT CAN FAIL by reverting fp.ts (exit 1, 5 of 6 fail, worst 1.273m in a 22ms frame vs 0.163m bound); the kerb case passes in BOTH worlds on purpose, as a change detector. Regressions green: w21-roof-climb, w29-sedan-climb, jump-walk (stairs+storeys), 17/17 vitest, bugsweep 0 STATION MISS. fp valid here (zero geometry added): textures/structure/tints IDENTICAL, places differs by 2 pigeons <5cm which fpdiff itself calls DRIFT. FOUND 2 INSTRUMENT FAULTS, not world bugs: my kerb case first measured HEAD BOB (bob=0.035, 0.07 peak-to-peak) and falsely reported the kerb changed; and the strafe onto the bed rail ignored facing (w21-roof-climb:159 has the right formula). NOT FIXED, please queue: (1) LANDING pops the camera up before settling - the mirror of this bug, pre-existing, one line next to mine but it re-times every climb so it wants its own item; (2) terrain drops are still instant at ANY height - groundAt finds 0.237m and 0.99m steps that still warp you down, but the item required the kerb unchanged so I did not touch terrain - user call; (3) scripts/interiors-walk.mjs imports src .ts so it CANNOT run against a built bundle (dev-only, undocumented). Ports 4187 dev + 4188 preview, both proved free first (4186 was taken); both shut down. Handoff: notes/w50-stepoff-fall.md | fp.ts — airY / groundY, the step-off fall | **The user: *"when i jump off of stuff i teleport straight down. please fix this."* A regression from the collider-height work — before it nothing could be stood on, so nothing could be stepped off.** **CAUSE LOCATED, VERIFY IT BEFORE BUILDING ON IT:** `fp.ts:553` is `this.airY = Math.max(0, this.airY + this.vy * dt)`, and `airY` is **height above the ground**, with world Y computed as `groundY(x, z) + airY`. Standing on the pickups cab roof, `groundY` returns **1.415** (via `standTop`) and `airY` is **0**. Walk off the edge and `groundY` returns the street — **0.000** — in that same frame, so world Y goes 1.415 to 0.000 instantly. **There is no fall to have: the player is not falling, the floor moved and took him with it.** The fix is that leaving a surface must convert the height you had into a real fall — keep world Y and let gravity bring it down — rather than re-basing to the new ground. **A SECOND EFFECT, IN THE SAME LINES, WORTH CHECKING WHILE YOU ARE THERE:** `:549` gates jumping on `airY === 0 && vy === 0`, which is **true the instant you step off** a raised surface, so stepping off a car hands you a fresh jump in mid-air. He has not reported it, but it is the same root. **`fp.ts` IS THE HIGHEST-RISK FILE IN THE PROJECT AND THE 2 m SIDEWALK LANE IS SACRED.** Stage it, commit each stage, and prove the ordinary world is unchanged with `fp before`/`after`/`fpdiff`. **Check what else reads `airY` or assumes `ground + airY`** before changing its meaning — the kerb, the stairs and the storey picker all deal in ground heights. **Other builders may hold `fp.ts`** (items 85 and 98 are both against `pickSpot`) — run `./scripts/claim.sh --stale` and coordinate or wait. DONE WHEN: stepping off the pickup bed, its cab roof and the sedans boot lid all produce a real fall at the same gravity as a jump; you cannot jump again in mid-air; walking off a kerb still feels as it does now; and you have WALKED all of it rather than warped. |
| 97 | DONE w46 — SEVENS read EVEN because the cap height came from the panel HEIGHT (112 texels of ink into a 92-texel canvas); fitTube now derives size from the width available and throws below a legibility floor. DEFECT 2 AS FILED IS WRONG: the marquee line is NOT clipped (72.2 texels in 96, 12 clear each end) — it was under-resolved, so the fascia doubled to 192x52 (exactly 2x, still hardLayer, not soft). The black bar IS the blade cabinet: its art is on the +/-x faces so from the road you only see 0.34x15.8m of unlit boardM — both earlier fixes MOVED the blade, which could never help; leading edge now carries the chase + a tube, and its rooftop twin had the same dead edge plus a dead 0.5x7.2m soffit. Vegas: CASINO over a framed socket-edged name board over a 4x bigger 777 with the blade's own chevron in the wings; the three risers that ran through the middle of the name now pair at the party edges and break at the board. Port 4180 dev / 4181 built. 0 STATION MISS, health OK, positive-controlled clipping check passes on the BUILT bundle. NOT FIXED, please queue: npm run fpdiff over-reports catastrophically (849/1461 textures) on any change that adds a texture, because scenedump seeds ONE global Math.random for the whole world's grain — grain-pinned, the real diff is 2 replaced + 7 added, all mine; and HOTEL ORPHEUS in the same file has the identical dead blade edge, left alone as out of scope. | ct/vice.ts (the SEVENS facade, marquee, blade sign) | **The user, with a screenshot: *"sevens casino front looks so messed up. take influence from vegas thanks."*** **THIS IS HIS THIRD COMPLAINT ABOUT THIS FACADE** — `ct/vice.ts:117` records *"the blur the user reported on the marquee"* and `:141` records *"the LEFT leaf is reversed"* on the SEVENS entrance. Read both notes before touching anything; two previous fixes here did not settle it. **THREE CONCRETE, VERIFIABLE DEFECTS IN THE FRAME — fix these regardless of taste:** (1) **`SEVENS` is CLIPPED AT BOTH ENDS.** It reads `EVEN` — the word overflows its panel and is cut by the band edges. The facade is the buildings name and it currently cannot be read. (2) **The marquees second line is clipped**: `$1 BLACKJA` where it should say BLACKJACK. (3) **A black vertical bar floats over the left edge** of the facade, unattached to anything. **THEN THE DIRECTION: Vegas.** What he is reacting to, beyond the clipping, is that the composition is mostly **empty maroon field** — `777` sits small and lost in the middle of it, and the sign furniture does not fill the frontage the way a real casino front does. Vegas means **density and hierarchy**: a dominant name, chase bulbs that read as a path rather than scattered dots, stacked copy, and no dead wall. **The world is unlit 8 px/m and the whole facade is texel work** — this is a painting job in the existing signage hand (the file says there is ONE signage hand for both buildings of the pair), not a modelling job, and it must not go soft: `:132` records that fixing alpha alone took a marquee line from 262 distinct colours to 45. DONE WHEN: SEVENS reads in full, the marquee copy is not clipped, the floating bar is gone or attached, the frontage reads as a casino at night from the users own viewing position, and you have the before/after night frames and are willing to defend them. |
| 95 | DONE w45 — PREMISE WRONG: there is ONE registry (props.lit), not four - sidestreet/traffic/crowd each declare a FIELD named lit and crosstown passes props.lit into all three; vice deliberately opts out; lampHeads is in props.ts not sidestreet.ts. Real cause, measured at the (4.1,-23) lamp: 3 materials held up vs 38 at the night floor, and the 3 are 0.1m posts. Everything was REGISTERED and then REFUSED - (a) the span taper zeroes sizeW past 12 m and every surface a lamp stands on is longer than that, so no ground could ever pool, and (b) the road/sidewalk/kerb are in wetMats, which BOTH grading loops skip, so the ground was never in the registry at all. The visible road pool is a painted additive quad, not light, and there is one on the roadway and none on the pavement - which is the user's sentence exactly. FIX: moved the same falloff (same LAMP_R/CORE/smoothstep/WARM) from per-material to PER FRAGMENT via onBeforeCompile, so a 92 m ribbon holds a gradient; retired sizeW and both centroid tests; height is now a per-fragment 2.2-4.5 m fade. NOT real lights - no normals, no diffuse, no light objects, so the brown-slab failure cannot recur. POOL_GAIN 12->6.5 and road decal 0.72->0.22 because at 12 the asphalt went flat grey (judged by eye, not by number). Published scene.userData.addLit for objects built after buildProps, which were permanently unlit (a carVariant car was 0/33 patched, now 27/33). fp: textures+structure+tints IDENTICAL, 4 places differ (pigeons). bugsweep 0 STATION MISS. Verified on the BUILT bundle. Ports 4189/4193, both shut down. OUT OF BOUNDS - PLEASE CHECK: the whole fix is in ct/props.ts, which item 95 does NOT name (it named sidestreet/traffic/crowd/vice/crosstown, all from the wrong diagnosis). No queue item names props.ts so no builder conflict, and I did NOT touch crosstown.ts because item 86/w41 holds it. NOT FIXED: (1) no parked car is under a lamp - nearest is 6.97 m against a 7 m reach - so the screenshot car was dark partly for a PLACEMENT reason; a car I placed under a lamp reads 1.82x. Parking is crosstown.ts, w41's. (2) night frame time +50% (53->79 ms), cut down from +160%; hybrid CPU-for-small-objects is the lever if too dear. Note at notes/w45-lamplight.md | the lamplight architecture — ct/sidestreet.ts, ct/traffic.ts, ct/crowd.ts, ct/vice.ts, crosstown.ts | **THE USER, WITH A SCREENSHOT: *"lighting needs a full refactor. it isnt consistent anywhere. this is a prime example. the lighting only affects the street but not the sidewalk. it doesnt affect the car at all."* He is right, and the cause is architectural.** MEASURED BY THE DESK, verify it yourself before building on it: the world is **unlit** — 671 `MeshBasicMaterial` uses and no real lights — so lamplight is faked by **tinting materials that OPT IN**. And there is **no single registry**: `ct/sidestreet.ts:57` keeps `lampHeads` *private to it* by its own comment, `ct/traffic.ts:133` has another, `ct/crowd.ts:28` a third, `ct/vice.ts:45` exposes `lit()`. **Anything that never registered is never lit** — which is precisely the screenshot: the road registered, the sidewalk and the parked car did not. Only **20 `noLight` opt-outs** exist against 671 materials, so opt-out is vestigial and opt-in is the real rule. **THIS IS THE LARGEST ITEM ON THE BOARD AND THE MOST VISIBLE. Stage it and commit each stage; a half-lit world is worse than an unlit one.** Suggested shape, yours to reject with reasons: (1) **one registry**, in the world rather than per-module, that any surface can be handed to; (2) make the pools **spatial** — a surface is lit because it is under a lamp, not because a module remembered to sign up; (3) migrate the modules that already do it, and **prove nothing dims that should not**; (4) only then extend to what was never lit. **Do not convert the world to real Three.js lights without saying so first** — unlit `MeshBasicMaterial` is the projects entire look and 8 px/m pixel art does not survive being shaded. If your measurement says that is the only correct answer, **stop and report it** rather than building it. DONE WHEN: a lamp lights the road, the sidewalk and a parked car under it consistently; nothing that was correctly dark becomes lit; `fp` before/after shows textures and structure unchanged where they should be; and you have night frames from the players own standing position that you are willing to defend. |
| 90 | DONE w43 — Items 90 AND 89 together (one geometry). The corner: the four edging strips each ran the FULL length of their leg while the legs stopped CHAM short, so all four overran into the turn and crossed each other -- he photographed a grey X on the path, and no work on the corner PATCH would have touched it. Also the turn was a separate quad 0.6 m too long and PATH_W/cos45 = 2.12 m wide where it crossed 1.5 m legs. Loop is now ONE octagonal ring; surface, edging and hoop rail all derive from it, so nothing can fail to mitre (width 1.5 m +/- 2e-15 through every turn, was 2.12). Surface, his 2nd rejection: it read as poured aggregate because it was isotropic 1-texel pepper at even density, identical shoulder to shoulder and end to end -- nothing said people walk there (and slabTex grain 0.18 was over its own 0.14 pebble threshold, so it was laying 2 px stones at full contrast). Answered with a CROSS-SECTION in vertex colour, not new noise: a pale spine that WANDERS with arc length (the direction of travel, and it carries through the turns) and shoulders that go olive into the turf. Edging is flat rather than a 70 mm ROAD kerb and now goes through wet(), which is why it photographed as near-white rails beside a dark path. Proof: walked the leg and the chamfer (5.05 m, 0.000 m off centreline), 338 floor samples level, built bundle same, bugsweep 0 STATION MISS. NOT MINE, FOR w44/item 88: four benches stand 0.160 m INSIDE the park path (list in notes/w43-park-path.md) -- pre-existing, item 88 only recorded the sidewalk case. INSTRUMENT FINDING: fpdiff's 671 texture / 1741 structure diffs are meaningless here -- a control adding ONE Math.random() call and nothing else reproduces the same 671. places is the only field with signal, and it says nothing outside the park moved. Also claim.sh --help claims an item. | ct/park.ts:307-328 (the path corners) | **The user, with a screenshot: *"look at this path corner it looks so messed up."*** Third park complaint in one sitting, and this one is **not** the surface — the corners are drawn separately from the legs. `ct/park.ts:311` says each corner is drawn **0.3 m long** and `:323-328` builds it as a patch rotated by `atan2(sx * CHAM, sz * CHAM)`. In the frame it reads as a **diamond of bare earth sitting proud of the two legs it joins**, with the grey kerb rails crossing it at angles that do not meet. **Look at the screenshot yourself and say what is wrong before changing anything** — candidate reads, none of them the desks verdict: the rotated patch is mitred at the wrong angle so it does not tile with either leg; the kerb rails are drawn per-leg and simply overrun into the corner rather than mitring; the corner is a different width from the legs it joins (the file notes legs at 2.0/2.5 and corners at 1.25). **Related but separate: items 88 (bench too close) and 89 (the surface reads wrong, his SECOND rejection of it). Read those rows before starting — if one fix serves all three, say so and take them together rather than fighting the same geometry twice.** DONE WHEN: a corner reads as the same path turning, the kerbs mitre rather than overrun, and you have stood at the corner at player height and are willing to defend the frame. |
| 86 | DONE w41 — Diegetic screen framework + ATM as first tenant, staged in 4 commits. PanelSpec.surface hangs a panel's own canvas on a mesh: [E] eases you to a standing pose derived from the mesh's world normal (ct/bank.ts untouched, mesh found by its own userData tag), the UI paints onto the machine's raked screen, the mouse raycasts to UV and is hit-tested in the machine's OWN canvas pixels, and a pixel-drawn Win98 arrow/hand replaces the OS cursor. ROW'S CAUSE WRONG IN ONE PLACE: the raked screen is ct/bank.ts's, not ct/atm.ts's — atm.ts only ever drew a DOM panel. Three real bugs: cam.fov had two owners (lock vs scroll-zoom smoother, meeting at a stable 66deg); main.ts:30 re-takes pointer lock on CLICK which was not in BLOCKED, so the first click froze clientX/Y and every click after it missed; an untouched canvas renders flat black on a mesh. Escape proven from ALL TEN screens with movement restored, on the built bundle. fpdiff textures/structure/tints IDENTICAL, bugsweep 0 STATION MISS, health OK. NOT FIXED, needs ct/bank.ts (not my item): the fascia re-arranges layout on [E] (idle texture has a card slot where the live one has soft keys), and the real 12-key pad is not clickable. ALSO: bank.ts invites atm.ts to import ATM_PALETTE, but that closes an import cycle (bank.ts:8 imports openAtm) and GOTCHAS 28 would silently drop the module from the BUILT BUNDLE ONLY — needs a third module. Port 4187/4199. Note at notes/w41-diegetic-screens.md, with a seam guide for item 100 (slots). | ct/atm.ts + ct/hud.ts panel framework + crosstown.ts (camera lock) | **THE USER, WITH A SCREENSHOT: *"this doesnt look integrated. i want when i hit e here to adjust my position and perspective and lock it to be looking at the atm and for the screen on the literal atm be the overlay that i can use my mouse to click through. the mouse cursor should be like a lil hand almost like win98 cursor"*.** This is the concrete form of his standing design law: *"i never want there to be menus popping up unless they are embedded to look as if they are in the actual game."* Item 0c removed the panels chrome; **this removes the panel.** WHAT EXISTS TODAY, measured: the panel framework in `ct/hud.ts` is **DOM, screen-space** (`position:fixed`, z-index, `pointer-events:none`) painted over a dimmed world — that is what the screenshot shows. `ct/atm.ts` already draws a real raked screen on the machine in the world. There is **no** raycasting, no UV picking, and the ATM has **no seat or pose** to lock a camera to. **FOUR PARTS. STAGE THEM AND COMMIT EACH — do not attempt it in one pass.** (1) **A pose to lock to.** `[E]` walks/eases the player to a standing pose facing the machine and locks look — the seat system (`ctx.seat`, used by slots/blackjack) is the prior art for locking the camera, but this is standing, not sitting. Escape must release it, and **a view you cannot leave is the worst bug this project ships** (BUILDER-BRIEF §11). (2) **The UI paints onto the machine.** The ATMs existing screen texture becomes the surface the interface is drawn to, instead of a DOM panel. (3) **The mouse reaches it.** Raycast the pointer against the screen mesh, map the hit to texture UV, hit-test against the ATMs own button layout. (4) **A Win98-style hand cursor**, pixel-drawn, shown only while the machine is focused. **DONE WHEN:** pressing `[E]` at the ATM locks you facing it with no DOM panel visible; the interface is legibly ON the machine in the world; a mouse click on a button in the world operates it; the hand cursor appears while focused and reverts on release; Escape always releases and gives back movement, proven from every screen the ATM has. **If a stage proves the approach wrong, say so and stop — a report beats a half-converted world.** This is the TEMPLATE: slots, blackjack and the library PC follow once it works, so build it to be reused rather than special-cased to the ATM. |
| 85 | DONE w40 — pickSpot: near tier gets a facing gate (3 tiers: standing-in-it/touching-and-aimed-at, then aimed-at, then touching-but-aimed-away). ROOT CAUSE: the 301 bed seat and door spot are 1.27 m apart while their aim-free touch circles reach 0.85 and 1.10 m, so they overlap over most of the floor between them and the near tier ranked by DISTANCE ALONE - the bed just won. Gate is the existing lookTolerance cone, not a new constant; tier 1 keyed on RADIUS (the player's own capsule) is what holds w9's repro and seats-walk at the other end. Walked it: 301 cells offering the bed while facing the door 10/19 -> 3/19 (the 3 are where the bed's centre is inside the player's own body). New check scripts/w40-bed-vs-door.mjs walks the band both ways + both ends; 2 canfail cases, one per end, 2/2 CAUGHT with a green run watched first. fpdiff textures/structure/tints IDENTICAL, bugsweep 0 STATION MISS. FOUND, NOT FIXED: seats-walk 115->103 and the 12 are worth a user call (5 seat you at a booth ~2.9 m away that you were aimed at); w9's note justifies the near tier with a No. 227 competitor that measurement shows does not exist. Port 4188. | fp.ts pickSpot (lookTolerance / bestNear vs bestLooked) | **The user, 2026-08-02: *"i dont want sit on bed and watch tv to be the main option if im facing the door to leave"*.** Standing near the bed in flat 301 and facing the door, the `[E]` prompt still offers the bed seat instead of the door. **HISTORY YOU NEED, because this is a knob with a user complaint at BOTH ends and swinging it back is not a fix.** `fa5c32e01` (item 0b) split `pickSpot` into two tiers — `bestNear` and `bestLooked` — with **near winning outright**, and it did that to satisfy the users EARLIER complaint: *"i dont want to be so far from the bed and the option is still to sit on the bed and watch tv"*. Before that change the comment claimed near-priority and the code did not do it. So: **do not simply restore looked-over-near**, or the desk will get the first complaint back within a day. The shape that satisfies both is something like: near wins **unless** the player is clearly aimed at another candidate — i.e. the near tier needs a facing gate of its own, not the removal of the tier. `lookTolerance(r, d)` at `fp.ts:683` already scales the cone by radius and distance and is the natural place to look. **WALK IT in flat 301: stand where the user stands, face the door, read the prompt; then face the bed and read it again. Both must be right.** DONE WHEN: facing the door offers the door, facing the bed offers the bed, the earlier far-from-the-bed case is still fixed (prove it, do not assume), and a check goes red if either end regresses. |
| 91 | DONE w44 — Item 91: the desk's premise was wrong both ways — the bracket was NOT missing, undersized, or behind the wall plane. It was drawn (0.34x0.10x0.26), in front, with the figure resting on it to 0.000 m, and attached to NOTHING: 0.78 m clear of the west wall at x -6.5. That is the float. Rebuilt the shrine off the wall plane (WALL = -hw) with a 3-stage corbel whose underside reads, and PAINTED the figure — cream robe, blue mantle, face, joined hands, halo — because the bigger failure was that he did not recognise it as a statue at all; it was a grey cylinder and a ball against grey plaster. Walked in and up the aisle for both frames; verified on the BUILT bundle; bugsweep 0 STATION MISS. NOTE FOR THE DESK: the fp 'textures' hash CANNOT match across any change that adds or removes a mesh — three's generateUUID() draws 4 Math.random() per object and scenedump seeds Math.random globally. See notes/w44-church-statue.md. | ct/int-church.ts:651,683 (the votive statue and its bracket) | **The user, with a screenshot: *"what is this floating thing in the church?"*** It is the small painted statue that is meant to stand **on a bracket** above the votive stand in the north aisle — `int-church.ts:651` describes *"a small painted statue on a bracket"* and `:683` draws it. **The statue reads; the bracket does not.** In the frame a pale rounded form hangs against the plaster with a thin sill under it and nothing connecting the two, and the user did not recognise it as a statue at all — which is the real failure. **Two things to settle, and say which you found:** whether the bracket is drawn but too small/too flush to read at player height and distance, or whether it is being drawn behind the wall plane. The world is unlit and 8 px/m, so a bracket that is a plausible size in metres can still vanish — it may need to be read as a silhouette rather than modelled honestly. DONE WHEN: from the players own standing position it reads as a statue standing on something, and you have the before/after frames from that exact spot to show for it. |
| 149 | DONE w58 — K-atm-walk now asserts what TAKE CARD actually does (panel closes, screen back to 'idle'), not the retired 'thanks' screen. ROW HALF WRONG — and it is the headline half: it says the check 'prints 1 FAILED and exits 0', making it the EIGHTH member of that family. IT DOES NOT. :205 is already process.exit(fails.length ? 1 : 0) and a plain run MEASURED exit=1 while failing; the only exit-0 path is --selftest, where exiting 0 because the mutation was caught is correct. There is no eighth member here — the desk's count should not grow on this row. Also fixed a guard that WOULD have slept: the 'ESC closes it' test ran against a panel TAKE CARD had already closed, so it passed without exercising ESC — it now reopens, waits out the 500ms DISMISS_LOCKOUT, and asserts the machine is up first. Assertion was not weakened: one stale fact became two live ones. Proved red by BREAKING THE WORLD (reverting atm.ts's TAKE CARD to 'thanks' -> 2 FAILED, exit=1), restored after; --selftest still 'caught it'; 29 OK exit=0 on the BUILT bundle, port 4192. NOT FIXED: the 'thanks' screen is now dead code in ct/atm.ts (Screen type, onKey branch) — the row says item 144 owns that and 149 does not name atm.ts, so I left it. Note at notes/w58-katm-walk-stale.md | scripts/K-atm-walk.mjs — STALE against the desk own change, and silently failing | **THE DESK BROKE THIS AND THE CHECK HID IT.** `scripts/K-atm-walk.mjs:157` expects the ATM to show `thanks` after TAKE CARD. Commit `1ab300666` — **the desks own one-line change, on the users instruction *"take card from atm should immediately get us out of the menu"*** — made TAKE CARD close the view outright, so that screen no longer exists. **The check now prints `1 FAILED` and exits 0**, making it the **eighth** member of the family found today. So the desk shipped a behaviour change, its check noticed, and the suite reported success. **Fix both halves:** update the expectation to the new behaviour (the view closes; there is no farewell screen), AND give it a non-zero exit on failure. **Do not weaken the assertion to make it pass** — that is the documented trap this project has paid for repeatedly. **Related: item 144 removes the now-dead `thanks` screen.** DONE WHEN: the check asserts what TAKE CARD actually does, exits non-zero when that is broken (proved by breaking it), and the walk is green on the built bundle. |
| 147 | DONE w57 — Item 147 DONE. ROOT CAUSE: buildRoom reaches a DoorDecl only through a building NAME, it has two sources for one (spec.building, the frontage), and a room on a CUT FACE publishes no frontage - so a chamfer room that omits building resolves null, looks up nothing, and every reader takes its ?? branch. The desk's premise was RIGHT and w56's 3-step repair held up in every particular. (1) MECHANISM, ct/interior.ts: buildRoom now warns on the kit's OWN [interior:<id>] channel when no building name resolves - a channel already wired to two registered checks (interiors-walk.mjs:284, G-rooms-walk.mjs:210 both collect and fail on that prefix), so a room that loses its door now FAILS A CHECK rather than merely looking wrong. Not a throw: a room with genuinely no declaration is legal. (2) CHURCH, ct/int-church.ts: building: 'ST BRIGID', a real leaf on DOOR, width:1.4 REMOVED from the spec's door (spec.door.width BEATS the declaration by design, so leaving it would have made the fix look like it did nothing), and the jail's recipe - hide the kit's 32x64 leaf, hang the room's own pair via leafPair, because a DoorLeaf cannot carry a leaf COUNT. LEAF SIZE MEASURED, NOT RETYPED: I ran civic.ts's archHW by hand first and got the height 0.1 m wrong; scripts/probes/w57-church-leaf.mjs reads the leaf timber off the west front's own canvas and converts with the ppm masonry() stamped on it - 2.750 m across both leaves, 3.750 m tall. That probe was wrong twice as an INSTRUMENT before it was right (filtered canvases at >200px when the whole 13 m church front is 104px; then matched the colour on six other facades' drainpipes) and both are written into it. PROOF: doormatch12 5-of-12 red -> 4-of-12, church now 'its own door' (2x28x76), other four rows BYTE-IDENTICAL, nothing loosened. scripts/probes/w57-church-walk.mjs WALKS the widened opening (1.4x2.15 -> 2.75x3.75 with two meshes in it): 0.20 m from the wall plane in the opening, wall stops me 2.91 m further out at the jamb, all three directions move from inside the doorway, [E] in and out on foot - green on dev AND the built bundle, exits non-zero. Deleting the one building: line makes the warning fire and the walk go red. bugsweep 0 STATION MISS on the built bundle, health OK, spots-walk 35 failures = the same 35 as mainline. fp NOT used, deliberately - this adds meshes, GOTCHAS 75. Frames from the player's own position: shots/w56/church-{inside,outside}.png - two tall boarded timber leaves, same timber, straps at the same relative heights, brass rings, inside and out. OUT OF BOUNDS, NOT TAKEN, AND IT IS WHY G-rooms-walk NOW READS 113/114: the HOTEL is the same bug still open. ct/int-hotel.ts declares clearW 2.2 / h 2.6 / leaves 2 and BUILDS ITS OWN STONE CASE AT EXACTLY THOSE NUMBERS, while its spec omits building - so the kit cuts its 1.15x2.15 fallback hole behind it. shots/w57/hotel-inside.png shows 2.2 m of doorcase over a 1.15 m opening. Same one-line fix, and the lobby window at [1.5,5.1] still clears a centred 2.2 m door by 0.4 m. ct/int-hotel.ts is item 96's file so I stopped and reported - the red is this item WORKING, not a regression; before today nothing could see it. ALSO NOT FIXED: ct/civic.ts's church painter should read doorLeafFor('ST BRIGID') instead of its own literals (hoist a churchLeafTex() like ct/jail.ts did); doorWidthFor() in ct/doors.ts has NO callers in src/ while the deprecated DoorDecl.width still OUTRANKS leaf.clearW via spec.door.width - the next version of this same trap. Item 145 (ct/int-church.ts) was TODO and unclaimed on --stale, so nothing collided. Ports 4185 + 4191, both 000 first, both shut down. Note: notes/w57-church-door.md | ct/interior.ts buildRoom — a chamfer room passing no `building` silently loses its door declaration | **STRUCTURAL BUG BEHIND THE USERS MOST-REPEATED COMPLAINT — found by the builder that repaired the door-match check.** The churchs interior door **never reaches `doorLeafFor` at all**: its `buildRoom` spec passes **no `building`**, and a chamfer room publishes no frontage, so **its own `DoorDecl` is never consulted** and it falls to the `?? 0x3a2c22` default branch. Result, which the user has now reported **twice**: outside is a **5.5 m pointed arch with two timber leaves**; inside is a **1.4 m brown domestic door with a grey pane**. **THE SILENCE IS THE BUG.** A room that declares a door and is then never asked for it should not build a generic leaf and say nothing — it should fail loudly, or the kit should refuse to guess. **Fix the mechanism first, then the church.** The 3-step repair is written up in `notes/w56-door-faces.md` §5a — read it. **⚠ `ct/int-church.ts` is named by item 145 (church darkness) — check `./scripts/claim.sh --stale` and coordinate rather than collide.** **The repaired `doormatch12.mjs` is RED on 5 of 12 rooms and MUST STAY RED until they are genuinely fixed** — `burger`, `diner`, `tax` and `thrift` are on the same generic leaf as the church. DONE WHEN: a room whose door declaration is ignored cannot do so silently, the churchs two faces match from the players own position, and `doormatch12.mjs` is green for church without its other rows being loosened. |
| 104 | DONE w59 — Item 104: the jail leaves were BURIED IN THE RECESS-BACK STONE with their front face coplanar to Δ0.0000 m — z-fighting, not glazing. Desk's lead WITHDRAWN: interior.ts:924 opacity 0.55 is a room WINDOW, and flat() is MeshBasicMaterial({map}) with transparent=false opacity=[1] measured on both leaves, so transparency was never the mechanism. One fault explains all three symptoms incl. the 'diagonal hatching' (tear between dithered canvases) and the 'misaligned leaves' (identical geometry, tie breaking differently). Assembly moved forward one leaf thickness, all parts now derived from LEAF_FRONT. Probe RED before (exit 1) / green after; bugsweep 0 STATION MISS; typecheck clean; WALKED in and entered. Port 4187. NOT FIXED, not my file: ct/int-jail.ts's interior leaves stand OPEN (canted ~31deg, bbox 1.00x3.00x0.61) while the street pair is shut — desk should decide. Also could NOT frame the interior face (camera landed inside a wall), so it is unmeasured, not cleared. ct/doors.ts and ct/interior.ts were named by the item but needed no change. | ct/jail.ts door + ct/doors.ts + the shared kit leaf in ct/interior.ts | **⚡ SECOND REPORT, 2026-08-02, IN DAYLIGHT: *"jail door is still messed up."* The lit frame shows what the dark one hid — THE DOOR IS SEE-THROUGH. Masonry coursing and the stone reveal are visible THROUGH the leaves.** **⛔ THE DESKS FIRST LEAD (stretched texture density) IS WITHDRAWN — do not chase it.** **NEW LEAD, verify before building on it:** `ct/interior.ts:924` builds a glazing material at **`opacity: 0.55`**. `ct/int-jail.ts:65` declares `frame: { colour: 0x3a3c40, material: "steel" }, glazing: "none"`, and `:59` says that declaration exists precisely so *"the kit would [not] give the room a 1.10 m"* glazed leaf. Meanwhile `ct/jail.ts:510` only **comments** *"Steel, two leaves, no glazing. `ct/int-jail.ts` declares the same leaf"* — it points at the interior file for the declaration. **SO THE HYPOTHESIS IS: the EXTERIOR leaf never reads that declaration, and is glazed at 0.55 while the interior is solid steel.** That would be this bug AND the inside/outside mismatch of item 105 in one — **read item 105 before starting; if one fix serves both, take them together and say so.** **Establish which file actually builds the leaf you are looking at**, and whether a declaration in an `int-*.ts` file can reach an exterior door at all. If it cannot, that is the real defect and it is structural. **Original: *"front door of jail graphics are messed up."*** In the frame the door carries **diagonal hatching that does not match its own panelling**, and the left and right leaves **do not align** with each other. **STRONG LEAD, NOT A VERDICT — verify it first:** `d3770c506` landed last night and *"size[d] every masonry faces texture to the face it is on"*, after a builder found a texture painted for 4 m stretched across a 14 m wall at 4.57 px/m against a declared 16. **A stretched or wrongly-rotated texture is exactly what diagonal hatching over a rectilinear panel looks like.** The jail door is a **different system** from that fix — `ct/jail.ts` publishes it as a world point and outward normal for `ct/doors.ts`, and the leaf itself comes from the shared kit — so it may simply not have been reached. Two other things worth checking in the same pass: a builder fixed the kits default leaf to read `LEAF.frame.colour`/`glazing` and noted the jail specifically was wrong before that; and the **leaf COUNT (1 vs 2) was reverted** in that work after it doubled the bank/casino/library doors, so a mismatched pair here may be a leftover of that revert. **The frame is also very dark — that is item 95 (the lighting refactor), not this. Do not fix darkness here.** DONE WHEN: the door reads as a door from the street at the users own approach, both leaves match, the texture density on it is derived from the face it is on rather than typed, and you have the before/after from that spot. |
| 108 | DONE w60 — Mug fixed, but NOT for the stated cause: the handle's hole axis was PARALLEL to the direction it stuck out (measured dot = 1.0000, a real handle is 0), so it was a detached hoop with a 9 mm air gap, not a handle. The item said the rotation was right and not to chase it - the rotation IS right, the OFFSET disagreed with it; moved it to +z so the loop hangs across the sightline against the dark glass. Shared material split (own tone 0xa79f8f), ring now passes 14 mm through the cup wall with 30 mm of open hole, dark disc above the top cap because he looks DOWN at it 22 deg and a flat cap read as a peg. TWO ITEM CLAIMS ARE FALSE: it does NOT sit proud of the sill (0.0000 m on the sill top, 0.072 m inside the room edge, unchanged before and after), and it is NOT three texels wide - it is 20x25 px from the room's own SPAWN, which is why a real handle was worth building. Also found 3 instrument faults, incl. a probe that filed a fully BLACK frame from a 600 ms settle. 0 STATION MISS. Port 4184. notes/w60-mug.md | the mug on the flat 301 windowsill — ct/apartment.ts | **⚡ SECOND REPORT, 2026-08-02 (close-up): *"the mug is messed up."* Promoted, and the desk found the cause — verify then fix.** **`ct/apartment.ts:1906-1912`: the body (`CylinderGeometry(0.038, 0.034, 0.095, 8)`) and the handle (`TorusGeometry(0.026, 0.008, 4, 8)`) SHARE ONE MATERIAL — `mugM`, `0xd8d2c4`.** Identical colour, so there is nothing to separate handle from body and the pair reads as a single lump. **The handle also has FOUR radial segments, which is a square ring, not a curve.** It IS rotated correctly (`rotation.y = Math.PI / 2`), so orientation is not the fault — do not chase that. **The fix is contrast and silhouette, not detail:** at this size the handle needs its own tone (a shadow side, or a darker material) so the eye can find the hole, and enough segments that the ring reads as round. **Original: *"mug looks messed up."*** The white mug on the windowsill in flat 301. In the frame it reads as **an undifferentiated blob**: the handle does not separate from the body at the distance the player actually stands, and the whole thing sits **proud of the sill edge** rather than on it. **The world is unlit 8 px/m — at that scale a mug is about three texels wide, so a handle modelled honestly will not read.** The question to answer is whether it should be **silhouetted** (handle as a notch of background colour rather than a modelled loop) or simply be a smaller, plainer vessel that reads cleanly. **Look at it from where the player stands, not from a close orbit** — this is a small prop and the only test that matters is whether it reads at standing distance and height. Check its footing on the sill in the same pass. DONE WHEN: it reads as a mug from the players own standing position, it sits ON the sill, and you have the before/after from that spot. |
| 100 | DONE w55 — SEVENS is diegetic: PanelSpec.surface called, not rebuilt — ct/hud.ts and crosstown.ts untouched, whole diff is ct/slots.ts + 4 probes. The ATM's answer did NOT transfer: the cabinet is one BoxGeometry(0.6,1.45,0.6) with an ARRAY OF SIX materials and no screen mesh, and hud.ts:899 would throw on it (.color.getHex() on an array) — so slots.ts supplies its own single-material plane, parented to the cabinet, found by a ray from the stool and sized from its bounding box (int-casino.ts not imported, no constants copied). Face re-cut 320x256 landscape -> 320x483 portrait to the cabinet's own aspect, 533 px/m square texels; adds a bill acceptor because the mouse had no way to insert. Clicks route through the same onKey as the keys, so SPACE/B/V/M/I/C all survive. Escape proven from all 6 states with feet actually moving; no DOM panel, no dimmed backdrop. fp: 1018 textures differ, ISOLATED to the three import alone — with it removed and everything else in place, textures/structure/tints IDENTICAL, so the face and surface move nothing. Built bundle green: mouse walk, escape-every-state, L-slots-inworld, L-slots-glass/rtp/feel, K-no-panel-traps, health, bugsweep 96 shots 0 STATION MISS. FOUND NOT FIXED: (1) ct/hud.ts throws on a multi-material mesh instead of degrading — next caller will hit it; (2) int-casino.ts:739 puts the whole cabinet-front texture on the 0.56x0.30 topper box, so every machine wears a tiny second slot machine; (3) live face vs baked face disagree (w41 finding 1, one room over); (4) scripts/K-atm-walk.mjs:157 is stale vs commit 1ab300666 (expects 'thanks' after TAKE CARD) AND prints FAILED while exiting 0. Handoff: notes/w55-slots-diegetic.md. Ports 4183 dev / 4194 preview, both shut down. | ct/slots.ts — **ITEM 86 HAS LANDED. THE GATE IS OPEN. START THIS.** | **The user, with a screenshot: *"slots similarly need to be embedded into the game like i mentioned with the atm. fixed perspective. embedded interactable overlay to make it look realistic and immersion forward."*** **⚡ THE USER ASKED ABOUT THIS DIRECTLY, 2026-08-02: *"slots overlay is not embedded like atm? is it still in flight? its been a while"* — it was NOT in flight; the desk left it ranked 21st after its blocker cleared. It is now top.** **THE FRAMEWORK EXISTS AND IS PROVEN:** `PanelSpec.surface` hangs a panel's own canvas on a named mesh; `crosstown.ts` owns the focus controller; the ATM is the worked example. Read `notes/w41-diegetic-screens.md` — **it carries a seam guide written specifically for this item**, and its author confirmed no re-scope is needed: `hot`/`click` are in **canvas-pixel space**, so a machine hit-tests the layout it drew, and `PanelSpec.key` is untouched so SPACE/B/M/I/C survive. **THIS IS NOT A SEPARATE FEATURE — IT IS THE SECOND APPLICATION OF ITEM 86.** Item 86 (the ATM) is building the mechanism and is explicitly scoped to put it **in the framework, not in `ct/atm.ts`**, precisely because the user said this would be general. **DO NOT START THIS UNTIL 86 HAS LANDED**, and when you do, **if you find yourself writing new mechanism rather than calling 86s, stop — that means 86 was not made reusable and the right move is to say so rather than build a second copy.** The desk has already caused two duplicate-work incidents this session by splitting one job across two rows; do not make it three. **What the slot machine needs, in the users terms:** `[E]` eases the player to a fixed standing pose facing the machine and locks the view; the interface is drawn on the machines **own screen and glass in the world**, not as a DOM panel over a dimmed room; the mouse operates the machines real buttons — `BET ONE`, `MAX BET`, `SPIN`, `CASH OUT` are already drawn as buttons and should be clickable **where they are**; the Win98 hand cursor appears while focused. **The current keyboard shortcuts (SPACE spin, B bet, M max, I insert, C cash out) should keep working** — this is about not forcing a menu, not about removing the keys. **Escape must release from every screen, and releasing must give movement back** (BUILDER-BRIEF §11 — the user has been trapped twice). DONE WHEN: no DOM panel appears; the interface reads as being on the machine; a mouse click operates a real button; the keys still work; Escape always releases; and the mechanism used is **86s, called — not reimplemented**. |
| 143 | DONE w58 — [E] now closes every machine view from ct/hud.ts's shared gate — ATM, slots, blackjack, loan desk, letters and pockets all got it without being edited; Escape kept as the net. Latched on the PHYSICAL key both ways (the [E] that opens cannot close, the [E] that closes cannot re-open) — the real bug was releaseHeld()'s SYNTHETIC keyup disarming the open-side latch microseconds after it armed, so only a trusted release disarms now. ROW'S PREMISE WRONG IN ONE PLACE: the item says the library PC must get [E] from the same place, but library-pc.ts:377 is a free-text search field — a global [E] makes the letter 'e' untypable and ejects the player mid-word (Emma, Frankenstein unsearchable). Added PanelSpec.typing opt-out; NEEDED ONE DECLARATION LINE IN ct/library-pc.ts, WHICH THIS ITEM DOES NOT NAME (queue checked first: nobody held it). Caption now reads '[E] leave' not a bare 'E' — changed after LOOKING: slots read '...C cash out . E' where every sibling token is key+verb. Proved on the BUILT bundle, port 4192: ATM from every reachable screen, slots/blackjack sat on for real, key leaned on in both directions, feet move after every close; mutation-tested 3 ways. bugsweep 0 STATION MISS, health OK, tsc clean. NOT FIXED: ATM 'thanks' screen is now unreachable dead code; probes that shoot right after waitForFunction(__ct) photograph a BLACK world because __ct publishes before geometry finishes — cost a bisect, wants a GOTCHAS entry. Note at notes/w58-e-closes-machines.md | ct/hud.ts panel framework — E closes a machine view, not just Escape | **The user: *"instead of getting out of the atm view or the slots or literally whatever. instread of using esc for that lets make it e."*** **`[E]` opens a machine; `[E]` should close it.** He named ATM, slots and *"literally whatever"*, so **this belongs in the panel framework, not in each machine** — ATM, slots, blackjack and the library PC must all get it from one place. **THE DESK RECOMMENDS KEEPING ESCAPE AS WELL, and this is the desks judgement rather than his instruction — he can overrule it.** Reasons: Escape is the universal expectation for "get me out"; **a view you cannot leave is the worst bug this project ships** (BUILDER-BRIEF §11) and the user has been trapped **twice**, once saying *"no im telling you i cant get up anything i do once i sit down"*; and a second way out costs nothing. **Make `[E]` the one he is told about; keep Escape as the silent net.** **⚠ THE REAL RISK IS TOGGLE BOUNCE.** `[E]` is edge-read once per rendered frame, so the SAME keypress that closes the panel can immediately re-open it — and this project has the scar: a *"stranded forceUp flag that ate a sit after Escape stood you up"*. **Latch it**: the key must be released before it can act again, and the spot must not re-offer on the frame it closed. **A held keypress is how the checks drive `[E]`** (`down` → 90 ms → `up`), so a naive edge test will pass a check and fail a human. **Prove BOTH ways out from EVERY screen each machine has** — the ATM has ten. Escape from each, `[E]` from each, and after each one confirm your feet actually move. DONE WHEN: `[E]` closes every machine view from every screen, Escape still does too, no keypress ever re-opens what it just closed, and movement is returned in all cases. |
| 136 | DONE w58 — The rooftop board's returns, soffit and cap are now painted metal instead of board black — ct/vice.ts, SAME BoxGeometry with its six face groups given three materials, so no geometry is added or moved and the silhouette he framed is unchanged. WHY IT READ BADLY: from his station the board shows the street only its narrow returns and its soffit, and in boardM (#24222a) those are a black wedge against a pale sky — at noon, with every socket unlit, nothing says it is a sign. THE ROW IS RIGHT THAT IT IS WORSE BY DAY AND THAT IS WHY THREE ROUNDS MISSED IT: bulbs are the night half and w51 already did the night half; no socket density changes a noon frame. So this is a cross-section fix, not a noise fix. THE SOFFIT WENT IN BECAUSE OF THE AFTER-FRAME: my first cut repainted only the front return and moved almost nothing — his eye is 5.3 m out and ~18 m BELOW the board, so the 0.5x7.2 m underside is most of what he sees and the front return is the sliver; that frame is kept as board-day-after.png. Trim values DERIVED from the sign's own gold #e8c25a (45% returns, 58% cap), no new colour. MY VERDICT: day is a real improvement — it now reads as a sign cabinet in the marquee's gold family rather than a blade stuck in the roof; night unhurt. HONEST LIMIT: it is still a sign showing the street its edge, and the only thing that would fully fix that is TURNING it, which would put a second SEVENS above the marquee's — the exact duplicate-name fault w51 removed the blade for. I did not turn it. W51'S REMAINING ADVICE IS PARTLY STALE: it asks for the uprights to be lit 'in steel rather than #09080a' but vice.ts:1596-1602 already builds them in steel — re-measure before re-queueing. fp NOT usable (2 materials added, GOTCHAS 75) and not used. bugsweep 0 STATION MISS, health OK, tsc clean, built bundle, port 4192. Note at notes/w58-rooftop-board-edge.md | ct/vice.ts — the rooftop board cabinet seen edge-on (the dark sky shape) | **The dark angular shape against the sky, identified but not fixed.** The user asked what it was in his casino frame; the builder that removed the blade found it: **the rooftop boards own cabinet**, `BoxGeometry(0.5, 6.6, 7.2)` at y 19.4-26, seen **edge-on**, with near-black supports set 2.4-4.8 m *behind* its leading edge and occluded by its own mass. **Same fault class as the blade the user just had removed** — an object whose art is on its faces, showing the street its unlit edge — and that has now appeared **three times** on this one building (the blade twice, this once). Not fixed because it is a geometry change on an object item 132 did not name. **It is WORSE BY DAY than by night**, so judge it in daylight, not only in his night frame. **Read `notes/w51-sevens-blade-and-bulbs.md` and `notes/w46-sevens-facade.md` first** — between them they hold the two prior rounds on exactly this. DONE WHEN: the board shows the street something deliberate from the users own station, checked in DAYLIGHT and at night, with before/after frames. |
| 189 | DONE sixtyeight — 189 DONE. Root cause confirmed as briefed (the desk was right, and sixtysix photographed rather than deduced it): poseFor takes the eye along the face's own normal, a form on a desk points straight up, so reading it IS the look-down gesture the watch rises on. Fix is one term at crosstown.ts:1949 -- hud.watch(rig.pitch < -0.95 && !panelUp(), ...) -- plus panelUp added to the existing ./ct/hud import on line 30. It is a per-frame RECOMPUTE, not an event, so no close path can be missed: Escape, [E], the ATM farewell timeout and any future self-closing panel all restore the watch on the next frame; all three are asserted anyway. Proof: scripts/probes/w68-watch-vs-panel.mjs, 20 assertions on the BUILT bundle (vite preview, port 4240 after ss -ltn). MUTATION RUN: with the term reverted and rebuilt it goes 17 pass / 3 FAIL and the watch covers 14,897 px^2 of the loan form; with the fix, 20/20 and 0 px^2. Four population floors so no phase can pass vacuously -- notably that the head is measurably down (-1.5707 rad at the loan, -1.25 elsewhere) whenever a STOWED assertion is made. Phase 3 uses ct-pockets, a screen-space panel with no surface, because it is the ONLY config where panel-up and head-down are independently controllable, so it isolates the !panelUp() term from the pose. Checked the horizontal case specifically as asked (ct-loan, the bank form). Looked at all three shots: the before reproduces sixtysix's exactly (OFFICER USE ONLY gone, signature rule half covered, caption broken across ENTER), the after is completely clear. tsc clean; sweep 96 shots 0 STATION MISS 0 COVERAGE; health WORLD OK exit 0; 0 console errors. Inherited warnings only (hotel DoorDecl NO BUILDING NAME, THREE.Clock deprecation, willReadFrequently, CONTEXT_LOST_WEBGL per GOTCHAS 80). ONE CHECK OF MINE WAS WRONG AND THE WORLD WAS RIGHT: my first shown/stowed heuristic wanted a third of the element visible and red-flagged all four SHOWN cases at 0.264 -- the element is 847.8 px tall because 600 px of it is forearm off-frame, shown exposes 223.9 px and stowed exposes exactly 0.0, so I moved the check to that binary rather than tuning a threshold to green. TWO THINGS I DID NOT DO, both outside the files this item names (BUILDER-BRIEF 9): (1) the probe is NOT registered in scripts/checks.mjs -- it is written to assert and exit non-zero and this fault will hit the mail (155) and library PC (157), so one line in checks.mjs should adopt it; (2) the WALLET is a bottom-centre DOM element in the same corner and may be the same bug -- opening it calls closePanels() so wallet-then-panel is safe, but I did not test flipping it out while a panel is already up. Note at notes/sixtyeight-watch-vs-panel.md. Also fixed on arrival: my worktree was checked out at the initial commit (BUILDER-BRIEF 0, now 14 agents), reset to add-stick-and-city98 + npm install. | crosstown.ts:1891 — the players wristwatch rises over a diegetic panel on a horizontal surface | **Found by worker sixtysix while building the loan (185), photographed rather than deduced, and it will bite EVERY future panel laid on a flat surface.** `poseFor` takes the players eye along the faces normal. **For a form lying on a desk that normal points STRAIGHT UP, so the player ends up looking down** — and `crosstown.ts:1891` is `hud.watch(rig.pitch < -0.95)`, which raises the wristwatch on exactly that pitch. **Result: the players own watch sits over the bottom edge of the panel.** Sixtysixs first SIGN box was behind a wristwatch. **The fix it suggests is one line: `hud.watch` should stand down while a panel is up.** That is almost certainly right — you are reading a document, not checking the time — but **confirm the watch comes BACK when the panel closes**, including when the panel is closed by `[E]`, by Escape, and by the ATMs farewell timeout, which closes itself. **This matters beyond the loan:** the mail (155), the library PC (157) and any future counter-top panel land on horizontal or near-horizontal faces. **The ATM and slots are VERTICAL, which is why nobody hit this until now.** DONE WHEN: no panel is obscured by the watch, the watch returns on every close path, and you have checked a horizontal-surface panel specifically. |
| 187 | DONE sixtythree — ct/crowd.ts: hs/ws are now per SPAWNED PERSON (bodyScale) not per cast member, widened ASYMMETRICALLY upward then jittered off a LOCAL seeded stream (ct/rng.ts's rnd at build time would shift every tree and pigeon, GOTCHAS 2); strideFor and cadence read the final hs. THE ROW'S NUMBERS ARE WRONG AND THE GAP IS THE FINDING (w63-heights.mjs, measured before I touched anything): the 6 crowd walkers ALREADY had 6 distinct heights — they were never the problem. There are 26 OTHER people placed by citizenSprite() from interior/shop modules and SIXTEEN OF THEM ARE THE IDENTICAL 1.900 m because their callers pass no h at all; 6 distinct heights across 26 people. That is the crowd of one height he is looking at, and it is in ct/citizens.ts + ~20 int-*.ts call sites, NONE of which this item names (BUILDER-BRIEF 9) — a default jitter inside citizenSprite() is one function and closes the request properly. SECOND: hs IS NOT METRES and it nearly cost me the item. citizenPlane is 1.9 m; the PAINTED figure is 55-56 rows of 64 = 1.633-1.662 m at hs 1 (w63-figure-rows.mjs reads every atlas back and counts opaque rows). My first cut widened symmetrically by 1.45 and put the shortest walker at 1.434 m — a child on the pavement, exactly what the row warns against, invisible from the plane height. Corrected: BEFORE 1.512-1.780 m spread 0.268; AFTER 1.496-1.881 m spread 0.385 (+44%), per person 1.496/1.534/1.567/1.719/1.791/1.881, no two within 3 cm. build untouched; ws capped 1.12 because the collider is a fixed ±0.25 box. PROOF on the built bundle: crowd-walk.mjs ALL PASS incl 'all 6/6 feet planted, worst gap 0.000 m' (nobody floats/sinks) and tightest gap past a stopped citizen 1.92 m (the 2 m lane holds); feet-check 16/16 toes point the way they walk; bugsweep 96 shots 0 STATION MISS 0 COVERAGE; health WORLD OK; tsc clean. ROW 93: I did NOT settle it by argument — seats-walk was run on BOTH builds (pre-change crowd.ts checked out of 02e9b62de~1 and rebuilt): 116 FAIL / 103-of-219 IDENTICAL before and after, and every failing seat is at x>=198 i.e. indoors while the walkers never leave the block. seats-walk being 116 red on mainline deserves its own row. Rows 173/174 unaffected: avoidance is x±0.28 and the collider ±0.25, both WIDTHS. Also caught the brief's own trap live: 'node feet-check.mjs | tail' printed EXIT 0 while the script had aborted with MEASURING THE WRONG WORLD — every exit code I quote is from the command, not after a pipe. NOT FIXED: (1) the 16 default-height citizenSprite figures, above; (2) ct/crowd.ts's six meshes carry NO userData at all, so anything filtering on userData.citizen (ct/slots.ts cabinetAhead, my ct/library-pc.ts finder) cannot see them — one line, real trap. Street frame /tmp/w63-crowd-after.png (needed 45 s of settling: the six spawn 16 m apart, so you rarely see two at once — a DENSITY problem, not a height one). Note: notes/w63-crowd-heights.md | ct/crowd.ts — only SIX heights exist and they repeat; give every person their own | **The user: *"make people different heights pls."*** **THE SYSTEM ALREADY EXISTS AND IS GOOD — DO NOT REBUILD IT. The problem is that it is COARSE, and the desk has already measured how coarse, so start from here.** `ct/crowd.ts` defines six cast members, each with a fixed height scale `hs`: **1.09, 0.91, 0.97, 1.05, 0.94, 1.02**. That is a spread of only **±9%**, and — more importantly — **`hs` is a property of the CAST MEMBER, not of the person on screen.** There is no per-instance jitter. **So a street with twenty people on it shows SIX heights, repeated, and the eye reads repetition as sameness.** That is why he cannot see the variation that is technically there. **THE FIX HAS TWO PARTS AND THE SECOND MATTERS MORE:** **(1)** widen the range a little, and **(2)** **give each spawned person their own jitter on top of their cast members `hs`**, so no two people in a frame are identical. **THE GROUNDWORK IS ALREADY DONE FOR YOU AND IT IS WHY THIS IS SAFE:** `notes/CITIZEN-STYLE.md:84` — *"the geometry is translated so the ORIGIN IS AT THE FEET, so scaling height"* never sinks anyone into the floor, and `ct/citizens.ts:514` says the same. **`strideFor(sp, hs)` at `ct/crowd.ts:70` ALREADY takes height into account** (`3.2 * √sp * hs`), so a taller persons legs will swing correctly without you touching it. **⚠ HEIGHT AND WIDTH SCALE INDEPENDENTLY — SCALING HEIGHT ALONE MAKES PEOPLE LOOK STRETCHED.** `ws` exists beside `hs` for this reason. Vary them together, with correlation rather than lockstep — real people are not all the same shape. **DO NOT touch `build`**: that is a SILHOUETTE change baked into the sprite atlas (torso and shoulder half-width, 6/7/8 texels) and is *"separate from the mesh scale, so the tall ones arent just the short ones blown up"* — that separation is deliberate and good. **Read `notes/CITIZEN-STYLE.md` before you start; it is short and it is the contract.** **⚠ THREE LIVE ROWS INTERACT — check before you claim:** **93** (*"when folks sit, they clip"*) — **changing heights can make seated clipping worse, and `ct/citizens.ts:187` records a past bug where a seated figure "stayed at standing height"; if you change scales, re-check every seat**; **173/174** (crowd steering) — the avoidance footprint is `x ± 0.28`, a WIDTH not a height, so it should be unaffected, but confirm; and **172**, where he asked for vertical variety in the park — **same underlying complaint, that the world is too uniform in y.** DONE WHEN: no two people in a frame are the same height, the range is visibly wider without anyone looking stretched or child-sized, seated figures still sit properly on every seat type, nobody floats or sinks, and you have a street-level screenshot with a crowd in it. |
| 186 | DONE sixtyfour — Item 186 DONE, and THE ROW'S DIAGNOSIS IS WRONG ON HIS SURFACE — that is the headline. ct/paint.ts:52's standing cause ('an untextured quad has no grain') DOES NOT APPLY to the alley floor he photographed. MEASURED (scripts/probes/w64-alleymouth-shot.mjs): the floor IS textured, 158x156 at 24 px/m, carrying 19.4% relative grain against the SIDEWALK'S 9.7% — TWICE AS MUCH. What it lacked was brightness: mean luminance 43.3 where the sidewalk is 123.5, and in HIS OWN FRAME (measured off his PNG) those rendered as 14.8 against 49.6, where an sd of 8.4 is about +/-3 levels. The world's grade is MULTIPLICATIVE and bottoms at FLOOR_GROUND 0.045, and this floor is also wet()-registered so updateRain darkens it further — whatever is painted here is SCALED, never offset. There is no shade in this world at all (entirely MeshBasic, no lights), so an alley is only darker than the street if somebody paints it darker. His word 'shadow texture' is literally accurate. FIX 1, ct/alley.ts:90: base #2e3034 -> #42454a, the same blue-grey scaled x1.43 in luminance, landing the mean at 64.7 beside the ROAD'S 61 rather than at a third of the walk. From the same camera and clock the alley rendered 45.2 -> 67.4 and the step against the sidewalk 0.41 -> 0.55. FIX 2, same texture: the NINE stain ellipses at alpha 0.32 and up to 2.2 m across on a 6.6 m floor were the other half of the report — four visible black blobs at MIDDAY before any grade, indistinguishable from a cast shadow when nothing in this world casts one. Now 16 marks at alpha 0.15, at most 1.2 m. Frames /tmp/w64-alley/{before,after,before2,after2,ship}.png. CENSUS: w5-shadow-census.mjs is REGISTERED in checks.mjs (grep it before today: zero hits), with a --selftest that strips the map off the alley floor and takes it red (BARE 62->63, area 145->188 m2, exit 1), and a BASELINE that only ratchets down. CURRENT COUNT 62 meshes / 145 m2 AGAINST B'S 123 / 454, and 61 of the 62 are INDOOR — the whole outdoors has ONE bare ground surface left, 16.7 m2 of tex-ground at (6, 2.6). FIXED TWO BUGS IN THE CENSUS ITSELF, both found by running it: (a) it counted FURNITURE as ground — a library bench is 0.92x3.2 with a horizontal top and its centre sits at y 0.37 inside the band, so it passed every clause; now dy<=0.35, because ground is thin and the deepest real ground box in the world is the 0.10 m sidewalk slab; (b) it MEASURED THE WEATHER — updateRain owns m.color and rewrites it every frame, so folding the tint into luminance gave 30 then 31 on two IDENTICAL runs; it reads the texture canvas only now and seeds Math.random before load as scenedump does, and three consecutive runs agree exactly. THE FOUR COMPLAINTS ct/paint.ts:58 NAMES: library forecourt CLOSED (no bare outdoor civic ground); driveway apron CLOSED (I-flatground: 0 surfaces, 0.0 m2 in the lot); park paths closed in the census's terms though item 89 is his SECOND rejection of that surface and is a separate row; the library INTERIOR blank slab STILL OPEN — the biggest indoor hit is 4.0 m2 at (1072.4, -5.8). ⚠ THE BIG ONE I COULD NOT DO: THE DETECTOR FOR THE DARK VARIANT DOES NOT EXIST AND I FAILED TWICE TO BUILD IT. Registering BARE does NOT answer the row's 'it will come back a seventh time', because BARE WAS GREEN ON THE EXACT SURFACE HE REPORTED — !mat.map is structurally blind to a textured-but-black floor. Attempt 1, ratio alone: I set 0.45 arguing the road-against-kerb step was 0.50; MEASURED, the road is 0.36-0.41 of the walk, DARKER than the 0.35 he rejected, so the complaint and the approved surface are the same number. It fired on 77 surfaces. Attempt 2, ratio plus FLUSH (a kerb is a 0.15 m step that explains a tone change; a shadow has no thickness): cut 77 to 28 and removed the nonsense pairings, then THE SELFTEST WENT GREEN because the alley floor's top is 0.07 m below the walk's — the clause excludes the very surface it was built for. So STEP prints as a DIAGNOSTIC and is NOT gated: a threshold I have twice been wrong about must not become a red light people learn to ignore (BRIEF 7, GOTCHAS 58, and the script's own note about masonry.mjs crying wolf on 42 of 109). All 28 rows print with their numbers for the third attempt. MY UNTESTED GUESS FOR IT: what separates the road from the alley is neither tone nor height but that the road is CONTINUOUS AND IDENTIFIED — lane markings, camber, kerbs both sides — while the alley floor is an isolated patch with a straight edge and nothing on it. PROOF, built bundle (preview 4202) as well as dev 4201: census green 62/62 and 145/146, --selftest exit 1, three runs identical, identical on dev and built; checks-registered no longer lists it; I-flatground 0 flat-colour in the lot; A-flat-ground exit 0; bugsweep 0 STATION MISS 0 COVERAGE; health WORLD OK exit 0; tsc clean. fp is deliberately NOT cited — this changes a texture's pixels, which is exactly what the texture fingerprint hashes. ALSO FOUND, NOT MINE, PRE-EXISTING: checks-registered is red on texdensity.mjs (has a --selftest, in no tier — item 161's file); checks-can-fail is red on w40-bed-vs-door (registered, no declared failing path); and every one of the census's 61 indoor hits reports '(unattributed)', so nobody can be told which module owns them — the interiors want userData.mod stamps before that number means much. Note: notes/w64-shadow-census.md | the "shadow texture" class — it is NOT a shadow, and the census that finds it is unregistered | **The user, for at LEAST the sixth time: *"get rid of shadow texture here pls."*** `ct/jail.ts:928` already records item 114 as *"his FIFTH report of the class"*, and before that: *"shadow fence still here. shadow geometry in general needs to be removed."* **HE KEEPS REPORTING IT BECAUSE WE KEEP FIXING THE WRONG THING. READ THIS BEFORE YOU DELETE ANYTHING.** **THERE IS NO SHADOW. `ct/paint.ts:52-60` already holds the diagnosis and it is a good one:** *"an untextured quad has no grain for the eye to attach to and no joints to give it scale, so it reads as a **TINT OVER the paving** rather than as a piece of paving."* **B measured 123 ground-facing surfaces in that state, about 454 m²** — civic 14 surfaces / 92 m², lot 12 / 82, street 27 / 43 — and the same comment names it as the cause of **four separate user complaints**: the shadow-geometry patches at the library forecourt, the driveway apron reading as a flat grey plane, the blank slab in the library interior, and the park paths reading as road. **This screenshot — the dark ground at the alley mouth by the phone booth and dumpster — is the next one in that list.** **SO THE FIX IS NOT TO REMOVE A SHADOW MESH. IT IS TO GIVE THE GROUND QUAD A TEXTURE THAT KNOWS WHAT IT IS.** **The tools already exist and you must adopt them rather than invent a generic one:** `ct/tex-ground.ts` has **`walkTex`, `apronTex`, `plazaTex`** — *"each an ANSWER for one surface: this is the sidewalk, this is an apron, these are civic flags"* — and `ct/paint.ts` is the fallback for everything else and **deliberately keeps the colour you already chose** (*"do not repaint anyones"*). **Fix the surface in his frame FIRST so he sees it gone, then work the census.** **⚠ AND HERE IS WHY IT WILL COME BACK A SEVENTH TIME IF YOU STOP THERE: `scripts/w5-shadow-census.mjs` EXISTS AND IS NOT REGISTERED IN `checks.mjs`.** Grep it — zero hits. **We built the detector for this exact class of bug and never wired it in.** That is the third unregistered-or-blind check found this week, after `masonry.mjs` measuring zero faces (GOTCHAS 79) and `texdensity.mjs` (item 161). **Register it, make it count untextured ground-facing surfaces, and BASELINE the number so it can only go down.** DONE WHEN: the ground in his screenshot reads as paving, `w5-shadow-census` is registered and running with a ratcheted baseline, you report the current count against Bs 123 / 454 m², and you say which of the four named complaints are now also fixed. |
| 185 | DONE sixtysix — Item 185: the loan application is DIEGETIC — it is painted on the room's own application-form mesh (a single-material PlaneGeometry(0.30,0.40) lying face up on the blotter), so the overlay is not a screen standing in for the paper, it IS the paper. ct/hud.ts and crosstown.ts are NOT touched: the whole diff is ct/int-bank.ts plus two probes. One field, PanelSpec.surface. Canvas re-cut 300x214 landscape -> 300x400 from the plane's OWN geometry at 1000 px/m both ways (square texels). The amount became five TICK BOXES because a caret row is nothing to a pointer (w41's PIN pad, w55's bill acceptor, same lesson) — declared once, read by both painter and hit test. Signing is the second act and moves NO money; WINDOW 2 still counts it out, so the desk's scope read is kept and I agree with it. Keyboard/wheel untouched, routed through the one setAmount. REVERSES int-bank.ts:1161's documented 'not a screen over it' decision BY REQUEST (BUILDER-BRIEF 6a) — comment left standing with a pointer to the note, since its WINDOW 2 half is still the design. NEW FINDING, cost the most: a diegetic panel on a HORIZONTAL face raises the player's own WRISTWATCH over its bottom edge — poseFor takes the eye along the face's normal (straight up here) and crosstown.ts:1891 is hud.watch(rig.pitch < -0.95), so the pose that makes a form readable is the pose that checks the time. My first SIGN box was behind a wristwatch, photographed not deduced. Every live band now ends by canvas y 300, standoff 0.55->0.60; eye lands at 1.354 over a 0.754 form, so the 1.05-1.75 clamp is NOT biting. PROOF: scripts/probes/w66-loan-mouse.mjs 15/15 by REAL PAGE CLICKS onto the mesh (three tick boxes each becoming the ONLY one ticked, SIGN stamping green, sheet stays up, ESC stands you up, feet move 1.55 m, [E] also exits); M-bank-int-walk ok 36s; K-atm-walk ok (money still one purse, ATM agrees); K-no-panel-traps ok; health WORLD OK; sweep 96 shots 0 STATION MISS 0 COVERAGE; tsc clean. Both of the probe's own reds were the INSTRUMENT (Math.abs(undefined-0.30)>1e-6 is NaN>1e-6 = false, so the mesh filter adopted the wrong geometry; and you stand up FACING THE DESK so W alone reads 0.00 m). FOUND NOT FIXED: (1) hud.watch should stand down while a panel is up — one line in crosstown.ts:1891, desk-owned, and the next horizontal surface will present it as 'the button does not work'; (2) ct/hud.ts:1091 STILL throws on a multi-material mesh (item 150) — this tenant only dodged it because the form happens to be single-material; (3) a diegetic panel still cannot nominate where its caption goes (w41 filed, w55 re-filed) — here it lands across the watch face; (4) SITTING IN THE CLIENT CHAIR STILL OPENS NOTHING — ctx.seat has no onSit, and I deliberately did NOT copy ct/slots.ts's __ct.seated() poll, because a room opening one panel from two unrelated mechanisms is how they drift; one field on ctx.seat closes it for both rooms and that is crosstown.ts. Handoff: notes/w66-loan-diegetic.md. ALSO: item 161 earlier this session shared scripts/checks.mjs with worker sixtyseven's item 182 — mainline merged CLEANLY at f43bb5254 with NO conflict because 182 had not landed; notes/w66-density-guards-registered.md names exactly where the two changes meet in the check loop and which lines must survive the resolution. | ct/int-bank.ts loan desk + the item-86 framework — FIFTH tenant, and it OVERRULES a design decision | **The user: *"the load [loan] application process should also be like atm and whatnot. you sit and its the loan process as an integrated overlay."*** Fifth application of the diegetic framework, after the ATM (86, done), slots (100), mail (155) and the library PC (157). **⚠ READ THIS FIRST: YOU ARE OVERTURNING A DELIBERATE, DOCUMENTED DECISION, AND THAT IS CORRECT.** `ct/int-bank.ts:1161` says the loan was built as three separate `[E]` interactions *"rather than as a screen over it, because every other verb in this world is an `[E]` on an object you can walk up to and this one should not be the exception."* **That reasoning was good when it was written and the user has now asked for the exception anyway. BUILDER-BRIEF §6a: his words outrank it. Do not argue with him in a comment — but DO leave a note saying the earlier decision was reversed by request, so the next reader is not confused by a comment that now contradicts the code.** **WHAT EXISTS TODAY, so you do not go hunting:** the loan desk sits at `DESK_X 4.4, DESK_Z 1.70`, the officers chair at `OFF_Z 0.82` and **the CLIENTS CHAIR at `CLI_Z 2.62`** — **he says "you sit", and the chair he means is already there.** Three interactions make the system: **(1)** the application form on the desk, `[E]` cycles the amount; **(2)** the loan officer across the desk, `[E]` hands it over — she approves or declines **and the decline says why**; **(3)** Window 2 at the teller line, `[E]` collects the cash and `[E]` pays it back. **THE DESKS READ ON SCOPE, and say so in your DONE message if you disagree: he asked for the loan APPLICATION to be an overlay, so (1) and (2) go on the screen and (3) STAYS IN THE ROOM.** The comment explains why (3) matters — *"the officer approves the loan and the TELLER counts it out, so the two halves of the room are one system"* — and collapsing the whole bank into one panel would throw that away. **SOMETHING GOOD WILL BE LOST AND YOU SHOULD KNOW WHAT IT IS:** today the form and the officer are separate spots a foot apart, resolved by the aim rule sorting on off-axis angle (`fp.ts`, `key = offAxis + d * 0.02`) — *"sitting in the client chair looking straight ahead you get HER, and looking down-left at the desk you get the FORM. That is the interaction — you read the form, then you look up and hand it over."* **That is a genuinely elegant piece of design. Try to keep its SPIRIT in the overlay** — reading, then handing over, should still feel like two acts. **CALL THE FRAMEWORK, DO NOT REBUILD IT:** `PanelSpec.surface`, `crosstown.ts` owns the focus controller. Read `notes/w41-diegetic-screens.md` and `notes/w55-slots-diegetic.md`. **Unlike the library PC (157) there is no free-text field here** — the form cycles amounts — so `PanelSpec.typing` is not needed. **Known framework gap: `ct/hud.ts` THROWS on a multi-material mesh (item 150)** — check the surface you intend to hang this on. **THE MONEY IS REAL AND SHARED:** `ctx.purse.cash`, the same number the wallet and the pavement ATM read. **Take a loan and the ATM must still say so.** The 5% down payment is tuned against the games economy (you start with $14.50) — **do not retune it**; at 10% the smallest loan is refused and the feature reads as broken on first contact. **`[E]` must still exit, and Escape must still work.** DONE WHEN: you sit in the client chair and the application runs as a diegetic overlay, the mouse operates it, the decline still says why, the teller still counts the cash out, `ctx.purse.cash` still agrees with the ATM and wallet, both exits work, and the mechanism used is the frameworks. |
| 184 | DONE sixtyseven — Item 184: ALL THREE DONE, and THE DESK'S BRIEF WAS RIGHT BUT INCOMPLETE IN THE WAY THAT MATTERED — the digit handler shadowing the shortcut broke the MOUSE too, which is almost certainly what the user was doing. clickAt routes clicks through onKey (deliberately, one dispatch) but encoded a soft-key press as THE STRING OF ITS NUMBER, so a click on CANCEL called onKey('5') and the PIN branch ate it: CLICKING CANCEL TYPED A 5 INTO THE PIN, while hotAt still offered a hand cursor over it. ROOT CAUSE: one namespace doing two jobs — a soft-key press and a digit were the same token, ambiguous on exactly one screen. FIX keeps single-dispatch and fixes the encoding: softKey() -> 'soft0'..'soft7', handled FIRST and on every screen; a digit on a menu screen re-enters through the same path instead of a second copy of the dispatch. CANCEL also now clears the entry (it left half-typed digits in pin — harmless until enrolment gave something a reason to read them). KEYBOARD CANCEL IS CLR ON AN EMPTY PIN: CANCEL's number is 5 and 5 is a digit this screen may eat, a collision no encoding removes and the user spotted himself, so the hatch is the machine's OWN key rather than an invented letter; CLR on a part-typed PIN still deletes. padActs had to stop gating CLR on pin.length>0 or the one escape key would draw dead. (2) AUTO-SUBMIT IS 240ms, NOT ZERO, AND THE BEAT IS LOAD-BEARING: padActs gates ENT on pin.length===4, so an instant submit would make ENT a key that can NEVER be live — the exact affordance logic the item warned about. With the beat you see the 4th star land and ENT+CLR stay meaningful. (3) ENROLMENT: first visit stores, later visits must match, wrong PIN clears and says INCORRECT PIN in the withdraw screen's existing orange, retry unlimited — NO strike count, NO lockout, per the item. Tube says CHOOSE A PIN / THIS WILL BE YOUR PIN first visit, ENTER YOUR PIN / CLR CANCELS after. PROOF: scripts/w67-atm-pin.mjs, 24 assertions ALL PASS, driven with the REAL POINTER (page.mouse at points projected from each control's own mesh) because the complaint is a control that LOOKED live and was not; it asks the machine where its controls are via the new __atm.buttonPoint rather than retyping BTN_Y/R_EDGE, and hit-tests the CANCEL point back to the CANCEL row before anything rests on it. EACH FIX CAUGHT INDEPENDENTLY: clickAt reverted -> RED with pin=3, the user's bug reproduced exactly; auto-submit removed -> RED, 9 fails; enrolment never stores -> RED on ONLY the 2 enrolment assertions (6 fails) with CANCEL and auto-submit still green, so the assertions are targeted not a cascade. REGISTERED in CHECKS with canfail case atm-cancel-shadowed: CAUGHT, file restored byte-for-byte. FIXED A FLAW IN MY OWN CHECK: the first mutation run threw on a waitForFunction and printed a stack trace instead of the verdict, losing the 10 assertions after it — waits are now soft and name what they waited for. LOOKED at all 5 states (shots/w67-atm-*.png): every string fits the 236px tube and CANCEL lines up against a lit button 5. typecheck 0, build 0, health 0 WORLD OK, sweep 0 (96 shots, 0 STATION MISS, 0 COVERAGE), all re-run after merge on build 503136984. Port 4230. SHARED FILES with worker sixtysix (item 161): scripts/checks.mjs AND scripts/canfail.mjs — NO CONFLICT; 161 landed before my final merge and both survived, texdensity registered at checks.mjs:261 and w67-atm-pin at :1116, my item-182 classifier still called at both sites. I appended my row at the FAR END of the array sixtysix was editing mid-file, which is what made it automatic. checks-registered and checks-can-fail re-run after the merge show ONLY their pre-existing reds — my check is registered AND has a declared failing path, so it added neither. NOT FIXED, for the desk: (a) the bottom hint line overlaps the keypad and the [E] leave label — PRE-EXISTING, visible on the untouched MENU screen with its original 34-char text; my first wording was 74 chars and made it worse, shortened to 48 after LOOKING; the real fault is that this hint has no width budget and it is not in ct/atm.ts. (b) ct/atm.ts still duplicates 12 palette literals from ct/bank.ts because importing would close an import cycle (GOTCHAS 28) — its own header proposes a third module, still outstanding. (c) Purse wants a pin?: string — storedPin is module state because Purse lives in ct/hud.ts which this item does not name; identical behaviour today since openAtm is one module, but it belongs on the card. Note: notes/w67-atm-pin.md | ct/atm.ts — the PIN screen: CANCEL is unreachable, 4 digits should auto-submit, and enrol the PIN | **The user, three things about one screen: *"trying to hit cancel on the pin keypad doesnt work cause its also 5? once you enter 4 digits it auto submits please. also the first time you go to the atm it saves your pin."*** **HE DIAGNOSED THE FIRST ONE HIMSELF AND HE IS RIGHT — the desk has confirmed the mechanism, so do not re-investigate it.** **(1) CANCEL IS UNREACHABLE FROM THE KEYBOARD WHILE ENTERING A PIN.** There is no CANCEL key on the physical pad — `PAD_KEYS` in `ct/atm-face.ts:69` is `1-9, CLR, 0, ENT`. **CANCEL is a FASCIA button beside the screen** (`ct/atm.ts:179`: `{ right: CANCEL, actR: () => go(card) }`), and fascia buttons are triggered by **pressing their number** — the on-screen hint at `:647` says so: *"click a button, or press its number"*. **But `ct/atm.ts:437` runs first and swallows every digit:** `if (/^[0-9]$/.test(k) && pin.length < 4) { pin += k; panel?.repaint(); return; }` — it `return`s, so the fascia handler is never reached. **The digit handler shadows the shortcut, exactly as he guessed.** Note `:487` says the fascia buttons are deliberately *"NOT GATED ON `screen === pin` any more"* — read that comment before changing the gating, someone already thought about this. **Whatever you do, CANCEL must work by CLICK as well** — verify both paths, because he may have been clicking. **(2) AUTO-SUBMIT ON THE FOURTH DIGIT.** Today `:442` needs `k === enter && pin.length === 4`. He wants the fourth digit to submit by itself. **Keep ENT working** — a real machine takes both, and removing it would break the pads own affordance logic at `:524–527`. **This also shrinks the collision in (1)**, since the PIN screen stops eating digits sooner. **(3) "THE FIRST TIME YOU GO TO THE ATM IT SAVES YOUR PIN" — read this as a REQUEST, because it is not what happens now.** **No PIN is stored anywhere in the code:** `:442` opens the menu on ANY four digits. So build enrolment — first visit sets your PIN, later visits must match — and **decide what a WRONG pin does** (a real machine says so and lets you retry; three strikes eating the card is period-true and is a nice touch, but do not build a lockout he did not ask for). **⚠ THE THREE ARE ONE STORY AND THAT IS WHY THEY ARE ONE ITEM: with CANCEL unreachable and no auto-submit, a mistyped PIN cannot be escaped — and once (3) exists, a typo you cannot cancel becomes your PIN permanently.** Fix (1) and (2) before (3) is worth having. **CLR already works (`:438` backspace, `:526`) — make sure it still does.** **The ATM is the reference implementation of the diegetic framework** (three more tenants queued: 155, 157, and slots 100) — do not break `PanelSpec.surface` or the pointer path. DONE WHEN: CANCEL works by click AND by key while the PIN screen is up, the fourth digit submits on its own, ENT and CLR still work, a PIN is remembered between visits, a wrong PIN is handled sensibly, and you have driven the whole flow with the mouse from a standing position at the machine. |
| 182 | DONE sixtyseven — Item 182: THE ITEM'S STATED CAUSE IS WRONG — npm run build does NOT kill the preview. Measured (scripts/probes/w67-does-build-kill-preview.mjs, polling a live preview through one build): 1175 of 6935 polls returned HTTP 404 between 0.67s and 0.89s, ZERO refused connections, still listening afterwards. ROOT CAUSE: vite build EMPTIES dist/ before writing and vite preview serves dist/ statically, so a healthy server has no page for ~220ms; checks.mjs asked response.ok, which is false for that 404 exactly as for ECONNREFUSED, and serverDied was a latch NEVER re-tested — one 220ms blink condemned every remaining check of a 12-minute run. So fixing only the wording (the item's cheap option) would have left the latch discarding good runs. FIX: classifier extracted to scripts/lib/server-state.mjs with 3 answers (ok/empty/dead) plus a 6s recovery pass giving a 4th, 'recovered'; a recovered blink marks THAT ONE check BUILD RACE and the run CARRIES ON instead of latching; pre-flight now separates 'alive but dist/ is empty' from 'nothing is serving'; three footers, one per cause, each naming the fix and the re-run command. PORT HALF: BUILDER-BRIEF.md s4's curl recipe replaced with ss -ltn — 000 does not mean free, it means nothing spoke HTTP, and a bound LISTENING socket answers 000; demonstrated on a bare TCP listener at 4239 (ss: LISTEN 0 511 127.0.0.1:4239 / curl: 000). scripts/reap-servers.sh counted free ports the SAME wrong way, so the script whose job is keeping ports available reported bound ports as free — one ss call replaces 20 curls, verified 17/20 against ground truth 4186/4190/4191 taken. GOTCHAS 81's claim that the build kills the preview RETRACTED as 81a with the numbers. PROOF: scripts/probes/w67-server-state-cases.mjs, 12 cases, all green — 6 drive the real classifier (imported, not retyped) through every answer, 6 drive checks.mjs end-to-end and assert the TEXT a builder reads; RED on 7 of 12 under two mutations (pre-flight branch disabled, 'empty' reclassified 'dead'), files restored byte-for-byte. Full fast-tier npm run checks on 4230: 158 rows, 91 ok / 17 red / 50 skipped, ZERO SERVER DIED / dist EMPTY / BUILD RACE / TREE MOVED — status-neutral, no false positives. All 17 reds pre-existing and other items: I ran the 3 that could have been mine — checks-registered is red ONLY on texdensity.mjs being unregistered (that is item 161), checks-can-fail on w40-bed-vs-door, mutations-quote-real-source on 4 dead cases in ct/bank.ts+ct/props.ts which I never opened. typecheck 0, build 0, health 0 WORLD OK, sweep 0 (96 shots, 0 STATION MISS, 0 COVERAGE), all re-run after merge on build 75d0767db. Port 4230, held all session, survived 4 builds. SHARED FILE with worker sixtysix (item 161): NO conflict in checks.mjs because 161 had not landed when I merged — our regions are disjoint (mine: import line 35, pre-flight branch, loop call sites, footers; its: the CHECKS array) so sixtysix is the one that will see the conflict and must keep BOTH. I DID resolve a conflict in notes/GOTCHAS.md — mainline added S82 while I rewrote S81's tail into 81a; kept both, 0 markers left. NOT FIXED, for the desk: scripts/guards.sh:26 uses a THIRD port-probe method (/dev/tcp connect — sound, but three methods for one question wants a shared helper); reap-servers.sh --dry exits before printing the free-port count so the number only appears on a run that kills servers; reap-servers.sh run from inside an isolated agent worktree resolves ROOT to that worktree and so reported MY OWN live preview as reapable — a desk running it from the wrong directory would kill working builders' servers. Note: notes/w67-server-died-was-a-lie.md | scripts/checks.mjs — `npm run build` while a preview is serving KILLS it, and every check then lies | **Reported by worker sixtyone. A time-waster with a misleading symptom, which is the class we have decided to stop shipping.** Running `npm run build` while a preview server is serving **kills the preview**, and every check afterwards reports **`SERVER DIED (unmeasured)`**. **The builder who hits this sees a wall of dead checks and goes looking at their own change.** This is the same family as `health.mjs` exiting 0 on a dead world (fixed 2026-08-02) and `interiors-walk.mjs` crashing against a built bundle (item 164): **the message does not describe the actual problem.** Either stop the build from killing the server, or make `SERVER DIED (unmeasured)` say plainly *"your preview was killed — most likely by a build running against the same tree; restart it and re-run"*. **The second is acceptable and is minutes of work.** **While you are here, one more from the same report: `ss -ltn` is the ONLY reliable free-port check.** Sixtyone found port 4183 answering `000` to curl and then refusing to bind, because another builder took it in between. **Builders are told to prove their port is free; the method they are told to use does not work.** Fix the guidance in `notes/BUILDER-BRIEF.md` and anywhere a script probes a port. DONE WHEN: a killed preview reports why, the port advice is corrected in the brief, and a builder hitting either one is pointed at the real cause. |
| 181 | DONE sixtythree — crosstown.ts publishes __ct.painted() -> {frames,triangles,calls} from renderer.info.render (the NUMBERS, not the renderer: every caller reads this through page.evaluate and a WebGLRenderer does not survive serialisation). New scripts/lib/painted.mjs: waitPainted() waits for the frame count to advance WITH triangles>0 — frames alone is a half-answer because a render call that drew nothing advances it and still leaves black — and it THROWS rather than degrading to no wait; blackFraction() lets a probe refuse to report on the void. GOTCHAS 78 rewritten (it named afterFrames as one of three honest options and it is NOT one) and 80 updated. THE ROW'S PREMISE IS NOT TRUE ON THIS MACHINE AND I COULD NOT MAKE IT TRUE: 17 attempts against the built bundle, 3 recipes each on a COLD browser context, plus 6 concurrent copies per GOTCHAS 30 — 0.0% BLACK EVERY TIME, with 13-15 frames already drawn and 982 triangles by the moment __ct first answered (/tmp/w63-painted-A0-ctonly.png is that instant and it is a fully painted bedroom). So the guard is worth having but it is NOT established that rAF was sixtyone's cause; CONTEXT_LOST_WEBGL (bugsweep logs it here, and NO frame counter can see it), a genuinely cold preview (sixtyone 1136ms vs my ~300ms) and viewport (1280x720 vs 1000x640) are all unruled-out and at least as likely. That is now written into GOTCHAS 80 rather than left in a note. MY OWN FIRST INSTRUMENT WAS THE BAD ONE: newPage() shares the HTTP cache, so recipes 2 and 3 measured a warm bundle — a cold-start race measured against a warm cache reports itself absent. PROOF: w63-painted.mjs 5/5 on the built bundle including 'waitPainted THROWS on a build with no __ct.painted', watched failing by deleting __ct.painted; w63-painted-load.sh 6-way 0 failures; health.mjs WORLD OK; tsc clean. Adopted it in my own w63-arm-angle.mjs (item 165), which now exits 3 on a >90% black frame — measured NOTHING, not measured BAD (GOTCHAS 32). NOT FIXED, outside my item: 100 of the 347 shooting scripts still wait only on rAF (measured, one line each); w61-doorflush.mjs has a hand-rolled PNG-size version that should be replaced; a lost WebGL context still defeats every counter including mine and a webglcontextlost flag on __ct would catch it; w63-painted.mjs is not registered in checks.mjs. Note: notes/w63-painted-frames.md | crosstown.ts:1339 — publish the renderer on __ct so a probe can ask whether a frame was PAINTED | **Reported by worker sixtyone, and it undermines every screenshot-based probe we have.** GOTCHAS 78 says to wait `afterFrames` before shooting. **`afterFrames` IS NOT SUFFICIENT: rAF fires whether or not the renderer actually drew.** Sixtyone hit this head-on — **all 8 of its built-bundle frames came back SOLID BLACK while the same bundles scene graph read perfectly**, and the first genuinely painted frame did not arrive until **1136 ms**. A probe that shoots on a frame count photographs a black screen and reports it as evidence. **THE FIX IS SMALL AND THE PAYOFF IS LARGE: `crosstown.ts:1339` does not publish the renderer on `__ct`, so a probe has no way to ask how many frames have been PAINTED.** Publish it (or publish a paint counter derived from it) so probes can wait on `render.info.render.frame` advancing rather than on rAF ticks. **Then update GOTCHAS 78 to say so** — it currently gives advice that is not safe, and a builder following it exactly will shoot black frames and not know why. **Consider auditing the probes that already shoot** — any that wait only on rAF are suspect, and *"I looked at the screenshot"* is a verification method we rely on for the items where looking is the only proof (165, 167). DONE WHEN: a probe can wait for a genuinely painted frame, GOTCHAS 78 is corrected, and shooting the built bundle no longer returns black. |
| 178 | DONE sixtyfour — Item 178 DONE, and THE ROW'S DIAGNOSIS IS STALE — it says in bold 'NONE IS EVER PLACED ... grepping all of src/ finds ZERO importers ... do not re-investigate'. That was true when it was written and is not true now: the WHOLE feature landed in mainline while the row sat open. ct/apartment.ts:2172+ 'PACKAGES ON THE LANDINGS' has the parcel meshes, the per-door-per-day hash with a final avalanche, side-of-door placement derived from DOORS, floor-gated colliders, two [E] spots per door, giveRandom() wired to the pockets and a published scene.userData.packages hook — and scripts/packages.mjs is a REGISTERED check that is green on ten claims. So I built nothing; I measured why he cannot see what exists. THE CAUSE IS A UNIT NOBODY CONVERTED: crosstown.ts:423 is 'one real second = one game minute', so a game DAY IS 24 REAL MINUTES. PKG_CHANCE was declared as a small chance per day, which sounds like a chance per day and is a chance per 24 minutes of play. MEASURED over 120 days at 0.08, sampling the world's own hash through packages.list() rather than re-deriving it (scripts/probes/w64-pkg-landing3.mjs): building-wide 76 parcels on 50% of days; HIS LANDING (301/302) 19 parcels on 14% of days; LONGEST RUN WITH NOTHING OUTSIDE 301 OR 302 = 19 DAYS = 7.6 REAL HOURS OF PLAY. That is not a rare event, it is an invisible feature, and his sentence is the correct report of it. CHANGED ONE CONSTANT, ct/apartment.ts PKG_CHANCE 0.08 -> 0.20, holding both of his statements — he asked for 'a small chance' and he says he has never seen one — against the row's own opposite failure, 'a landing with a parcel at every door reads as a depot'. AFTER, same 120-day measurement: building-wide 208 on 81% of days, his landing 52 on 38% of days, longest gap 19 -> 10 days, and the building averages 1.73 of its 8 doors so most doors are still empty on most days. AND I WALKED HIS OWN LANDING, WHICH NOTHING HAD: scripts/packages.mjs proves the arithmetic and the GROUND floor only — it takes the first present parcel, deterministically 102's — while his sentence is about floor 3 where 301 is his flat and 302 is the neighbour he sees. scripts/probes/w64-pkg-landing3.mjs, 6/6 on the BUILT BUNDLE: both floor-3 doors carry a parcel when forced (301 at (200.26,-15.69) side +1, 302 at (202.15,-17.32) side -1); the landing offers '[E] steal 302's package'; [E] takes it off the landing; the HUD says what was in it ('pair of trainers - two sizes too big, and white.', and 'pack of tube socks' on a second run); and THE LANDING STILL WALKS after — 0.86 m backwards, so the parcel's collider does not trap you. Frames /tmp/w64-pkg/landing3{,-after}.png. PROOF, all on preview 4202: packages.mjs all ten still green at the new rate ('89 parcels over 40 days - 35 days had any at all, most ever on one day was 5'; its rarity clause is withAny<40 so 35/40 keeps a 5-day margin); bugsweep 0 STATION MISS 0 COVERAGE; health WORLD OK exit 0; npm test 17/17; tsc clean; my own item-186 census still 62/62 and 145/146. fp not cited: the diff is one number and two probe files, no geometry and no texture. FOUND NOT FIXED: (1) 0.20 IS A JUDGEMENT and it is one knob — if he says it is now too busy, PKG_CHANCE is the single constant and notes/w64-packages.md has the arithmetic for picking another; show him a frame rather than the number. (2) THE LONG TAIL IS INHERENT to an independent daily roll — even at 0.20 the longest gap outside his own two doors is 10 days, 4 real hours. If that is still too long the answer is a different mechanism (a pity timer guaranteeing one on his landing after N empty days), not a bigger number, and he has not asked for it. (3) scripts/packages.mjs never walks above the ground floor; a merged version of it and my probe would be better than two scripts. Note: notes/w64-packages.md | ct/apartment.ts landings + ct/inventory.ts — packages exist in theory ONLY. Put them on the doors. | **The user: *"i havent seen a single package outside my neighbors doors?"*** **HE IS RIGHT AND THE ANSWER IS THAT NONE IS EVER PLACED. The desk has already proven this, so do not re-investigate:** `ct/inventory.ts` defines `PACKAGE_TABLE` (`:165`) and `rollPackage()` (`:247`), and **grepping all of `src/` finds ZERO importers of either.** The loot half of this feature is finished and good — a table of 1997 mail-order contents, *"a videotape, a pair of trainers, a small appliance, and the disappointment, which is the joke"* — and **the half that puts a package outside a door was never built.** The comment at `:158` records the handoff that stalled: *"builder C is putting packages on the walk-up landings, and *if you steal you get a random item and then it goes in your inventory*."* **BUILD THE MISSING HALF. Do not redesign the table; it is done and it is funny.** **What is needed: a package MESH outside neighbours doors on the walk-up landings, an `[E]` spot to take it, and the existing `rollPackage()` wired to inventory on the take.** `ct/tenancy.ts:160` already says the roll *"is rolled once when you steal it"*, so read that first — some of the wiring may already be waiting for you. **THE LANDING GEOMETRY IS DELICATE AND DOCUMENTED — READ IT BEFORE PLACING ANYTHING.** `ct/apartment.ts:194–255`: the landing is 2.6 m deep, and *"the landing geometry, the floor-picker and the guard collider all read"* from the same derived constants, with a probe (`scripts/probes/w28-227-landing.mjs`) behind the clip resolution. **A package must not foul the stairs, the guard collider or the doorway, and must not be reachable through a wall.** **Not every door, and not forever** — a landing with a parcel at every door reads as a depot. A few, and they should feel like they arrived. **VERIFY BY WALKING THE WALK-UP**, taking one, and confirming it lands in inventory. DONE WHEN: packages appear outside neighbours doors, you can take one, `rollPackage()` supplies the contents, it enters your inventory, the landing still walks cleanly, and you can say how many spawn and where. |
| 176 | DONE sixtythree — HANDED BACK UN-STARTED, no world file edited — BOTH halves of this row are outside the three files it names, and each is a one-line correction to the row rather than a day of work. (1) THE EXTERIOR OF BOTH BUILDINGS IS ct/vice.ts, NOT ct/street.ts. street.ts:913 dispatches them away: 'if (vice.VICE.includes(b.nm)) vice.placeShell(...)' plus vice.placeSigns(sideSpans), and street.ts's own comment says 'SEVENS and HOTEL ORPHEUS are built by ct/vice.ts... street.ts still owns where they stand: the roster above and this cursor.' So street.ts owns TWO NUMBERS AND TWO NAMES; the facades, canopy, blade sign and rooftop pylon — every pixel of 'reads as one establishment' — are in vice.ts. AND A RENAME IN street.ts ALONE IS A BREAK, NOT A FIX: nm is the key into vice.VICE and into placeSigns, so renaming SEVENS makes the dispatch miss and the casino gets rebuilt as a generic shopfront with no sign. ITEM 176 NEEDS ct/vice.ts ADDED TO ITS FILE LIST. (2) THE INTERNAL DOORWAY CANNOT BE A DOORWAY, AND THE REGION CULL IS NOT WHY. Measured on the built bundle (scripts/probes/w63-orpheus-belt.mjs, committed): casino centre x 680, hotel centre x 920, 240 m apart, **229.0 m OF DEAD GROUND BETWEEN THEIR WALLS**, with CHURCH and DINER parked in between. ct/interior.ts:45 is SLAB_X0=400, SLAB_W=80 and every room takes the next 80 m slab IN BUILD ORDER (bank/bodega/burger/casino/church/diner/hotel/jail/library/pawn/tax/thrift). Two rooms that do not touch cannot share an opening whatever the cull does — and crosstown.ts's region cull hides only top-level children ENTIRELY WEST OF x=100 while the player is east of it; it has NO per-room behaviour at all. GOTCHAS 79's line that the cull 'hides every interior you are not currently standing in' describes room.inside() gating, not REGION_X, and that conflation is what put the wrong blocker in this row. SO 'walk from one into the other' = an [E] in each room that jumps to the other's threshold, dressed as an interior door, the same mechanism every street door already uses — and THAT is buildable in the two int-*.ts files the row does name. ALSO: the row's justification for the pinned width is misattributed — it cites ct/apartment.ts pinning the walk-up door, which is the EAST roster on the main block; these two are on NORTH2, the side street, whose constraint is 'both rosters stop dead on x=57'. Same conclusion (23.55 must hold, redistribute freely) but you will not find the apartment reason on this roster. Note with everything the next holder needs: notes/w63-orpheus-blocked.md | ct/street.ts + int-hotel.ts + int-casino.ts — the Orpheus Hotel & Casino, one property | **The user: *"make it a combo orpheus hotel and casino. connect them internally and outside. i should be able to walk from one into the other."*** **THE BIGGEST ITEM ON THE BOARD. Read all of this. If you cannot finish it, do the EXTERIOR half and hand back the interior half with notes — do not half-connect the interiors and leave a door to nowhere.** **THE FACT THAT MAKES THIS FEASIBLE, ALREADY CHECKED SO YOU DO NOT HAVE TO: THE TWO ARE ALREADY NEIGHBOURS.** `ct/street.ts:332` is `{ nm: HOTEL ORPHEUS, col: #6a4a2c, w: 12, brick: #7a4a3a, floors: 5 }` and **:333** is `{ nm: SEVENS, col: #8a2c42, w: 11.55, brick: #5c4436, floors: 4 }` — consecutive rows in the same EAST roster. Nothing has to move for them to become one property. **⚠ THE WIDTHS ARE LOAD-BEARING. 12 + 11.55 = 23.55 AND THAT TOTAL MUST NOT CHANGE.** The rosters own comments say the run totals are pinned because **`ct/apartment.ts` fixes the walk-ups door and interior at a set z** — change the total and **you move the players own front door.** You may redistribute between the two frontages; you may not change their sum. **THE FOUR DELIVERABLES, in his words — *"connect them internally and outside"* and *"walk from one into the other"*:** **(1) ONE PROPERTY OUTSIDE.** It should read from the pavement as a single establishment, not two shops that happen to touch: shared name, shared frontage language, one canopy or one facade rhythm across the seam. The floors differ (5 and 4) — a real hotel-casino often steps down over the gaming floor, so **use that rather than fighting it.** **(2) THE NAME.** He said *"combo orpheus hotel and casino"* — Orpheus wins, SEVENS becomes the casino wing. The interior already says ORPHEUS in eight places in `int-hotel.ts`. **(3) AN INTERNAL DOORWAY between `int-hotel.ts` (1,133 lines) and `int-casino.ts` (1,403 lines) that you can WALK THROUGH.** This is the hard part and it is a real engineering problem, not decoration: **the interiors are separate modules parked away from the street (x ≈ 199) and the region cull hides an interior you are not standing in** — GOTCHAS 79 is about exactly this and it is why a texture check went blind. **Work out how the cull treats a player mid-way between two interiors before you build the door**, or you will make a doorway that opens onto nothing. **(4) IT MUST ACTUALLY WALK.** Verify by walking hotel → casino → hotel, both directions, and by walking in off the street to each. **DO NOT CHANGE THE GAMES.** Slots, blackjack and roulette live in their own modules (`ct/slots.ts`, `ct/blackjack.ts`) which read their palette off `int-casino.ts`; if you restyle the casino, check them (`slots.ts:818`). **COORDINATE — three live rows touch this:** **121** (the casino sign: he asked to drop the side sign and put flair into the bulbs — **a rename makes 121 partly yours, so talk to whoever holds it**), **96** (*"hotel interior is strange. needs some work"*), and **159** (interior/exterior door leaves, which will now include a new internal door). DONE WHEN: the street reads as one Orpheus Hotel & Casino, the width total is still 23.55, you can walk hotel↔casino in both directions and in from the street to each, the games still work, and you have screenshots from the pavement and from the connecting doorway. |
| 174 | DONE sixtythree — HANDED BACK UN-STARTED, no world file edited — the row's LEAD IS EXACTLY RIGHT and its FILE LIST IS NOT. THE CAUSE IS ONE MISSING CALL, and the correct pattern is already in the file next door: ct/park.ts:91 is 'const solid = (b) => { colliders.push(b); obstacle(b); return b; }' while ct/street.ts:242 is 'const solid = (b) => { colliders.push(b); return b; }'. obstacle is crosstown.ts:330 (propColliders.push + citAvoid.push), so park registers a prop with BOTH the player and the crowd and street registers it with the player ONLY. ct/bodega-corner.ts is handed street.ts's local solid, so the crates at bodega-corner.ts:519 land in street.colliders, get spread into the player's list at crosstown.ts:691, and NEVER REACH citAvoid — crowd.ts:285's x±0.28 footprint test has nothing to find. HOW BIG: not one crate. 16 solid() call sites are on the broken path — ct/street.ts 10, ct/bodega-corner.ts 4, ct/vice.ts 2 — and most are inside per-building loops, so the runtime count is much higher: every shopfront relief, recess and awning post the roster lays down the block. ct/park.ts's 13 are FINE (its own solid calls obstacle); ct/civic.ts's 11 need tracing and were not on my item. I did NOT produce a runtime number and the reason is the second half of this row: citAvoid is a closure local in crosstown.ts with NO accessor on __ct, so the probe the DONE WHEN asks for ('fails if a solid prop is missing from citAvoid') CANNOT BE WRITTEN until __ct publishes the list. 'SOMETIMES' IS EXPLAINED AND IT IS NOT APPROACH-ANGLE: the crates are invisible to the crowd from every angle; what varies is whether a walker's route crosses that ground. THE FIX IS ONE LINE — make ct/street.ts:242 match ct/park.ts:91 — and it fixes all 16 call sites at once without touching any of them; then publish citAvoid on __ct and write the guard, which can be watched failing by reverting that line. ITEM 174 NEEDS ct/street.ts AND crosstown.ts ADDED TO ITS FILE LIST. Note: notes/w63-solid-vs-citavoid.md | ct/crowd.ts citAvoid + ct/bodega-corner.ts — pedestrians walk through the produce crates | **The user: *"pedestrians sometimes clip into the fruit in the sidewalk outside the bodega."*** **A STRONG LEAD, OFFERED AS A LEAD AND NOT AS A CAUSE — PROVE IT BEFORE YOU ACT ON IT.** There are TWO separate collision lists in this world and they are populated separately: `ct/bodega-corner.ts` takes a **`solid: (b: AABB) => AABB`** callback (declared at `:72`, used at `:195` and `:218`) which is what stops **the player**; the crowd steers around a **different** list, **`citAvoid: AABB[]`** (`ct/crowd.ts:21–26`, tested at `:179–182` against a citizen footprint of `x ± 0.28`, `z ± 0.28`). **A prop registered as `solid` but never added to `citAvoid` blocks the player and is invisible to pedestrians — which is exactly the symptom he describes.** The produce crates are built at `ct/bodega-corner.ts` ~439–473. **Check whether they reach `citAvoid` at all.** **IF THAT IS THE CAUSE, THE ONE-CRATE FIX IS THE WRONG FIX.** Two lists that must agree and are filled by hand will drift again, and this is the second class of "why arent we catching these?" bug this week. **Ask whether a prop can declare itself solid ONCE** and have both lists derive from it, or failing that, add a check that lists props present in one and absent from the other. **Say in your DONE message how many OTHER props are in `solid` but not `citAvoid`** — that number is the real size of this bug, and if it is large it deserves its own row. **"Sometimes" is a clue worth chasing:** if pedestrians clip the fruit only occasionally, the crates may be avoided from some approach angles and not others, or only certain citizens are affected — reproduce it deliberately rather than watching until it happens. **Do not shrink or move the crates to dodge the problem** — the awning and produce are a deliberate piece of the corner (`ct/bodega-corner.ts:18`). DONE WHEN: pedestrians walk around the fruit from every approach, the fix works at the list level rather than one crate, you have reported how many other props have the same gap, and there is a probe that fails if a solid prop is missing from `citAvoid`. |
| 165 | DONE sixtythree — hud.ts WATCH_TILT -5deg -> -18deg + WATCH_DROP 30px + the forearm now tapers (column staircase, not an antialiased diagonal) + recede ramp 0.18 -> 0.32. Measured in HIS posture and room at 1280x958 (scripts/probes/w63-arm-angle.mjs, renderer hidden so the brown floor cannot be counted as skin): bottom edge of the screen covered 65.5% -> 42.6%, HUD share of frame 11.5% -> 6.2%, top-edge angle 6.2deg -> 20.7deg. IDENTICAL on the built bundle (4191). w57's own probe reports the LCD texel box 638-681 x 21-43 UNCHANGED, so the watch face is not redrawn. THE ROW'S DIAGNOSIS WAS RIGHT and so was his. TWO THINGS THE ROW DID NOT SAY: (1) the tilt was in THREE places and WRAP_CSS's -6deg had NEVER reached the screen, because hud.watch() overwrites transform on the first frame you look down — one WATCH_TILT now; (2) rotating about a wrist pivot lifts the HAND end 75px at 18deg, so the fist left the bottom-right corner and floated on the floorboards — /tmp/w63-arm-t18.png. My first fix derived the drop as PIVOT*sin(tilt)=75 and that was WRONG: it pushed the LCD half off the bottom (/tmp/w63-arm-t18d.png). 30px is what puts the hand back on the edge with the time still legible. PROOF: w63-arm-stow.mjs 5/5 on the built bundle (comes up, STOWS clear of frame, slides not swings, one tilt both states) and watched failing — stow set to translateY(10%) gives 2 FAILED exit 1. bugsweep 96 shots 0 STATION MISS 0 COVERAGE. health.mjs WORLD OK exit 0 build fa606ff1e. K-no-panel-traps all good. tsc clean. NO fp/fpdiff quoted: this is DOM+CSS on a HUD overlay, a scene fingerprint measures nothing about it. NOT FIXED, outside my item: scripts/probes/w57-watch.mjs asserts nothing and exits 0 whatever it prints, AND its stated goal ('the arm must run to x=0') is superseded by this row — someone must re-point or retire it. Nothing runs my two probes; w63-arm-stow belongs in checks.mjs with a canfail case. Frames: /tmp/w63-arm-{before,after,after1920,built}.png. Note: notes/w63-watch-arm-angle.md | the watch/arm HUD — all three of his arm reports, together | **THIRD REPORT ON THE SAME OBJECT. Supersedes 111, which is now marked SUPERSEDED — do not claim 111.** He has now said, in order: (1) *"can we move the watch arm thing as a whole over to the left a little bit?"*, (2) *"for the watch i would like the rest of the arm (to the left) rendered as well. should be simple. just a continuation of the arm"*, and (3), 2026-08-02, with `/home/erick/Pictures/Screenshots/Screenshot from 2026-08-02 20-29-35.png`: **_"too much arm here i think it shou;ld have a bit of a steeper angle maybe?"_** **READ (3) AGAINST (2), BECAUSE THEY PULL IN OPPOSITE DIRECTIONS AND THAT IS THE WHOLE ITEM.** He asked for MORE arm; he got it; now there is too much of it. He is not retracting (2) — he is telling you the continuation was added along the wrong axis. **In his frame the forearm runs very nearly HORIZONTALLY across the entire width of the bottom of the screen, an unbroken flat slab of one flesh tone with no elbow, no taper and no foreshortening.** That is why it reads as "too much": a real forearm seen from your own eyes goes DOWN and AWAY steeply, so it occupies a corner, not a full-width band. **He has diagnosed it himself and his diagnosis is good: steepen the angle.** Rotate the arm so it enters from the lower-left corner and rises to the watch, rather than lying flat across the bottom — that shortens its apparent length without removing the continuation he asked for in (2). Consider a slight taper toward the wrist and a tone break at the elbow; a single flat quad is what makes it read as a plank. **Do not simply shrink it or crop it** — he asked for the arm to be there. **Do not consult the OLD 111 row for a cause; its diagnosis was lost in the queue rebuild.** Verify the way he sees it: **stand in his position, look down at the floor at roughly the pitch in his screenshot, and screenshot it** — this is a HUD element fixed to the camera, so a walk proves nothing and a scenedump proves nothing. This is one of the few items where LOOKING is the correct verification, because the complaint is purely about apparent proportion. DONE WHEN: the arm enters steeply from the corner, the full continuation is still present, it no longer spans the bottom edge as a flat band, and you have a screenshot from his pitch showing it. |
| 161 | DONE sixtysix — Item 161: registered scripts/texdensity.mjs in checks.mjs (fast tier, 3.0s measured) — checks-registered.mjs was RED on it and is now green. ROW PREMISE (2) IS FALSE: masonry has carried 'true' in the selftest column since 6f3fc33a6, and checks.mjs:1207 appends --selftest for every boolean row, so the flag HAS been passed all along; the run that reported green over zero faces was the PLAIN one, which no edit to checks.mjs could catch. So the real fix is a POPULATION FLOOR: masonry.mjs fails below 250 stamps (305 measured on be9340006), texdensity.mjs below 3000 measurable faces (4087 measured) and refuses --bless below it. Proved the DONE WHEN by hand: re-added the visible===false skip to masonry.mjs and the suite went RED reproducing the historical numbers exactly (7795 meshes, 1903 textured, 0 stamps, FAILED (1)); reverted. Also watched texdensity fail to fail — blinded, pre-floor, it printed 'IMPROVED since... no owner got worse' and exited 0 over 1895 faces instead of 4087. New canfail case masonry-blind (renames the stamp in tex-world.ts; walls unchanged, guard blind) = CAUGHT, every file restored byte-for-byte. checks.mjs also gains --only <name> (a mistyped name exits 2, never an empty green run) and a 6th column so masonry runs BOTH its flag and its canfail case. Baselines preserved: 305 stamps / 16 disagreements all rounding / 0 authored wrong / 188 gross. Full default suite 133 rows through the modified runner: masonry, texdensity, checks-registered all green; 15 reds all pre-existing, none in the density area. sweep 96 shots 0 STATION MISS 0 COVERAGE 0 console errors; health OK; tsc clean. Port 4220. OUTSIDE THE NAMED FILE (BUILDER-BRIEF 9): masonry.mjs, texdensity.mjs, canfail.mjs. FOUND NOT FIXED: checks-can-fail.mjs:95 parses only to end-of-line, so any WRAPPED registry row reads as undeclared — that is the whole of its current red (w40-bed-vs-door, which declares two good canfail cases); mutations-quote-real-source is red on 4 dead needles (rulings-atm, grade-twice, grade-nan, glow-pool); aimed red on 86 scripts; a fresh worktree cannot run masonry.mjs at all because shots/ is absent and writeFileSync throws ENOENT AFTER the verdict, turning a green world red with a stack trace. Handoff: notes/w66-density-guards-registered.md | scripts/checks.mjs — register texdensity.mjs AND masonry --selftest | **THIS IS THE ROW THAT KEEPS THE EIGHTH SILENT CHECK FROM COMING BACK. Do it before the other 107 follow-ups.** Worker sixtytwo found `masonry.mjs` printing `FACES ACTUALLY AUTHORED AT THE WRONG DENSITY: 0` and exiting 0 **while measuring nothing at all**: it skipped `visible === false` meshes, and commit `5016d26b5` (region cull) hides every group west of `REGION_X` plus every unentered interior — which at spawn is **all 305 masonry stamps in the world**. `7792 meshes · 1902 textured · 0 carry a masonry stamp`. The check is fixed, but two gaps remain and both are one line of `checks.mjs`: **(1) `scripts/texdensity.mjs` (new, sweeps every textured face and judges undeclared ones by whether a texel is SQUARE) is not registered at all** — an unregistered check is a check nobody runs; **(2) `masonry --selftest` is never invoked** — it had been failing with exit 2 the entire time and nobody knew, because `checks.mjs` never passes the flag. **A check whose selftest is not itself run is a check that can silently stop detecting** — that is precisely how this one died. Register both. **The registered figures to preserve as the baseline: 305 stamps, 16 disagreements (all rounding), 0 authored wrong, 188 gross faces.** DONE WHEN: both are registered, `npm run checks` runs them, the selftest runs and passes, and deliberately re-adding a visibility filter to masonry.mjs makes the suite go RED rather than green. |
| 160 | DONE sixtythree — scripts/queue-backup.sh (new, 190 lines) + a 1-line trap change in claim.sh:213/done.sh:50/add.sh:46 — QUEUE.md is snapshotted to notes/.queue-history/QUEUE-<epoch>-<pid>.md on every lock release, deduped against the newest, pruned to 200. --selftest runs all three real scripts against a scratch queue and is 6/6 green; watched red by dropping the hook from done.sh (1 FAILED, only that line). Destroy-and-restore of the scratch queue lost ZERO operations. --restore takes the lock and files the broken queue before overwriting. notes/.queue-history/ added to street/.gitignore:12; QUEUE.md stays untracked per 0d1e61de5. Also: done.sh and add.sh now honour CLAIM_QUEUE (claim.sh already did) — that hook is what makes the selftest possible without touching the live queue. NOT DONE, outside my item: nothing RUNS the selftest — scripts/queue-check.sh or an npm script would be its home and neither is named by this row. Live queue is already protected: a snapshot landed at 20:30. | scripts/queue-backup.sh (new) + a hook in claim.sh/done.sh/add.sh | **DESK, 2026-08-02, filed the hour QUEUE.md was destroyed. THE QUEUE HAS NO BACKUP AND IS UNTRACKED, WHICH IS THE WORST OF BOTH WORLDS.** It was untracked on purpose — ordinary git merges were silently reverting builders' DONE rows (commit `0d1e61de5`) — and that decision was right. But it left the single most important coordination file in the project with **no protection of any kind**, and today an aborted merge deleted it outright. Everything the desk had written on ~45 open rows is gone. **BUILD THE CHEAPEST THING THAT WORKS: on every lock release in `claim.sh`, `done.sh` and `add.sh`, copy QUEUE.md to a rotating snapshot** (`notes/.queue-history/QUEUE-<epoch>.md`, keep the last ~200, they are a few KB each). The lock is already held at that moment, so the copy is atomic and costs nothing. **Do NOT re-track QUEUE.md in git to solve this** — that reintroduces the exact reverting bug the untracking fixed, and it is documented. **Do NOT make the snapshot depend on a builder remembering to call it.** DONE WHEN: killing the file mid-session and restoring from the newest snapshot loses at most one queue operation, the history directory is gitignored, and a snapshot is provably written by all three scripts. |
| 109 | DONE w61 — Cut a real doorway on every floor: the walls were pierced only at 2*ST..2*ST+2.1, so six doors (not seven — 302 already had a real opening) sat 10mm PROUD of the plaster. Column now cut per-floor from ST/DOOR_HEAD; leaf hangs on the room face via wallN/WALL_T, which lands on AX(-0.09), the exact x 301's pivot was hand-tuned to. LEAF_W/H hoisted (renamed FLAT_LEAF_* — a different LEAF_W already exists at :3078). Recessing exposed the 0.03 undercut and opened a sight line OUT OF THE BUILDING: sampled #8a97a2, daylight; recess now has a floor. door301.mjs green, bugsweep 0 STATION MISS, all 8 doors walked on all 4 floors + 7m halls + stair climb, verified on the BUILT bundle. All 8 red checks in npm run checks are pre-existing — N-post-waiting reproduced identically at 3185632ae. GOTCHAS 78: afterFrames is NOT enough on a cold preview, all 8 built frames came back black; renderer isn't on __ct, worth queueing. | ct/apartment.ts (the seven non-301 flat doors) | **The user: *"doors in apt are flush with wall on every floor except my floor."*** [DIAGNOSIS LOST — see FEATURE-REQUESTS.md:2564.] Every flat door in the walk-up except the player's own sits flush with the wall; 301's is recessed. Match the rest to whichever is correct and make the choice come from one place. IN FLIGHT — do not claim. |
| 107 | DONE w62 — Item 107: masonry.mjs was GREEN BECAUSE IT MEASURED NOTHING — it skipped visible===false meshes and 5016d26b5's region culler hides all 305 masonry stamps at spawn, so it printed '0 carry a masonry stamp' and exited 0 (its own --selftest had been failing, unrun). The desk's cause (only masonry faces are checked) is true — 7.4% of faces declare a density — but the binding cause is that interiors are hidden, which is why the complaint was a JAIL INTERIOR. Unblinded masonry.mjs (now 305 stamps, 0 authored wrong). New scripts/texdensity.mjs sweeps every textured face and judges undeclared ones by square-texel, needing no declaration. Real defect found in ct/interior.ts, shared by ALL TWELVE interiors: wall end-returns wore the wide face's repeat (2394 px/m vs 11.9), lintels wore the whole room canvas (162 px/m), and short runs were clamped (177). Gross faces 392 -> 188. Verified on the BUILT bundle, bugsweep 0 STATION MISS, geometry identical (0 BoxGeometry differs). NOT DONE, needs an item: texdensity is not registered in checks.mjs (file not named by 107); 188 gross faces remain baselined; 3782 faces cannot declare a density at all because declareSurface declares a KIND, not px/m. | scripts/masonry.mjs -> a world-wide density sweep + the undeclared surfaces | **The user: *"interior jail textures look off. again why aren't we catching these? what's causing them and do we need to set a rule against them so they aren't created?"*** [DIAGNOSIS LOST — see FEATURE-REQUESTS.md:2547.] He is asking for a RULE and a CHECK, not a one-off fix: texture density must derive from the face it lands on (BUILDER-BRIEF §7b). ~76 surfaces were undeclared. IN FLIGHT — do not claim. |
| 157 | DONE sixtyfour — Item 157 DONE — the library PC now paints on its own CRT. ct/hud.ts and crosstown.ts UNTOUCHED; the whole diff is ct/library-pc.ts + 3 probes + 2 lines of w8-frameless-panels. THE ROW'S OPEN QUESTION ANSWERED: item 150's multi-material throw does NOT bite — measured first (scripts/probes/w64-pc-mesh.mjs), int-library.ts:1121 already builds the CRT as PlaneGeometry(0.30,0.24) with ONE material, mats 1, map 20x16, graded false, 1.02 m in front of the chair. So this file builds NO geometry: three is a TYPE-ONLY import and the mesh is found by arithmetic over scene.traverse (dot products off the seat's own yaw), not by a Raycaster. That means fp is a VALID proof here where it was not for w55: textures IDENTICAL, structure IDENTICAL, tints IDENTICAL, places 2 of 8422 differ and both have a partner within 5 cm (pigeons). objects=8422 uniqueTextures=1473 both sides; 'before' taken by checking out mainline's library-pc.ts, rebuilding, re-dumping the same preview. CANVAS RE-CUT 320x220 -> 320x256: 320/220=1.4545 against the glass's 0.30/0.24=1.25 was a 16% horizontal smear. 320x256 = 1066.7 px/m both ways, square texels, and the same face size slots(FACE) and blackjack(FELT) already use. The 36 new rows are not padding — at 320x220 the Minesweeper board ran to MS_Y0 26 + 9*22 = 223 on a 220-TALL CANVAS, so the bottom row of squares was drawn off the canvas; MS_Y0 is 34 now and it ends at 232, asserted by the probe. Aspect is COPIED WITH A CITATION (int-library.ts:1122) because the canvas is a module constant and the mesh resolves at open time — guarded by faceAspect() reading the LIVE local bounding box (never geometry.parameters) on every open, warning past 2%, published via __librarypc.face(). MOUSE: icons, a Win95 close box (= TAB, back to the desktop, NOT out of the machine), CLEAR (= backspace held, same code path), NEW (= r), and a FLAG MODE because ScreenSurface.click carries no mouse button — every rect declared once and read by the painter, hot and click; all clicks route through the same onKey the keyboard uses. FRAMING CHOSEN BY LOOKING at three settings, not by arithmetic: 0.55/42 is very readable and reads as a monitor against a featureless brown field (keyboard 7 deg below the frame); 0.78/50 gets desk+keyboard+mouse+stair but drops 6px type to ~5 screen px; SHIPPED 0.68/46 — glass ~42% of frame height, the CRT's beige case around it, keyboard/mouse/desk edge entering at -30 deg against a -32 deg bottom edge, eye measured at (1082.94, 1.05) looking 9 deg down. PROOF, all on the BUILT BUNDLE (vite preview 4202) as well as dev 4201, both proved 000 first: w64-pc-walk.mjs 36/36 by REAL PAGE CLICKS onto the mesh — and it proves the thing the other three tenants did not have to, that typing still types: 'frankenstein' and 'emma' typed through the page, all 12 characters land, three e's do NOT eject the player, ESC leaves from the catalogue and [E] from the other two, fov handed back 46->88, feet walk 1.42 m after. w2-library-pc all pass; K-no-panel-traps all good; w10-caption-double ALL OK; bugsweep 0 STATION MISS 0 COVERAGE; health WORLD OK exit 0; npm test 17/17; tsc clean. FOUND AND NOT FIXED, and #1 is the big one: (1) YOU CANNOT SIT AT THE PC. ct/int-library.ts sets TERM_TAKEN_Z = BZ0+0.55, which is the 'pc' — the one machine that IS a beige Windows box — so its chair registers no seat (:1274) and the only two 'sit at the computer' seats are the AMBER 1989 serial terminal (z 4.00) and the DEAD one (z 5.05). Sitting down therefore paints a Windows 95 teal desktop onto amber glass, or wakes a screen the room says is switched off. That is the 'one object that does not agree with itself' class he has now caught on the bank door, the ATM palette, the ATM fascia and the slot faces. ONE CONSTANT in a file this item does not name — move the sitter to the amber machine, which is the better joke anyway. (2) ScreenSurface.click carries no mouse button, so no diegetic screen can tell left from right click; additive fix in ct/hud.ts. (3) scripts/w8-frameless-panels.mjs has TWO OTHER stale expectations, both pre-existing and provably not mine (my diff is library-pc.ts + 3 new probes + that check's own 2 library lines): ATM expects 300x214, atm.ts:263 says H=205; slots expects 320x256, slots.ts:855 says FACE={w:320,h:483} which w55 re-cut deliberately. I did NOT update either — I have w55's evidence the slots world is right but none for the ATM, and re-typing an expected value to whatever source says is how a guard stops guarding. (4) scripts/probes/w58-e-closes-machines.mjs is red on one leg, 'ATM: could not reach the card screen (landed on thanks)', stale after mainline db4f31e5c 'ATM: one TAKE CARD press, not two'; all six library legs pass. (5) the desktop is very empty at 5:4 — two icons in a big teal field, left alone deliberately. NOTE ON THE QUEUE ITSELF: my first claim was item 156 (the invisible-wall lamplight) and it was lost when the desk rebuilt QUEUE.md; ~20 min of groundwork on it is committed as scripts/probes/w64-{pooled,poolwhere,poolflags,street,lampwall,scanline,find-view,where}.mjs, and the one finding worth keeping is that ct/props.ts's POOL_FRAG multiplies the pool out by 1-smoothstep(2.2,4.5,y) and SKIPS the block entirely for any fragment above y=4.5, so the tall wedge in his 20-09-54 night frame CANNOT be the per-fragment pool the row blames. Ports 4201 (dev) + 4202 (built preview), both 000 before binding, both shut down. Note: notes/w64-library-pc-diegetic.md | ct/library-pc.ts + the item-86 diegetic framework — FOURTH tenant | **The user: *"i need the pc in the library to be like the atm too. intergrated overlay. realistic setup."*** Fourth application of the diegetic framework, after the ATM (86), slots (100) and mail (155). A 1997 library PC is the most natural fit of the four — it genuinely IS a screen you sit at. **CALL THE FRAMEWORK, DO NOT REBUILD IT:** `PanelSpec.surface` hangs a panel's own canvas on a named mesh; `crosstown.ts` owns the focus controller. Read `notes/w41-diegetic-screens.md` (the mechanism) and `notes/w55-slots-diegetic.md` (the worked second tenant). **⚠ CONSTRAINT THE OTHERS DID NOT HAVE: THE PC HAS A FREE-TEXT SEARCH FIELD** (`ct/library-pc.ts:377`). Item 143 already added a **`PanelSpec.typing` opt-out** because a global `[E]`-to-close made the letter **e** untypable and ejected the player mid-word — *Emma* and *Frankenstein* were unsearchable. **Mouse must operate the screen AND typing must still type.** Known framework gap: `ct/hud.ts` **throws on a multi-material mesh** (item 150) rather than degrading — check the PC's mesh first; if it bites, 150 is the real fix and this waits. Escape must still work; it is the only exit while typing. DONE WHEN: no DOM panel, the interface reads as being on the CRT, mouse operates it, typing including **e** works, both exits work, and the mechanism used is the framework's. |
| 159 | DONE sixtyfive — Item 159: ct/vice.ts now owns ONE leaf angle (LEAF_AJAR=0) and leafPair NO LONGER TAKES ONE, so a caller cannot copy the wrong one. THE ROW UNDERCOUNTED: measured in the built world (scripts/probes/w65-leaf-angles.mjs reads each leaf's WORLD NORMAL, since interior.ts and int-library.ts use opposite sign conventions) it was EIGHT angles across 13 rooms, not five - bank/casino/church/jail 31.5, hotel 28.6, library 48.7, pawn 77.3, the kit 48.7, apt301 90. All six named rooms now read 0.0 (int-bank:246, int-casino:383, int-church:281, int-hotel:268, int-jail:324+340-344, int-library:743). ct/doors.ts was the obvious home for the field and is a TRAP: doors.ts:146 eagerly globs int-*.ts and every one imports only 'type DoorDecl' precisely so there is no runtime edge back - a runtime import closes that cycle and GOTCHAS 28 drops the module from the BUILT BUNDLE ONLY. HEADLINE MEASURED, scripts/probes/w65-jail-both-faces.mjs on the built bundle: street face (61.0,-103.0) 2 leaves 0.0 0.0, lobby face (1000.0,13.0) 2 leaves 0.0 0.0 - PASS. Mutation LEAF_AJAR=0.55 -> exit 1, lobby 31.5/-31.5 with the street face still 0.0; restored -> exit 0. RED AND GREEN BOTH WATCHED. SECOND DEFECT FOUND AND FIXED: shut leaves photographed with a bright grey slit down the meeting stile - 'gap' is documented as the shadow line and with the leaves ajar never had to be one; new doorRebate() in vice.ts puts one dark plane across the opening 0.012m behind the leaves (also closes w60's 'the interior doorway opens onto nothing'). I got it wrong first - DoubleSide was in the comment and not the material, so it was invisible from inside all six rooms, caught only by RE-SHOOTING. WALKED, not screenshotted: interiors-walk.mjs per room, all six print 'ok walking to the inside of the door raises the way-out prompt' and 'ok E at the inside door puts you back on the street' from 0.4m off the shut leaves - the sight-line risk int-pawn.ts:170 documents ('prompt live 0.16m from the spot and null at 0.37m') cannot occur because the spot sits at hd-0.55 and the leaves at hd-0.12, 0.43m PAST it, and shutting them moves them further away. NOT MINE, EACH PROVED: doormatch12 exit 1 / 4 of 12 (burger,diner,tax,thrift) byte-identical table before and after; jail 'keeps its own light' 6/501 identical after checking mainline's 7 files back out; casino/hotel 'customer station' pre-existing. INSTRUMENT FINDING - casino 'keeps its own light after dark' is FLAKY: 109,109,110,0 over four runs across BOTH source versions. I nearly reported the 0 as my fix. interiors-walk.mjs:1130 compares two material-colour samples BY ARRAY INDEX 500/900ms after a clock snap, in a room whose bulbs animate - GOTCHAS 76 in a new file. NOT TOUCHED, both are places a leaf angle is still chosen and both are outside the item (BUILDER-BRIEF 9): (1) ct/interior.ts:1466 SWING=-0.85, the kit leaf worn by burger/diner/tax/thrift at 48.7 and apt301 at 90 - left because those four are ALREADY RED in doormatch12 for wearing the wrong leaf entirely, and the recipe that fixes that hangs leafPair's pair, inheriting LEAF_AJAR for free; one line if the desk wants it sooner: 'const SWING = -LEAF_AJAR;'. (2) ct/int-pawn.ts:185 OPEN=1.35 - DO NOT change without reading its comment, it is the only one of the eight chosen on purpose, quotes the user ('the door reads as SHUT-BUT-OPEN') and records a MEASURED sight-line trap. Also unrelated and pre-existing on mainline: '[interior:hotel] NO BUILDING NAME' fires every bugsweep - int-hotel.ts:82 declares building 'HOTEL ORPHEUS' on its DOOR but its buildRoom spec does not name one, so w57's guard is correctly reporting a dropped declaration; worth a row. Files: ct/vice.ts + the six int-*.ts named by the item; vice.ts and interior.ts are NOT named by the row and I edited only vice.ts, because it already owns leafPair and imports nothing reaching doors.ts. Built bundle after merging mainline 3c81d04b2: typecheck clean, health WORLD OK exit 0, bugsweep 96 shots 0 STATION MISS 0 COVERAGE. fp/fpdiff NOT used and would be invalid - this adds 6 meshes, GOTCHAS 75. Traps I hit: pkill -f vite killed my own shell twice (GOTCHAS 64, in bold, and I did it anyway); interiors-walk cannot run on a built preview at all. Ports 4210/4211, both --strictPort, both released. Note: notes/w65-leaf-angle.md | int-bank/casino/jail/hotel/library.ts + the church — one shared leaf angle | **SUPERSEDES 153 AND FOLDS IN THE CHURCH. The user has reported this TWICE, in two buildings:** *"jail door is still messed up"*, *"jail interior front door also looks bad and doesnt match outside"*, and *"inside door of the church is still mismatched from the doors outside."* **Worker sixty proved it is not a jail bug:** bank, casino, jail, hotel AND library all hang interior leaves ajar while the street face is shut, and `int-jail.ts`'s own comment says its 0.55 was **copied from the casino's**. Fixing the jail alone was refused, correctly — it would make the jail the only building out of step. **DESK DECISION: the STREET face is the truth.** The player sees the exterior first, from further away, more often. **Do NOT go door by door reproducing 0.55 by hand — that is exactly how five buildings came to share one copied mistake.** Find the one place a leaf angle is chosen and make interior and exterior read from the same source. **Survey the CHURCH too**; sixty did not. Sixty already photographed the jail interior from a legal standing position and it is good, so the jail may need nothing once the shared source is right. **`doormatch12` is ALREADY RED on mainline (4 of 12 rooms) for reasons predating this** — do not report it as yours, and do not "fix" it by loosening it. |
| 197 | DONE sixtyeight — 197 DONE. Published __ct.citAvoid() in crosstown.ts beside colliders()/actorColliders()/staticColliders(), following that precedent rather than inventing a mechanism: citAvoid: () => citAvoid.map((b) => ({ ...b, actor: actorBoxes.has(b) })). WHY IT WAS A BLOCKER, one line: colliders stops the PLAYER, citAvoid is what ct/crowd.ts steers PEDESTRIANS around, and only the first was published -- so the DIFFERENCE between the two lists was unobservable, and that difference is exactly what separates 173 (the crate IS listed, steering too weak) from 195 (the crate was NEVER listed). A watcher sees one pedestrian walk through one crate and cannot tell which. THREE DECISIONS, each the item's own instruction or the file's precedent: (1) NUMBERS not the live array as the item required -- each entry a fresh spread, and PROVED not merely claimed: the probe pushes onto the returned array AND writes to a returned box, then re-reads; neither reaches the world. colliders() still returns by reference because interiors-walk --selftest walls doors shut by pushing onto it, so I did not touch it. (2) The actor flag is computed INSIDE the world because it can only be -- it is an identity test against actorBoxes and identity is what does not survive page.evaluate, the same point actorColliders() already makes; cars and citizens push onto citAvoid too, and a citizen's box is 0.5x0.5 which is also plenty of real furniture, so shape could not have separated them. (3) A spread not a hand-typed field list, so rot/minY/maxY/tag come along and the view cannot drift from the type. PROOF: scripts/probes/w68-citavoid.mjs, 7 assertions green, including a population floor (empty list = vacuous) and the two mutation tests. IT ANSWERED 195 ON THE FIRST RUN, which is the item's DONE WHEN: 508 static colliders stop the player, 136 static boxes are in the crowd's list, 359 are in the player's and NOT the crowd's, and 6 of those are on the bodega's stretch -- including two 0.62 x 0.56 boxes at (10.75, -96.41) and (11.45, -96.41), i.e. the fruit he reported. ⚠ FLAG FOR WHOEVER TAKES 195: its row says 'one line' and '16 call sites'. The call sites may be 16; the BOXES are 359 of 508, so 71% of the world's static geometry is currently invisible to pedestrians. Adding all 359 to the avoidance set at once is a STEERING change, not only plumbing, and it may surface 173 rather than resolve it. Worth knowing before the fix lands. The probe deliberately does NOT assert missing === 0 -- 197 owes the question being answerable, whether the answer is zero belongs to 195, and a check that fails on a bug it does not own would also go green when someone else fixes it and credit the wrong item. tsc clean; sweep 96 shots 0 STATION MISS 0 COVERAGE; health WORLD OK exit 0 on build 55692e0f4; 0 console errors. Purely additive, no geometry moved. NOT FIXED: nothing in citAvoid carries a name or tag, so callers key on extents (works, and matches how the red-dump probes already key boxes) -- but 'assert a NAMED prop' is only reachable via coordinates today; giving obstacle() an optional tag is a small separate item and 197 did not ask for it. Note at notes/sixtyeight-citavoid.md. | crosstown.ts — publish `citAvoid` on `__ct` so crowd-avoidance can be PROBED at all | **A one-item blocker sitting under two user-facing bugs (173 and 195), reported by worker sixtythree.** `citAvoid` is the list of solid props the crowd steers around. **It is not published on `__ct`, so no probe can inspect it** — which means the "pedestrians walk through the fruit" bug (195) and the "people get stuck by cars" bug (173) can only be verified by watching, never by assertion. **Every other test affordance in `crosstown.ts` is already a function on `__ct`** (`clock`, `hermit`, `debugSpots`, `colliders`, and now `painted`) — `__ct.colliders()` already serialises the PLAYERs collider list, so this is the exact same job for the crowds. Follow that precedent; do not invent a new mechanism. **Publish the NUMBERS, not the live array** — sixtythrees `painted()` deliberately returns `{frames, triangles, calls}` rather than the renderer, *"every caller reads this through page.evaluate"*, and a probe that can mutate world state is a probe that can lie. DONE WHEN: a probe can list what the crowd avoids, and 195 can assert that a named prop is in the list rather than watching for a clip. |
| 192 | DONE sixtyfour — DESK-CLOSED IN ERROR — THE WORKER WAS ALIVE. The desk read an earlier completion notification as the agent exiting and closed the row while sixtyfour was 6 minutes into a 20-minute suite run, so its own done.sh had nothing to write to. The fix itself is sound and landed. Verified landed on mainline: interiors-walk.mjs:1166/1191 now key on material.uuid, not array index. Two causes, not the one the row named (moving 8 m sample box AND animating bulbs); five runs identical. Pattern found in 3 more probes -> item 209. | scripts/interiors-walk.mjs:1130 — a FLAKY check that nearly certified a false result | **Found by worker sixtyfive, which very nearly reported the flake as its own success — that is why this is ranked high. A FLAKY CHECK IS WORSE THAN NO CHECK, because it teaches builders to discount reds.** The instrument `casino: keeps its own light after dark` returned **109, 109, 110, and 0** across four runs — **on BOTH the changed and the unchanged source.** Sixtyfive says plainly: *"I nearly reported the 0 as my fix."* **THE CAUSE IS NAMED: `scripts/interiors-walk.mjs:1130` compares two material-colour samples BY ARRAY INDEX, in a room whose bulbs ANIMATE.** Index N is not the same bulb from one sample to the next, so the comparison is meaningless the moment anything moves. **That is GOTCHAS 76 reappearing in a new file** — read it before you start. **Fix it so the comparison is stable under animation:** identify the surface you are sampling rather than trusting ordinal position, or sample at a fixed animation phase, or compare a population statistic that does not care about ordering. **Whatever you choose, PROVE IT IS STABLE: run it at least five times on unchanged source and show identical results.** A single green run is not evidence for this row — the broken version passed three times out of four. **⚠ WHILE YOU ARE IN THIS FILE, two known limits are already recorded and are NOT yours to fix here:** it cannot run against a built preview at all (it imports `doors.ts` inside the page — that is item 164), and running it against dev while editing source is useless because HMR reloads the page underneath it. **Say in your DONE message whether any OTHER check in the suite compares by array index** — if this pattern is elsewhere, that is the real size of the problem and deserves its own row. DONE WHEN: the check returns the same answer five runs in a row on unchanged source, it still goes red when the casino genuinely loses its light, and you have reported whether the by-index pattern appears elsewhere. |
| 188 | DONE sixtynine — 188: a seated player can now use what they are aiming at. THE ITEM'S CAUSE WAS STALE — the exit stopped being a spot when ctx.seat made it a state exit, so nothing was out-scoring anything; the whole engine limit was one unconditional 'if (rig.seated) rig.stand()' in crosstown.ts:2077 that threw away the pick it had just computed. fp.ts pickSpot gains opts.seated (aim-free proximity off — the nearest thing to a sitting man is his own chair; reach shrinks to the spot's own r+REACH_MARGIN, no new constant); with opts absent it is unchanged, which is why the doorway-turn number could not move. crosstown.ts consults the pick while seated and JOINS the exit rather than replacing it: '[E] read the loan application  ·  [ESC] stand up'. MEASURED: all 219 seats head-straight still offer only stand up and 0 lose their prompt (probes/w69-seated-offers); 6 of 219 have anything in arm's reach at any heading and every one is right — bed/sleep, 2x bus stop/newspaper, client chair/loan, 2x diner/order fries (probes/w69-what-a-seat-can-reach); the user's case end to end, form opens ON THE PAPER canvasHidden fov45 eye1.354, ONE ESC closes it and leaves the chair, 0 console errors (probes/w69-seated-loan, shots/w69-loan-1..3). seats-walk 112/219 before AND after, same 107 inherited FAILs in the same 7 classes. I-seat-exit 29 released by E, 0 trapped. K-no-panel-traps all good. bugsweep 0 STATION MISS 0 COVERAGE, 96 shots. health WORLD OK. tsc clean. CORRECTION: the '3.26 -> 1.42 do not regress' bar cites an UNLANDED candidate (notes/w54-item140-candidate.patch); mainline is 3.26 and my run reads 3.26/worst 6/14-of-19, identical. NOT DONE, needs its own row: the library terminal is NOT reachable because ct/library-pc.ts registers NO ctx.spot at all — it polls seatedAtComputer() from a frame hook (library-pc.ts:831-841). The engine supports it now; the fix is one ctx.spot on the CRT, in a file item 188 does not name and that GOTCHAS 82 records a collision in. Same for ct/slots.ts. ALSO: opening a diegetic panel from a chair ejects you from the chair on ESC (hud.ts FOCUS.enter calls rig.sit, fp.ts:230 early-returns when already seated); not a trap, one ESC still works, but leave() should re-sit the remembered pose — 5 lines in crosstown.ts:1413-1442, which is inside item 189's region. AND: ctx.seat should NOT get an onSit — it fires on the seat not on aim, and would re-open a panel the player just dismissed. Ports 4250 (preview) + 4251 (dev, for w54's dynamic TS import); both proved with ss -ltn, both --strictPort. Note crosstown.ts overlaps item 189 (sixtyeight, line 1891); my edits are at 380-405/1790/2064-2110 and merged clean. | fp.ts [E] dispatch + ctx.seat — NO SEAT CAN CARRY AN INTERACTION YOU USE WHILE SEATED | **THIS BLOCKS A THING THE USER HAS ASKED FOR REPEATEDLY AND IT IS AN ENGINE LIMIT, NOT A ROOM BUG.** He asked for the loan: *"you sit and its the loan process as an integrated overlay"*, and for the library PC: *"like the atm too. intergrated overlay. realistic setup"* — a PC is a thing you SIT at. **Today you can sit, and then nothing else is reachable.** The limit is documented at `ct/int-bank.ts:1414-1417`, found by walking rather than by reading: *"the stand-up spot is registered at the seat itself, so while you are seated it is at d 0 and NOTHING else can ever win. **No seat in this world can carry an interaction you use while sitting on it.**"* **THE MECHANISM:** the `[E]` dispatch sorts on `key = offAxis + d * 0.02` (`fp.ts`). **A spot you are standing ON has `offAxis` 0 and `d` 0, so it beats everything, however squarely you are looking at something else.** The same footgun already bit the approach point once — putting it in front of the desk *"ate the loan officer"*, and the fix there was to move the approach to the side, which is a workaround, not a cure. **WHAT IS WANTED: while seated, `[E]` should be able to offer what you are LOOKING AT, with standing up still always available.** Stand-up must never become unreachable — **a view you cannot leave is the worst bug we ship (BUILDER-BRIEF §11)** — so do not solve this by demoting stand-up into something you have to aim at. Consider whether the seated case wants a different rule entirely (aim decides, and stand-up is the fallback when you are looking at nothing), and **whether `ctx.seat` should take an `onSit` at all** — worker sixtysix noted it has none and deliberately did NOT copy `ct/slots.ts`s polling workaround, which was the right call. **⚠ `fp.ts` IS THE WORLDS SELECTION RESOLVER AND THREE OTHER ROWS ARE IN IT — 98 (aim reach), 112, and the doorway-turn work.** Read `notes/w66-loan-diegetic.md` first; it is sixtysixs handoff on exactly this. **Measure before and after**: the doorway-turn work is scored in prompt changes per 360° (3.26 → 1.42) and you must not regress it. **VERIFY BY SITTING AND WALKING**, not from a screenshot: sit in the banks client chair, reach the loan; sit at the library PC, reach the PC; and in both, stand up again. DONE WHEN: a seated player can use the thing in front of them, standing up always works from every seat in the world, the doorway-turn numbers do not regress, and every existing seat still behaves. |
| 177 | DONE sixtyeight — 177 DONE (clipping half fully; crowding half addressed and measured). ROOT CAUSE, one line: the coffee station was RELOCATED to the front-left (its own comment records the move) onto floor the deli case already held, and nothing connected the two coordinates -- hd-2.2 and hd-1.5 are each individually reasonable, which is why re-reading either line could never catch it. BOTH HALVES OF HIS COMPLAINT ARE THE SAME TWO FIXTURES, which I did not expect. CLIPPING: swept the whole room as asked, not just his pair. The bench ran 0.36 m INTO the case -- 0.198 m2 of shared collider floor and 0.157 m3 of interpenetrating carcass, the largest overlap in the room by 10x and the only one that was not a wall corner. Now 0. CROWDING: the case's right end left 0.51 m at the mouth of the left aisle against a 0.72 m player, so THE LEFT AISLE COULD NOT BE ENTERED FROM THE FRONT OF THE SHOP AT ALL -- you had to walk the middle aisle to the cooler and come back up. WALKED, not deduced: held-key walk stops the player dead at z 5.53, 11.1 m short. Now 0.98 m and it walks 11.18 m. Standable floor 46.54 -> 47.10 m2. FIX IS A DERIVATION, NOT A NUDGE: the case declares its extents once (they were typed 3x each), the bench's z is DELI_FRONT - gap - CF_W/2, and the case's x derives from the wall face -- it was floating 0.41 m off the left wall, dead floor, and that 0.41 was what jammed the aisle. DID NOT EMPTY THE ROOM: fixture count unchanged, per the item's warning. PROOF: probes/w68-bodega-clip.mjs (collider + fixture-cluster sweep), w68-bodega-walk.mjs (13 assertions, real collision -- MUTATION-CHECKED: fails on the pre-fix bundle at z 5.53), w68-bodega-shots.mjs, w68-yawcheck.mjs. Looked at before/after: the before shows the bench driven straight through the case, chopping its glass meat display mid-run -- that is his photograph; the after has a 60 mm shadow gap and the display runs full length. tsc clean; sweep 96 shots 0 STATION MISS 0 COVERAGE; health WORLD OK; 0 console errors. I CAUSED THREE INSTRUMENT FAULTS AND ALL THREE PASSED GREEN FIRST -- worth the desk's attention: (a) a 1-D lane derivation found 0 LANES and still printed BODEGA WALK OK, because the back-wall cooler projected onto x swallows the room; there is now a population floor. (b) yaw=PI walked five routes into the front wall for 2.4 s and reported the room impassable -- measured instead: yaw 0 is -z. (c) distance-travelled was the wrong assertion; two clear aisles read red at 7.97/8.06 m because the CLOCK was the constraint, not a fixture. All three are documented at the line that bit. FOUND AND NOT FIXED: (1) ct/interior.ts's chamfer jambs interpenetrate its threshold, 2.3 L each -- NOT MY FILE (kit, shared by every chamfered room) and I do not think it is a defect, a sill under the jambs is ordinary joinery; recording it so the next sweep does not re-file it. (2) keeper sprite grazes the counter top by 12 mm, radio rests 10 mm into it -- both the normal resting-on-a-surface idiom. (3) I COULD NOT FIND A MAGAZINE RACK. The item quotes his shot as showing one 'very close in on the right'; int-bodega.ts has no magazine or newspaper fixture at all. I may have fixed the LEFT of his frame and left the right alone -- someone should put the screenshot in front of him. (4) item 174's solid-vs-citAvoid gap: I did NOT look, no pedestrians walk inside the bodega, so no evidence either way. (5) item 115 was not in flight so I could not coordinate; my working definition of 'not crowded' was 'every aisle walks end to end for a 0.72 capsule and the floor is one connected region' -- mechanical and portable if 115 wants it. Note at notes/sixtyeight-bodega-clipping.md. | ct/int-bodega.ts — crowded, and things intersect each other | **The user: *"bodega is a bit crowded and lots of clipping inside."*** Pasted screenshot at the counter end: **the L-shaped serving counter and a second dark cabinet visibly intersect each other** — the cabinets corner passes through the counters return rather than sitting beside it — and a magazine rack stands very close in on the right. **TWO COMPLAINTS, AND THE CLIPPING IS THE OBJECTIVE ONE. FIX THAT FIRST**, because it is provable and does not need taste: **no two pieces of furniture may interpenetrate.** Sweep the whole room for overlapping AABBs rather than nudging the one pair in his frame — he says *"lots of clipping"*, plural, so the pair he photographed is a sample. **THE CROWDING IS THE SECOND HALF and it is the same complaint he made about the library** (*"library is crowded in some areas and spacious in others. try a different layout"*, item **115**) — **read that row and coordinate; if a builder is holding 115, the two of you should agree on what "not crowded" means rather than each inventing it.** **WALK IT, do not judge from a screenshot:** a shop is crowded when you cannot get past the fixtures, so the test is whether you can walk every aisle, reach every `[E]` spot, and get to the counter from both approaches — **the 2 m lane discipline is a street rule but the spirit of it applies indoors.** **Do not empty the room to fix it.** A 1997 corner bodega SHOULD feel dense and stocked; he said *"a bit crowded"*, not bare. Space is what he wants, not subtraction. **⚠ RELATED, DIFFERENT ITEM, DO NOT ABSORB IT: item 174** covers pedestrians clipping the produce crates on the pavement OUTSIDE this shop, and it turns on `solid` versus `citAvoid` — **if you find the same two-list gap indoors, say so in your DONE message** rather than fixing it here, because that is a world-wide problem and deserves its own row. DONE WHEN: no two fixtures interpenetrate anywhere in the bodega, you have walked every aisle and reached every spot, the room still reads as a stocked corner shop, and you have a screenshot from his position. |
