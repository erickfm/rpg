# Completed queue rows — 2026-08-01/02

Archived out of QUEUE.md so the live queue shows only live work.
Two generations of ids ran here, so some numbers appear twice.

| 2 | DONE w14 — item 2: measured zero LANDED rows exist (ledger.sh --stats, ledger-intact.mjs 253/253 intact) -- duplicate of item 12 (DONE w3), which already established this. Nothing to demote/confirm. Flagged 3 stale AUDIT OPEN rows referencing 'the eight LANDED rows' for the desk. notes/w14-verify-landed-rows.md | `notes/LEDGER.md` | **Verify the LANDED rows and the ledger generally.** Confirm or demote each by walking or looking, never by reading code. **Demoting is a success.** GOTCHAS 49: a row can be CONFIRMED and untrue — that is how the shadow geometry survived four reports. |
| 3 | DONE w14 — item 3: re-verified via roomDims() (unchanged since w3's pass) + scripts/stale-coords.py; all 3 flagged candidates (204,262,300) are false positives (1 unrecognized correction phrasing, 2 already-adjudicated non-faults). Zero real stale citations remain -- nothing to repair/demote. Flagged stale-coords.py's narrow regex for the desk. notes/w14-stale-ledger-coords-2.md | `notes/LEDGER.md` | **~15 CONFIRMED rows cite interior coordinates that now name a different room** (everything shifted when `int-bank.ts` was inserted, and again later). They *look* evidenced. Recompute what each names now; repair the citation if the evidence still holds, demote to LANDED if it does not. Partially done — an earlier pass repaired 6. |
| 4 | DONE w14 — item 4: both SHA citations (06f0a1eca, 0c9b5cd7f) already repaired by w2's follow-up (queue item 11) -- independently re-verified with git merge-base --is-ancestor against add-stick-and-city98, both exit 0. Nothing to do; queue text describes a pre-repair state. notes/w14-sha-repair-check.md | `notes/LEDGER.md` | **Two SHA citations (`06f0a1eca`, `0c9b5cd7f`) are presented as live but do not resolve**, and 6 more are honestly flagged unrecoverable. Finish the repair that reached 106 of 114. A citation nobody can reach is not evidence. |
| 8 | DONE w17 — NEW scripts/seat-facing.mjs asserts every registered seat looks at something (rule A nose-to-the-wall, rule B turned-away-from-your-own-furniture); it went red on 105 seats and is now green, mutation-tested red 4 ways. ROOT CAUSE: three modules typed a seat yaw MIRRORED in z - right where cos=0, backwards elsewhere. Fixed int-casino slot stools (96, exactly inverted - players sat facing away from the machines while the NPCs beside them faced them), int-casino gameStool (a+PI replaced by a derived faceAt(); its approach term had the same mirror and was putting craps approach points inside the craps table), int-library reading chairs (yaw 0 against a table at higher z). The row's named file int-tax.ts was ALREADY CORRECT (w9's 5f) - I changed nothing there; mutation M4 proves the check would now catch it. Also removed 9 casino slot stools whose [E] approach points were inside the pit tables (2 had nowhere legal to stand at all). NOT FIXED, needs queueing: scripts/seatface.mjs filters colliders to |minX|<500 and the interior belt starts at x~600, so it has NEVER seen an interior wall and called 222/228 seats clear while 105 faced backwards; 1 roulette seat is still unreachable (0.08m sliver between felt and slot bank, pre-existing, needs a layout call); outdoor benches cannot be covered by rule B as written; seated NPCs are unguarded because citizenSprite keeps facing in a closure. NOTE: my port 4196 was held by a stale server so I used 4189, and an early 'git add -A street/scripts' from the shared checkout swept 2 of w16's in-flight files into my commits - nothing lost, but those diffs are not all mine. | `ct/int-tax.ts` and any room with seats | **A check that fails when a seat faces a wall.** Five facing bugs have shipped from typed or wrongly-referenced yaws — burger barn guy, librarian, casino sitter, park benches, tax seats. `interiors-walk.mjs` already asserts *"the keeper is looking at you, not away"* for shop staff; the same test over every registered seat would have caught all five. **Write the check first, then fix whatever it finds.** |
| 0 | DONE w2 — Closed the flank gap: added thin (0.2m) stone-then-brick screen walls along both property lines from BX to FENCE_X (9.65m), NOT restoring DEPTH per the constraint. Verified with 2 new scripts (w2-jail-look.mjs: 8 camera angles, before/after; w2-jail-aerial.mjs: structural mesh check, +6 walls exactly matching intended profile). interiors-walk.mjs jail 24/25 (pre-existing unrelated FAIL, confirmed via stash before/after). bugsweep.mjs zero STATION MISS on both dev and built bundle. Found a second, much smaller, unrelated gap at the forecourt corner (JAIL.FORE vs SEVENS' flush frontage) -- written up, not fixed, out of scope. | `ct/jail.ts` | **URGENT, USER CALLS IT GAME BREAKING, AND IT IS A REGRESSION THE DESK APPROVED.** *"the jail has empty gaps around it."* This morning's setback fix (`d8987737e`) cut `JAIL.DEPTH` from 12 m to **4.0** (`ct/jail.ts:108`) and pushed the face back by `FORE: 4.0`. The site is x 57-75; the building now occupies only 61-65, so **ten metres of the site are open** where twelve metres of building used to stand, and from any oblique angle you see past its flanks into void. **The desk verified that change from ONE angle — head-on from the middle of the side street, the single view where the facade hides its own flanks — and wrote 'the depth change is not perceptible from the street'. That was wrong.** **CONSTRAINT: do not simply restore DEPTH to 12.** The setback exists because the site was two thirds unwalkable and a warp there wedged the player; the forecourt and the yard behind must survive. Close the flanks so the jail meets its neighbours, keep the walkable ground, and **verify from at least four angles including both obliques** — `node scripts/interiors-walk.mjs jail` must stay green and `bugsweep.mjs` must show zero STATION MISS. |
| 0a | DONE w5 — Fixed 4 real ground-quad shadow-geometry defects (pawn worn patch 20.2m2, church dais 16.6m2, casino runner 4.5m2, jail threshold 1.5m2) via slabTex(); found+fixed a 4x-inflating bug in the census predicate itself (box side faces flagged even when top was textured) plus a GOTCHAS-4 narrow-strip exclusion; world-wide count now 75 meshes/172m2 down from 121/271, verified identical on dev+built bundle; most remaining indoor hits are furniture false positives (pews/shelving/tables), not the ground-quad bug -- see notes/w5-shadow-geometry.md | `ct/paint.ts` callers, world-wide | **THE SHADOW GEOMETRY IS STILL EVERYWHERE — fourth report, and the class was never closed.** *"what is with these shadow geometries. they're all over the place and i need you to get rid of them."* **The desk marked three rows CONFIRMED yesterday having verified ONE location (the civic forecourt). That was wrong — same error as the jail: verify one instance, retire the class.** `notes/AUDIT-shadow-geometry.md` said so at the time: *'a live scan found 131 untextured ground meshes (~1092 m2) remaining'*, naming the car-lot bays and ~92 untextured interior floor meshes. Only the four originally-named spots were ever fixed. **MECHANISM IS KNOWN AND THE CURE EXISTS** — `ct/paint.ts:50-93`: an unmapped ground mesh beside grained neighbours reads as a translucent shadow patch, because nothing here casts real shadows (unlit MeshBasicMaterial). `slabTex`/`plazaTex`/`apronTex` are proven at several sites. **This is a CENSUS, not a hunt:** enumerate every ground-facing mesh with no map, and texture it. Report the count before and after. If a mesh belongs to a file the item does not name, list it for the desk rather than editing it. |
| 0b | DONE w9 — fp.ts pickSpot: split near/looked into two priority tiers so a touching-but-facing-away spot always beats a merely-aimed-at one (comment already claimed this, code didn't do it) -- repro'd with the apartment door-vs-bed case from the ticket; REACH_MARGIN=0.6 half of the item was already fixed 26 Jul (b1707b600, before this was even queued), see notes/w9-interaction-reach.md | `fp.ts` (desk-owned; the claim grants it) | **Interaction reach is far too wide — measured, and it is the unfixed half of an older complaint.** *"the radius for interaction is far too wide. i dont want to be so far from the bed and the option is still to sit on the bed and watch tv."* The bed declares `r 0.7`; **`fp.ts:463` adds `REACH_MARGIN = 0.6` to every spot**, so it is live to **1.3 m**. In July he said *"i feel like i select stuff without even looking at it"* and the desk answered by narrowing the AIM CONE 35.5 -> 15 deg (`lookTolerance`, fp.ts:474) — **the cone was tightened and the reach never was.** **Same bug as his front door from the other side:** standing in the doorway looking in, the bed is inside the cone and the door is at his shoulder outside it, so the bed wins where the door is obviously the thing to use. The hall-side door spot that just landed does NOT settle this alone. **Two separate questions — treat them separately:** (a) `REACH_MARGIN` is global, so lowering it touches every interaction in the world; its own comment justifies 0.60 against *the sacred 2 m walk*, which is an outdoor argument, and a bedroom is not a pavement. (b) **a door you are standing in should beat furniture across the room regardless of margins** — that is the resolver, not the radius. Verify by walking the flat: door at the threshold, bed only when near the bed. |
| 0c | DONE w8 — chrome:'none' in hud.ts (no bezel/screws/title/caption, ESC/hint moved to a DOM caption below the glass) applied to atm.ts, slots.ts, blackjack.ts, library-pc.ts -- each already painted a complete self-contained fascia, so the framework was drawing a second cabinet around a picture of a first one. Verified spot/seat-open + Escape-close + exact canvas size on dev AND built preview (new scripts/w8-frameless-panels.mjs), plus existing K-atm-walk/L-slots-inworld/L-blackjack-inworld/w2-library-pc suites all still pass, bugsweep zero STATION MISS. NOT converted: pockets (needs held-object shape like the wallet, not this machine shape) -- queue separately. Same redundant-chrome bug also in int-bank.ts's loan desk and tenancy.ts's letter panel, neither named by this item -- queue separately. Found a PRE-EXISTING bug (not caused by this change, reproduced on unmodified code via git stash): sit at slots, stand, walk to blackjack table, press E -- offers the prompt but does not seat you; looks like it lives in crosstown.ts/fp.ts seat handling, neither of which this item grants. Full writeup in notes/w8-panel-chrome.md. | `ct/hud.ts` + each panel's module | **STANDING DESIGN LAW: no pop-up menus. Every interface must read as part of the world.** *"i never want there to be menus popping up unless they are embedded to look as if they are in the actual game. i dont want to break immersion."* Today `hud.ts`'s `makePanel` draws a floating bezel with a title bar and an ESC hint, centred on screen — a dialog over a game. **He has already conceded the implementation, which makes this tractable:** *"maybe we lock the view once we sit and we create a table view/slot view/atm view thats actually a fixed dialog but to the player it just looks like its actually the game."* So keep the panel machinery; **sitting locks the camera to a fixed view of the object and the panel is drawn exactly where that object's screen is — no chrome, no title bar, no floating box.** The illusion only has to hold from ONE known camera position. **The TV is the worked example** — you sit on the bed and watch it in the world — so copy its shape. Applies to ATM, slots, blackjack, inventory, and the library PC that landed tonight. **DO THIS BEFORE BUILDING MORE PANELS**, or every new interface is more to convert. **HARD RULE: locking the camera adds a second way to trap the player, and he has already been stuck in a TV seat once — Escape must leave and standing must leave, proven from inside every view.** Start with ONE (the ATM or the slots), get his verdict, then convert the rest. |
| 0g | DONE w11 — Removed the fixed title/feel/controls overlay from index.html's #style-hud + main.ts's load() (item 0d was misfiled against ct/hud.ts, actual location was here). Kept the [E] prompt (separate element, #ct-prompt) and the build-stamp watermark (ct/hud.ts, load-bearing for scripts/lib/which-world.mjs -- different thing, not in scope). Verified tsc/build clean, bugsweep 0 STATION MISS, screenshot on dev+built preview shows overlay gone. Writeup notes/w11-hud-overlay-removed.md. | `src/main.ts` + `index.html` | **Remove the on-screen HUD text — RE-FILED, the desk named the wrong file.** *"get rid of the overlay descriptions here, controlls and all."* Item 0d said `ct/hud.ts`; w4 measured and the overlay actually lives in **`src/main.ts` and `index.html`**, so it correctly released the item without editing. Remove the `CROSSTOWN '97 1/1` title, the *'The small world — one hand-made street'* line and the controls strip. **KEEP the `[E]` prompt** — that is the world telling you a thing is usable. Same law as 0c: nothing on screen that is not in the world. |
| 9e | DONE w12 — item 9e: crosstown.ts now imports JUNCTION_CROSSINGS from tex-ground.ts instead of a hand-typed copy; verified pure refactor via fp before/after (textures+structure byte-identical), tsc/build clean, health.mjs OK -- notes/w12-crossing-constants-export.md | `crosstown.ts` | **The crossing constants are now stale copies.** w4 hoisted `JUNCTION_CROSSINGS` as a real export in `ct/tex-ground.ts` (item 9b), but `crosstown.ts` still carries the hand-copied gap coordinates from before that export existed. **Point it at the export and delete the copy** — a duplicated constant is exactly what left two instruments measuring things that no longer existed. |
| 5j | DONE w12 — item 5j: widened all 4 sub-passable gaps w4 flagged near the library gallery (OPAC/printer 0.44m, trolley/desk 0.64m, gallery shelf-wall 0.46m, gallery shelf-shelf 0.92m) by moving/trimming, all now >=1.0m; verified trap probe 4->0, overlap scan unchanged, interiors-walk 25/25, bugsweep clean on built preview -- notes/w12-library-gallery-traps.md | `ct/int-library.ts` | **Four leftover trap corridors near the library gallery**, flagged by w4 with file and line while fixing item 5g. Sub-0.95 m gaps by `ct/gap.ts`'s own rule — the same measure that proved the newspaper-stand pinch real. **Spread, do not subtract:** the user has twice asked for the library to be MORE ambitious. Verify with V on: no red inside the room. |
| 0d | DONE w4 — item 0d misfiled: ct/hud.ts has nothing to do with this; the overlay text is built in src/main.ts:90-93 (from crosstown.ts's name/feel) + index.html's #style-hud CSS. Released without editing unnamed files, see notes/w4-hud-overlay-text.md for the desk to re-file. | `ct/hud.ts` | **Remove the on-screen HUD text.** *"get rid of the overlay descriptions here, controlls and all."* The bottom-left block: the `CROSSTOWN '97 1/1` title, the *'The small world — one hand-made street'* line, and the whole controls strip. **Same law as 0c (no pop-up menus): nothing on screen that is not in the world.** A title card and a keyboard legend are the most literal break of it. **KEEP the `[E]` prompt** — that is the world telling you a thing is usable and he has never objected to it. Small item, do it before 0c since it is the same file and the same principle. |
| 0e | DONE w10 — Item 0e: silenced the world's [E] prompt while any panel is open (hud.ts prompt() now checks panelUp()) -- root cause was crosstown.ts's per-frame [E] prompt never hiding for a panel, not the hint+ESC combining item 0c already got right. Verified all 4 panels (atm/slots/blackjack/library-pc) dev+built preview with new scripts/w10-caption-double.mjs, plus existing w8-frameless-panels.mjs/K-atm-walk.mjs still green, tsc/build clean, bugsweep zero STATION MISS. Notes: notes/w10-caption-double.md | `ct/hud.ts` | **The frameless panel caption is DOUBLE-RENDERED.** Desk saw it on build `c374d8415`: the strip under the ATM screen reads `press ithe numberedl buttonsteh ESC` — two strings drawn over each other, the panel's own hint and the framework's ESC caption. Item 0c moved the ESC affordance to a DOM caption below the canvas; it did not remove the one already being painted. **Check all four converted panels** (atm, slots, blackjack, library-pc), not just the ATM. |
| 0f | DONE w11 — Fixed: fp.ts stand() never cleared its own forceUp latch, so when Escape stood the player up via hud.ts's synchronous panel-close path (not fp.ts's own consumption path), forceUp was stranded true and fired again on the player's NEXT unrelated sit, un-seating them one frame later -- reproduces exactly (sit slots, stand via Escape -- the only way to stand while a panel has E -- walk to blackjack, E does nothing, 2nd E works). Same root cause as archived notes/L-for-C-escape-eats-the-next-E.md, now closed too. Fix: clear forceUp unconditionally at top of stand(). Verified before/after on dev+built bundle with new scripts/w11-slots-then-blackjack.mjs + w11-reverse-order.mjs, plus L-every-stool-seats-you.mjs twice (L's own repro, now passes), tsc/build/bugsweep clean. Writeup notes/w11-blackjack-after-slots.md. Did not touch hud.ts/crosstown.ts -- not needed. | `ct/hud.ts` or the seat resolver | **You cannot sit at blackjack after standing up from the slots.** Found by w8 while doing something else, and it **reproduces on unmodified code** — sit at a slot machine, stand up, walk to the blackjack table, press E: nothing seats you. This is the trap family the user has already been bitten by twice (the TV seat, his own front door). A seat that silently stops working after an unrelated interaction is worse than one that never worked, because he will not know what he did. **Verify by walking the casino: slots -> stand -> blackjack -> sit, and the reverse order too.** |
| 5i | DONE w4 — dropped the framework's redundant chrome on the loan desk and letter panels, same fix as item 0c (chrome:'cloth' -> 'none'); mutation-tested (fails at old padded sizes 328x274/220x224 on unfixed code, passes at true 300x214/192x178 on the fix); existing M-bank-int-walk.mjs 54/54 and N-post-waiting.mjs unaffected; tsc/build/bugsweep clean; see notes/w4-loan-letter-chrome.md | `ct/int-bank.ts`, `ct/tenancy.ts` | **Two more panels still wear the framework's double cabinet** — the bank's loan desk and the tenancy letter. Same fix as item 0c (`chrome: 'none'`), flagged by w8 as out of its item's named files. Also NOT converted and needing a decision: the pockets/wallet panel, which w8 judged needs the wallet's held-object shape rather than this treatment — **ask the desk before changing it.** |
| 9b | DONE w4 — hoisted JUNCTION_CROSSINGS as a module-level export in tex-ground.ts, buildGround now reads it instead of redeclaring; verified as a pure refactor via fp before/after+fpdiff (textures/structure byte-identical); did NOT touch crosstown.ts's now-redundant copy (desk-owned, not named by this item) -- flagged precisely with the 2-line fix in notes/w4-junction-crossings-export.md | `ct/tex-ground.ts` | **Export the crossing coordinates.** `crosstown.ts` now segments the centre line around the crossings, but the gap coordinates were COPIED from `tex-ground.ts:1351-1352` with a citation, because the item was read-only on that file. A copied constant rots — that is GOTCHAS 56 and it has cost four instruments. Hoist a named export and have the line read it. |
| 9c | DONE w11 — Deleted the dead door-end dais box (leftover from the pre-fix wrong-end chancel attempt, hd-2.2, unreferenced) -- the real nave-width chancel platform built later in the file already covers that ground. Confirmed via grep + a new structural scene scan (scripts/w11-dais-gone.mjs, 0 matching meshes), interiors-walk church 25/25 unchanged, tsc/build/bugsweep clean. Writeup notes/w11-dais-removed.md. | `ct/int-church.ts` | **A dead `dais` box is left in the church**, a leftover from an earlier wrong-end chancel attempt. Flagged by w5 rather than removed because it fell outside that item. Confirm it is unreferenced, then delete it. |
| 9d | DONE w4 — added claim.sh --stale (ages every DOING row, flags >=90m) and --release <item-id> (forces a stuck item back to TODO regardless of holder); found+fixed 2 bugs in my own first cut before trusting it (UTC/local clock mismatch giving a 425m phantom age, and a dash arithmetic error on 10#/leading-zero) via sandbox testing; sh -n/dash -n clean; see notes/w4-stale-claim.md | `scripts/claim.sh` | **A stale claim blocks an item forever.** Item 9 sat `DOING w1` after the desk stopped w1; nothing could take it and nothing said so. `claim.sh` stamps the holder and a time — make it report claims older than a threshold, or let a claim be released by name. **A stale claim is currently indistinguishable from active work**, which is the same class as GOTCHAS 58: the dispatcher cannot see its own state. |
| 1 | DONE w3 — queue's own 'already fixed' claim was false on this checkout (grepped for it, not present) -- measured, reproduced, fixed with a mirrored hall-side spot; extended A-verify-301-door.mjs with the hall case + fixed 2 real timing/display bugs in that check itself; verified failing on unpatched code, passing with fix, dev+built bundle; see notes/w3-301-hall-door.md | Fixed: added a mirrored hall-side [E] spot (200.64) for 301's door — the room-side spot at 199.36 r0.95 never reached the hall and a shut door blocks line-of-sight for the aim fallback too, so the door was unopenable from the landing. Extended A-verify-301-door.mjs with a hall-side station; confirmed it fails on unpatched code and passes on dev + built bundle. | `ct/apartment.ts` | **You cannot open your own front door from the hall.** The `open/close the door` spot sits at x 199.36 r0.95; the hall runs x 200.0–202.4, so its reach dies at 200.31, and the closed leaf is solid. No prompt from the landing at any distance. **Shut your door behind you and it never reopens.** `scripts/A-verify-301-door.mjs` passes because it only ever tests x 199.3 — inside the flat. Fix the spot AND give the check a hall-side case. |
| 2 | DONE w3 — interior.ts default leaf now reads LEAF.frame.colour/glazing (fixes jail's colour+glazing); leaf COUNT (1v2) reverted after it doubled bank/casino/library's doors -- see notes/w3-door-leaf-default.md | `ct/interior.ts` | **7 of 12 rooms have mismatched door faces, and 6 share one cause:** `interior.ts`'s default door leaf is hardcoded and never reads a room's declared `leaves`/`frame`/`glazing`, only width and height. The jail even declares the right leaf and never applies it. Fix the default to honour the declaration; `notes/door-faces-match.md` has the per-room table. Bank is already done as the worked example (`BANK_DOOR` in `ct/bank.ts`). |
| 3 | DONE w1 — item 3: fixed 3 of 6 real gap.ts traps (newspaper stand, returns trolley, printer stand), 2 remaining judged instrument artefacts (gallery shelving, only reachable upstairs where the deck is 3m wide) -- see notes/w1-library-spread.md; could not reproduce the tilted lectern, likely stale/pre-2026-07-25-rebuild; renamed OPAC label to 'sit at the computer' on the amber chair only (pc is permanently occupied, dead is off) | `ct/int-library.ts` | **Library is cramped, and a lectern is tilted.** The V overlay shows its left half nearly all red — red is `ct/gap.ts`'s own sub-0.95 m trap rule, the same threshold parked cars obey. **Spread, do not subtract**: the user has twice asked for the library to be *more* ambitious. Also: a brown plinth carrying a printed panel sits at a drunken angle, clipping the shelving and the floor — establish whether it is a wrong-axis rotation or a stale relative placement, and fix the relationship, not the coordinate. Also rename `label: 'sit at the terminal'` → **exactly** `'sit at the computer'` (item 4 joins by that string). |
| 4 | DONE w2 — Built ct/library-pc.ts (new file, not int-library.ts): a working library PC in the slots.ts/blackjack.ts bridge shape, opening on seat label 'sit at the computer'. Two real apps — a search over 30 real books, and a complete keyboard Minesweeper (safe-first-click, flood fill, flag, win/lose, restart). Item 3's rename hasn't landed in this worktree yet so the seat join itself is unverified end-to-end here (my new scripts/w2-library-pc.mjs correctly aborts that one sub-check rather than faking a pass); everything else verified against dev AND the built bundle, plus world-wired.mjs (12/12) and bugsweep.mjs (0 errors). | *new file* | **A Windows-style PC you can actually use**, opening when the player sits at a library machine. Joins by seat label `'sit at the computer'` — the pattern `slots.ts` and `blackjack.ts` already use; register via `ct/world.ts` auto-incorporation, export `register(ctx)` + `ORDER`. **Do not edit `int-library.ts`.** Two or three apps that genuinely work beat ten stubs — the library catalogue searching real books is the best fit, plus a real game. §11 of the brief applies hardest here. |
| 5 | DONE w3 — added scroll-to-zoom in crosstown.ts, clamped 88(rest)->64, smoothed, disabled behind panels via bubble-vs-capture phase ordering (no hud.ts coupling); 8/8 checks pass on dev+preview, see notes/w3-scroll-zoom.md | `fp.ts` or `crosstown.ts` (desk-owned; the claim grants them) | **Scroll to zoom, with a limit.** *"i want scroll to be zoom. it shouldnt be able to zoom too much though."* Nothing handles the wheel in the world today. **`ct/hud.ts` already owns the wheel while a panel is open** — it is in `BLOCKED` (hud.ts:168) and panels get `spec.wheel` (hud.ts:359) — so scrolling must NOT zoom the world while the ATM, slots or blackjack are up; that is already handled for free, do not break it. The camera is `PerspectiveCamera(88, ...)` at `crosstown.ts:39`; **88 deg is the deliberate wide 1997 look and must stay the resting value.** Zoom pulls in from it and springs back. **Clamp tight — err on too little:** this is a first-person street, not a sniper scope, and *"shouldn't be able to zoom too much"* is the entire spec. Smooth it rather than stepping per notch. Verify by walking and scrolling: sidewalk, indoors, and with a panel open. |
| 5c | DONE w4 — gave the pole sign a cabinet: added a BoxGeometry inset 0.01m from each existing artwork face, filling the 0.38m gap between them so the sign has real depth edge-on/from-below; verified structurally (new scripts/w4-polesign-depth.mjs) + visually at genuine 3/4 angle + tsc/build/bugsweep all clean; see notes/w4-polesign-cabinet.md | `ct/lot.ts` | **The used-car pole sign has no thickness.** *"used car sign is completely flat."* Confirmed at `ct/lot.ts:1109`: it is **two `PlaneGeometry` faces offset +/-0.03 m** either side of the mast, with nothing between them — edge-on or from below it is paper. **Do NOT collapse them into one DoubleSide plane:** GOTCHAS 10 says a DoubleSide sign renders MIRRORED from behind, and a HOTEL blade shipped that way because only the E and L gave it away. Back-to-back planes are the cure for mirroring; they just give no depth. **Build the cabinet**: a box with returns down its edges, deep enough to plausibly house the lamps that light it, keeping the two artwork faces as they are. **THIRD time this sign has come back** — first the panel was tiny against the mast, then it carried four messages and was simplified to one. Read those rows before touching it, and check it still reads from the far side of the street, which is what the last fix was for. |
| 5d | DONE w5 — Pews were sinking into the chancel platform (legs 0.18m below true local floor for the first 4 rows, not just close to the altar) because the pew loop never added CHANCEL_Y like every other sanctuary object does; derived first-pew z from CHANCEL_Z+1.0m clearance instead of the stale altar-footprint literal, threaded through PEW_ROWS/pz/PRAY_Z; 25/25 interiors-walk church on dev, bugsweep clean dev+preview, build clean; flagged an unrelated dead 'dais' box near the door end for the desk -- see notes/w5-church-chancel.md | `ct/int-church.ts` | **Pews run right up to the altar — 0.425 m of clearance.** *"why are there pews where the alter is?"* Measured: the altar is at `z = -hd + 2.4`, 0.75 m deep, so its back edge is `-hd + 2.775`; the first pew row is laid at `-hd + 3.2` (line 192). **A church needs a chancel** — several metres of clear floor in front of the altar, usually a step up and often a rail, because that is where the service happens. The comment at line 156 says rows are *'laid from the ALTAR end'*, so that 3.2 m offset is doing double duty as 'where pews start' and 'how much sanctuary there is', and it was chosen against the WALL, not the altar. **Do not just delete rows** — `PEW_ROWS` derives from the space, so pushing the start back re-flows them; check the count still reads as a nave. His screenshot also shows the nave dense with red trap boxes (sub-0.95 m), so widening here likely helps twice. |
| 5e | DONE w4 — derived bench facing from its own leg (axis-aligned per-leg formula) instead of a bearing to the loop centre, which only squared up at each leg's midpoint; mutation-tested (fails 7/7 on old code, passes 7/7 on fix) with new scripts/w4-bench-square.mjs; existing E-benchface.mjs still 9/9; tsc/build/bugsweep clean; see notes/w4-bench-square.md | `ct/park.ts` | **Park benches sit askew to the path.** *"these park benches are askew. they should be in line with the path."* CAUSE FOUND at `ct/park.ts:1030`: `facingIn` yaws each bench with `Math.atan2(loopCx - bx, loopCz - bz)` — it points at **the centre of the loop**. **That is a radial direction; the path is a straight run.** A bench is square to the path only at the exact midpoint of its side; anywhere else it rotates toward the centre and goes off-square, worsening with distance from the midpoint. **Keep the principle, change the reference** — the comment above that line is right that yaws must never be typed literals (rebuilding the bench once silently reversed every one of them, GOTCHAS 27), so **do not go back to literals**. Derive from the PATH SEGMENT the bench stands on: square to the run, facing the park side. On a loop that still gives *'it looks INTO the park, its back to the fence'*, which was the original requirement, so nothing is traded away. **Fourth time the benches have come back** — also check the old clipping complaints (bin, drinking fountain) have not returned. |
| 5f | DONE w9 — int-tax.ts waiting row: the queued yaw diagnosis had the sign backwards (yaw:0 already faced -z, into the room, correctly) -- real bug was the backrest mesh sitting on the room side instead of the wall side, fixed by mirroring one offset; screenshots + live sit-yaw repro confirm, see notes/w9-tax-waiting-seats.md; also flags seats-walk.mjs failing ~40 unrelated seats consecutively past #200 as a likely harness artifact, not investigated further | `ct/int-tax.ts` | **Tax office waiting seats face the wall.** *"seats in the tax office are reversed."* CAUSE: `ct/int-tax.ts:449` places the row at `WAIT_Z = hd - 0.62`, against the far wall, with **`yaw: 0` typed as a literal**. Forward here is `(sin yaw, cos yaw)`, so yaw 0 faces **+z** — into that wall. **Derive it** from the wall the row stands against, or from the room centre it should face; do not type the opposite literal, which is the same mistake with a different sign. The file already knows the rule — line 185 derives the preparer's facing from the chair positions — so this row is the one place a literal survived. **FIFTH facing bug of this kind** (burger barn guy, librarian, casino sitter, park benches, this). **Bonus, and probably worth more than the fix: add a check that fails when a seat's facing puts a wall in front of it.** `interiors-walk.mjs` already asserts *'the keeper is looking at you, not away'* for shop staff — the same test over seats would have caught all five. |
| 5g | DONE w4 — stated cause (door leaf) was wrong -- entrance leaves carry no collider at all, cannot trap; real cause was the periodicals alcove's newspaper stand centred in its strip, 0.75m bare on both sides. Pushed it flush to the wall; also fixed a second trap that move introduced against the chair (derived its position from the case's known edge + gap.ts's PASSABLE instead of a literal offset). Verified via colliders()+gap.ts probe before/after, J-library-room.mjs 6/6, interiors-walk.mjs library 25/25, tsc/build/bugsweep clean. 4 unrelated pre-existing traps found near gallery/front-hall, NOT fixed, coords in notes/w4-library-alcove-trap.md for the desk to route | `ct/int-library.ts` | **A door leaf makes the rear aisle too cramped.** *"this door is making it a little too cramped in the back of the library."* An open leaf stands proud into the back aisle with the stacks opposite; his own V overlay paints the pinch **red**, i.e. under `ct/gap.ts`'s 0.95 m trap threshold. **Check it together with the library spacing item, which was released just before this arrived** — a spacing pass that ignores a door swinging into the aisle has not fixed the aisle. Options in order: hang the leaf to open the other way, move the doorway, or widen the aisle. **Third fault found in this one room tonight** (cramped stacks, tilted lectern, this), all of them invisible before the collision overlay shipped this morning. |
| 5h | DONE w8 — Split the main+side street centre lines into segments either side of each street's junction crossing (crosstown.ts), deriving dash pitch so splitting doesn't squeeze it. Gap values (XA_Z/XA_HW/XB_X/XB_HW) copied from tex-ground.ts:1351-1352 with citation -- not exported there, item was read-only on that file, so queuing an export as follow-up. Verified structurally via window.__ct.scene() mesh geometry/position (exact match to intended math), tsc/build/bugsweep/health all clean on dev+preview. Writeup: notes/w8-crossing-lines.md. | `crosstown.ts` (desk-owned; the claim grants it) + read `ct/tex-ground.ts` | **The yellow centre line runs straight through the pedestrian crossing.** *"remove the yellow stripes where the cross walk is. it doesnt look right."* Real centre lines stop short of a crossing; ours does not, so two sets of markings share the same asphalt. **Cause is structural:** `crosstown.ts:73` draws the line as ONE plane spanning the whole street with a repeating dash texture (`lineT.repeat.set(1, 38)`), so it has no knowledge of anything else painted on the road — the crossings live in `ct/tex-ground.ts` (B's), a different file. **Break the line into segments that stop short of each crossing, and DERIVE the gaps from where the crossings are actually defined — do not type z values.** A hand-typed gap stops matching the moment a crossing moves, which is exactly how four instruments rotted this week. Read `tex-ground.ts` for the crossing positions; **do not edit it** — if it does not export them, say so and the desk queues that export. Check the side street's line (`line2`) for the same fault. |
| 6 | DONE w3 — released untouched — fp.ts is desk-owned per BUILDER-BRIEF.md/my brief, highest-risk item in the project, needs the desk to start it | `fp.ts` + callers | **Colliders have no height, so you cannot stand on anything.** `fp.ts:9` is `AABB = { minX, maxX, minZ, maxZ }` — every collider is a footprint extruded to infinity. The user: *"i want the collision to be a bit more accurate to the objects. the cars for instance. we should be able to jump on the cars."* Add `minY`/`maxY` and let the floor picker stand on a collider's top when the player is above it; `ct/interior.ts` and `COURT.climbable` are prior art. **DESK-OWNED, HIGHEST RISK IN THE PROJECT — ask before starting.** Every existing `ctx.obstacle` must behave exactly as it does now unless it opts in. |
| 6b | DONE w5 — RELEASED UNTOUCHED — desk-owned, highest-risk item per BUILDER-BRIEF.md/my own brief ('release immediately with a note, do not start'); fp.ts collider-height/oriented-collider work needs the desk to scope it, not a builder to start unasked | `fp.ts` + `ct/gap.ts` (desk-owned; ask first) | **Angled surfaces are approximated by a stair of small boxes.** *"we should fix this so its not just a bunch of separate rectangles and its just made properly."* `AABB` is axis-aligned, so the bodega's 45 degree chamfer is faked with a staircase — measured live, **13 colliders round that corner, four of them tiny** (0.62x0.56, 0.62x0.56, 0.40x0.40, 0.24x0.16 m). **PAIR THIS WITH ITEM 6 (collider height): same root cause.** The primitive has no height and no rotation, and the user has now hit it from three directions — jump on the cars, collision accuracy, and this. An oriented box or a segment collider fixes all three; more small boxes fixes none. **Also fix the false alarm:** `trapAgainst()` paints these red because adjacent boxes sit under 0.95 m apart, but boxes forming ONE wall are not a trap corridor — the overlay currently over-reports worst exactly where the geometry is worst, which costs the tool its credibility. **DESK-OWNED, highest risk in the project: ask before starting.** |
| 7 | DONE w3 — hoisted ATM_PALETTE export in bank.ts (pure refactor, fp before/after IDENTICAL); ct/atm.ts's own duplicate copy still needs a follow-up import, flagged in notes/w3-atm-palette.md | `ct/bank.ts` | Hoist a named `ATM_PALETTE` export. The cabinet's colours are inline literals in a closure; `ct/atm.ts` now carries a cited copy because exporting them needed A's file. Correct today, will rot — §8. |
| 8 | DONE w2 — Fixed church half fully: hoisted CHURCH_FACE + standOff() in int-church.ts, replacing 5 agreeing literals (three copies of -79.5, plus 8.85 and 7.2 as unrelated-looking offsets) with one derived source, mirroring int-jail.ts's JAIL_DOOR/standOf pattern. Verified with interiors-walk.mjs church (25/25, identical coords), world-wired.mjs, bugsweep.mjs on built bundle. Library half (int-library.ts, a 1-line fix, written up in the note) NOT done — it was DOING w1 under item 3 for this item's entire duration, same-file collision per ownership rules. | `ct/church.ts`, `ct/int-church.ts`, library pair | Church and library hand-duplicate their door position instead of sharing one constant, as ten other rooms do (`DECLS.at`, `VICE_DOOR_X`, `JAIL_DOOR`). Correct today, able to drift — and drift is how a five-times request returns a sixth. |
| 9 | DONE w17 — RELEASED UNTOUCHED, not done: item 9 is a verbatim DUPLICATE of item 5 in the ranked block above it (same file scripts/checks.mjs, same text about the suite killing its own preview server and classifying 52 failures). Item 5 is DOING w14 as of 23:45 and w14 has ALREADY LANDED part of it (615e804ff 'Land w14: checks.mjs pre-flight fix + server-death classifier', plus 39649f49b and 632141c90). Working it would have been a same-file cross-builder collision, which BUILDER-BRIEF section 9 says to skip. DESK ACTION: strike row 9 as a duplicate of row 5 so the next builder does not lose the same twenty minutes to it. I touched nothing. | `scripts/checks.mjs` | The full check suite **kills its own preview server** partway through, so ~half its 52 failures are that rather than real faults. Fix the cause, not the symptom — restarting the server after each check would hide a check that kills servers. Then classify all 52: real vs artefact, and queue the real ones. |
| 10 | DONE w3 — audited ~30 candidate rows against live roomDims() (belt shifted twice: bank then jail); 6 were genuinely stale and repaired (thrift, library-stair x2, tax-clock routing, bodega-arrival, church-sitter), rest already correct; see notes/w3-stale-ledger-coords.md | `notes/LEDGER.md` | **~15 CONFIRMED rows cite interior coordinates that now name a different room** (everything moved +80 m in x when `int-bank.ts` was inserted). They *look* evidenced. Recompute what each names now; repair the citation if the evidence still holds, demote to LANDED if it does not. |
| 11 | DONE w2 — Appended a FOLLOW-UP REPAIR note to the AUDIT row (54 of 176 build SHAs...): both previously-bad citations (06f0a1eca, 0c9b5cd7f) now have pickaxe-matched replacements already annotated on their own rows by an earlier AUDITOR SHA REPAIR pass; I independently verified both replacements with git merge-base --is-ancestor against add-stick-and-city98 (not just git cat-file, which note-hashes.mjs itself warns proves little). Updated the honest score to 108 repaired/6 dead/0 wrong. Left the row OPEN -- it stays open for tasks 2 and 3 (items 9/10), not touched here. ledger-intact.mjs: 253/253. note-hashes.mjs: clean. | `notes/LEDGER.md` | Two SHA citations (`06f0a1eca`, `0c9b5cd7f`) are presented as live but do not resolve; 6 more are honestly flagged unrecoverable. Finish the repair started at 106-of-114. |
| 12 | DONE w3 — found and verified the ledger's only LANDED row (church pillars/windows) by walking E's station + re-running E's predicate on dev and built bundle, 4/4 PASS; promoted to CONFIRMED, fixed a malformed table cell; no other LANDED rows exist; see notes/w3-verify-landed.md | — | **Verify the LANDED rows**, and the ledger generally. Confirm or demote by walking or looking, never by reading code. Demoting is a success. |
| 13 | DONE w3 — established the jail's 107 like-for-like seam disagreements (23 faces) as REAL not an artefact: selftest still sound, cluster confined to jail's own site bounds, and visually confirmed (pier draws smaller brick than flanking wall, same declared density); did not touch ct/jail.ts (not named); recommend queuing the flank-wall fix to O; see notes/w3-seampairs-jail.md | `scripts/seampairs*` | 103 brick seam disagreements, sample dominated by the jail block. Establish real-vs-artefact **first** — §7. |
| 5 | DONE w14 — checks.mjs pre-flight fix + server-death classifier; 3 ledger items verified; landed 615e804ff | `scripts/checks.mjs` | **The full check suite kills its own preview server** partway through, so ~half its 52 failures are that rather than real faults. **Fix the cause, not the symptom** — restarting the server after each check would hide a check that kills servers. Then classify all 52 real vs artefact and queue the real ones. |
| 6 | DONE w15 — Item 6 seams fixed, 227 -> 0 (grown from the item's 103-107). ROOT CAUSE, and the item's stated cause was WRONG: shell() in ct/jail.ts handed the FLANK material to material index 0 too, but on BoxGeometry(depth,height,width) the +/-x faces span WIDTH and the +/-z faces span DEPTH, so a depth-sized texture was stretched over a width-sized face — worst case a 4 m canvas over the 14 m BACK wall, 4.57 px/m against a declared 16. w3's note blamed the east FLANK; the flanks measured 15.96-16.09 and were always fine. Fixed with a 'back' material on shell() defaulting to 'face' (derived: both +/-x faces span exactly width x height), 0.2 m end caps on the yard screen walls (was 770 px/m), own-height flanks on the lintel/recess shells, and dressed stone for the five trim boxes that shared one 1 m ashlar canvas (0.08 m sill drew at 200 px/m). Trim density DERIVED from masonry(1,1,0,2).ppm so the 'detail' declaration is not what makes it pass. Mutation-tested: reverting the back slot returns 126 disagreements; seampairs --selftest still PASSES; the walk's collision legs go red when told to walk through the fence. World unmoved: objects 8351->8351, zero geometry params changed, the 3 'places' rows are 4 props decals that reroll y on EVERY unseeded load. bugsweep 0 STATION MISS, walk 8/8, all re-verified on the BUILT bundle. NOTE: this row had already been marked done once and the desk's own QUEUE.md commit d4f511e1b overwrote it — see notes/w15-jail-seams.md. | `scripts/seampairs*` + `ct/jail.ts` | **103-107 brick seam disagreements**, sample dominated by the jail block; a worker established these as REAL, not artefact, with visual confirmation. Now fix them. |
| 7 | DONE w16 — rain was FRUSTUM-CULLED (the directional bug), not a contrast problem; RAIN_N 500->2600, storm strength drives count+speed | `ct/props.ts` | **Rain never gets heavy.** Scanning 72 half-hour steps, peak material opacity was 0.155 (`0.55 * rainLevel`), so `rainLevel` never exceeds ~0.28 — it is permanently a faint drizzle at every hour sampled. The desk added a dark sheath to each drop for contrast but could not judge it because a downpour never arrives. **Raise the ceiling so heavy rain is heavy, then judge the sheath by facing four directions at peak.** The comment at `ct/props.ts:101` records rain was already made "findable" once; this is the same complaint one level deeper. |
| 5b | DONE desk — superseded by w16 item 7: the real cause was culling, not contrast | `ct/props.ts` | **Rain contrast — desk applied the fix, and found a SECOND bug that blocks verifying it.** Cause measured, not guessed: drops per view facing three ways from one spot were **142 / 129 / 126** — the volume is even, so `RAIN_BOX` and the wrap are NOT the bug. It is contrast: the streak was `rgba(214,222,232)` pale blue-white against fog `0x8a97a2` pale blue-grey, so along the street it dissolved into sky while across the street dark brick made it read. **FIX APPLIED: a dark sheath either side of the bright core** — against sky the sheath bites, against brick the core does, and the core stays one texel so the streak does not fatten into 'falling grit' (the previous note's warning, still valid). **SECOND BUG, UNFIXED, and it is why the desk could not confirm the first by eye: THE RAIN NEVER GETS HEAVY.** Scanning 72 half-hour steps, peak material opacity was **0.155** — `0.55 * rainLevel`, so `rainLevel` never exceeds ~0.28. It is permanently a faint drizzle, in every direction, at every hour sampled. **Whoever takes this: raise the ceiling so a downpour is a downpour, THEN judge the sheath by walking four directions at peak.** The comment at `ct/props.ts:101` says rain was already made more findable once — this is the same complaint one level deeper. |
| 1 | DONE w13 — Height/stage 1-3 of item 1 done: opt-in AABB.maxY, height-aware blocked()/floor-picker, pickup bed floor made standable and walked onto (jump, land at 0.500, walk off, drop to 0.000 - dev+built bundle). Also fixed gap.ts's trapAgainst() false alarm at the bodega chamfer staircase (root cause: pairwise check counted a wall as a hole in itself; fixed with an interval-union fill check). Rotation NOT done - queued explicitly, see notes/w13-collider-volume.md. jump-walk/interiors-walk(bodega,diner)/bugsweep all clean on dev+built. | `fp.ts` + `ct/gap.ts` + callers | **COLLIDERS HAVE NO HEIGHT AND NO ROTATION — the user has reported this from three directions.** *"we should be able to jump on the cars"*, *"i want the collision to be a bit more accurate to the objects"*, and *"we should fix this so its not just a bunch of separate rectangles and its just made properly."* `fp.ts:9` is `AABB = { minX, maxX, minZ, maxZ }`: every collider is a footprint extruded to infinity, so a car is a car-shaped wall with no top, and the bodega's 45 degree chamfer is faked with a staircase of small boxes (13 round that corner, smallest 0.24x0.16 m). **Give a collider a top (`minY`/`maxY`) and let the floor picker stand on it when the player is above** — `ct/interior.ts` and `COURT.climbable` are the prior art for standable surfaces. Rotation (an oriented box or a segment) fixes the chamfer; more small boxes fixes nothing. **THIS IS THE HIGHEST-RISK CHANGE IN THE PROJECT.** `fp.ts` is the movement core, the 2 m sidewalk lane is sacred, and **every existing `ctx.obstacle` call must behave exactly as it does now unless it opts in** — an opt-in default is what makes this safe. Do it in stages, committing each: (1) add the fields, defaulted so nothing changes; (2) prove the world is unchanged with `npm run fp` before/after; (3) make ONE object standable — a parked car — and walk onto it; (4) only then widen. **Also fix the false alarm this exposes:** `trapAgainst()` paints adjacent boxes forming one wall as red trap corridors, which is why the bodega corner lights up worst exactly where the geometry is worst. Verify by WALKING, with V on, and re-run `interiors-walk.mjs` for every room. |
| 20 | DONE w18 — Item 20: the desk's 'sleep spot wins the pick' hypothesis is WRONG - the seat is reachable and E seats you; only the TV was broken. Root cause: the seat's x was declared TWICE, and when it moved to the foot of the bed (4d5729246) only one copy moved - apartment.ts's lit-test still compared the player against AX(-2.10) while rig.sit() puts him exactly on AX(TV_X)=AX(-1.56), 0.54 m against a 0.20 m tolerance, silently, forever. ctx.seat is now registered FROM TV_SEAT_X/TV_SEAT_Z so the coordinate is declared once and cannot drift again; foot-of-bed view untouched. Check red-before/green-after on dev AND the built bundle, WALKED it on WASD (probes/w18-walk-to-the-tv.mjs: out to the door, back, sit->on, stand->off, not wedged), mutation-tested by re-drifting the pair (walk goes red). bugsweep 93 shots, zero STATION MISS. ALSO FIXED: the check's own --selftest was decoration - it pinned a property on an object apartment.ts reassigns every frame, so it printed 'NOT CAUGHT' against a healthy world; it now pins the userData slot and bites. NOT FIXED (needs ctx.ts/crosstown.ts, outside my item): ctx publishes no seated() a module can reach, so apartment.ts infers sitting from POSITION - that inference is this entire bug class. Also the check's 'off is not black' assertion never runs (the module publishes no .mat). Ports: my assigned 4197 was already serving another builder's world; I used 4193 + 4181. | ct/apartment.ts + scripts/K-tv-off-unless-seated.mjs | **Sitting on the bed no longer turns the TV on** — a feature the ledger calls CONFIRMED. w14 reproduced it standalone twice (`scene.userData.tv.on` stays false to a 6s waitForFunction, not a timing artefact). **The desk moved that seat to the foot of the bed this session** (4d5729246, per the user: *"sitting on the bed should have a perspective more from the foot of the bed"*) so start there — the check finds the seat by matching `/sit on the bed/i` over `__ct.spots()`, and its own failure text says the SLEEP spot may now win every pick. Decide which is true: the pick moved, or the TV trigger broke. **Do NOT undo the foot-of-bed view; that is what the user asked for.** DONE WHEN: the check is green AND you have walked it — sit, TV on; stand, TV off. |
| 21 | DONE w19 — Registered seat-facing in CHECKS (fast tier, measured 4.4s) with TWO canfail cases, not one: the check has two rules that fail apart. seat-facing mirrors the 96 casino slot stools back to the ternary that shipped (rule B, turned away from your own furniture - the clause a wall test structurally cannot see, since a backwards stool in an 11m room faces open floor); seat-facing-wall turns the tax office waiting row into the plaster 0.58m behind it (rule A). 2/2 CAUGHT. ROOT CAUSE of the item: seat-facing.mjs carries no --selftest, and checks-registered.mjs takes its population from scripts that DO - so the audit built to catch invisible checks was itself blind to this one. Baseline before I touched anything was 219/219 seats green, so the 105-seat red is already fixed; this was pure guarding. Full default-tier run confirms the runner executes it (progress line + 'ok seat-facing' row). NOT FIXED, for the queue: mutations-quote-real-source is red on the 'rain' case, whose needle matches props.ts 0x and is guarding air; checks-registered is red on H-flare-silhouette.mjs and ledger-intact.mjs, which have selftests and run never. | scripts/checks.mjs + scripts/canfail.mjs | **Register `seat-facing.mjs` in CHECKS so it actually guards.** w17 wrote it and it went red on **105 seats** — 96 casino slot stools sat with their backs 0.37 m from the machines, next to NPCs already facing the right way. That is the fifth facing bug class and the check is currently a one-off that no suite runs, so the sixth will ship too. Needs a `canfail.mjs` case like every other registered check. DONE WHEN: `seat-facing` appears in the CHECKS table, `node scripts/checks.mjs` runs it, and its canfail case goes red when the mutation is applied. |
| 22 | DONE w20 — Item 22: seatface.mjs now agrees with seat-facing.mjs (0 disagreements, was 181 of 202 indoor seats) - fixed, not deleted, because AUDIT-INSTRUMENTS lists it as carrying a live method and two notes quote it. ROOT CAUSE was TWO bugs, only one of them in the item: the |minX|<500 ceiling hid 275 of 514 colliders because the interior belt starts at x~600, AND its './lib/which-world.mjs' import was never rewritten when the file moved into scripts/probes/, so it threw ERR_MODULE_NOT_FOUND and could not run at all - the item's '222 of 228' is pre-move behaviour. I reproduced the lie exactly (213 of 219 'look at open ground') before changing anything. NEW scripts/probes/seatface-agrees.mjs proves agreement and can fail 3 ways; rule 0 reads seatface.mjs off disk because rules 1-2 run a COPY of its march and would have stayed green if the ceiling came back - the same cannot-fail trap. Mutation-tested both directions (ceiling back = exit 1), and the mutation run caught a \d-vs-\d+ bug printing the bound as '< 5'. bugsweep 93 shots zero STATION MISS; seat-facing still 219/219. ITEM'S PATH IS WRONG: it is scripts/probes/seatface.mjs. NOT FIXED, needs queueing: 288 of 406 probes are dead on load with the same unrewritten './lib/' import (which-world 180, frames 111, clock 59, reachable 16, args 5, modes 4) - before my 2 files ZERO probes had the correct path; hoist the seat geometry into scripts/lib/ (3 copies now, I copied with line-number citations per BUILDER-BRIEF 8); seatface defaults to port 4184, someone else's live world. Port: used 4190, my assigned 4199 was held. | scripts/seatface.mjs | **An instrument that lies, and it lied about the exact class w17 just found.** `seatface.mjs` filters colliders to `|minX| < 500`, but the interior belt starts at x ~ 600 — so it is blind indoors. It reported *"222 of 228 seats look at open ground"* on a world where **105 seats faced backwards**. Either lift the filter or delete the file; a check that cannot fail is worse than one that is wrong (BUILDER-BRIEF §7). DONE WHEN: it either agrees with `seat-facing.mjs` on the same world, or it is gone. |
| 23 | DONE w13 — L-blackjack-inworld.mjs rewritten to sit at the real seat (4 seats exist at ct/int-casino.ts's felt table now, BLOCKED-L.md is stale) instead of calling open() directly, which 27be185fc's not-seated-not-open tick correctly closed a frame later. Added an explicit force-stand (not Escape) regression proof per the item's own ask. All 3 modes green on dev+built bundle; L-blackjack-reachable.mjs still fully green. Flagged BLOCKED-L.md and blackjack.ts's SEAT_LABEL comment as stale docs outside my claimed file - not edited. | scripts/L-blackjack-inworld.mjs | **A check contradicts a deliberate safety fix — reconcile it WITHOUT weakening the world.** It calls `__blackjack.open()` with nobody seated (line 93; line 191 says *"NOT TESTED HERE: sitting down"*). 27be185fc made **not seated = not open, unconditionally**, because an open panel with an empty seat killed `[E]` everywhere until reload — the trap the user hit twice. So the tick closes it and the check reads 8 sub-failures. **The world is right; the check is wrong.** Do NOT relax the seat rule to make it pass. Give the check a seat, or the table a test-only hook outside the shipped path. DONE WHEN: `L-blackjack-inworld` green, `L-blackjack-reachable` still green, and you have proved the trap has not returned. |
| 24 | DONE w15 — Item 24, all three. ROOT CAUSE: instruments lying about the rain, not the rain. (1) rain-check.mjs had THREE faults not two — it kept the LAST of three Points sets so it asserted world-lock about 13 STARS; it hand-copied the weather as imul(h,2246822519)%100<22 while props.ts now uses murmur3 at 30% + the opening hour; and it sampled before rainLevel came up, where 0 is a legal 'world-locked' answer. Fixed: select on material.map, ask the published scene.userData.rainAt, use an ABSOLUTE hour, wait for the level, and FAIL if no drop moved. Now measures the 2600-point rain: 16/16 locked, 16 actually wrapped, deltas of exactly -30/-60 m. (2) rainlive's %24 is REAL — clock(36%24=12) reads rainLevel 0.0000 where clock(36) reads 0.9851, and rainAt disagrees with rainAt(h%24) on 8 of the 11 wet hours >=24 — but it was DORMANT, not mis-measuring: hours[0] is 0 because the search starts at 0, so h%24===h. Fixed anyway; it breaks the moment the search is narrowed to daylight, which rain-check now does. (3) NOT APPLICABLE, measured: both star sets DO follow the player, via starDome's parent transform (props.ts:2033), which is exactly why they do NOT have the rain's bug — three culls against the local sphere transformed by matrixWorld, so it follows for free with no cache to go stale. Sphere centre tracks a 75 m walk exactly (-75.0 m) and both are drawn in 155 of 155 render calls across 16 night views with culling ON. ct/props.ts UNCHANGED, so no source moved at all. Mutation-tested: camera-locking the drops goes red, and reverting the selector to the original shipped bug goes red ('0 of them actually wrapped'). I also report a mutation that did NOT reproduce — skipping the rainLevel wait still passes, so fault (c) was never sufficient alone. All verified on the BUILT bundle. NOT FIXED, precise: canfail.mjs's 'rain' case substitutes 'const RAIN_N = 500;' and w16 changed it to 2600 in fc332c5c5, so that selftest has patched ZERO bytes since — w19 holds canfail.mjs (item 21), one-liner for them. Also: w16-rainlock.mjs is now a duplicate of rain-check.mjs; and the dev server HMR-reloads under long instruments on this shared worktree, so instruments should point at a preview. See notes/w15-rain-followups.md. | ct/props.ts + scripts/rain-check.mjs + scripts/rainlive.mjs | **Three rain follow-ups w16 reported rather than fixed.** (1) `rain-check.mjs` keeps the LAST `Points` of three and has been asserting world-lock about a 13-point set that never moves — the tell is every drop delta reading exactly 0.000, which the wrap cannot produce; `w16-rainlock.mjs` is a working replacement. (2) `rainlive.mjs` steps the clock with `h % 24`, but `hourAbs` is absolute and `rainAt` hashes it through murmur3, so it is not periodic in 24 — it tests a DRY hour believing it is wet. (3) The other two `Points` are also `frustumCulled` at the origin, which was exactly the rain bug; check whether either follows the player. DONE WHEN: each is fixed or shown not to apply, with the measurement. |
| 25 | DONE w20 — Item 25: GOTCHAS renumbered to a clean 1..62 - 51 and 52 were each used twice with the second 51 AFTER the first 52, so everything appended since inherited a number two behind reality (the desk's '59' was the 61st entry). Nine entries renumbered (53-61); 1-50 untouched so most citations are unaffected; NO entry text changed. THIRD DEFECT THE ITEM DID NOT NAME, and it is why the other two survived: the last entry was '## 59.' with NO TITLE AT ALL - titled from its own opening sentence. Mapping table left at the TOP of GOTCHAS.md where a stale citation will meet it. Every live citation updated after mapping each hit to the entry it MEANS not the number it prints (BUILDER-BRIEF x4, LEDGER 6 refs across 4 rows, QUEUE, FEATURE-REQUESTS, w14-sha-repair-check, bedcavity.mjs, doorside2.mjs, queue-check.sh, apartment.ts, + 5 cross-refs inside GOTCHAS itself); 2 hits correctly needed NO change (LEDGER:78 and L-blackjack-reachable.mjs:88 both mean the spawn-inside-301 entry, which kept 51). DELIBERATELY NOT REWRITTEN, both documented in the mapping note: notes/archive/* (8 files - rewriting history falsifies the record) and w15-jail-seams.md:142, which QUOTES A REAL COMMIT SUBJECT (52b7c8a99) that cannot be edited without misquoting git. Added GOTCHAS 62 (the yaw clash w17 asked for), VERIFIED IN SOURCE not assumed: ctx.ts:72-74 says Seat.yaw 0=-z, citizens.ts:528 says citizenSprite facing 0=+z; the useful part is WHY it keeps shipping - the conventions AGREE at yaw +-pi/2 where cos=0 and are opposite at 0 and pi, so the error is zero at the angles a builder tries first. NEW scripts/probes/gotchas-numbering.mjs (no browser, milliseconds) asserts unique+monotonic+contiguous+titled; MUTATION-TESTED twice - re-adding the duplicate reproduces all three original symptoms at once including 'out of order: 51 follows 52', and untitling an entry also goes red. tsc --noEmit clean; queue-check.sh still runs. OUTSIDE MY BOUNDARY, FLAGGED: I edited src/proto/ct/apartment.ts, which this item does not name - a ONE-DIGIT comment change (GOTCHAS 56->58), no behaviour, but the desk should see it flagged. NOT FIXED, needs queueing: gotchas-numbering.mjs is a probe nothing calls, and the defect it catches was introduced BY THE DESK APPENDING AN ENTRY, so it is worth little until checks.mjs or a pre-commit hook runs it. | notes/GOTCHAS.md | **Two numbering defects and a missing entry.** §51 and §52 are each used twice and §51 appears after §52 (w14 found it); the desk then appended §59 assuming 58 was the last. Renumber so every entry is unique and ordered, **without changing any entry text** — other files cite these by number, so leave a mapping note. Then add the entry w17 asked for: **`ctx.seat` yaw is `0 = -z` but `citizenSprite` facing is `0 = +z` — they are 180 degrees apart**, and that convention clash is plausibly where several of the five shipped facing bugs came from. DONE WHEN: no number appears twice, order is monotonic, the yaw-convention entry exists, and a grep for citations of any renumbered entry has been updated or noted. |
| 26 | DONE w15 — Item 26. TWO corrections to the item: it is THREE places unreachable, not one, and the gap is 0.80 m, not 0.08 — out by a factor of ten, which hid the diagnosis. ROOT CAUSE: the five places ringed the wheel's +z side, 0.80 m from the last slot bank, and a place needs 2.35 m of clear radius there (1.55 to the stool + 0.80 to its [E] approach). No stool radius and no approach distance can close a 0.40 m shortfall, which is exactly why it is a layout call. FIXED by moving the ring to the side that is actually open: +x is 4.05 m of avenue against 0.80 to the bank, 1.25 to the west wall, 1.40 to poker. ROU_OPEN/ROU_SPAN are read by BOTH the stool ring and the wheel head, so the head stays opposite the players instead of being a second hand-typed assumption (RZ-0.42 was the other half of the same belief). The arc is centred on 1.81 rad, NOT pi/2: solving against the two colliders gives a legal arc of 0.810..2.807, which leans off +x because the bank is nearer — centring on pi/2 wastes the slack and squeezes the stools to 0.58 m, at which point [E] picks a neighbour. Using the whole arc gives 0.74 m spacing against 0.89 before the move. DONE WHEN all met: 5 of 5 have a legal approach; seat-facing 219 seats / 219 look at something; bugsweep 0 STATION MISS on the BUILT bundle. And I WALKED it — in from the avenue, [E] held, seated on that stool, [E], back up, 16/16. Mutation: restoring the original ring gives 'FAIL: 3 of 5' and 11 walk failures. TWO OF MY OWN INSTRUMENTS WERE WRONG FIRST and both accused the room of what the check was doing: the walk overshot 1.8 m onto the next stool, then its replacement warped its start point INSIDE the poker table. Fixed by approaching from the avenue, the way a player arrives. NOT FIXED: the roulette collider is a 2.30 m SQUARE round a 2.20 m ROUND table (its corners are why 'clear behind' reads 0.00 for the outer places — harmless, but PIT is also read by the bank stool-skipping logic so it is its own row); poker's clearance at the far approach is 0.02 m and will bite any future change silently; and the real structural fix is BANK_Z's last row at 2.8 hanging over the pit, but moving it back drops the next aisle from 1.90 m to 1.10 m, a user-driven number, so I did not trade one against the other. See notes/w15-roulette-reachable.md. | ct/int-casino.ts | **One roulette seat is unreachable** — an 0.08 m sliver between the felt and a slot bank, found by w17 while fixing 105 backwards seats and left alone because it is a layout call, not a seat call. Pre-existing, not a regression. The 2 m lane is sacred indoors too. DONE WHEN: the seat has a legal standing approach, `seat-facing.mjs` is still green, and bugsweep reports zero STATION MISS. |
| 27 | DONE w18 — Item 27: count is 16, not 39 (the jail seam commits d3770c506/45a93b7a9/d1847bb20 closed 23), and ALL 16 are explained - they are not defects. ct/jail.ts needed NO change. Real cause: a face that is not a whole number of texels CANNOT draw at its declared px/m - tex-world.ts sizes canvases W=round(wMeters*ppm), so a 0.6 m band at 16 px/m wants 9.6 px, gets 10, measures 16.67; all 16 reproduce exactly from that one expression. A fixed 0.6 px/m tolerance is the wrong SHAPE, since the half-texel error is 0.5/faceMetres and is unbounded as the face thins (the 0.2 m jamb has a 2.5 px/m floor). THE DESK ALREADY RULED THIS on 2026-07-25 (665629c5a) and predicted this exact row - 'one unluckier face and that check goes red on a surface behaving exactly as the desk just ruled it should. The tolerance would then get widened, which is how a check stops being able to catch the thing it exists for' - and shipped the remedy, userData.masonry.ppmW/ppmH, saying a checker should compare against those. So I did NOT widen the tolerance; I changed which field is compared. TWO ITEM ERRORS: (1) the named script is wrong - seampairs.mjs compares NEIGHBOURS and is already green ('brick vs brick, a real seam question: 0', selftest passes); the number comes from scripts/masonry.mjs, which the item does not name. (2) there is no shared jail-masonry cause - the jail is just where the thin faces are. BOUNDARY I CROSSED, FLAGGING IT: I edited scripts/masonry.mjs, which my item does not grant, because the acceptance test is about a number only it produces and no queue item names it. It now reports 'FACES ACTUALLY AUTHORED AT THE WRONG DENSITY: 0', still prints the 16 labelled as rounding, and gained --selftest + an exit code so it can guard - the mutation (double one repeat.x) is caught as 1 while the 16 stay classified as rounding. NOT FIXED: 'declared OFF the 8/16 grid: 1' at (8.3,0.1,-77), the lone mult-4 stamp, unexplained and out of scope; and seampairs' 22 UNJUDGEABLE undeclared faces, which it already names with the one-line declareSurface fix. | scripts/seampairs* + ct/jail.ts | **39 declared-vs-mapped density mismatches, clustered at the jail exterior** (x~63, z~-103..-107) — the same location class as the seam disagreements, so possibly one underlying jail-masonry cause rather than two. Check whether the seam work already landed fixes them before doing anything else; if it did, say so and mark it done. DONE WHEN: the count is re-measured after the seam fix and either goes to zero or is explained with its real cause named. |
| 28 | DONE w20 — Item 28: outdoor seats and seated NPCs are now covered by NEW scripts/bench-sitter-facing.mjs, mutation-tested red on the WORLD both ways. ROOT CAUSE: outdoor seats fell outside seat-facing.mjs's room-gated rules, and citizenSprite kept its heading in a CLOSURE so nothing could read it - mesh.rotation.y is not a substitute, it is the billboard angle rewritten each frame to face the camera, so it measures where the OBSERVER stands. ct/citizens.ts now publishes mesh.userData.citizenFacing. THE DESK/w17 DIAGNOSIS WAS WRONG ON ONE POINT AND I DID NOT MAKE THE CHANGE IT ASKED FOR: w17 concluded outdoor benches need ctx.seat to declare a look-at target; they do not. Measured all 17 outdoor seats - EVERY ONE has its nearest solid BEHIND and open ground ahead (park benches 3.4-4.7m behind/nothing in front, bus stop 1.57, lot chairs 0.57-0.93, tyres 1.71) - so the COMPARISON decides it, same shape as rule B with the inequality reversed. No ctx.seat change. ct/park.ts is named by the item and I changed NOTHING in it; the benches are already correct. NEAR-MISS WORTH THE DESK'S ATTENTION: my first draft published userData.citizen={facing,seated} and userData.facing, and BOTH keys are already taken - userData.citizen is a BOOLEAN tag rooms stamp (read by J-library-people.mjs, int-thrift) and userData.facing is the building-shell string 'x'/'z' that shells.mjs:124 traverses EVERY mesh for, so a number there makes it parse citizens as buildings. Renamed to citizenFacing; re-ran J-library-people.mjs, interior-people-close.mjs and shells.mjs, all three still pass. I ALSO FOUND TWO FALSE-POSITIVE FAMILIES IN MY OWN RULE BEFORE TRUSTING IT: 5 of 14 sitters failed on a 0.16m pew back, a 0.18m diner partition and two 0.18m jail walls (fixed with seat-facing.mjs's DEEP=0.80) and a bank counter 4.79m away (fixed with its REACH=0.80) - the world was right and my rule was wrong both times. MUTATIONS: park.ts 'Math.PI - yaw'->'-yaw' turns every bench round, rule C reports 6; citizens.ts facing+PI turns every person round, rule D reports 7; both reverted. UNDECIDABLE CASES ARE PRINTED, NEVER SCORED AS PASSES - 2 park benches open both ways, 7 sitters with nothing substantial within reach. tsc clean, seat-facing still 219/219, bugsweep 93 shots zero STATION MISS. NOT FIXED, needs queueing: THE APARTMENT IS NOT A REGISTERED ROOM so its seats read as outdoor - meaning seat-facing.mjs rules A and B have NEVER applied to any apartment seat, a whole interior with no facing guard; the 2 undecidable park benches need a declared look-at target (this is the real residue of w17's proposal, 2 seats not all 17); hoist DEEP/REACH out of seat-facing.mjs, THREE files now carry 0.80 and this is the second item running where I had to copy rather than import; bench-sitter-facing.mjs is not in checks.mjs (same gap as item 21); __ct has seats() but no people(), so rule D depends on scene traversal and on every room remembering to tag. | ct/park.ts + ct/citizens.ts (outdoor seats) | **Outdoor benches and seated NPCs are still unguarded** by `seat-facing.mjs`, which covers only `__ct.seats()`. w17 wrote up the reasons and the cheap fixes in `notes/w17-seat-facing.md`. Five facing bugs have shipped indoors; the same class outdoors is currently invisible. DONE WHEN: outdoor seats and seated NPCs are covered by a check that goes red when one is deliberately turned around. |
| 30 | DONE w18 — Item 30: HANDED BACK, NOT DONE — I did not implement rotation, because doing it now collides head-on with item 1's DONE-but-UNLANDED work. Item 1 (w13) added minY/maxY to the very AABB declaration at fp.ts:9 and made blocked() height-aware; those commits (3b3818ce6, 3a4acff02) live on worktree-agent-ad0271eaabaa38440 and git merge-base says they are NOT in my history — my fp.ts:9 is still the 4-field AABB. Adding rot means a conflict on that exact line plus a SECOND independent rewrite of blocked(), the world's collision predicate. Item 30's own text says 'same staging discipline as item 1', which presumes item 1 is in the tree; it is not. LAND ITEM 1 FIRST, then this is straightforward. WHAT I DID DELIVER: the acceptance test, red right now — scripts/probes/w18-chamfer-diagonal-walk.mjs. The desk's description is CORRECT (CHF=WALK=2.0, BAND=0.25 -> exactly 8 bands at bodega-corner.ts:166, all on x+z=-87.00, read back off the live world). The defect measured by WALKING: stand 1.2 m off the face on its perpendicular, walk in until stopped, 29 points along it — the stop distance takes exactly two values, 0.375 and 0.458, spread 0.083 m, byte-identical over three runs. That is the staircase wedging the capsule into step corners; one rotated collider must give one value, and more/smaller bands will NOT pass it. TWO MEASUREMENT TRAPS documented in the file, both of which read as PASS on the broken wall: (1) 'hugging' with W+D cancels in z at 45deg so the player never traverses and the profile goes flat; (2) time-sampling a moving player aliases against the 0.25 m band pitch and reports a different shape every run. I also had to TIGHTEN my own threshold from 0.09 to 0.04 after measuring — 0.09 came from the theoretical worst case BAND/sqrt2=0.177 and passed the very wall the file exists to fail. NOT COVERED: the V-overlay clause of the DONE WHEN still needs a human to press V and look. | ct/gap.ts + ct/bodega.ts (chamfer) + fp.ts | **Colliders still have no ROTATION — the other half of the users request, explicitly deferred by w13.** *"whats going on with the collision geometry here? we should fix this so its not just a bunch of separate rectangles and its just made properly."* The bodegas 45-degree chamfer is still faked with a staircase of 8 small boxes. w13 fixed the symptom (`trapAgainst()` no longer raises phantom traps between non-adjacent bands of the same wall, 8/8 false positives to 0) but not the cause. An oriented box or a segment collider fixes the chamfer; more small boxes fixes nothing. **Same staging discipline as item 1: add the field defaulted so nothing changes, prove it with fp before/after, convert ONE wall, walk it, then widen.** DONE WHEN: the bodega chamfer is one rotated collider, the V overlay shows no red along it, and you have walked the diagonal without catching. |
| 31 | DONE w19 — BLOCKED-L.md (now at notes/archive/) and blackjack.ts's SEAT_LABEL docstring both closed against ae4147cee, verified an ancestor of HEAD so the citation resolves. MEASURED rather than read back off the commit (scripts/probes/w19-blackjack-seats.mjs): 219 seats, 4 carrying 'sit at the blackjack table' at z -12.15, EACH WITH ITS OWN STAND POINT 0.80m behind - the approach the ask called NOT OPTIONAL. ROOT CAUSE: ae4147cee fixed the world and neither document describing the world was in its diff. Comments and notes only; no code line changed in blackjack.ts, checked not asserted. MUTATION-TESTED, and the first attempt failed to fail: changing SEAT_LABEL leaves it GREEN because int-casino.ts IMPORTS the constant, so both sides move together - proof the derivation is real. Dropping the label at the felt table's own gameStool() call, the pre-ae4147cee state verbatim, goes RED: 0 blackjack seats, 'sit at the table' 21 -> 25. NOT FIXED, outside the item's named files: TWO MORE COPIES of the same stale statement exist - scripts/L-blackjack-inworld.mjs says it in its header AND PRINTS IT TO STDOUT EVERY RUN, and the L-blackjack-inworld comment in scripts/checks.mjs says it too. The item implies that script was rewritten to sit down; it was NOT, it still drives the cabinet from __blackjack.open(). Also: notes/BLOCKED-L.md no longer exists at that path (moved to notes/archive/ on 08-01) and FOUR files still cite it there - citations-resolve cannot see this because it only checks file.ts:line pointers, so a bare note path is an uncovered shape. Also: L-blackjack-inworld is RED 8/15 starting at 'the table opens as a panel', pre-existing - control is /tmp/w19-checks.log:1150 from a full run made BEFORE I touched blackjack.ts. | notes/BLOCKED-L.md + ct/blackjack.ts (comment) | **Two stale statements w13 flagged but did not edit, being outside its claimed files.** `notes/BLOCKED-L.md` and a comment in `ct/blackjack.ts` both still say the felt table registers no seats — it does now (`ct/int-casino.ts`), which is why `L-blackjack-inworld` could be rewritten to actually sit down. A stale comment that contradicts the code is how the next builder gets sent the wrong way; this project has paid for that repeatedly. DONE WHEN: both read true against the current world, with the commit that made them false cited. |
| 33 | DONE w18 — Item 33: THE PREMISE IS WRONG — there are not three hand-typed copies, and no hoist was correct to perform. Measured first: DEEP has exactly ONE declaration in the whole tree (scripts/seat-facing.mjs:69) and on every other branch; REACH has two, but they are DIFFERENT QUANTITIES sharing a name — 0.80 m in seat-facing.mjs ('how close furniture must be to count as furniture you sit at') vs 0.6 m in A-verify-select-through.mjs (margin over a spot radius). Welding those into 'one declaration' as the row asks would have been actively wrong. The seat-facing pair's siblings (WALL_MIN/BEHIND_DEG/AHEAD_DEG) also exist in exactly one file, and w17's own handoff (notes/w17-seat-facing.md:154) already says 'No number in the check is a second hand-typed copy' — the row's premise is that w17 reported the opposite. I checked the other branches too, since item 30's blocker turned out to be unlanded work invisible from here; no declaration exists on any of them. THERE IS ONE GENUINE DUPLICATION, A DIFFERENT ONE: A-verify-select-through.mjs:40 hand-types 0.6, which is fp.ts:486's EXPORTED REACH_MARGIN. It cannot be imported (plain .mjs vs TypeScript) and crosstown.ts:27 imports REACH_MARGIN without republishing it on __ct, so there is no runtime path either. I cited it with a line reference per BUILDER-BRIEF §8 (comment-only, value unchanged, nothing in src/ touched). NOT TAKEN, ONE LINE, PLEASE QUEUE: add 'reachMargin: () => REACH_MARGIN' to crosstown.ts's __ct surface beside camY/yaw — it is already imported there, so it is a pure re-export — and the copy disappears for good. crosstown.ts is outside this row. ON THE DONE WHEN: I did NOT run fp/fpdiff, deliberately — DEEP and REACH live in scripts/ (instrumentation), not src/, so they cannot move a pixel and an fpdiff would have measured the noise floor and reported it as evidence. The diff itself is the stronger proof: one file, comment-only. That clause was written assuming these were world constants; they are not. | wherever DEEP/REACH are declared (3 copies) | **`DEEP` and `REACH` now exist in three hand-typed copies.** The second builder in a row had to copy rather than import them, and reported it rather than duplicating silently — which is the right call and also the signal that this needs hoisting. A second hand-typed copy of a number is the most expensive habit in this codebase (BUILDER-BRIEF §8): it is how one instrument spent a week measuring a truck that no longer existed. Hoist to one exported declaration and have all three read it. DONE WHEN: exactly one declaration exists, `fp before`/`after`/`fpdiff` shows textures and structure identical (this must be a PURE refactor), and a grep proves no hand-typed copy survives. |
| 49 | DONE w26 — Item 49: groundPick was a query with a side effect and the desk's cause was RIGHT. ROOT CAUSE: every return in groundPick went through apt.setGy (the walk-up's storey picker), and canSee calls it once per candidate [E] spot EVERY FRAME at the SPOT's coordinates - so lastGy ended each frame describing the last thing the prompt-aimer looked at, not the ground under the player; at the kerb edge that was a road-level spot at 0.00 under a player standing on 0.14. It survived because the damage and its repair are ONE FRAME apart. FIX: groundPick(x,z,commit=false) with one local put() as the only side-effect site, and exactly one caller passes true - the rig's per-frame groundY, the only one that passes the player's own position (fp.ts 146/390/495 all use this.pos). SECOND HALF THE ITEM DID NOT NAME: crosstown hands x>100 to ct/apartment.ts's aptGround, which assigns lastGy ITSELF from its own hysteresis, so a pure wrapper in crosstown alone would still have leaked for every interior coordinate; aptGround took the same commit flag (and lostAbove is now raised only on a committing call). That is one file beyond the item's named crosstown.ts:1125 + groundPick - reporting it as §9 requires; no other builder holds apartment.ts. Apartment.ground's interface gained an optional 3rd arg; that is the whole API change. PROOF: w25-kerb-gy.mjs 2 FAILED -> all green exit 0. Wrote scripts/probes/w26-storey-query-pure.mjs for the half the kerb probe structurally cannot reach - it enters No. 227 through the door with a held E, FINDS flight A by sweeping groundAt for a height that is not a storey multiple (nothing about the staircase is typed), proves a query about the stairs leaves the storey at 0.000, then WALKS UP: 0.000 -> 1.350 m through 37 intermediate heights. MUTATION-TESTED BOTH HALVES SEPARATELY, bytes confirmed: crosstown put() always committing => kerb probe 2 FAILED exit 1; aptGround lastGy=best unguarded => walk-up probe query goes 0.000->0.184 exit 1 WHILE THE CLIMB STAYS GREEN, so it cannot pass by refusing to write. Green on dev 4190 AND the built bundle 4191. bugsweep 96 shots zero STATION MISS. scenedump before/after from two built bundles: textures 44c087f0 and structure b9c8813c IDENTICAL, fpdiff only 3 casino-chase tints and 5 pigeons drifting. npm run checks fast tier: 7 real reds, ALL ATTRIBUTED - D-outline-debug-only's 'close the door' reproduces 18-pass/2-fail identically on the pre-fix build (I rebuilt at 4d35e1b1b to prove it, since it was the one that could plausibly have been mine); door301 is a FLAKE that goes green on re-run against the byte-identical build; mirror-walk, spot-coverage, floaters-walk, checks-registered and hashes-resolve are standing reds; every WRONG WORLD was my own commits landing mid-run, which the runner says itself. NOT FIXED, wants rows: (1) THE No. 227 DOOR DROPS YOU ON THE ONE x THAT DOES NOT CLIMB - jumpTo lands you at AX(1.2), the boundary between flight A and flight B, and holding W from there walks you into something and stops 0.37 m short of the bottom step after 5 s; stepping to the middle of the run (x 200.60) climbs cleanly. Found while aiming my own probe, outside this item, worth one walk. (2) scripts/door301.mjs is flaky as above - measure it, do not loosen it. (3) jump-walk.mjs in this checkout is still the pre-item-42 version whose three storey rows all read gy 0.00 and test nothing about the picker, so it was useless to me as a regression test (items 42/50 already in flight). Ports 4190 (dev) and 4191 (preview), both proved 000 before use and both shut down. Handoff: notes/w26-groundpick-side-effect.md | crosstown.ts:1125 (canSee) + groundPick | **`groundPick` is a query WITH A SIDE EFFECT, and that is the real cause of the kerb-edge drift.** `canSee` calls it every frame at **spot** coordinates, so `lastGy` ends each frame describing the last spot probed rather than where the player is standing. w25 proved it with a single `groundAt` call moving `gy` 0.140 -> 0.000 **in the same tick**, repaired one frame later — which is exactly why it survived: w25s own first test sampled across two frames and reported "pure read, OK". `apartment.ts` is the victim, not the cause; the earlier item named the wrong file and w25 said so rather than editing `crosstown.ts` outside its claim. **A query that mutates shared state is the bug** — fixing the symptom at the call site leaves the next caller to rediscover it. `scripts/probes/w25-kerb-gy.mjs` already fails by design and is your acceptance test. DONE WHEN: `groundPick` no longer mutates `lastGy` as a side effect of being asked a question (or the mutation is made explicit and intentional at every call site), `w25-kerb-gy.mjs` is green, and it goes red again when the side effect is reintroduced. |
| 45 | DONE w25 — Repacked dist/artifact.html (1,115,685 bytes, build 48c9277ce, dist==HEAD); ROOT CAUSE: the two hygiene rules are NOT in conflict -- pack-artifact.mjs already builds-then-packs, so packing LAST (after the final commit) satisfies both; only a bare 'npm run build' AFTER packing destroys the artifact. Also fixed pack-artifact.mjs printing out.length (UTF-16 units) labelled 'bytes' -- a 210-byte undercount. New probe scripts/probes/w25-sit-in-artifact.mjs walks to a stool and SITS (selftest 3/3). | street/dist + scripts/pack-artifact.mjs | **The artifact was built, then destroyed by the build that followed it — repack it and DO NOT rebuild afterwards.** Item 38 packed `street/dist/artifact.html` (1,113,501 bytes, build 69a466e15) and met all four DONE WHEN clauses on the built bundle. Then, correctly worrying that a `dist`-vs-HEAD mismatch makes `checks.mjs` exit 2 and abort the next persons whole suite, it rebuilt `dist/` — and the rebuild **wipes the output directory**, taking `artifact.html` with it. `dist/` is gitignored, so no commit preserved it. Both worktrees now hold only `index.html` and `assets`. **Nobody did anything wrong; the two hygiene rules are in direct conflict and nothing wrote that down.** So: repack it, then **STOP** — leave `artifact.html` on disk and do not run another build. Report the absolute path and the byte count. If `dist` must match HEAD for the suite, say so in your handoff and the desk will decide the ordering rather than you undoing your own deliverable. **This is the only item on the board the user experiences directly.** DONE WHEN: `artifact.html` exists on disk at a path you state, `check-artifact.mjs` passes against it, and you have opened it and sat down at one game. |
| 42 | DONE w19 — jump-walk.mjs now tests the storey picker it is named for. ROOT CAUSE: the three storey spots were written as coordinates and never checked against the building, and warp's 'gy ?? 0' silently normalised the null-storey case - the case under test - into setGy(0), so every row agreed with every other row and the file looked healthy. The lobby is now the ONE warp that names a storey (arriving in a four-storey stack is exactly when you must), and everything after it is REACHED BY HOLDING W: the climb up flight A is asserted 0 -> 0.16 -> 0.48 -> 0.82 -> 1.13 -> 1.35, that it passes through heights BETWEEN storeys, that a null-storey warp keeps the storey it had (1.35 stayed 1.35), that groundAt is non-zero on the stairs (0.38) and upstairs (5.40). The ramp position CANNOT be written down - it is wherever the climb got to - which is what stops it rotting back into a meaningless coordinate. The building's frame is derived from the world's own scene.userData.spawn, not retyped. THE ROW'S MEASUREMENT WAS WRONG in one place: groundAt reads 5.400 at those spots, not 0.000, and 'in no room at all' is true of the four OUTDOOR spots this file always got right too, because roomDims() publishes only the twelve shop interiors. What 5.4 actually meant is worse - the picker has hysteresis and the player spawns on floor 3, so all three spots reported the same height as each other, an artefact of where the run started. MUTATION-TESTED with the stair ramp in aptGround flattened: the rewrite goes red SIX ways and exits 1; the OLD file on the identical broken world printed 'jump lands you on the floor you left, everywhere' with 'upstairs gy 0.00 -> 0.00' in its own output. Also caught my own instrument mid-fix: I first used pos()[1] for the rest height and it is the height WITHIN the storey, so upstairs read the 5.4 m of building as a 5.875 m hop; fixed with camY() at rest, measured as gy + 1.6200 exactly, so the 0.45-0.8 apex band is untouched and I did not re-centre it. NOT FIXED, and it is the one DONE WHEN clause I cannot close from inside this item: the mutation has no durable home. It is proved BY HAND, which is the excuse canfail's own header names. The durable form is a canfail case plus a one-word change to the jump-walk row in checks.mjs - both files outside this item - and I have written the exact paste-ready case into the handoff note. A runtime --selftest is NOT the answer: the picker is internal to apartment.ts and the only handle from the page is warp, so a harness mutation would break the check's view while leaving the world intact (GOTCHAS 34). Also: jump-walk now takes ~50 s, up from 22 s, because it walks - still well inside the 180 s fast-tier ceiling, but flagging the tier as the desk's call rather than moving it myself. | scripts/jump-walk.mjs | **A check the whole suite depends on has never tested its own subject.** `jump-walk.mjs` exists to exercise the stacked-storey picker, and its three "storey" spots — labelled *inside, ground floor* / *apartment stairs* / *upstairs* at `(104/112/120, -16)` — **are in no room at all.** `roomDims()` puts every interior at cx 440-1320 and the walk-up at x=200; `groundAt` reads 0.000 at all three. Worse, `warp(x, z, gy ?? 0)` converts the lists `null` (meaning *leave the storey alone*) into `setGy(0)`, pinning "upstairs" to storey 0. So the picker this file is named for has never once been exercised. **Retarget the three spots onto the real walk-up and WALK UP THE STAIRS** — do not warp to a coordinate and trust it. DONE WHEN: each of the three spots is inside the room its label names, `groundAt` is non-zero where it should be, the null-storey case no longer collapses to 0, and the check goes red when the storey picker is deliberately broken. |
| 39 | DONE w22 — Item 39 SETTLED: the finding was the OBSERVER. Sabotaged ct/blackjack.ts's chip rate to make the world genuinely fail, then ran the check twice in the same minute — unpiped $?=1, '| tail -3' $?=0, which is the 'prints FAILED and exits 0' report reproduced exactly ($? after a pipeline is tail's status). Reverted the sabotage, tree clean. No bypass of line 259 exists: every other process.exit in the file is 1, 2 or 3, and bad/check are single module-scope definitions. checks.mjs is sound too — spawnSync + r.status, no shell. DID NOT touch a working exit path. BUT the defect class IS real and it is elsewhere, and this is the deliverable half: scripts/health.mjs prints 'WORLD BROKEN — __ct never appeared' and EXITS 0 — no process.exit, no exitCode, it falls off the end — and it is registered in checks.mjs, so a world that stops initialising renders as a green 'ok' row. It is also the command CLAUDE.md hands every new agent. Measured, not inferred: served a page with no __ct on 4214 (reportWorld only aborts on a stamp MISMATCH, not absence) and got exit 0. Swept all 122 registered checks with scripts/probes/can-a-check-print-fail-and-exit-0.mjs: 121 can go red on their own verdict, health is the only one that cannot — it escaped earlier sweeps because it prints 'WORLD BROKEN', not 'FAIL'. One-line fix, plus a second flaw (it still defaults SHOT_URL to 4177 and will report WORLD OK about someone else's world), in notes/w22-exit-code-claim-settled.md. | scripts/L-blackjack-inworld.mjs + how checks.mjs reads exit codes | **CLAIM TO SETTLE, NOT A FIX TO APPLY.** A builder reported that this check "prints 8 FAILED and exits 0" — a check that reports failure as success would be the worst defect class this project has. **The desk checked the source and could not reproduce the premise:** line 259 is `process.exit(bad === 0 ? 0 : 1)`, and `git show` proves that line was there both before and after the rewrite. The likeliest explanation is a pipeline swallowing the status (`node x.mjs | tail` returns tails status, not nodes) — which would be an observation artifact, not a world defect. **Establish which.** Run it against a world where it genuinely fails and read `$?` directly, unpiped. If it really does exit 0, find the path that bypasses line 259. If it exits 1, the finding was the observer and **say so** — that is a success. **Do NOT "fix" a working exit path.** DONE WHEN: `$?` is reported from an unpiped run of a genuinely-failing case, and if any check in the registry really can report FAILED while exiting 0, that one is named. |
| 29 | DONE w21 — You can now climb onto a car roof: the pickup's cab box became two tiers (hood 0.94, roof 1.50, seam derived at PICKUP_COWL_Z) and the bed gained its two side rails at 0.97. ROOT CAUSE: the roof was never out of reach because the jump is too small — the truck's own staircase was collapsed into ONE box pretending to be a wall, and the bed rail at 0.97 sits exactly between bed floor and roof. Footprint union unchanged, fp.ts untouched, 175 trap-flagged boxes before and after. WALKED pavement->bed->rail->ROOF->hood->street on dev:4188 AND the built preview:4180 (4187 was taken), mutation-tested twice red. DESK'S APEX FLAG CONFIRMED AND CORRECTED: fp.ts:446's 0.571 is the continuous apex, never reached; measured 0.475, and main.ts:107's dt clamp BOUNDS the worst case at 0.471 so margins against it are guarantees. Reach is 0.551/hop, not 0.651 — I had written 0.651 into 3 comments and 2 probes and fixed all five. The pickup bed is NOT marginal (+0.051 off road, +0.191 off kerb) but item 1's REASON was wrong: 0.50 is ABOVE the true apex and lands only on TOP_EPS — corrected in PICKUP_BED's docstring. The roof hop clears by only 21mm worst-case and the code says so. NOT FIXED: sedan/hatch/van have no first step (only candidate is the tyre at a measured 0.66, clearing by 28mm; van's 1.78 roof unreachable regardless); cars.ts comments say the tyre is 0.68 in two places, measured 0.66; w13-bed-check's find(maxY!==undefined) went ambiguous and now asks by tag. Note: notes/w21-car-roof-climb.md | ct/cars.ts + crosstown.ts + fp.ts (read-only) | **You still cannot jump on a CAR — only into the pickups open bed.** The user asked for this directly: *"i want the collision to be a bit more accurate to the objects. the cars for instance. we should be able to jump on the cars."* w13 landed collider height and made the pickup bed standable at 0.50 m, because it is the only surface in the fleet under the jumps ~0.57 m apex. A car ROOF is ~1.4 m, so it stays unreachable and the request is only half met. Options, in preference order: (a) make the route real — bed or bonnet at ~0.5 m, then cab/roof as a second step, so a player climbs rather than levitates; (b) raise the jump, which touches `fp.ts` and the sacred 2 m lane and is the riskier call. **Prefer (a).** Do NOT make roofs standable at a height nothing can reach — that is a collider nobody meets. DONE WHEN: you have WALKED a full route from pavement onto a car roof and back down, on the built bundle, and `scripts/w13-bed-check.mjs`-style proof exists for each new surface. |
| 32 | DONE w22 — Item 32: the apartment was GENUINELY ABSENT, not filtered — SLABS.push happens only inside buildRoom and ct/apartment.ts never calls it, so roomDims() had no rectangle and seat-facing skipped BOTH rules on the bed seat (roomOf null => rule B loops over an empty array). Added declareRoom() to ct/interior.ts as a dims-only registration (interiorGround/MaxX/MaxZ/RoomIds stay slab-only) and declared flat 301 from constants hoisted out of its own walls. apt301 now reports 1 seat; turning it to yaw PI goes red with 'nose to the wall: 1.01 m'; 219/219 green on dev and on the built bundle; fp before/after identical but one 1cm pigeon; bugsweep zero STATION MISS. CAUSED AND NOT FIXED, not my file: bugsweep warps with a literal gy 0, which is 3 storeys below flat 301 — it photographs the outside of the building and verifyLanded only checks x/z. I published RoomDims.y so the fix is 'gy: r.y' at 3 call sites; exact diff in notes/w22-apartment-room-registry.md. Also flagged there: bugsweep's 'roomDims.length < 12' guard now sleeps at 13 and should count __ct.rooms(); and WALL_MIN 1.20 catches only 1 of the 4 wrong yaws in a 3 m bedroom, which wants a lookAt on ctx.seat, not a loosened constant. | ct/apartment.ts + the room registry | **The apartment is not a registered room, so `seat-facing.mjs` has never checked a single apartment seat.** w20 found this while covering outdoor seats. That check just caught 105 backwards seats elsewhere; the apartment — the room the player spawns in and the one item 20 is currently about — is invisible to it. Find out whether the apartment is genuinely absent from the registry or registered under something the check filters out, and say which. DONE WHEN: `seat-facing.mjs` reports a non-zero seat count for the apartment, and goes red when an apartment seat is deliberately turned to face a wall. |
| 34 | DONE w23 — Item 34: the 5.260 m apex was the INSTRUMENT; the world is right and fp.ts is untouched (0 diff lines). ROOT CAUSE: jump-walk.mjs computed rise = apex - (pos()[3] + 1.62), but pos()[3] is apt.gy() (the hysteretic storey picker) while fp.ts builds the camera from height + groundY(x,z) + airY (fp.ts:459-468) — so any picker/ground disagreement was subtracted straight into the apex. 7.02 (the walk-up floor-3 spawn eye, apartment.ts:104) - (0.14+1.62) = 5.260 exactly, and a warp leaves exactly one stale frame reading 7.020 that the unsynchronised 30ms Math.max poll could catch; the pavement is the first spot, the only one whose previous camera is the spawn, which is why it alone read 9x. Caught the same fault live at 0.14 m on the kerb edge (old code reported 0.615 = 0.475 + exactly that gap). FIX: settle on 6 stable frames, baseline = measured rest camera, apex sampled in-page on rAF, plus a guard that the camera rests one consistent eye height above groundAt; the hand-typed 1.62 is gone entirely. Band left at 0.45-0.8, not loosened. Mutation-tested both ways with bytes confirmed (vy 4.0->2.0 => 7 fails exit 1; camera +0.5 m off ground => 4 baseline fails exit 1). Green on dev 4182 AND built bundle 4201; bugsweep zero STATION MISS. NOT FIXED, needs rows: (1) jump-walk's three storey spots (104/112/120, -16) are in NO room — interiors are at cx 440-1320, walk-up at x=200 — so the stacked-storey picker the file exists to test has never been exercised, and warp(x,z,gy ?? 0) turns the list's null into setGy(0), pinning 'upstairs' to storey 0; (2) kerb-edge apt.gy() 0.00 vs groundAt 0.14 (ct/apartment.ts); (3) fp.ts:446's comment claims a 0.571 m apex the world never reaches — real apex is 0.475-0.525 and frame-rate dependent, since semi-implicit Euler decrements vy before the position update. | scripts/jump-walk.mjs + fp.ts (read-only first) | **A jump on the pavement was measured at 5.260 m apex against an intended 0.6 m hop** — 9x every other surface tested (kerb, road, stoop and three interior floors all landed at 0.48–0.62 m). Reported by w14 off the check suite. **Establish first whether this is the world or the instrument**: a later builder working the collider item measured the jump apex at ~0.57 m and used that figure to decide which car surfaces were reachable, so the two readings disagree by an order of magnitude and one of them is wrong. Half of all apparent defects here are the instrument (BUILDER-BRIEF §7), and a 10x error hid a diagnosis on the roulette item today. **Do not change `fp.ts` until you know which reading is true** — if the instrument is wrong, fixing it IS the item. DONE WHEN: the two readings are reconciled with the cause named, and whichever of the two is wrong has been fixed. |
| 35 | DONE w18 — Item 35: both rows re-adjudicated against today's build (64b63cf5a), each now citing evidence I reproduced today. (1) TV row (C) STAYS CONFIRMED but now records that it was UNTRUE from 4d5729246 until c400c5c73 today — and that the cause was the very mechanism the cell boasts about: 'being at the seat's x/z means one thing only' is a POSITION TEST against a hand-typed copy of the seat's x, and when the desk moved the seat to the foot of the bed the copy did not move. Re-verified on dev AND the built bundle and walked on WASD. The cell's standing ask for a real ctx.seated() is re-stated as the fix for the whole bug class, not a nicety — until it lands, any future move of that seat kills the feature the same silent way. Also noted: the 'off is not black' claim is asserted by no script (the module publishes no material) and holds only by eye. (2) BLACKJACK row (L) — THE ROW STANDS, ITS CHECK DOES NOT, and I verified rather than assumed: L-blackjack-table and L-blackjack-felt pass in full with no browser; L-blackjack-reachable passes in-world and asserts the casino stools sit you down and deliberately do NOT open blackjack, which is the safety fix working. L-blackjack-inworld reports 8 FAILED while still printing 'NOT TESTED HERE: sitting down. The felt table registers no seats' — a premise L-blackjack-reachable contradicts on the SAME build. So the check is the stale party, not the world; that reconciliation is correctly already scoped as item 23 and I did not touch it. NEW DEFECT FOUND, NOT FIXED (outside this row, worth a one-line item): L-blackjack-inworld.mjs prints '8 FAILED' and still EXITS 0, so it cannot fail a runner and any suite shelling out to it has been reading it as a pass. Demotion was not warranted for either row — both are true today; what was wrong was the evidence, and both cells now say so. LEDGER structure verified before/after (330 lines, 253 table rows, both rows still 5 pipes) because this file has a documented history of losing rows to bulk edits. | notes/LEDGER.md | **Two CONFIRMED rows were contradicted today and need re-adjudicating.** (1) The blackjack row (`DESK CONFIRMED 2026-07-31, build c90d5b8e6`) was reported broken, then shown to be a stale CHECK rather than a broken world — the row is probably right but its evidence is not. (2) `K-tv-off-unless-seated` is a CONFIRMED headline feature currently failing, being worked as item 20. Re-state each row against what is true now, and **demoting is a success** (GOTCHAS: CONFIRMED can be untrue). DONE WHEN: both rows cite evidence that reproduces today, or are demoted with the reason. |
| 37 | DONE w18 — Item 37: rain needle re-aimed (RAIN_N 500 -> 2600 per ct/props.ts:87; re-quoted, not redesigned, still six drops). THE ROW'S SECOND HALF WAS ALREADY DONE and I measured before building it: canfail ALREADY asserts a mutation changed bytes — line ~805 scores 'NEEDLE matched 0x, not 1', line ~868 counts anything not CAUGHT as bad, line ~903 exits non-zero, and there is a second guard INERT at ~834 for a mutation that compiles to identical bytes. The harness was never certifying itself mutation-proof; canfail.mjs:169 records it having said so plainly before. So the rain case was not invisible, it was VISIBLE AND UNREAD — a different problem, because what was actually missing is a way to spot a stale needle without paying a build plus a browser per case, which is why nobody ran it. So I added scripts/probes/w18-canfail-needle-audit.mjs: it reads the CASES table and the file-constant table out of canfail.mjs itself (no second copy of either) and greps every needle against its target file — 35 cases, about a second, no build. All 35 now aim at exactly one place. IMPORTANT SELF-CATCH: that probe's FIRST run reported a second dead case, park-partial 0x. It was MY INSTRUMENT, not the world — I was unquoting with JSON.stringify, which escapes the backslash, so any needle containing a newline escape could never match and every multi-line case was a false FAIL. Fixed; 35/35 aim true. I was one commit from filing a defect against a healthy case. NOT DONE, and it is the outstanding work on this row: I did NOT run canfail.mjs end to end (a build plus a browser across 35 cases) — the needle is provably correct now, but nobody has yet confirmed whether the rain case scores CAUGHT or SLEPT, and a SLEPT would mean rain.mjs does not notice a storm of six drops. Also: the audit only covers plain quoted needles (35); a computed needle would be skipped silently, so compare its printed count against the table. | scripts/canfail.mjs | **A mutation test that patches zero bytes passes while mutating nothing.** `canfail.mjs`s `rain` case substitutes on the literal `const RAIN_N = 500;`; the rain work changed that constant to `2600`, so the substitution has matched nothing since `fc332c5c5` and the selftest has been certifying itself mutation-proof. Found by a builder, not by the harness — which is the real problem. **Fix the instance, then close the class: make the harness assert that a mutation actually changed bytes before it trusts the result.** A case that silently stops matching is invisible by construction, and this project has a documented family of guards that slept. DONE WHEN: the rain case patches real bytes again, AND a deliberately-broken case (one whose target literal no longer exists) makes the harness fail loudly instead of passing. |
| 38 | DONE w19 — Rebuilt and repacked the artifact: street/dist/artifact.html, 1,113,501 bytes, build 69a466e15 (dist/ deliberately left matching HEAD - checks.mjs exits 2 on a dist-vs-HEAD mismatch and would abort the next person's whole suite). ROOT CAUSE: nothing was broken, the artifact was simply older than a day's landed work. All four DONE WHEN clauses met ON THE BUILT BUNDLE, not dev: npm run build clean; check-artifact passes (7775 meshes, mean luminance 64.8); bugsweep 93 shots against vite preview with ZERO STATION MISS; and I SAT DOWN - L-games-in-artifact only drives the games through their API, so I also walked L-slots-inworld inside the packed artifact: sat at the stool at (682.44, 8.18), E seated the player, sitting opened the machine, SPACE spun it, ESC left both machine and stool, held W then moved 0.69 m, all 69 credits back in the wallet, 0 console errors. Spot-checked the headline changes in the ARTIFACT: seat-facing 219/219 seats look at something (the 105-seat bug is really gone from the build the user opens), and rain stays wet 0.309 -> 0.565 -> 0.824 after it stops. PORTS: every port in 4180-4199 was taken by other builders, so the preview is on 4211 - said here because the brief requires it. NOT FIXED: scripts/checks.mjs:863 documents the by-hand artifact command as 'node scripts/L-games-in-artifact.mjs' and the file moved to scripts/probes/ - copying that line verbatim, as this queue item told me to, gives MODULE_NOT_FOUND. Same reorganisation checks.mjs's own pre-flight guard was written for, but that guard covers the CHECKS table and this is a command inside a comment. Also: Pages is not deployed by this item - it auto-deploys on push to mainline, so it follows whenever the merge train lands this branch. DESK PUBLISHES the artifact; I have not touched the artifact URL. | street/dist + scripts/pack-artifact.mjs | **The playable artifact and the Pages deploy are stale — the user cannot actually play any of todays work.** Landed since the last publish: rain that is no longer culled away on half of all headings and now actually gets heavy, 105 seats that faced backwards, collider height with a standable pickup bed, the jail masonry, the roulette places, panel chrome removed, scroll-to-zoom. **This is the only item on the board the user experiences directly.** Procedure is in `CLAUDE.md`: `cd street && npm run build && node scripts/pack-artifact.mjs`, which produces `street/dist/artifact.html`. **Verify the BUILT bundle before declaring it done** — the panel/keydown class of bug has shipped differently than it renders in dev, and `L-games-in-artifact.mjs` exists specifically to test the packed artifact (it is deliberately NOT in the CHECKS registry; run it by hand against a preview of the packed file). DONE WHEN: `npm run build` is clean, `check-artifact.mjs` passes, `bugsweep` on the built bundle reports zero STATION MISS, and you have opened the packed artifact and sat down at one game. Report the artifact path; the desk publishes it. |
| 40 | DONE w19 — Published __ct.reachMargin() in crosstown.ts and removed FOUR hand-typed copies, not the one the row named: scripts/O-jail-walk.mjs (a REGISTERED check), scripts/D-look-selects.mjs (where every verdict in the file is a comparison against r + REACH_MARGIN), scripts/probes/F-diag-owalk.mjs and scripts/probes/O-jail-walk-fix.mjs. ROOT CAUSE: __ct published a spot's r but never the margin added to it, so the only way to reconstruct the world's own near-test was to retype the number - not carelessness, a missing reader. Two of the four also hand-computed the SUM ('gap > 1.65' against a message reading 'r 1.05 + REACH_MARGIN 0.6'); both terms come off the world now. Three cited fp.ts:425 for a constant that lives at fp.ts:486 - right number by luck, wrong pointer. Did NOT merge the two REACHes, as instructed; fp.ts's 0.6 and seat-facing.mjs's 0.80 are different quantities. Published as one reader rather than a per-spot 'reach' field because the margin is ONE global and 200 per-row copies would put the duplication back inside the payload meant to remove it. MUTATION-TESTED: with reachMargin() returning 0.05 the check prints 'r 1.05 + REACH_MARGIN 0.05 = 1.10' where it used to print 1.65 regardless. A RED-producing mutation is not reachable and I am not faking one - margins big enough to fail the assertion (1.5, 5.0) change spot selection inside the jail so the way-out E stops winning the pick and the run aborts at exit 3 before reaching it; that is the world breaking first, not the check sleeping. DONE WHEN grep passes: fp.ts:486 is the only executable assignment left. NOT FIXED: (1) O-jail-walk has a pre-existing red of the SAME class - 'FAIL and on the PAVEMENT 55 < 60.12 < 57' from a hand-typed const FX = 57.0, while the rebuilt jail forecourt now reaches x~61; the check is wrong and the world is right, and I did not widen the bound because loosening a failing check is the one thing the brief forbids and FX is shared with the lane assertions. Proved pre-existing by running the unmodified script from git side by side - identical failure, identical numbers. (2) 288 files in scripts/probes/ still import './lib/...' and cannot run at all since the reorganisation moved them a directory down; I repointed only the two I had to edit, because editing a file that cannot run is cosmetic. (3) Two source comments still hand-type the value and are outside this item: int-casino.ts:257 and int-hotel.ts:176, the latter with the fp.ts:425 pointer again. | crosstown.ts (__ct) + the script that hand-types it | **A script hand-types `fp.ts:486`s exported `REACH_MARGIN`** instead of reading it — found by a builder that went looking for three copies of `DEEP`/`REACH`, correctly concluded the premise was false (`DEEP` is declared once; the two `REACH`es are DIFFERENT quantities that share a name, 0.80 vs 0.6, and merging them would have been actively wrong), and found this real duplication instead. **Do not merge the two REACHes.** The fix is one line: expose `reachMargin: () => REACH_MARGIN` on `__ct` and have the script read it. A hand-typed copy of a number is how one instrument spent a week measuring a truck that no longer existed (BUILDER-BRIEF §8). DONE WHEN: the script reads the value through `__ct`, and a grep proves no hand-typed copy of `REACH_MARGIN` survives outside `fp.ts`. |
| 41 | DONE w22 — Item 41: ran canfail end to end — 42/43 CAUGHT, 1 SLEPT, 0 unscored, tree restored byte-for-byte, .canfail-last.json now stamped 42/43 asleep=[wetness]. The item's premise held: the static '35/35 needles aim true' audit was wrong (and covered 35 of 43 cases). THREE findings, two fixed in my file. (1) rain's needle read 'const RAIN_N = 500;' and props.ts has said 2600 since the storm was made heavier — it matched 0 bytes, so scripts/rain.mjs had NEVER been shown a broken world. Re-aimed; CAUGHT. (2) That stale needle then made canfail print 'RESTORE FAILED — props.ts does not hold its original text' and exit 3 on a byte-perfect tree, because the restore assertion re-read each case's NEEDLE instead of the file's bytes — the worst thing a source-editing tool can falsely say, since the natural response is 'git checkout --'. It now digests each file before the run and compares after; a stale needle prints a named NEVER RAN/UNPROVEN block and exits 1. Both directions mutation-tested unpiped: stale needle => exit 1 + 'restored byte-for-byte'; poisoned baseline => exit 3 + RESTORE FAILED. (3) THE ROOT CAUSE OF THE 'flaky wetness': servedEntry() is only reached when a case does NOT go red, so it lands precisely on a sleeping guard — and undici's pooled keep-alive socket is dead by then (UND_ERR_SOCKET, swallowed, reported only as 'served null'), so canfail scored genuine SLEPTs as unscored NOT-RUN. With the error surfaced and one retry on a fresh connection, wetness scores SLEPT 5/5. The server was never dead (HTTP 200 before and after), so the 31 CAUGHTs following it are sound. NOT FIXED, not my file: wetness's own verdict 'streetStillWet' is broken — it asks whether the surface colour DIFFERS FROM THE STORM, which is what drying does, so it passes on a street I measured bone dry (road and gutter both 3c3c3c from +8s). The only verdict that catches the mutation is gutterHolds, and only because samples[3] lands at the exact second road and gutter converge — that is the mechanism of the documented CAUGHT/CAUGHT/SLEPT/SLEPT/CAUGHT non-determinism. Full traces and a suggested fix in notes/w22-canfail-end-to-end.md. | scripts/canfail.mjs | **Nobody has run `canfail.mjs` end to end.** Its rain needle was just re-aimed after patching zero bytes for hours, and a static audit says 35/35 needles now aim true — but a static audit is a hypothesis about the source, and this projects rule is that the source is the answer only when you have run it. **Run the whole harness and report, per case, CAUGHT or SLEPT.** A needle that aims at real bytes can still fail to change behaviour. DONE WHEN: every case has a CAUGHT/SLEPT verdict from an actual run, and any case that SLEPT is either fixed or queued with its reason. |
| 43 | DONE w25 — Item 43 both halves, comment-only. (1) fp.ts: ALL six figures in the jump block were analytic, not just line 446 -- ROOT CAUSE: semi-implicit Euler decrements vy before integrating position, costing v0*dt/2, so apex(dt)=0.5714-2*dt and the analytic 0.571 is a dt->0 limit; main.ts:107's 0.05 clamp puts a hard floor at 0.475. Fresh run reproduces 0.4750 m at 20fps to 0.5383 m at 62fps, every hop within 0.008 m. (2) Kerb edge: the item's LOCATION is wrong -- apartment.ts is the victim, setGy stores exactly what groundPick hands it. ROOT CAUSE: groundPick is a query with a side effect and canSee (crosstown.ts:1125) calls it per-frame at SPOT coords, so lastGy ends each frame describing the last spot probed, not the player; proved by a single groundAt call moving gy 0.140->0.000 in the SAME TICK, repaired one frame later. Cause named in apartment.ts; the fix is in crosstown.ts which this item does NOT name, so I left it. Could not find the 'new guard that already catches it' -- A-eye-height-holds tolerates 0.5 m, jump-walk compares groundAt not gy; my scripts/probes/w25-kerb-gy.mjs is now that guard and fails by design. Also fixed my own probe reporting a 0.1632 m apex from a fixed 1100 ms window (jump-walk.mjs still has it). tsc clean, bugsweep 93 shots zero STATION MISS. | fp.ts:446 (comment only) + ct/apartment.ts (kerb edge) | **Two bookkeeping defects, no gameplay change expected.** (1) `fp.ts:446`s comment claims a **0.571 m** jump apex that **the world never reaches** — that is the analytic `16/28` from vy 4.0 and gravity 14, but semi-implicit Euler decrements `vy` before the position update and loses ~`v.dt/2`, so the measured apex is **0.475-0.525 m and frame-rate dependent**. Anyone tuning the jump against that comment is tuning against a number that does not exist, and a builder is choosing climbable surface heights against it right now. Correct the comment to the measured range and say why the analytic figure is not reachable. (2) At the kerb edge `apt.gy()` reads 0.00 while `groundAt` reads 0.14 — the camera is right, the bookkeeping is adrift; a new guard already catches it. DONE WHEN: the comment states a range that a fresh measurement reproduces, and the kerb disagreement is either fixed or explained with its cause named. |
| 44 | DONE w19 — Swept all 767 scripts: 648 carried a hardcoded default port across 21 different ports. ROOT CAUSE: the '??' produces a correct-looking run against whatever is on the port, and it is invisible in the output - the one fact that would make the reading suspect is the one fact never printed. Added scripts/lib/aim.mjs: returns SHOT_URL in silence when set (the normal path is unchanged, and checks.mjs passes SHOT_URL to everything it spawns so no registered check reaches the other branch), otherwise returns the SAME fallback the script had plus an unmissable five-line stderr banner naming the port and saying nobody chose it. It ANNOUNCES rather than refuses, deliberately unlike canfail.mjs - that is one tool with one documented invocation, this is 648 mostly one-shot probes whose value is being runnable in one line; refusing would trade a silent wrong answer for a blanket 'cannot run'. Stderr so a pipe cannot swallow it. Applied by scripts/probes/w19-aim-codemod.mjs, kept so the sweep is auditable as uniform rather than by hand. Confirmed the item's premise: 4185 was serving another builder all session and an unaimed jump-walk against it does not even reach CROSSTOWN - it times out. TWO THINGS EARNED THEIR KEEP: (1) node --check over all 767 files caught a real collision - scripts/probes/E-circuit.mjs declares its own module-level 'aim' as a bearing helper, so the import would not parse; aliased there, and 767/767 parse now. (2) scripts/aimed.mjs, the guard I registered in CHECKS, caught one the codemod MISSED on its first run - a double-quoted URL where the codemod only matched single quotes. That is why the durable guard is worth more than the sweep; the problem was never the 648 but the 649th. Its --selftest plants all four spellings of the bug and requires each caught, plus three shapes of the FIX that must not be: 4/4, 0 false positives. Spot-ran seat-facing, jump-walk, D-look-selects, O-jail-walk aimed - all green and SILENT, which is the other half of the claim - and checks-registered confirms 'aimed' is properly registered with its two pre-existing reds unchanged. bugsweep zero STATION MISS. Nothing outside scripts/ changed. NOT FIXED: (1) the banner cannot tell you the port is OCCUPIED, only that you did not choose it - detecting occupancy needs a request and aim() must be synchronous because it is called in expression position like 'await p.goto(aim(...))'. In practice reportWorld covers the next step by refusing a world that is not this checkout; the gap is scripts that do NOT call reportWorld, which I have not counted and which wants its own item. (2) The 288 probes with broken './lib/' imports are still broken - they now carry a correct ../lib/aim.mjs alongside, so no worse, but they still die before a browser opens; same shape as this sweep and it wants one of its own. (3) aimed.mjs exempts three files BY NAME because they quote the pattern in comments; a detector that told code from comment properly would be a second parser and the likeliest thing to break. | scripts/ (every hardcoded default port) | **Instruments carry hardcoded default ports, and those ports are occupied.** `jump-walk.mjs` defaults to **4185**, which has been serving another builders world all session — so anything run without an explicit `SHOT_URL` tonight measured someone elses street and reported confidently about it. That is GOTCHAS 48, and it is a plausible contributor to the 5.260 m jump report that cost a builder a full item to disprove. **Sweep every script for a hardcoded default and make the failure loud rather than silent**: an instrument with no `SHOT_URL` should either refuse to run or announce the port it chose in its first line of output. DONE WHEN: no instrument silently measures a default port, and a run with no `SHOT_URL` against an occupied port is unmistakable in the output. |
| 47 | DONE w28 — HANDED BACK — item 47 CANNOT be done in ct/cars.ts, the file it names. Every car collider in this project is built in crosstown.ts (:488 gives parked cars a box with NO maxY, so they are full-height walls; :745-835 is the pickup's four standable tiers). cars.ts draws meshes and exports constants and registers no colliders, so furniture added there would be a step nobody can stand on. crosstown.ts is held right now by w26 under item 49 (DOING 02:02) — BUILDER-BRIEF 9 says skip. Item 29's own LEDGER row lists 'ct/cars.ts + crosstown.ts + fp.ts'; item 47 is its follow-up and lists only cars.ts, which looks like a dispatch slip. REFILE as crosstown.ts + ct/cars.ts after 49 lands. DESIGN MEASURED SO NOBODY REPEATS IT (notes/w28-car-climb-route.md): recommend the HATCH climbed up its back — rear step bumper 0.60, rear spoiler 1.05, roof 1.44, margins +0.091/+0.101/+0.161, worst 91mm = 4.3x the pickup's 21mm; one-part fallback is the step bumper alone at +0.091/+0.211/+0.051; the van is genuinely impossible (0.94->1.78 with nothing between). LANDED inside my file: the tyre's top is 0.6634, NOT the 0.68 cars.ts stated in four places — ROOT CAUSE the wheel is CylinderGeometry(...,10), a DECAGON standing on a flat, so its top is the apothem 0.34+0.34*cos(pi/10), not the radius; predicted and measured agree to 4dp on all four kinds (scripts/probes/w28-tyre-top.mjs). w21 flagged the 17mm and could not explain it. It halves the tyre route's kerb clearance from 45mm to 28mm, which is the number item 47 is argued from. 'Tyre 0.68 across' elsewhere is CORRECT (two vertices land on the diameter) and I left it, saying why. tsc clean, comment-only. | ct/cars.ts | **DESK DECISION — do NOT build the tyre route onto sedans/hatches; give ONE car a real one instead.** w21 measured the only candidate first step on a sedan as the **tyre at 0.66 m, clearing the kerb by 28 mm**, then 0.66 → 0.94 → 1.46 with another **31 mm** margin. Reach is 0.551 m/hop and the worst case is bounded (`main.ts:107` clamps dt at 0.05), so those margins are guarantees rather than averages — but **two of those three steps are tighter than the 21 mm roof hop that is already the tightest thing in the world**, and a route the player must chain three times at that tolerance will read as broken, not as a climb. A curved tyre is also a strange thing to stand on. **The pickup works because it has real furniture — bed, rail, cab — not a found ledge.** So: pick ONE car and give it an honest route by shaping its own geometry (a bonnet the player can actually reach, or a deliberate step), the way the pickup got one. The vans 1.78 m roof stays unreachable and that is fine. **One car you can reliably climb beats three you can almost climb.** DONE WHEN: the route is walked end to end on the built bundle, every hop has a margin at least as generous as the pickups 21 mm at the dt clamp, and it is mutation-tested red by flattening one step. |
| 48 | DONE w19 — PART 1 DONE, PART 2 BLOCKED. (1) MEASURED the tyre off the rendered geometry (scripts/probes/w19-tyre-top.mjs, 22 cars, identical to four decimals): top 0.8034 world, on lot paving at KERB_H 0.14, so 0.6634 above the ground it is parked on; mesh height 0.6467. The row's 0.66 is right and the file's 0.68 was wrong by 20 mm. ROOT CAUSE: 0.68 is exactly 2 x TYRE_R - the number was computed from the radius instead of measured, and a tyre is a low-segment cylinder whose silhouette is a polygon inscribed in that circle, short of it by the sagitta. 0.68 is the circle nobody drew. THE ROW SAID TWO PLACES; IT IS FIVE, AND THEY ARE NOT THE SAME CLAIM: lines 200 ('0.68 m tall'), 244 ('THE TYRE'S TOP IS AT 0.68') and 948 ('tops out at 0.68', whose '18 cm proud of the floor' follows to 16 cm) are height claims and are corrected; lines 232 and 251 say 0.68 ACROSS, which is the nominal diameter the arch geometry is derived from in code - a DIFFERENT QUANTITY WEARING THE SAME NUMBER, the same trap as REACH_MARGIN 0.6 vs seat-facing's REACH 0.80 one item ago. Rewriting those would have been wrong in the other direction, so they are labelled rather than changed. Recorded the whole finding at g.userData.tyre, where somebody will next be tempted to write 2 * tyre. This matters because item 47 costs its first climbing step at a 28 mm margin - a 20 mm error is most of what it is compared against. Comments only, no code line changed; build clean, K-tyre-has-arch still green, probe re-measures identically. (2) BLOCKED: scripts/w13-bed-check.mjs does not exist in this checkout, and neither does the maxY it tests - fp.ts:9 defines AABB as { minX, maxX, minZ, maxZ }, colliders here are 2D with NO HEIGHT, so there are no 5 standable boxes to disambiguate and no box to flatten. Same missing w21 tree that blocked 46 and 47 (notes/BLOCKED-w19.md). I did NOT confirm w21's tag change 'is right and complete' because there is nothing here to confirm it against; reading a diff I cannot run is not confirming it. ALSO FOUND, not fixed: the tyre does not touch the ground - bottom at world 0.1566 against paving 0.1400, floating 16.6 mm, despite eaec0710f 'grounded wheels touch down like the rest of the fleet'. Same order as item 47's margins, so whoever costs a climb should know. | ct/cars.ts (comments) + scripts/w13-bed-check.mjs | **Two small truths to restore.** (1) `cars.ts` says the tyre top is **0.68** in two places; measured **0.66**. A hand-typed number that disagrees with the world by 20 mm is exactly what item 47 will be reasoning against. (2) `w13-bed-check.mjs`s `find(c => c.maxY !== undefined)` went ambiguous once there were 5 standable boxes — w21 changed it to ask by tag and **flagged that it edited a file its item did not name**, which is the correct handling. Confirm that change is right and complete rather than assuming it. DONE WHEN: both comments match a fresh measurement, and `w13-bed-check.mjs` names the box it means and still goes red when that box is flattened. |
| 51 | DONE w28 — L-games-in-artifact.mjs now WALKS to each game's own stool and sits down instead of calling open(). ROOT CAUSE: it drove both games from __slots.open()/__blackjack.open() — the module API, not how a player reaches them — so a game with no working seat passed green. PROVED by removing the blackjack seat at source (int-casino.ts:1272), rebuilding and repacking: the OLD check printed 'all checks pass — both games ship', exit 0; the new one goes red with 0 seats found. Seat labels are read out of slots.ts:1247 / blackjack.ts:1198 rather than retyped, facing is derived from the pose->approach geometry not pose.yaw, and the walk stops on ARRIVING at the stool (1.14-1.75 m covered) because the trigger radius made a prompt-stop walk 0.10 m. selftest 6/6 CAUGHT, bugsweep zero STATION MISS, port 4180. NOT FIXED, not my files: checks.mjs:857 still cites this file at a wrong path (w25 reported twice, and item 51 inherited the wrong path); slots.ts docstrings say 96 slot stools, the world publishes 87; w25-sit-in-artifact.mjs:173,223 read __ct.pos().gy but pos() returns an array. | scripts/L-games-in-artifact.mjs | **The artifact check would pass a game you cannot actually sit down at.** `L-games-in-artifact.mjs` only calls `__slots.open()` — so a machine whose stool no longer seats you passes it, which is precisely the class that has already shipped here five times (105 seats facing backwards, a bed seat whose coordinate drifted, a blackjack table with no seats). w25 wrote `scripts/probes/w25-sit-in-artifact.mjs`, which WALKS to a stool and sits, selftest 3/3 CAUGHT — promote that approach into the artifact check. The artifact is the only thing the user actually plays, so its check should exercise it the way he does. DONE WHEN: the artifact check walks to and sits at each game rather than calling `open()`, and goes red when a seat is deliberately removed. |
| 53 | DONE w28 — You can walk up your own stairs now. ROOT CAUSE: the No. 227 door landed you at AX(1.2), the arithmetic middle of the lobby — which IS the middle of the CORE WALL between the two flights (collider AX(1.04)…AX(1.36)). The mean of the two flights is not a place, it is the wall; the staircase was fine, as the item said. REPRODUCED FIRST: from AX(1.20) holding W stops at z -11.99, 0.39 m short of the stair foot at -11.6, gy never leaves 0.00 (item said 0.37). WALKED THE LOBBY AT 1 cm: the band that climbs is AX(0.41)…AX(0.67) — above it you clip the core wall's SW corner, below it the west lobby wall; AX(1.80) (flight B) is blocked too because at lobby level that is the dead space under the return flight. Both bounds are STATIC collider corners so no frame time enters them. FIX: 0.6 is now FLIGHT_A_X (and 1.8 FLIGHT_B_X), read by BOTH the tread meshes and the door's jumpTo — the same number was written twice and only one copy was ever about where a player stands. +0.07/-0.19 inside the band; I picked flight A's own centreline over the band midpoint 0.54 because it is derived and self-documenting, and I am saying the 7 cm upper margin out loud rather than burying it. PROOF WALKS, IT DOES NOT WARP (the item was explicit): scripts/w28-227-stairs.mjs finds the door via __ct.spots(), stands on the STREET, holds [E], and the only further input is a held W — 9.37 m walked, gy 0.00 -> 1.35 at the half-landing, ON THE BUILT BUNDLE. MUTATION: landing moved back to AX(1.2), rebuilt, 2 FAIL exit 1 stopping at -11.98, and the content-hashed bundle moved index-CtzTte_8.js -> index-BIS-ksD8.js so it was a real build. Verdict is gy, not camY — camY is a constant 1.62 whatever is underfoot and reading index 1 cost the first probe a run. bugsweep zero STATION MISS, health WORLD OK, tsc clean. TRAP WORTH KNOWING: the DEV server served a STALE landing (198.60,-16.30 — an origin that does not exist) via HMR after I edited apartment.ts, three runs running, while a fresh page and the build both read 200.60,-18.70. I nearly went hunting a second apartment block. NOT FIXED: w25-kerb-gy.mjs fails 2/9 here but it is NOT mine — git merge-base proves w26's item-49 fix 4747db57b is not an ancestor of my HEAD, so it lands with the merge train; apartment.ts:3127's SPAWN jumpTo and the interior door spots were not audited against the same flight-boundary problem and now have FLIGHT_A_X to compare to; and 3 cm of the upper bound (0.67 measured vs 1.04-radius~0.70 predicted) is unexplained. Ports 4181 dev + 4180 built, both proved free and both shut down. | ct/apartment.ts (the No. 227 door landing) + crosstown.ts jumpTo | **You walk into your own building and cannot go up the stairs.** w26 found this while fixing the storey picker and did not fix it. `jumpTo` lands you at `AX(1.2)` — **exactly the boundary between flights A and B** — and holding W from there walks you into something and stops **0.37 m short of the bottom step** after five seconds. `x 200.60` climbs cleanly, so the staircase is fine and the landing spot is wrong. This is the front door of the players own home and the first thing he does after opening it. **WALK IT — do not warp to a coordinate and call it proven** (a check that warped instead of walking is how the storey picker went untested for its whole life). DONE WHEN: entering through the No. 227 door and holding W reaches the first step and climbs, walked on the built bundle, and a check goes red if the landing x is moved back to the flight boundary. |
| 46 | DONE w22 — Item 46 NOT REPRODUCED, taking the clean-runs branch: 27 roof exits under CPU throttle (2x, 3x, 4x), all four directions, every one escaped and every one could still walk afterwards. Wrote scripts/probes/w22-roof-escape.mjs, which climbs by the REAL route (a warp onto the roof cannot reproduce a bug in how you arrive), walks off one named direction, and then proves the player is not wedged where he LANDED by trying all four directions from there — 'stuck' means moving nowhere, and that distinction is the whole point: w21's 'STUCK left' was a blocked EDGE on a roof with four open sides, not a trapped player. Coming off the flanks is a 1.5 m drop into the gap between a vehicle collider and a kerb, which is the shape of every trap this project has shipped, so 'off the roof' is not 'free' and w21's script never checked it. ALSO TESTED w21's own hypothesis without a player: it is structurally REAL — ct/traffic.ts sets maxY nowhere and fp.ts:12 says an uncapped collider is a wall at every height — but in 2347 frames nothing reached the roof's footprint; closest was 0.418 m, and it is a 0.5x0.5 person-sized box on the PAVEMENT, not a car in the lane. Even confirmed it blocks one edge while alongside, which is why it cannot trap. NOT FIXED, files not mine: (1) w21-roof-climb.mjs is NOT registered in checks.mjs and nothing else calls it — it 'stays in as a guard' in name only; exact row in the note. (2) It only ever leaves the roof forwards; three of four exits unguarded. (3) Traffic and citizen boxes are walls at every height — the fix is the same opt-in maxY the truck already uses, but the blast radius reaches street-level collision. (4) The bed hop is unreliable under load — 52 failed first hops this session, no surface ever held the player at a wrong height. NOTE FOR THE DESK: this item was only doable after 'git merge add-stick-and-city98' — my worktree had neither the roof nor the diagnostic, exactly as BLOCKED-w19 describes. The merge conflicted in canfail.mjs where mainline and I had independently fixed the same false RESTORE FAILED; resolved keeping mainline's account plus my stronger byte-digest assertion, and re-verified with guards.sh. | ct/cars.ts + scripts/w21-roof-climb.mjs | **A player was seen STUCK on the cab roof once, under load, and not reproduced in three further runs.** w21 added a diagnostic rather than claim a fix, which was right. **Being unable to get off something is the worst bug this project ships** — the user has been trapped twice before and his words were *"no im telling you i cant get up anything i do once i sit down"*. The roof is brand new and is the first place in the world a player can stand that is not a floor. **Reproduce it under sustained load (CPU throttle, several browsers) before deciding it is rare.** If it is real, the fix is an escape path, not a smaller roof. DONE WHEN: either the STUCK reproduces and is fixed with an escape proven from all four directions under throttle, or 20+ throttled roof-exit runs are clean and the diagnostic stays in as a guard. |
| 36 | DONE w24 — Bodega chamfer is now ONE turned collider, not 8 axis-aligned bands. ROOT CAUSE: fp.ts's AABB had no orientation, so a 45-degree wall could only be faked as a staircase — and the fake is FELT: the collision surface stepped 152 mm in and out along the cut (measured by bisecting on fp.ts's own predicate; a walk-only profile could only see 83 mm because frame quantisation is the same size as the defect). Staged as asked: rot added defaulted and proven inert (fp textures/structure IDENTICAL), then the corner converted to 3 boxes, then gap.ts + the V overlay taught to read a turned box. Surface now FLAT to 0.0 mm at 0.359 across the whole face; red on the chamfer 3 -> 0; static red 164 -> 160 with all four removals accounted for (2 dead bands, 2 produce crates that were false-red BECAUSE of the staircase); walks the diagonal clean on dev AND the built bundle. Wall depth is CFW/2 (derived: the cut triangle's own height) and must CONTAIN the triangle — a thin skin is tunnellable at the 42 m/s debug sprint into a sealed void. Mutation-tested x3, bytes confirmed. NOTE the item names ct/bodega.ts, which does not exist (it is ct/bodega-corner.ts). TOUCHED 2 files the item does not name, both flagged in notes/w24-collider-rotation.md: debug-collision.ts (1 line, or the V overlay draws the chamfer in the wrong place) and interiors-walk.mjs (its [E]-spot check read a turned box's LOCAL extents as world and failed a door I proved you can walk to and open). FOUND, NOT FIXED: interiors-walk bodega is flaky 1 run in 4 — PRE-EXISTING, measured 0,0,6,0 on the staircase vs 0,0,0,6 on the fix; a real 0.45 m trap between a prop at x 5.75-6.25 and the block face at x 6.7; and gap.ts still measures a turned box by its bounding box, which is safe today but must become exact before a SECOND turned collider is added. Ports 4215 dev / 4221 preview — 4180-4199 all occupied. | ct/gap.ts + ct/bodega.ts + fp.ts | **Colliders still have no ROTATION — requeued now that the blocker is gone.** w18 handed item 30 back rather than start it, because rotation collided head-on with item 1s collider-height work while that was DONE-but-UNLANDED. **That was the right call and item 1 is now landed** (typecheck clean, merged), so the collision no longer exists. This is the users own words: *"whats going on with the collision geometry here? we should fix this so its not just a bunch of separate rectangles and its just made properly."* The bodegas 45-degree chamfer is still a staircase of 8 boxes. An oriented box or a segment fixes it; more small boxes fixes nothing. **Stage it exactly as item 1 was staged, committing each stage:** add the field defaulted so nothing changes, prove it with `fp before`/`after`/`fpdiff`, convert ONE wall, walk the diagonal, only then widen. Read `notes/w13-collider-volume.md` first — it is the prior art and it names the traps. DONE WHEN: the bodega chamfer is one rotated collider, the V overlay shows no red along it, and you have walked the diagonal without catching. |
| 50 | DONE w27 — jump-walk.mjs now ends its sample on world state (risen, then one height held 6 rendered frames) instead of a fixed 1100 ms; root cause is the 0.05 s dt clamp in main.ts:107, so under load the window closes mid-ascent and the peak-so-far is reported as the apex. NOTE: x8 does NOT break the old file — it needs ~x40, where the old one raised 4 FAILs against a healthy world (0.390 vs the true 0.475). Also: the old 0.45 band floor sat BELOW the 0.475 the physics cannot go under, so truncated 0.450 readings passed as healthy; replaced with a floor derived from the world's own integrator. Ports 4187/4194. | scripts/jump-walk.mjs | **A fixed wall-clock wait is one slow frame from a false reading, and this instrument still has one.** w25s first apex probe reported **0.1632 m** — below a floor the physics cannot reach, so it was the instrument: a fixed 1100 ms window truncates the ascent at the dt clamp. w25 fixed its own probe to settle on stable FRAMES instead of milliseconds, but **`scripts/jump-walk.mjs` still carries the same fixed wait**, and this machine has been running twenty browsers at once. This is the same instrument that produced the 5.260 m apex that cost a full item to disprove. DONE WHEN: `jump-walk.mjs` settles on frames rather than wall-clock, and produces the same apex under CPU throttle x8 as it does idle. |
| 52 | DONE w28 — claim.sh now resolves every path in an item's file(s) column against the tree the builder is standing in, and warns loudly (advisory, never fails the claim — the row is already DOING by then). ROOT CAUSE: the column was never checked against anything, so a stale path cost a whole claim to discover. Four verdicts because the fix differs: resolved; stale PATH (says where the file actually is); missing NAME (suggests neighbours); and present in the MAIN tree but not yours (says your worktree is BEHIND and gives the reset) — that last is the 'desk ranked against mainline while the builder held a snapshot' case, 2 of the 3 wasted claims. THE HARD PART WAS NOT FIRING ON GOOD ROWS: the column is prose ('crosstown.ts:1125 (canSee) + groundPick') and uses short names (ct/cars.ts is really src/proto/ct/cars.ts), so parentheticals drop, +/, separate, :1125 strips, only KNOWN extensions count (a bare 'has a dot' rule matched the prose 'process.exit' and 'r.status'), and a token resolves if a real path ENDS with it anchored at a separator. SWEPT ALL 21 ROWS the live queue has held: 2 warn, 19 silent, ZERO false positives including on 15 DONE rows builders completed — and BOTH warnings are true positives. LIVE CATCH: item 36 is DOING RIGHT NOW against ct/bodega.ts, which does not exist (there is ct/bodega-corner.ts and ct/int-bodega.ts) — tell whoever holds it. Selftest 11/11 (scripts/probes/w28-claim-selftest.sh) running REAL claims against a scratch queue via a new CLAIM_QUEUE test hook, so the live queue was never touched; the lock moves with it and is byte-identical to done.sh:28 when unset. MUTATION-TESTED TWICE: disabling the call site = 7 FAIL (bytes 19635->19645); making the resolver match on BASENAME = 3 FAIL (19635->19641) — that second one would silently 'resolve' item 51's stale path, and it exposed a weak assertion of mine that was passing because the check SLEPT, now tightened. NOT FIXED: item 39's file(s) column is shifted because the row's text contains an unescaped | (harmless here, but --stale/--release/the reaper all parse on the same assumption); checks.mjs:857 still prints the wrong L-games-in-artifact.mjs path, which is where the queue copied item 51's bad path from — third report. | scripts/claim.sh (or a new ranking check) | **Three items this session named files that did not exist in the tree the builder was standing in**, and each cost a claim to discover. Two of those were the desk ranking against mainline while the builder held a snapshot; one was a genuinely wrong filename. A builder suggested the fix and it is cheap: **at claim time, check every path the item names actually exists, and say so loudly if not.** The item text puts its files in a `file(s)` column, so the check is mechanical. This does NOT replace the builder measuring — an item can name a file that exists and still be wrong about it — it only catches the case where the work cannot even begin. DONE WHEN: claiming an item whose named file is absent prints a clear warning naming the missing path, and a deliberately-broken item row triggers it. |
| 61 | DONE w31 — health.mjs had NO process.exit/exitCode at all, so its verdict only reached stdout and node returned 0 - the suite reads status, so a dead world scored a green ok row. Verdict now sets exitCode (1 broken, 0 alive) plus a NEW exit 3 for nothing-measured, because exit 1 alone would have made a dead port indistinguishable from a broken world. Registered health with a real canfail case 'health-dead' (was 'false' - no selftest), which withholds (window as any).__ct in crosstown.ts. Proved by hand, not by canfail's CAUGHT: bundle hash moved so the mutation was not inert, reportWorld did NOT abort so it was not a wrong-world exit-3 false green, and the pre-fix health.mjs exited 0 on the same world where the fixed one exits 1. Console errors left REPORTED-NOT-JUDGED (w22's open question). NEEDED A FILE THE ITEM DID NOT NAME: scripts/canfail.mjs, for the mutation case the DONE WHEN requires. Clause 3 (defaulted port) was ALREADY satisfied before I touched anything - aim() announces and reportWorld refuses with exit 3. FINDING: w22's static probe can-a-check-print-fail-and-exit-0.mjs can no longer detect a regression of this fix - my exit 3 blinded it, since it asks only whether a non-zero exit exists in the file, not whether the VERDICT reaches the status; the canfail case catches it. NOT FIXED, desk one-liners: CLAUDE.md:103 still says DO NOT TRUST ITS EXIT CODE ... until that lands, and GOTCHAS 65 is present-tense. Port 4192, bugsweep 0 STATION MISS, git diff -- src/ empty so the world did not move. | scripts/health.mjs + scripts/checks.mjs registry | **THE CANARY CANNOT DIE.** `scripts/health.mjs` prints `WORLD BROKEN — __ct never appeared` through `console.log` and then **falls off the end, exiting 0** — it contains no `process.exit` at all (desk confirmed: `grep -c process.exit` = 0). It is **registered in CHECKS**, so a world that has stopped initialising renders as a green `ok` row, and it is the command **`CLAUDE.md` hands every new agent** as the way to answer "does the world actually initialise". w22 measured it against a page with no `__ct`: exit 0. It escaped every earlier sweep — including the desks own, which found it and wrongly explained it away as "reports state, probably intentional" — because it prints "WORLD BROKEN", not "FAIL". Of 122 registered checks it is the only one that cannot go red. **It also still defaults `SHOT_URL` to 4177**, so it can answer about somebody elses world. DONE WHEN: it exits non-zero on a broken world (proved against a page with no `__ct`), exits 0 on a healthy one, refuses or announces a defaulted port, and a canfail case makes it go red. |
| 57 | DONE w30 — PREMISE FALSE, and it is the most useful thing here: there is NO prop at x 5.75-6.25. That 0.5x0.5 box is a CITIZEN on the east walk lane - crowd-net.ts:87 EAST_X = ROAD_HALF + IN = 6.00, crowd.ts:167 gives it lane +/- 0.25 - and w24's red-dump read a MOVING box out of colliders() and wrote it down as static geometry. ROOT CAUSE of the item existing: trapAgainst / the V overlay treat moving citizen boxes as trap candidates (debug-collision.ts:128-153 colours whatever crosstown.ts:1387 hands it; crowd.ts:168 o.solid(box) puts every citizen in there), so the overlay paints red along the WHOLE east walk and one instance got filed as a prop. ct/bodega-corner.ts contains no 6.7 and registers no prop; the 6.700 face is its own :220 solid({minX: BX0-0.3}) with BX0=FACE=7.0. 6.70-6.25=0.45 DERIVED from those three source lines, nothing retyped, and confirmed by a synthetic citizen at lane 6.00 giving exactly 0.450 anywhere in z -94..-86 - the only east face at 6.70, which is why the same citizen reads 0.630/0.750 everywhere else. WALKED, not measured from above: 8/8 traversals of the band both directions crossed, and 6/6 deliberate wedges against a citizen actually standing in it left the player free (worst case 3 of 4 directions, never 0; caught ghost=true in the act, player walked 4.43 m through). MUTATION-TESTED by BUILDING the item's own hypothesis - a real static 0.5x0.5 prop at that exact spot - which took the walk to 8/8 FAIL stopping within 2 cm of what fp.ts RADIUS=0.36 predicts; md5 5f33fd0f->d20b4818->5f33fd0f, bytes confirmed both ways. NO world file changed; 5 probes in scripts/probes/, note at notes/w30-east-walk-trap.md. NOT FIXED, files not mine: (1) the mover-vs-trap bug above - ct/gap.ts + ct/debug-collision.ts, fold into item 59; (2) bodega-corner.ts:220 eats 0.30 m of the sacred 2 m lane (1.70 m clear vs 2.00 elsewhere) using the OLD blanket-footprint pattern street.ts:807-819 already deleted for the church, and tex-world.ts:1038-1046 records that 0.30 reserve was never what made the relief safe - needs its own item, pulling it back could expose the mouldings; (3) three genuine static reds under 0.72 m on the east walk that are NOT citizens (gaps 0.490, 0.490, 0.620); (4) w30-trap57-walk.mjs is a probe nothing calls, guard in name only. Port 4193, proved free, shut down. bugsweep 0 STATION MISS, 96 shots. | ct/bodega-corner.ts (the 0.45 m trap) | **A real 0.45 m trap, found while fixing the chamfer and left alone.** Between a prop at `x 5.75-6.25` and the block face at `x 6.7`. The 2 m lane is sacred indoors too, and 0.45 m is well under the 0.95 m the V overlay paints red for. w24 reported it rather than widening its own scope. DONE WHEN: the gap is passable, the V overlay shows no red there, and you have WALKED through it — not measured it from above. |
| 54 | DONE w29 — Parked sedan is climbable: road -> hitched trailer deck 0.50 -> boot lid 0.93, walked end to end on the BUILT bundle, tiers seamed off the car's own drawn panels (no ct/cars.ts edit - it is held by item 46). ROOT CAUSE the item got wrong: the tyre route is not TIGHT, it is IMPOSSIBLE - blocked() pads by RADIUS 0.36 and standTop() pads by nothing, so a step must sit RADIUS clear of everything walling it, and the tyre sits under a body collider 0.39 m wider than itself. BIGGER FINDING: the '21 mm margin' acceptance bar measures the wrong quantity. A hop must also cross RADIUS horizontally while above maxY-TOP_EPS; at the dt clamp that is 0.165 m per frame, so rise<=0.52 gets 3 frames (0.495 m, clears) and rise=0.53 gets 2 (0.330 m, does not). I built the boot->roof hop with EXACTLY the asked-for 21 mm and it failed 3/3; traced per frame in probes/w29-roof-hop.mjs. THE PICKUP'S SHIPPED ROOF HOP IS THE SAME 0.53 AND WINS THE TIE ONLY BY FLOAT LUCK (its heights are exact doubles; the sedan's come off a Float32 bbox and lose by 38 nanometres). So I did NOT ship a roof nobody can reliably reach - greenhouse stays a wall; the two hops shipped clear by 4 and 5 frames. Mutation-tested 3x with byte proof: deck flattened -> red, greenhouse made standable -> red, boot lid flattened -> PASSED, ie MY CHECK WAS ASLEEP (it asserted feet===boot.maxY, reading its expectation from the collider under test); now pinned against a freshly built sedan + a 0.52 m rise budget, and mutation 3 goes red. w21-roof-climb.mjs has the SAME tautology and needs the same fix. Traps: real before/after build 524->526 colliders (exactly my 2 tiers), 175->175 flagged. bugsweep 0 STATION MISS, 0 errors, 96 shots on the built bundle. Ports 4188 dev / 4189 preview, both shut down. WARNING for the fleet: git stash is SHARED across worktrees - my 'git stash push <path>' stashed nothing and the follow-up pop grabbed ANOTHER builder's stash into my tree; theirs survived and I restored cleanly, but use 'git checkout <ref> -- <path>' instead. Note: notes/w29-sedan-climb.md | crosstown.ts:488 (parked-car colliders) | **Item 47 named the wrong file and w28 handed it back rather than build something useless — requeued against the right one.** Every car collider in this project is built in **`crosstown.ts`**, not `ct/cars.ts`: `:488` gives parked cars a box with **no `maxY`** (so they are full-height walls) and `:745-835` holds the pickups four standable tiers. `cars.ts` draws meshes and exports constants and registers **no colliders at all**, so furniture added there would be a step nobody can stand on. w28 was blocked further because `crosstown.ts` was held by another builder at the time; it is free now. **The desk decision stands: do NOT build the tyre route** (0.66 m first step, 28 mm over the kerb, and a second hop at 31 mm — both tighter than the pickups 21 mm, which is already the tightest thing in the world). Pick ONE car and give it an honest route by shaping its own geometry. Useful measured numbers: apex is `0.5714 - 2*dt`, dt is clamped at 0.05 in `main.ts:107`, so **0.475 m is a hard floor** and margins against it are guarantees. DONE WHEN: the route is walked end to end on the built bundle, every hop has at least the pickups 21 mm margin at the dt clamp, and it is mutation-tested red by flattening one step. |
| 55 | DONE w27 — wetness's SLEPT was a race, not a blind needle: drying is wetness -= dt/dryFor with dt clamped at 0.05s (main.ts:107), so the street dries in SIMULATED time while the guard sampled it on 7x2000ms of WALL CLOCK — and canfail is the loaded case by construction (a build + a browser per case), so it is the one place the ladder falls short. Ladder is now 7x60 rendered frames. ALSO FOUND: streetStillWet was a sleeping verdict INSIDE the guard — it asked whether the surfaces had CHANGED since the storm, which drying instantly satisfies; watched it print OK over road 3c3c3c gutter 3c3c3c three times. Now stated against scene.userData.wetness with a bound DERIVED from the drying law. Full run: 45/45 CAUGHT, .canfail-last.json asleep=[], unprovable=[]. Proof it was the clock: at CPU_THROTTLE=20 the old guard was RED on a healthy world and GREEN on the mutation. Port 4194. | scripts/canfail.mjs + whatever guards wetness | **One guard is asleep: `wetness` SLEPT in the first end-to-end canfail run** (42/43 CAUGHT, 1 SLEPT, 0 unscored). A sleeping guard is a check that cannot fail, which this project rates worse than a check that is wrong. **Find out whether the needle misses or the guard genuinely does not care about the mutation** — those are different repairs, and only one of them is in `canfail.mjs`. DONE WHEN: `wetness` scores CAUGHT in a real end-to-end run, or the guard itself is fixed so it does, and `.canfail-last.json` records `asleep=[]`. |
| 65 | DONE w30 — The V overlay no longer calls pedestrians traps. ROOT CAUSE: ct/crowd.ts:168 (ctx.solid) puts every citizen box into the SAME colliders array the overlay scores, and traffic's vehicleBox hook does it for vehicles - so trapAgainst was measuring corridors against things that walk away. The citizen lane is x=6.00 (crowd-net.ts:87 EAST_X = ROAD_HALF + IN) spanning +/-0.25, against block faces at 6.70 (bodega-corner.ts:220) and 6.88 = 0.45 and 0.63 m, both under the 0.95 m that reads red, so every walker painted a standing red slot the length of the east walk. FIX: actors are still DRAWN (they really do stop you) in amber 0xffb020, but never SCORED - neither a trap candidate nor a wall that can form one - filtered by object IDENTITY against a Set filled at the only two hooks where an actor box enters colliders. NOT by shape: a size test works today and quietly excuses every real 0.5x0.5 crate with it. Added __ct.actorColliders() as a SEPARATE accessor rather than a flag on colliders(), deliberately - colliders() returns the live array BY REFERENCE and interiors-walk --selftest walls every door shut by pushing onto it, so a mapped copy would have left that selftest mutating a throwaway and passing having tested nothing. MEASURED, both rules scored in the SAME sample so it cannot be two worlds: OLD red 169-173 and VARYING frame to frame because actors move; NEW red 160-160 STABLE, which is exactly the static count w24 previously had to reach by filtering movers out by hand. Of the old red, 5-6 were actors themselves and 4-7 were static boxes red ONLY because an actor stood beside them - so 9-13 phantom reds per frame, and a non-deterministic count. VERIFIED ON THE BUILT BUNDLE by reading each wireframe's RENDERED material colour, not by re-deriving the rule: 346 green + 160 red + 12 amber = 518 colliders +1 player box = 519; all 12 actors amber and none red; red still 160 so nothing was silenced; V turns it fully OFF again 519->0; 0 console errors. MUTATION: a genuine static trap planted beside the walking lane - the exact geometry item 57 claimed and did not have - is STILL CAUGHT at gap 0.450 m. fp before/after on the built bundle: textures 44c087f0 and structure b9c8813c IDENTICAL, objects 8315 both sides, only 6 places differ and every one is within 5 cm - they are at x 6.00 and -6.00, i.e. THE SIX CITIZENS. bugsweep 96 shots 0 STATION MISS; tsc clean. NOT FIXED: (1) ct/gap.ts DELIBERATELY UNTOUCHED though item 65 names it - w27 holds item 59 against the same file and BUILDER-BRIEF §9 says skip on a collision; trapAgainst itself still has no notion of a moving actor, so any OTHER caller handing it an array containing actors has the same bug - one line or a doc comment once w27 lands; (2) nudgeClear's parked-car decisions (crosstown.ts:662) also call trapAgainst and I did NOT audit whether their array can contain actors at that point in build order - if it can, parking has been decided against pedestrians; (3) npm run fpdiff IS BROKEN AND CLAUDE.md DOCUMENTS IT AS THE WAY TO PROVE A CHANGE - package.json:15 runs fpdiff.mjs with NO arguments but the script requires two file paths and dereferences A._structure immediately, so the documented recipe crashes with a TypeError every time; works as 'node scripts/fpdiff.mjs shots/before.json shots/after.json'; (4) instrument caveat - my dev-side probes match actors BY VALUE because identity does not cross the browser boundary, and the idle vehicle pool is all parked at x=999 so their keys collapse; one probe said '27 actor colliders' where there are 12, do not quote the 27. Note notes/w30-overlay-actors.md. Ports 4193 dev + 4191 preview, both proved free, both shut down. | ct/debug-collision.ts + ct/gap.ts + crosstown.ts:1387 | **The collision overlay paints red for PEDESTRIANS, and the user uses that overlay to find bugs.** w30 established this while disproving item 57s premise: there is no prop at x 5.75-6.25 — that 0.5x0.5 box is a **citizen walking the east lane** (`crowd-net.ts:87` EAST_X = ROAD_HALF + IN = 6.00, `crowd.ts:167` gives it lane +/-0.25), and `crowd.ts:168`s `o.solid(box)` puts every citizen into `colliders()`. So `trapAgainst` and the V overlay treat **moving actors as static trap candidates**, and the overlay paints red along the whole east walk lane. A previous builders red-dump read a moving box out of `colliders()` and wrote it down as static geometry — which is how the desk came to queue a trap that does not exist. **This also means some earlier trap findings may be phantom; say which, if you can tell.** The overlay is the users own tool — *"it is how the user found two real bugs on its first day"* — so red must mean something he can act on. DONE WHEN: the overlay and `trapAgainst` distinguish moving actors from static geometry, the east lane shows no standing red, and a genuine static trap beside a walking citizen is still caught. |
| 58 | DONE w30 — CAUSE NAMED AND FIXED, ACCEPTANCE RUN BLOCKED - both halves matter. ROOT CAUSE: interiors-walk.mjs:801 hold('w', 2600) does not walk a DISTANCE, it walks however far the frame budget got through, and §5 starts 0.9 m from a door whose way-out spot has r=1.4 (ct/interior.ts:1255, not typed by me) - so on a fast frame budget the player walks out the FAR side of the trigger, the prompt is gone when read, press() is a no-op, 'back' is still an interior position, and the FIVE checks after it all read it. That is w24's '0,0,6,0 / 0,0,0,6' - one cascade, not six flakes. THE ITEM'S GUESS IS BACKWARDS AND THIS IS THE IMPORTANT PART: it says 'a fixed wall-clock wait under load'; the wait is right, the load is inverted. Old code measured 8/10 and 5/10 at CPU x1 (7 failures in 20, matching '1 in 4') but 10/10 AND 10/10 under throttle x8, always walking exactly 0.83 m - throttling makes the player travel LESS so he cannot overshoot. Opposite sign to item 50's jump-walk, which a slow frame TRUNCATED. HAD I ONLY RUN THE DONE WHEN'S THROTTLED RUNS THIS BUG WOULD HAVE PASSED 10/10 AND I WOULD HAVE REPORTED IT FIXED WITHOUT TOUCHING IT. FIX: new holdUntil() settles on the world condition with wall-clock only as a cap; §5 walks at the door until the way-out prompt is actually up. Settled measures 10/10 at x1 AND 10/10 at x8, travel 0.33-0.53 m. NOT A LOOSENING - the assertion is unchanged, it still reads the live prompt and still goes red if it never appeared; MUTATION-TESTED by settling on a condition the world can never satisfy, which degrades to the full cap and STILL FAILS 5/10, so holdUntil cannot manufacture a pass. BLOCKER, reported rather than worked around: interiors-walk exits 2 for EVERY room - 'the world publishes rooms this suite does not test: apt301' - because 23e7dcb3b registered apt301 TODAY and it is an ancestor of mainline, and checks.mjs:1051 renders exit 2 as FAILED (2), so THIS IS A LIVE RED CHECK ON MAINLINE RIGHT NOW, correctly red. So the DONE WHEN's '10 consecutive runs' is not executable today and my fix is downstream of the guard. I did NOT add apt301 to ROOMS: every ROOMS entry gets the full street-approach treatment and there is no inside-reached flag, while apt301 has NO street door (ct/doors.ts does not declare it, it is off-belt at y=2*ST, door internal to the stairwell) - dropping it in manufactures false failures, and giving it real coverage is a feature, not a flake fix. NOT FIXED: (1) queue 'add apt301 to interiors-walk ROOMS' ahead of re-running this item - today only seat-facing covers it, for its seat; (2) interiors-walk.mjs:838-840 states something FALSE - 'crowd and traffic actors are NOT in __ct.colliders()' - they ARE (crowd.ts:168 o.solid(box); crosstown.ts:1094 colliders:()=>colliders returns the live array; I measured six moving citizen boxes in it on item 57), and that false belief is the stated reason the landing check is a 3-attempt retry instead of deterministic; (3) the other fixed holds in §5 (2000 ms landing legs, 2600 ms §1 approach) are the same pattern, unaudited because the block fails before reaching them. Note notes/w30-interiors-walk-flake.md. Port 4193, proved free, shut down. node --check parses; no src/ file changed. | scripts/interiors-walk.mjs | **A suite that fails a quarter of the time cannot verify anything.** `interiors-walk.mjs bodega` is flaky **1 run in 4**, and w24 established it is **pre-existing** rather than caused by the chamfer work — measured both ways: the old staircase failed `0,0,6,0`, the turned-box version `0,0,0,6`. Same rate, different run. **Measure the flake; do not loosen the check.** Tonights two known causes for exactly this shape are a fixed wall-clock wait under load and sampling across two frames while a value is repaired in between. DONE WHEN: the cause is named and the check passes 10 consecutive runs under CPU throttle x8, still going red when the room is deliberately broken. |
| 59 | DONE w27 — gap.ts now reports the TRUE clearance between two boxes (every vertex against every edge, exact for convex polygons); the separating-axis test is kept only to decide THAT they face each other and to name the frame orientedFilled works in. Root cause: the corridor test only ever considered world X and Z, and an axis separation is the real clearance only when the boxes meet face to face. I GOT IT WRONG TWICE (narrowest-axis, then widest-axis, 0.634 m understatement) and all 16 hand-written cases passed the first wrong version — what caught both was a property test over 4000 random pairs against an independently-written exact polygon distance. First unit tests in the repo, test/gap.test.ts, 17 cases in 4 ms. WORLD DOES NOT MOVE: every static collider byte-identical before/after (512 of 518), red dump identical at 160. REGRESSION I CAUGHT: a vitest file under src/ kills the DEV world (vite executes it in the browser, __ct never appears) while npm run build stays clean — moved to test/, needs a GOTCHAS line. HAD TO REBASE onto 2c5ae6df2 first: my base predates the turned-box work entirely, and jump-walk.mjs conflicted (tip had rewritten it) - resolved by hand, re-verified green. NOT FIXED: interiors-walk.mjs refuses to run at all on the tip (unregistered room apt301), which may make item 58 unstartable. Ports 4187/4194. | ct/gap.ts | **`gap.ts` measures a turned box by its BOUNDING box** — safe today because exactly one turned collider exists, and w24 said so plainly rather than leaving it to be discovered. **It must become exact before a second turned collider is added**, and item 54 (a climbable route on a car) or any further chamfer work could be that second one. A bounding box around a rotated rectangle over-reports its extent, so `trapAgainst()` will start raising phantom traps the same way it did around the staircase — the bug this project has already paid to fix once. DONE WHEN: `gap.ts` measures a turned box in its own frame, a deliberately-placed second turned collider raises no phantom trap, and a genuine trap beside a turned box is still caught. |
| 60 | DONE w31 — Repacked the playable artifact on the collision work - no world source touched (git diff <pack>..HEAD -- src/ is empty), so this was a refresh not a repair. PATH /home/erick/projects/rpg/.claude/worktrees/agent-a1f8b52d50550c3c4/street/dist/artifact.html, 1117520 bytes FROM ls (pack-artifact.mjs printed the same number - the old UTF-16 undercount in GOTCHAS 63 is genuinely fixed), md5 73c2bdfc96ae9ad9a3171c54e7d1061b, build stamp df0f27deb = HEAD because I packed AFTER the final commit and ran no build after. Staged at street/artifact/ per GOTCHAS 63. check-artifact.mjs passes AND its --selftest is caught. bugsweep on the built bundle: exit 0, 0 STATION MISS, 96 shots. WALKED the bodega chamfer in the artifact from file:// - ONE rotated collider rot 0.7854, surface FLAT to 0.0 mm, no walk inside the wall. SAT AT A GAME in the artifact - walked 2 m in on held W, held [E] seats you at the slot, and Escape gets you back up (BUILDER-BRIEF 11); mutation-tested by holding the wrong key, which turns it red. NOT A CLEAN SWEEP, and I am not claiming one: 1 of 5 chamfer runs went RED on section 4a 'cleared the corner', which cleared 2.58/3.48/4.63/8.32/8.41 m across runs on IDENTICAL world bytes - it walks a fixed number of steps so it measures frames-under-load, not the chamfer; every clock-free verdict passed all 5. Reported, NOT re-tuned - w24's file. ALSO FOUND: w24-chamfer-walk.mjs has a bare default URL of 4210 instead of lib/aim.mjs (GOTCHAS 48). Publishing to the artifact URL is still the desk's step. Port 4192. | street/dist + scripts/pack-artifact.mjs | **Refresh the playable artifact — both halves of the users collision request have now landed.** Since the last publish: **collider ROTATION** (the bodega chamfer is one turned box instead of a staircase of 9; the collision surface went from stepping 152 mm to flat at 0.0 mm), the **groundPick** storey fix, `seat-facing` registered as a standing guard, and the canfail restore bug that had been scoring eight healthy guards as failed. The user asked for this directly: *"we should fix this so its not just a bunch of separate rectangles and its just made properly."* **Pack LAST, after your final commit, and do not run another build afterwards** — `npm run build` wipes `dist/` and takes `artifact.html` with it (GOTCHAS 63). Report the absolute path and byte count from `ls`, not from the script. DONE WHEN: `artifact.html` exists at a path you state, `check-artifact.mjs` passes, `bugsweep` on the built bundle is zero STATION MISS, and you have opened it and WALKED the bodega chamfer plus sat at one game. |
| 62 | DONE w29 — bugsweep now photographs interiors: the 3 room stations pass the room's own RoomDims.y instead of a literal gy 0, and verifyLanded checks the STOREY it landed on (it compared x/z only, which is the whole reason this survived - a camera at street level under flat 301 scored a pass). Eyeballed shots/bug-apt301-entry.png: it is now unmistakably the inside of 301 - 1997 calendar, bed with the [E] sit-and-watch-TV prompt, radiator, brick wall opposite through the glass. Room-count guard is now DERIVED from the ct/int-*.ts files on disk (writing one is what puts a belt room in the world), verified the 12 stems are exactly the 12 ids rooms() publishes; it reports 3 distinct failures (file with no room = failed to build, room with no file = roster rotted, room with no dims). apt301 is a DECLARED extra from ct/apartment.ts, not an int-*.ts, hence roomDims 13 vs rooms 12 - photographed, not counted in the belt roster. THIRD DEFECT FOUND AND FIXED, same family as item 61: bugsweep printed STATION MISS and EXITED 0, so a wrong-place sweep scored green for anything reading $?. It now sets exitCode on its OWN findings only - errors[] also collects benign browser warnings (THREE Clock deprecation, Canvas2D getImageData, WebGL ReadPixels) and gating on those would make it red always. Mutation-tested twice with byte proof and exit captured off-pipeline: entry back to gy 0 -> 'aimed at floor y=5.40 but settled on y=0.00, WRONG STOREY', exit 1; a room dropped from the registry with its file left -> 'ct/int-diner.ts exists on disk but that room never registered', exit 1. Both reverted, cksum back to baseline, tree clean. Clean run 96 shots, 0 STATION MISS, 0 COVERAGE, exit 0. NOT FIXED: nothing asserts a room's 3 stations are DISTINCT (all three could collapse and you'd still get 3 files); errors[] still conflates console noise with findings; rooms() vs roomDims() differing by design is undocumented at the __ct surface. Port 4188, shut down. Note: notes/w29-bugsweep-storeys.md | scripts/bugsweep.mjs | **Bugsweep photographs the outside of the building while reporting success.** w22 caused and flagged this: bugsweep warps with a literal `gy 0`, **three storeys below flat 301**, so it shot the exterior while `verifyLanded` (which compares x/z only) passed. w22 published `RoomDims.y` so the fix is `gy: r.y` at **3 call sites**. Second defect in the same file: its `roomDims.length < 12` guard **now sleeps at 13** — the apartment was just registered, so the count moved past the threshold and the guard stopped biting. DONE WHEN: bugsweep photographs the interior of every registered room (spot-check the apartment shot by eye), the room-count guard is derived rather than a typed threshold, and it goes red when a room is removed from the registry. |
| 63 | DONE w29 — wetness now asks whether the street IS wet. ROOT CAUSE confirmed exactly as the item stated: the verdict was 'last.broad !== wet.broad', i.e. the surface differs from mid-storm, which is what DRYING does - the check was satisfied by the very thing it was written to detect. Fix: a DRY reference taken at the SAME HOUR before the storm (props colours a surface base*ambient when dry and multiplies toward WET_WALL when wet, so the hour's own light changes the colour by more than the rain does - a cross-hour reference would measure the sun). Free to take there because wetness starts at 0 on load and the spawn is indoors where rainLevel is forced to 0; measured wetness exactly 0, and the script now refuses to continue if it is not. New predicate has two legs and asks for a DIRECTION: world's own wetness > 0.004 (props' own early-out epsilon) AND both surfaces DARKER than dry by >0.02 luminance. Two legs because a scalar-only check passes a world whose tint stopped being applied, and a colour-only check passes a world that froze the tint on; luminance rather than !== because a tint that LIGHTENED the road is the inversion bug this file exists for and !== cannot tell those apart. Threshold is not tuned-until-green: measured margin 0.1336 road / 0.1462 gutter, an order of magnitude above 0.02, and the failing case reads 0.0000. VERIFIED BOTH DONE-WHEN CONDITIONS: canfail end-to-end 'OK wetness CAUGHT the street bone dry on the last drop of rain', 1/1, every mutated file restored byte-for-byte; and red on a deliberately dry street (exit 1, bytes 15395->15458, reverted to 874926634 15395). IMPORTANT METHOD NOTE: my first mutation (never let it rain) goes red but proves NOTHING - the OLD predicate catches it too, since with no storm the mid-storm sample is dry and dry!==dry is false. The real sleeping case is 'rained then dried completely', unreachable live inside a check because drying is 48*(1+soak*1.5) seconds by design, so probes/w29-wetref.mjs applies BOTH predicates to readings measured in the same run: OLD -> PASS (3c3c3c !== 1e1f21), NEW -> FAIL. NOT FIXED: gutterHolds is the SAME weak shape ('strip !== broad' asks only that they differ, so a world drying the gutter FASTER would pass - same one-line luminance fix, left as outside this item); stillFilling/stillDark/individual/spread are computed and used in no verdict, and 'enough' is a hardcoded true in the exit condition - dead predicates reading as coverage that is not there. Port 4188, shut down. Note: notes/w29-wetness-predicate.md | scripts/wetness.mjs | **`wetness` SLEPT 5/5 — its predicate asks the wrong question.** Now that canfail scores sleeping guards correctly (it had been recording genuine SLEPTs as unscored NOT-RUN, because `servedEntry()` is only reached when a case does *not* go red — landing precisely on a sleeping guard, by which point undicis pooled keep-alive socket is dead and the error swallowed), `wetness` is exposed. Its verdict `streetStillWet` asks whether the surface **differs from the storm** — which is exactly what drying does — so w22 measured it passing on a street that was bone dry. **Fix the predicate to ask whether the street is wet, not whether it differs.** DONE WHEN: `wetness` scores CAUGHT in an end-to-end canfail run, and goes red on a deliberately dry street. |
| 64 | DONE w29 — (1) w21-roof-climb registered in checks.mjs on the SLOW tier (that file's own documented home for walking suites, alongside spots-walk and lotwalk) and upgraded: it now exits the roof ALL FOUR ways. THREE defects fixed in the script itself: it printed 'FAIL: route incomplete' and EXITED 0 (survivable while nothing ran it, fatal once registered - third file in this family after health.mjs item 61 and bugsweep item 62); it was TAUTOLOGICAL, asserting feet === box.maxY from the collider under test, so flattening a tier stayed green - each tier is now pinned against a freshly built pickup measured off its own panels via carVariant; and it only ever left the roof forwards. TRAP I WALKED INTO AND FIXED, worth recording: my first four-direction test WARPED to the roof and PASSED all four with feet 0.00 - warp writes x/z but not your height, so it put the player at street level INSIDE the truck and unstick shoved them out sideways; 'did you get down' was trivially true because they were never up there. It now CLIMBS for real before each direction and asserts feet ~= roof.maxY first, and an unreachable direction prints MISS and fails rather than scoring. Mutation-tested with byte proof: PICKUP_CAB.roofY -> 1.85 (96994->97004) gives 'FAIL: tier pickup-cab-roof stands at 1.85 - the truck has no panel at that height', exit 1 - and the OLD check would have expected 1.85 and passed, which is the tautology demonstrated. Reverted to 274969861 96994. (2) TRAFFIC/CITIZEN COLLIDERS: MEASURED AND CLOSED WITH THE NUMBER, not fixed. traffic.ts contains no 'maxY' at all, confirmed. The real risk is the REVERSE of standing on a bus: a full-height box sweeping a standable tier's footprint blocks a player standing there and unstick shoves them off. probes/w29-traffic-reach.mjs, 2400 frames, 'moving' derived from the box changing rather than from a tag since neither traffic nor citizens stamp one: closest any moving collider EVER comes to any standable tier is 0.778 m against RADIUS 0.36 - a 2.2x margin at the worst point in the world (pickup tiers 0.778, sedan 4.4-5.4). So no maxY is INERT; adding one would be a collider nobody meets. IT ALSO DISPROVES w21's OWN HYPOTHESIS for the one unreproduced STUCK on the cab roof - w21 guessed 'a passing vehicle blocking at roof height', and traffic never gets within 0.778 m, so it cannot have been that; nobody should spend time there. NOT FIXED: probes/w21-roof-exit.mjs is now redundant with the promoted coverage and should be deleted so nobody maintains two; the check costs ~4 min because each direction re-climbs (if too slow, rotate one direction per run - do NOT go back to warping); jump-walk's hardcoded default port is still 4185. Ports 4188/4189, both shut down. Note: notes/w29-roof-check-and-traffic.md | scripts/w21-roof-climb.mjs + ct/traffic.ts | **Two loose ends from the roof work.** (1) `w21-roof-climb.mjs` — the only proof the climbing route works — **is registered nowhere**, so nothing re-runs it and the route can rot silently. It also only ever exits the roof forwards; w22s throttled probe exits all four directions and found no wedge in 27 runs, so promote that coverage. (2) **Traffic and citizen boxes are walls at every height** — `ct/traffic.ts` sets `maxY` nowhere. w22 found nothing reached the roof in 2347 frames (closest 0.418 m, a person-sized box on the pavement), so this is **not urgent**, but it is the same class the parked cars had. DONE WHEN: a roof-climb check is registered and runs in the suite, exits all four directions, and the traffic/citizen collider question is either fixed or measured and closed with the number. |
| 71 | DONE w28 — All three orphan checks REGISTERED after running each by hand first, as item 71 required — none was measuring a dead world, nothing deleted. ROOT CAUSE: each ships a working --selftest and was in no tier of npm run checks, so all three ran exactly never; a check that is not run cannot fail. EVIDENCE, by hand against the built bundle: ledger-intact 253 rows -> 253 intact, 0.06 s, no browser at all; masonry 305 stamps, 16 disagreements ALL explained by whole-texel canvas rounding and 0 faces actually authored wrong, 2.9 s; H-flare-silhouette 23 cars, 0 meshes outside the tyre-and-body silhouette, 1.0 s. Every --selftest was also run and all three go red ('DAMAGED: lost rows, shrunk evidence' / 'selftest: caught it' / 'SELFTEST PASSED — the re-attached flare was caught', naming the BoxGeometry at half-width 1.64 against a 0.90 body). THE ONE THAT NEARLY WENT THE OTHER WAY: masonry looked like a duplicate of the already-registered density and would have been a delete-with-reason — it is not, it is density.mjs's documented SUCCESSOR ('that fixes what broke density.mjs: its filter was geometric, so foliage, ground decals and signage sat in a net meant for walls') and asks a second question density cannot, whether each stamp AGREES with the face it is mapped to. All three are DEFAULT tier, not slow: none walks and the slowest is 2.9 s, which is this file's own rule that SLOW is a runtime tier. checks-registered.mjs is GREEN, 136 registered, and MUTATION-TESTED: delete the masonry line and it exits 1 naming scripts/masonry.mjs, restore it and it exits 0, with checks.mjs bytes moving 77523 -> 77436 -> 77523 — so the audit can genuinely fail and the registration is what fixed it. NOT FIXED: (1) density and masonry now audit the same property twice with two different filters and the REGISTERED-longer one is the one masonry exists to have fixed — retiring or demoting density is a real decision with a real argument and is not what this item asked; (2) masonry correctly exits 3 against a stale dist/ via its which-world guard, so if the runner is ever aimed at a stale preview masonry will be the check that reports it and it will look like a masonry defect until someone reads the message; (3) ledger-intact needs no server but is registered into a runner that short-circuits every remaining check on serverDied, so a browser casualty silently takes the one check guarding LEDGER.md with it — moving the no-browser checks first is a change to the runner's control flow rather than its registry and I would rather it were queued than slipped in under a registration item. Port 4180; rebuilt once because masonry's which-world guard correctly refused a dist/ older than HEAD. | scripts/checks.mjs registry | **Three checks offer a `--selftest` and are registered nowhere, so they have never run: `H-flare-silhouette`, `ledger-intact`, `masonry`.** `checks-registered.mjs` — the audit built to catch exactly this — is **red on mainline** saying so. `ledger-intact` is the one that guards `notes/LEDGER.md`, the file this project treats as its record of what is done. **Decide each on evidence rather than registering all three reflexively**: a check that has never run may be measuring a world that no longer exists (that has happened here repeatedly). Run each by hand first and see what it says. DONE WHEN: each of the three is either registered and passing, or deleted with the reason, and `checks-registered.mjs` is green. |
| 69 | DONE w33 — Item 69 settled by counting frames, not vertical margin: a hop must cross RADIUS horizontally while above maxY-TOP_EPS, and blocked() leaves the player up to a FULL FRAME short of the pad, so the real standoff is 0.515 m (measured), not 0.36. Shipped roofY 1.50 cleared by EXACTLY ZERO (a 100-nanometre raise took it 4/4 -> 0/4) and the new rule shows it does not land at a 0.515 standoff at all — a real defect in the published artifact, not just fragility. roofY -> 1.415 gives 5 frames vs 4 needed, centred 25 mm from both band edges: 20/20 at the strongest CPU throttle this sandbox actually sustains — **x8 kills the headless browser here** (software WebGL), so a builder measured at x4 on a loaded box and said so rather than quietly running x2. **Report the multiplier you achieved, not the one asked for** AND 20/20 unthrottled. w21/w22 were right that it climbs; w29 was right about the mechanism and its 2-frame table is right for the SEDAN (Float32 roof) but the pickup gets 3; the desk's 21 mm bar measured the wrong quantity. w21-roof-climb.mjs section 8 now measures standoff, per-frame walk and frames-above-threshold IN-WORLD (no fp.ts constant typed in) and fails unless spare >= 1; mutation-tested red at 1.50 and 1.470. bugsweep 0 STATION MISS, sedan-climb + jump-walk pass. NOT FIXED: crosstown.ts lines ~725-737 and ~915-923 still assert the old '21 mm' and 'wins the tie by luck' — stale prose, no functional dependency, but that file is not named in item 69 so I left it; also bed-floor->rail has the same zero-spare shape and no check. | ct/cars.ts (PICKUP_CAB.roofY / PICKUP_BED.railY) + scripts/w21-roof-climb.mjs | **BUILDERS DISAGREE ABOUT WHETHER THE SHIPPED ROOF CLIMB WORKS, AND THE DESK HAS ALREADY PUBLISHED IT. Settle it.** The rail is 0.97 and the roof 1.50, a **0.53 m rise**. Three accounts, all from builders who measured: (a) w21 built and walked the route, mutation-tested red twice; (b) w22 climbed it 27 times under CPU throttle x2/x3/x4 and escaped every time; (c) **w29 says 0.53 is on the wrong side of a cliff** — a hop must also cross `RADIUS` 0.36 **horizontally** while above the threshold, which at the dt clamp is 0.165 m/frame, so rise <= 0.52 gets **3** frames and 0.53 gets **2** (2 x 0.165 = 0.33 < 0.36). It built a roof hop to the desks own 21 mm bar and it **failed 3/3**, and calls the shipped one a tie won by float luck. **The desks 21 mm acceptance bar measures the wrong quantity** — it is a vertical margin on a constraint that is also horizontal, and that is the desks error, not w21s. **Do not assume any of the three is wrong.** Reproduce the climb many times at the dt clamp and on a fast frame, count frames above the threshold directly rather than inferring, and if it is marginal **lower the rail or the roof until it is not**. This is in the published artifact, so a coin-flip climb is shipping to the user. DONE WHEN: the climb succeeds 20/20 at the strongest CPU throttle this sandbox actually sustains — **x8 kills the headless browser here** (software WebGL), so a builder measured at x4 on a loaded box and said so rather than quietly running x2. **Report the multiplier you achieved, not the one asked for** AND 20/20 unthrottled, the frames-above-threshold figure is stated, and the acceptance rule is rewritten to measure horizontal clearance too. |
| 68 | DONE w32 — roomDims() was two registries answering two questions and the coverage guard asked the wrong one: apt301 (a 3m third-floor flat, cx198/cz-16/y5.4, no street door) made interiors-walk exit 2 for ALL 12 rooms. ct/interior.ts now publishes RoomDims.belt (derived, not typed); the guard partitions on it — belt rooms must be in ROOMS, off-belt in OFF_BELT naming the check that walks their door — both still exit 2 when unaccounted for. apt301 is now genuinely walked at built.y. Full suite 312/318, all 13 rooms, no FAILED(2); the 6 remaining fails are pre-existing (4 keeper-station, 2 night-light) and the belt loop is byte-identical. Port 4188. | ct/interior.ts ROOMS registry + scripts/interiors-walk.mjs | **A registered check has been RED for every room on mainline since tonight, and it is a regression from tonight.** `interiors-walk.mjs` exits **2 for every room** since `apt301` was registered (`23e7dcb3b`), and `checks.mjs:1051` renders that as `FAILED (2)` — so the suite has been reporting a wall of room failures that are one registry omission, not twelve broken rooms. The apartment was added to the dims registry so `seat-facing` could finally see it (a genuinely good fix) but was never added to `ROOMS`, which `interiors-walk` iterates. **A second builders fix for this file is downstream of that guard and unreachable until this lands.** Note the shape: the same "could not measure vs measured and wrong" confusion this project has now paid for five times — the check cannot start, and prints as though the world were broken. DONE WHEN: `interiors-walk` runs against every room including apt301, the suite shows no room FAILED(2), and removing a room from the registry still makes it go red. |
| 56 | DONE w28 — door301's flake is fixed and the cause is MATHEMATICAL: __leafSig() returned the sum of squares of the leaf's world matrix — the Frobenius norm — and a door rotating about its own hinge CANNOT change it (translation terms never move; an orthonormal basis has norm 3 at every angle). So press()'s 'wait for the swing to start, then stop' never armed — every press reported moved=false ON RUNS THAT PASSED — and it silently degraded to a fixed wall-clock 1500 ms (its own START_CAP). That is ~36 frames idle (door finishes, green) and ~6 frames under load (collider read on a moving door, red). Byte-identical bytes, green then red, exactly what w26 saw. The bitter part is that press() already carried a long correct comment saying the swing is driven by frames and must not be slept through; the reasoning was right and the instrument did not implement it. REPRODUCED FIRST at DOOR301_CPU=4: 0/3, same two assertions every run. FIVE instrument faults fixed, NO expect() tolerance touched: (1) signature now transforms local (1,0,0) to world so it carries the rotation basis — moved=true in 2-3 frames; (2) __leafSig traversed the WHOLE scene with updateMatrixWorld(true) every rAF — 2.25 SECONDS per frame at x4, the instrument dominating what it measured — mesh is cached now; (3) both caps were wall-clock in a frame-driven loop (START_CAP was 6 frames at x4, RUN_CAP was 4) and are frame counts now; (4) keyboard.press('e') was a TAP (BUILDER-BRIEF 5) — E is held across 3 painted frames and waits for an [E] prompt first; (5) a dead server crashed to exit 1, which here means 'the door is WRONG' — a soak scored two reds against a good door when the preview was reaped — now exit 3 (GOTCHAS 32). press() also logs moved/frames/ms every time, which is what turned this from a hunt into a diagnosis. AFTER: x4 11 passes, most recently 6 CONSECUTIVE, taken at machine load average 18-21 which is a heavier real throttle than the synthetic x4; x1 3/3 with every press moved=true; --selftest still CAUGHT (6 FAILs, 'SELFTEST PASSED'). COULD NOT DO x8 AND IT IS NOT THE DOOR: at x8 the headless BROWSER PROCESS dies ('Creation of StagingBuffer's SharedImage failed' -> 'Target page has been closed') because Chromium here is --enable-unsafe-swiftshader software WebGL; identical with screenshots on and off, before and after my changes. I substituted x4 on a loaded box and said so rather than quietly running x2 and calling it x8. Also did NOT reach 10 consecutive in one unbroken block: one x4 run exited 1 with NO assertion FAIL printed (an exception, right after I restarted the preview) and never recurred in the 11 runs since — flagging rather than rounding away. NOT FIXED: that one exception; the fixed 350-500 ms sleeps before reads OUTSIDE press(), notably the waitForTimeout(900) for unstick which is frame-driven for the same reason; and DOOR301_CPU/DOOR301_NOSHOTS are not wired into checks.mjs, which is where throttled soaking belongs. Port 4180. | scripts/door301.mjs | **A check that fails and then passes on the byte-identical build is a check nobody can act on.** w26 saw `door301.mjs` fail inside `checks` and go green on re-run against the same bytes. **Measure the flake; do not loosen the check** — this project has a documented family of guards that slept, and the cheapest way to join it is to widen a tolerance until an intermittent failure stops showing. Likely candidates given tonight: a fixed wall-clock wait under load, or sampling across two frames while a value is repaired in between. DONE WHEN: the cause is named and the check passes 10 consecutive runs under the strongest CPU throttle this sandbox actually sustains — **x8 kills the headless browser here** (software WebGL), so a builder measured at x4 on a loaded box and said so rather than quietly running x2. **Report the multiplier you achieved, not the one asked for**, still going red when the door is deliberately broken. |
| 66 | DONE w35 — Re-verified a seeded random sample of 20 CONFIRMED ledger rows against the live world: 19 HOLD, 1 DEMOTED, pass rate 95%. ROOT CAUSE of the one demotion (L260, wall pooling): it is not a world defect but an unfalsifiable check — wallpool.mjs reimplements props.ts:786's smoothstep instead of reading it, and userData.sizeW is on 0 of 8324 objects, so the check agrees with its own copy of the rule; its own auditor had already written CANNOT VERIFY and nothing has changed. Backed the 19 holds with 8/8 canfail mutations CAUGHT. Nearly filed a FALSE demotion on the cat row by selecting the nearest litter as 'the paper' when props.ts:3142-3144 shows both near pieces are cardboard and the newspaper is 2.6 m away. | notes/LEDGER.md | **Sample-verify the ledger: 248 rows say CONFIRMED and tonight proved that CONFIRMED can be untrue.** Two headline rows were contradicted in one evening — the bed TV (a coordinate declared twice, so the seat moved and the lit-test did not) and blackjack (whose check had gone stale) — and both had been sitting green. **Take a RANDOM sample of 20 CONFIRMED rows and re-verify each against the world as it is now**, not against the evidence the row cites. **Demoting is a success** and is the entire point; a row that cannot be reproduced today is not confirmed, whatever it says. Report the pass rate — that number is worth more than the individual repairs, because it tells the desk how much of "done" it can trust. DONE WHEN: 20 randomly-chosen rows each have a verdict from a fresh measurement, every failure is demoted with its reason, and the sample pass rate is stated plainly. |
| 70 | DONE w32 — PARTLY DONE, handing the big half back. Landed: scripts/checks-can-fail.mjs, the guard DONE WHEN asks for — a registry row must declare a failing path, be EXEMPT with a reason, or sit on an explicit debt register; both mutations red (new undeclared row; a register entry that gains a proof is flagged stale). Inventory (probes/w32-failpath-inventory.mjs): of 125 checks, 99 declare a failing path, 23 declare NONE, 3 exempt. Fixed a real one: L-every-stool-seats-you's --selftest set mode to a string not in MODES, so the flag's only effect was exit 2 — an unreachable selftest. NOT DONE and not faked: the behavioural sweep of all 125. There is no universal way to force a check to fail (a dead server tests 'could not measure', exit 3, correctly), so it means running 99 selftest/canfail mutations — hours, interiors-walk alone is 10-25 min. Queue it as a multi-hour item split by tier. Also found: checks-registered.mjs is RED on mainline, not my doing — H-flare-silhouette, ledger-intact, masonry offer a --selftest and are registered nowhere, so they run never. Port 4188. | scripts/ (every registered check) + scripts/checks.mjs | **Sweep for checks that print failure and exit 0 — three are already known and the desks own earlier sweep was inadequate.** Confirmed so far: `health.mjs` (fixed under item 61), `bugsweep.mjs` (printed STATION MISS and exited 0 — fixed under item 62), `w21-roof-climb.mjs` (fixed under item 64). **The desk swept for this once and found only one**, because it grepped for whether `process.exit` appears **anywhere in the file** — and all three of these DO call it, just never on the path that matters. **The right test is behavioural, not textual: make each check fail and read its status.** GOTCHAS 65 is the standing entry. DONE WHEN: every registered check has been made to fail at least once and its exit status recorded, any that exit 0 on failure are fixed, and a guard exists so a new check cannot join the registry without a failing-path exit code. |
| 72 | DONE w35 — PARTIAL, HANDING BACK 14 of 15 — please re-queue. Split the item's 23 into 15 fast-tier + 8 walking (the item did not say which). All 15 go NON-ZERO on a dead port, so there is no health.mjs-style 'printed a failure and exited 0' guard in the fast tier — but 12 of 15 exit 1 where they should exit 3, making 'no server' look like 'broken world'. Cleared 1: A-eye-height-holds now has canfail case eye-gate-flat, proven CAUGHT (debt register 23->22). ROOT CAUSE of my first failed attempt, and the warning for whoever takes the other 14: I mutated the number the script PRINTS instead of the symptom it ASSERTS on, canfail said SLEPT, and the check was right — several of these scripts deliberately separate diagnosis from assertion. SEPARATELY: scripts/checks-registered.mjs EXITS 1 TODAY and predates me — H-flare-silhouette, ledger-intact and masonry have selftests and are in no tier, so they run never. | scripts/ (fast-tier checks only) + scripts/canfail.mjs | **23 of 125 registered checks declare NO failing path — sweep the FAST TIER only.** This is the first of a split; the full behavioural sweep is hours of walking and a builder correctly refused to fake it. **A textual audit is not enough and the desk proved that the hard way**: it grepped for `process.exit` anywhere in the file, found one of three real cases, and the other two called it just never on the failing path. **Make each fast-tier check actually fail and read its status UNPIPED** (`$?` after a pipeline is the last commands status — that has already produced one false bug report here). Note the shape from `health.mjs`: the naive repair is its own bug, because a dead port already exits 1, so "exit 1 on failure" sends someone who forgot to start a preview to debug their own code. Three statuses, not two. `scripts/checks-can-fail.mjs` already exists as the guard. DONE WHEN: every fast-tier check has been made to fail once, its status recorded, and any that exit 0 on failure are fixed. |
