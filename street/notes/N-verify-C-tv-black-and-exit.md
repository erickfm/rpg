# VERIFY C's two TV rows — both hold, and one has a before/after I happen to own

Builder N, verifying rows I did not build. **Not marked CONFIRMED** — only the
desk or the auditor may. Built bundle on 4195, HEAD `278c7c8a9`.

---

## "how do i stop watching the tv" — all three claims, exactly

This is the one that matters: the user's report was **player-blocking**, and the
row's own evidence records that it was nearly lost twice in bulk ledger edits.

**1. The prompt says the right thing and does not flicker.** 6 yaws × 5 pitches
from the seat:

```
30 look directions -> 1 distinct prompt:  "[E] stop watching TV"
```

**One**, not two. C's point was that *"stand up"* is right for a bench and wrong
for a state you stay in, and that the prompt must not depend on spot selection —
both hold. It never changed and never blanked.

**2. E leaves the seat from every direction.** Re-seating between each so the
test cannot pass on a stale state:

```
E leaves the seat: 18 of 18   (6 yaws × 3 pitches, including straight up and down)
```

**3. Escape leaves any seat — and I checked a SECOND KIND of seat, because
"any" is the claim.** C says this world had no cancel binding at all before:

```
bed seat, Escape  -> out
a church PEW, Escape -> out
```

Both. Testing only the bed would have proved the bed (GOTCHAS §41 — a claim
about a class needs a second instance, and the second one is where it hides).

Worth connecting to two earlier findings of mine, because this row closes them:

- I recorded on C's seat-exit row that *"there is no Escape binding anywhere"*
  had **gone stale** — `hud.ts:299` had one, in K's panel gate. C has now added
  the seat half, so a seated player has the second exit that row was about.
- C's own blast-radius measurement — **149 of 225 seats with a non-stand spot
  inside the stand radius, 78 of them slot stools** — is what made this
  worth fixing rather than patching. With E and Escape both leaving, the
  ambiguity C measured no longer decides whether you get up.

---

## "i want the tv black" — it is black

**STATION as C gave it: 301 at 23:30 with the set OFF**, which is the hard case
because a dark casing against a dark room is where a flat black would disappear.
`shots/N/tvblack-night-off.png`, clock verified at 23:30 and `tv.on` false.

**I have the before by accident**, from verifying a different row of C's two
hours ago: `shots/N/tv-e-off-facing.png` shows the same set in the same room
with a **beige/cream** case. The pair is better evidence than either frame — the
shape is unchanged and only the casing colour moved, which is precisely what the
row claims.

C's *"black plastic is not black"* holds in the geometry, not just the
screenshot. Sampling the untextured materials at the set's own position:

```
#36363f   the top face, where the moulding catches light
#1f1f25
#161619   darker still
```

A very dark neutral grey with a lighter top, exactly as described — not `#000000`
anywhere on it. It separates from the wall, from the crate under it, and from
the dark grey-green of the dead screen.

---

## What I did not check

The row also claims *"the bezel's shape is untouched."* I have no structural
before/after for the bezel — my earlier shot is a photograph, not a fingerprint —
so I can say the shape **looks** unchanged between the two frames and not that it
**is**. `npm run fp` would settle it and I did not run it.

— N
