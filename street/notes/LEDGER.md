# Request ledger

One line per user request. The desk's own accountability record: it exists
because requests were coming back a second and third time, which always meant
the same thing — the desk routed it, assumed, and never checked.

**Format is parsed. Keep it.**

    | STATUS | owner | request (user's words, trimmed) | evidence |

`STATUS` is one of:

- **OPEN** — routed, not yet landed
- **LANDED** — the builder says it is done, nobody has checked
- **CONFIRMED** — the desk or the auditor saw it working in the world
- **REJECTED** — the user looked and said no; it goes back OPEN with a note

**LANDED is not done.** The only status that counts is CONFIRMED, and only the
desk or the auditor may set it, never the builder that did the work.

`scripts/ledger.sh` lists everything not CONFIRMED. Run it before telling the
user anything is finished.

| STATUS | owner | request | evidence |
|---|---|---|---|
| CONFIRMED | B | "make street light a bit more broad in their emitted light (like a wider beam) and make the unilluminated stuff darker. it should feel scarier at night i want to be able to see stars sometimes" | **HAD NO LEDGER ROW AT ALL** — routed to B in FEATURE-REQUESTS and never tracked, so nobody could see it was done. All three parts walked at night on build 98042722a. (1) WIDER BEAM: `LAMP_R` 7.0 with a full-strength core to 1.8 m, chosen against the 16.4 m head spacing so consecutive pools still fall 2.4 m short of each other — wider, not continuous. (2) DARKER UNLIT: per-surface night floors; main street reads 0.5278 under a head against 0.0450 mid-block, an 11.7x range (side street 12.6x). `shots/night-street.png`. (3) STARS: dome present and player-anchored, `shots/night-stars.png`. The word "sometimes" is honoured literally — stars are gated on WEATHER, `clear = max(0, 1 - rainLevel*2.4)`. Watched it: in a night storm outdoors, rainLevel 0.942 -> dome visible FALSE, opacity 0.0038. NOTE this request asks for the OPPOSITE of B's own queued finding "lamp spacing leaves the middle of the block dark" — the user wants the gaps dark. That finding should be dropped, not fixed. — **AUDITOR CONFIRMED on all three parts by walking it at 22:00 (build aea86749a).** `shots/nl-street.png`, down the west walk: the lamp heads throw **broad soft pools**, the pavement **between** them is genuinely dark, lit windows glow warm opposite, a pedestrian reads as a silhouette, and stars are visible overhead. It reads as scarier at night, which is what the user asked for. **Stars measured, not just seen:** looking up at 22:00, **453 pixels of 38,400 in the upper sky are brighter than 90** against a sky mean of 13.8 — a star field, present and distinguishable from sky glow. **[What I could NOT reproduce, said rather than glossed]** B quotes **11.7×** between a surface under a head and one mid-block. I tried twice and neither attempt is comparable: rendered frame brightness looking down gave 4.0×, but that mixes kerb, walk and road in one view; reading per-mesh colour on the walk gave 596×, but that compares *different objects* rather than one surface lit two ways. Notably **0.0450 appears exactly where B says mid-block is**, so the figure is real — I just could not identify the matching under-head surface to divide by. **The confirmation rests on the walk and the star count, not on reproducing that ratio.** **Note:** B records that this request **had no ledger row at all** — the fourth untracked request found, and the first a builder caught themselves. scripts/lampbeam.mjs, lampfloor.mjs, nightlook.mjs. |
| CONFIRMED | F | wheel arches read as arches | +0.057 m arch above tyre, walked from the kerb |
| CONFIRMED | E | library steps climbable | gy 0.42 → 0.99 |
| CONFIRMED | E | church steps + churchyard | gy 0.31 → 0.51, in through the gate |
| CONFIRMED | E | park lit | 20 light sources, ten lanterns, three ranks |
| CONFIRMED | E | park not a yard | 42.5 m walkable, 569 meshes |
| CONFIRMED | F | library courtyard benches sittable | `[E] sit` on the frontage |
| CONFIRMED | F | interior people on the 8-angle atlas | 4 rooms, 8 distinct frames each |
| CONFIRMED | G | casino + hotel blades read correctly | both directions, from the street |
| CONFIRMED | D | burger barn red + beige | no yellow remains |
| CONFIRMED | C | car lot: enterable, office at back, rows | walked in |
| CONFIRMED | D | bodega entry blocker | three approaches |
| CONFIRMED | B | night: road darkened, lamps reach objects | measured |
| CONFIRMED | desk | world stops reloading under the player | two runs, no change |
| CONFIRMED | D | alley grate matches the kerb inlet | frame, bars and depth visible in the user's own shot |
| CONFIRMED | C | TODAY ONLY board removed | only a comment remains recording its removal |
| OPEN | E | park: "some way to represent a grass field" (mowing stripes) | **AUDITOR WITHDRAWS ITS OWN CONFIRMED — the user is right (build f4e54cbce, 13:00).** My earlier evidence read *"unmistakably as mown grass from both ends and from the mound"*. That was true of the viewpoints I chose and it is not true of the one the user judges from. **The stripes ARE real** — `shots/ps-along.png`, standing on the field looking down its length, shows broad alternating bands unambiguously, and re-probing at 8 points looking ahead finds banding everywhere, so E's work is in. **What is still wrong is where it carries to.** From the park entrance (`shots/ps-enter.png`, standing at (−9, −72) walking in off the street, which is how anyone first sees it) the mown panel is a **small distant rectangle behind railings**, the foreground lawn you actually stand on reads as plain mottled green, and the sightline is substantially blocked by a grey mass filling the left third of frame and a lamp post splitting the right. **First impression of the park is unstriped grass.** Actionable for E: carry the stripes to the entrance lawn, and/or widen them so one band is legible at close range, and/or clear the entry sightline. **[Instrument caveat, so nobody inherits a bad number]** my light/dark amplitude metric selects green-dominant pixels, which counts TREE FOLIAGE as grass — the 42–54% readings at the gate and far west are leaves, not mowing, and must not be quoted. Second CONFIRMED I have withdrawn this audit, both for the same reason: judging from a viewpoint chosen to show the feature rather than the one a player arrives at. — auditor |
| OPEN | E | park: "topographical changes" | MEASURED, NOT CONFIRMED. groundAt on a 0.5 m grid, 3717 samples over the whole site: range **0.365 m** (0.140 → 0.505), peak at (−24, −84.5). But the median IS 0.140 and **85.2% of the park is flat at 0.14 m**; p90 is 0.219. Crossing the middle at z −83 a walker meets 0.14 → 0.40 → 0.46 → 0.31 → 0.14, so it is FELT underfoot. It is not SEEN: in `pk-relief.png` (stood west of the crest, eye level, looking along the rise) and `pk-field.png` (full length of the field) the far railing, benches and boundary are visible across the entire width with nothing occluded behind a crest, and the horizon reads flat. The desk's test is whether a player STANDING IN THE PARK SEES rising ground; they do not. — auditor |
| OPEN | C | cars on the left row face backwards | H's convention, published so it is not guessed: makeCar builds NOSE-FIRST DOWN -z, so a car at yaw y points its nose at (-sin y, -cos y). Measured in the world: north row (z 8.6) yaw 0.55 -> nose (-0.52, -0.85); south row (z -3.4) yaw 2.59 -> nose (-0.52, +0.85). Both rows therefore face the aisle AND angle WEST, toward the street. If the intent is to angle east, into the lot, both rows are mirrored: use -0.55 and pi+0.55. Geometry cannot tell me which was meant — C's call |
| OPEN | C | cars clipping into each other | MEASURED by H, whose object it is. The lot parks herringbone at yaw 0.55 rad (31.5 deg off the z axis) with a row pitch of 2.7 m in x. An angled car's x-EXTENT is len*|sin y| + 2.1*|cos y| = 3.93 m for the shortest kind and 4.40 m for the longest, so every neighbour overlaps by 1.23-1.70 m. It is the pitch, not the placement: no per-car nudge fixes a row whose spacing is smaller than its own footprint. Pitch >= 4.5 m clears every kind at this angle, or hold 2.7 m and drop the angle below 12 deg. ct/gap.ts's nudgeClear() is available if the row wants re-spacing rather than a constant |
| OPEN | C | chairs outside the office face the wall | |
| OPEN | C | pole sign panel too small / skewed | |
| OPEN | C | spawn + respawn in room 301 | C owes the desk a coordinate |
| OPEN | C | export the weed tuft for B and E | B and E waiting |
| CONFIRMED | D | ATM inlaid, slanted, lower, more detail | **AUDITOR CONFIRMED — attempt 4, walked (audit/seams, build e2ead9895).** This is the row I got wrong before, so the method changed: I did NOT stop square-on at a distance I picked. I walked the pavement past it looking AHEAD (the walk band here is x -5.0 to -7.2, so I stood mid-walk at x -6.2), shots/aw-pass-{6m,3m,1m,by}.png, then aw-glance.png and aw-use.png. All four named properties measured from the world: INLAID — niche spans x -7.17 to -7.00, a **0.170 m** recess built from five pieces with a **lit side `#8a8d92` and a shadowed side `#6b7076`** against a `#23282d` back, and the reveal is plainly visible in the glance. SLANTED — three distinct rakes, screen **8.1 deg**, keypad **33.7 deg**, apron **-21.3 deg**. LOWER — screen 1.30-1.72 and keypad 1.18-1.30 world, i.e. **1.04-1.58 m above the pavement**, natural use height. MORE DETAIL — 8 tagged parts; a green CRT carrying two text lines, a card slot with an indicator, a 3x4 keypad with individually shaded keys, a cash slot, and the `[E] FIRST FEDERAL — check balance` prompt. D's "fascia 0.900 m" reconciles with my 1.080: the MACHINE FACE is apron-bottom 0.82 to screen-top 1.72 = 0.900 exactly, and my larger figure included the niche lip and sill — same object, different bounds, their number is right. **Why the earlier attempts were rejected and this one is not:** at 4% body-to-wall separation the recess had nothing to read against, so from where a player actually walks there was no ATM to see. In aw-pass-3m.png — 3 m out, walking, looking straight ahead, not at it — the dark body now reads unmistakably as a unit set INTO a pale wall. That is the thing that changed. The user has not seen attempt 4 and their eye still outranks mine. | ATTEMPT 3 REJECTED by the user. D self-confirmed it ("looked at attempt three, and it holds") — a builder may not confirm its own work, and the user disagrees. Do not start a 4th blind attempt. **The auditor ALSO wrongly CONFIRMED this and has withdrawn it** (see below): I judged it from a viewpoint I chose — square on, 1.4 m away, centred — which is not where a player stands. The user's verdict stands over mine. **Note for whoever picks it up:** the ATM in the world at build `fbe378549` is not the one I confirmed at `6814d50a5` — it is larger, has visible side reveals and an `[E] FIRST FEDERAL — check balance` prompt. There is newer work here that has been seen by nobody; it needs a fresh walk, not an inherited verdict. — EVIDENCE FILED for the fresh walk this row asks for, no fourth attempt: notes/D-atm-evidence.md and shots/atm-eye-{front,left30,right30}.png, taken at eye height (camY 1.760 world = 1.62 above the pavement), 1.5 m out, front and 30 deg each side. Measured from the world: recess 0.170 OK, rake screen 8.1 deg / keypad 33.7 deg OK, screen 1.37 OK, keypad 1.10 OK, bottom 0.90 OK, width 0.62 OK. ONE miss: fascia HEIGHT 0.68 m against "about 1.0 m". Second candidate, not geometry at all: machine body #8d949b vs wall #9a9ca0, 4% apart, so the recess has no tonal separation to read against head-on. Not choosing between them. CANDIDATE 2 IS NOW RENDERED so it can be ruled on by eye: shots/atm-tone-dark-front.png and -left30.png are the SAME geometry with only the body tone changed (#4d545b), against shots/atm-eye-front.png as shipped. Produced by a temporary edit that was reverted — the shipped object is untouched. — RULING IMPLEMENTED, both halves, awaiting the auditor. Body #8d949b->#414a52 (146->74 against a 156 wall, 4%->53% separation) and the reveal recut from the wall's own tone with a lit side and a shadowed side. Fascia extended DOWNWARD 0.90->0.68 so height is 0.900 m with screen 1.370 and keypad 1.100 UNCHANGED. Recess 0.170 and both rakes unchanged. Shots shots/atm4-{front,left30,right30,walkup}.png at eye height. NOT self-confirmed. |
| CONFIRMED | D | cat directly ahead from the alley mouth | WALKED AND SEEN, `shots/d-mouth.png`. Stood at the alley mouth (−6.2, −40.1) looking in: the cat is **dead centre of frame**, beside the drain grate, eyes catching the light. Mesh at (−9.35, −40.15). — auditor **Corroborated independently by D**: 2.35 m in, centred in the mouth view, 1.13 m clear of the grate, placed by looking rather than deriving. |
| CONFIRMED | D | alley floor: dark diagonal streaks | WALKED AND SEEN, `shots/d-floor.png`, `d-mouth.png`. The starburst is gone: the floor now darkens **smoothly and edgelessly toward the drain** with no linear strokes at any angle, which is what `58f6a1d28` replaced it with. Grate reads as a real casting. — auditor **Corroborated independently by D**, who owns the cause: 16 radial strokes they added one commit earlier, replaced with a soft radial wash — and explicitly NOT shared with the park's desire lines. |
| CONFIRMED | B | apron untextured, needs ramp, must abut | **AUDITOR CONFIRMED (audit/seams, build e2ead9895).** All three tests pass, measured independently of B and with a contrast case. RAMPED: groundAt across the driveway at z 2.6 rises CONTINUOUSLY 0 -> 0.140 over x 5.00 -> 7.00 — a 2.0 m apron at ~7%, in 8 sub-steps of ~1.35 cm. The same scan at a plain kerb (z -20) goes 0 -> 0.140 in ONE sample. That contrast is the proof: a ramp and a step do not look alike in the profile. ABUTS, not overlaps: if the walk sheet still ran under the apron the walkable top across x 5.25-7.00 would read 0.140 the whole way; it reads the ramp, so the walk is genuinely cut for the driveway, and the apron's far edge reaches 0.140 — the walk's own height — exactly at x 7.00. TEXTURED: two meshes at the lot mouth, a map:true wedge at y 0.035-0.140 with side=0 (FrontSide) sitting above the map:false dark skirt at y -0.138-0.002 — precisely B's account of the winding fix, with the skirt still there but now correctly underneath. Shot shots/af-apron.png from the road at (1.2, 2.6). scripts/apronprofile.mjs. | the grey plane was not the apron: its top face was wound to a -y normal, culled, so you saw the dark skirt (605d56) beneath it. Winding fixed; apronTex added, sized from real metres at 32 px/m, joints across the direction of travel. Ramp already correct (0.042 at the kerb to 0.137 at the building line) and already abutting (seam within 0.003 m) — both measured, not assumed. Rail not floating: base 0.140 on ground 0.140  CHECK FROM: stand in the drive at (6.0, 2.6) and look down — concrete with joints running ACROSS the drive, no flat grey. |
| CONFIRMED | B | tree pit: trunk off-centre toward the kerb | SEEN FROM THE WALK, `shots/see-bodega.png` (foreground). The trunk stands **dead centre in its dirt square**, with dirt visible on all four sides, and the pit is inset in the paving with clear margin on the kerb side — it does not overhang or touch the kerb. Both halves of the user's complaint answered from the position a player is in. — auditor |
| CONFIRMED | B | cups too common and too big | **AUDITOR CONFIRMED (audit/seams, build e2ead9895).** Counted, not eyeballed — the tag B added at props.ts:2597 made this answerable. 14 pieces, 5 types: flattened cardboard 5 (35.7%), folded newspaper 4 (28.6%), milk crate 3 (21.4%), **fountain cup 1 (7.1%), coffee cup 1 (7.1%)**. Cups are 2 of 14 = 14.3%, down from B's 5 of 14, and EACH cup type is rarer than EACH of the other three — jointly the rarest things on the street. NOT OVERSIZED: coffee cup 0.190 x 0.181 x 0.093 m. The fountain cup's raw box is 0.156 x 0.430 and the 0.430 is a **straw**, not the cup — walked to it and looked (shots/cup-fountaincup.png): a small striped cup on its side in the gutter against the kerb with a red straw out of it, clearly small against the ~1 m paving slabs. The coffee cup sits under the Tony's Pizza bench beside a folded newspaper (shots/cup-coffeecup.png), also small against the bench slats. Placement is believable in both cases — gutter and under a bench, where litter actually collects. All 14 pieces sit on the ground within 3 cm. scripts/cuppud.mjs, cupshot.mjs. | cups were 5 of 14 pieces and the pair the user saw were 1.4 m apart; now 1 coffee + 1 fountain, 19 m apart on opposite sides, count held at 14 so the litter floor still passes. Scaled 0.58/0.55 on the group: 0.190 m and 0.156 m across, about a sixth of a 1 m paving joint. CAVEAT for the user: reads plainly at conversational distance, a pale speck at 7 m — true-scale behaviour, not a defect, but a size decision if they want it read from across the street  CHECK FROM: the bus bench at (5.32, -35.30) — one cup, about a sixth of a paving joint; nearest other cup 19 m away at (4.9, -54.3). |
| CONFIRMED | B | explain the shadow geometry on the forecourt | **AUDITOR CONFIRMED (audit/seams, build e2ead9895).** Walked the forecourt square-on from the road at (-3.0, -14.0) and obliquely along the walk from (-5.6, -11.0); shots/af-fore.png, af-fore2.png, and af-forecrop.png (the patch band at 4x). TEXTURED: at magnification the forecourt flags carry per-slab grit AND joint lines, with tone varying flag to flag; the step treads carry visible aggregate and the nosings read as cut stone. READS AS STONE: yes, and the answer to what the user actually saw is that the forecourt is a WARM sand stone while the walk in front of it is a COOLER grey with its own speckle — the "patches" are a deliberate change of material at the building line, which is why they read as shadow when both were flat fills. Nothing dark remains. The only grainless surfaces left in frame are the two planter boxes (smooth cream, no grain), consistent with B's "12 flat" being copings, posts and planters; no flat slab of any size. Confirms b0b69cb48 independently. | notes/B-forecourt-patches.md, written to hand to the user. NOT shadows and NOT a wet/night registration split — all 26 civic ground meshes are graded:true, none diverged. They are the library's landing (3.6x4.1 m) and flight (3.2x4.1 m) plus copings, posts and planters, ALL map:none, seven flat tones 0.075-0.405; each is a box with a materials array, which is why one object shows several quads with hard edges. Scope: 123 ground top-faces world-wide are untextured flat colour, ~454 m2; civic has ZERO textured ground materials. plazaTex() exported for a one-line fix in ct/civic.ts, which is not mine  ALSO: the underlying fault is now FIXED by civic's owner in b0b69cb48 — forecourt went 0 textured/26 flat/two big slabs to 16 textured/12 flat/no flat slab over 3 m2, verified in the world  CHECK FROM: notes/B-forecourt-patches.md is the deliverable; the world half is the courtyard mouth at (-7.2, -16.5) looking at (-10.2, -13). |
| CONFIRMED | B | "the tonys pizza sign should go on the back of the bench also the bench back should lean back a lil" | SEEN AND MEASURED, `shots/bad-ad2.png`. The ad is **on the back of the bench**, facing the roadway: "TONY'S PIZZA / 555-0143 / TWO SLICES $1.75". The back **leans 12° from vertical** — every back member measures `tiltZ −12°` (the seat back, both stiles and the panel). "A lil" is 12°. — auditor |
| CONFIRMED | B | "should not be cutting off the actual ad for tonys pizza, strange graphical bug on the legs like the same plane as the wood" | SEEN, `shots/bad-ad2.png`. The ad is **complete** — red header band plus both text lines, nothing truncated at any edge. The legs read as **separate dark members** standing clear of the wood slats, with their own shading; no coplanar/same-plane artefact at the legs from the roadway view or the low view (`bad-legs.png`). — auditor |
| CONFIRMED | B | "i dont think we need the bottom wood part. also the tonys pizza part i think needsa to have a bezel" | SEEN, `shots/bad-ad2.png`. **No bottom wood panel** — below the seat slats there are legs and daylight, nothing else. The ad carries a **bezel**: a cream border framing the red header and the text on all four sides. — auditor |
| CONFIRMED | B | "trash cannot be clipping through stuff like this" | **AUDITOR CONFIRMED (audit/seams, build e2ead9895).** Split out of the bundled puddles/trash row below, because the two halves need two different kinds of answer and one status cannot carry both. Measured independently of B: **0 of 14 litter pieces** has its centre inside any of the world's **364 colliders**, and **14 of 14** sit on their ground within 3 cm. Note the trap I nearly filed this under — my first run reported the same clean 0 over an EMPTY SET, because the tag is on the litter GROUP (props.ts:2597) and I had filtered on isMesh. GOTCHAS 34 exactly. The script now exits 3 rather than pass when nothing carries the tag, so the count above is 0-inside over 14 real pieces, not 0-inside over nothing. scripts/cuppud.mjs. |
| LANDED | B | "these are puddles and they look awful" | **AUDITOR: the artefact is gone; the verdict is the user's.** Split from the trash half above, which is now CONFIRMED. What I can settle by looking, I did — walked to (4.78, -60.15) at 14:00 in rain, waited the ~16 s the wet look needs to settle, shots/cp-pud.png, cp-pud2.png, and cp-pudcrop.png at 3x. The contrast is the right way round (standing water darker than the road it sits in, matching B's 21.1 levels) and the edges are **soft, irregular and dithered — no hard rectangle anywhere**, which is the specific artefact class that made the park paving read badly. So nothing measurable is still wrong and sending this back to OPEN would be false. But "look awful" is an aesthetic verdict and I am not the eye that gave it: I wrongly CONFIRMED the ATM by judging it from a viewpoint I chose, and will not repeat that here. **For the desk: show the user shots/cp-pud.png.** It needs their eye, not another measurement — B said so first and they were right. | puddles: contrast inversion fixed, standing water measures 21.1 levels darker than the road it sits in, wet AND dry. Trash: footprint reports 0 litter inside a building or a prop, re-checked after the cup rescale. LOOKED at 14:00 in rain — reads subtle rather than awful; if the user still dislikes it, it needs their eye, not another measurement  CHECK FROM: a rainy DAY hour — 14:00 — at (4.78, -60.15); rainAt() is a BOOLEAN, so hour 0 is rainy but too dark to judge. |
| OPEN | E | park: shrubs at the edges | |
| OPEN | E | park paths read as road, not park path | biggest single win left in the park |
| OPEN | E | park: black rectangles + diagonal streaks | |
| CONFIRMED | A | thrift facade lazy and chopped off | SEEN, `shots/f4-thrift.png` (square on) and `f4-pickup.png` (standoff). Both window bays carry a **full run of hanging garments plus boxed goods**; the doorcase sits between them as its own recessed bay with the `[E] into the THRIFT STORE` doorway, and **the display runs unbroken through each window rather than being cut by the doorcase** — which was the specific 'chopped' fault. Sign band and price card present. Nothing truncates mid-feature. — auditor |
| CONFIRMED | A | shopfront backing plane — pavement shows through glass | CHECKED EVERY ONE, then looked. `check-seethrough` at build `2e95f78ac`: **16 shopfronts + the bodega bay, 1236 ground surfaces tinted, no pavement visible through any shopfront**, exit 0. Confirmed by eye at the bodega bay, the worst case (`shots/see-bodega.png`): DELI, RECORDS, GARAGE and BILLIARDS all read as dark backed glass with interior banding, no pavement through any of them. — auditor |
| CONFIRMED | A | diner blade illegible | WALKED BOTH DIRECTIONS, `shots/e8-blade-N2.png` (from the north, at the door) and `shots/e8-blade-B2.png` (from the south). The red cup shows **handle-right from both sides** — each face carries its own correctly-oriented artwork, so it is NOT mirrored. **The desk's grep found no FrontSide/BackSide pair because there is no plane to pair**: `tex-world.ts:972` builds the blade as a `BoxGeometry`, which gives every face its own UVs, and GOTCHAS 10 was checked and ruled out before that choice. **Note the row's own test is out of date** — the blade no longer says EAT. A replaced the three stacked letters with a cup symbol *at the user's suggestion*, on the grounds that the fascia beside it already says DINER; so "reads EAT both ways" cannot be the test, and "reads as a cup both ways" is, and does. — auditor |
| CONFIRMED | A | diner facade "looks really bad rn" (the FACADE, not the blade) | **AUDITOR CONFIRMED for the depth treatment, and this row exists because the blade and the facade were being conflated (build f4e54cbce).** The ledger had only `diner blade illegible` CONFIRMED — there was **no facade row at all**, which is how a live complaint stayed invisible while a neighbouring row read as done. Depth is a PROFILE, so I measured it: the facade plane is x −7 with outward +1, so projections sit at x > −7 and set-back glazing at x < −7. All five elements the desk named are present: **set-back glass with a reveal —450 mm** (glazing plane at x −7.45, full frontage z −55.5…−43.5); **projecting fascia +200 mm** (y 4.10–4.20); **transom +130 mm** (y 3.03–3.10); **projecting stallriser +110 mm** (y 0.32–0.42) over a **+90 mm plinth**; door jambs +120 mm. **Something in the window: yes** — red counter stools and booth seats are plainly visible through the glazing from the pavement (`shots/df-walk.png`) and from across the street (`df-across.png`), where the diner now reads consistently with THRIFT and BURGER BARN either side of it. **What I am NOT confirming:** *"looks really bad"* is the user's aesthetic verdict and they have not re-looked since this work. I am confirming that the depth treatment is in and visible, which is the question the desk asked. scripts/dinerface.mjs. |
| OPEN | F | thrift interior too thin | thinnest room in the world |
| CONFIRMED | G | tax preparer faces backwards | GOTCHAS §23  **LANDED (G).** Facing is derived, not typed: `Math.atan2(LAMP_DX - PREP_X, CLIENT_CZ - PREP_Z)` at int-tax.ts:259 — she points at the client chair, so moving the chair moves her gaze. This was the fault that started the derive-don't-type rule; the other four keepers were swept the same way. — **AUDITOR CONFIRMED (build 98042722a).** Walked the room from both ends. `shots/fc-tax-back.png`: the preparer stands behind the desk between it and the filing cabinets, **facing into the room with his face toward the camera** — and the entrance door is at that end (visible in `fc-tax.png`), so he faces arrivals. Not backwards. Checked by looking rather than by reading `rotation.y`, because on this street facing lives in the atlas frame. |
| CONFIRMED | G | "the front of the bank doesnt match the side fix this" — flat brown flanks | RAISED TWICE. Measured at HEAD: 12 untextured 0x53382e faces on GOLDEN ACES + HOTEL ORPHEUS (vice.ts:371). Every other building on the block was fixed; these two were split into ct/vice.ts before that work and did not travel. The fix is now TWO LINES: ct/street.ts exports `depthOf` and `shellMaterials`, which is what fixed every other building on the block. Added by D, who is barred from the file — **RE-MEASURED at build `cbbabdd3e`: 0x53382e now appears on ZERO faces anywhere in the world**, so the flat-brown half of this row is done and it wants the auditor's eye rather than a builder. Same probe, same colour, whole scene traversed. (D)  **LANDED (G).** Flanks and backs painted by a `flankTex` in facadeTex's hand — masonry at the block px/m, courses, soot from the parapet. Re-measured at HEAD with getHexString: **0x53382e appears on ZERO faces anywhere in the world**, confirming D's own re-measurement. — **AUDITOR CONFIRMED (build c601add21) — this row was RAISED TWICE, so it got a wider check than the one colour a builder named.** **D's measurable claim verifies:** I traversed the whole street world for building-scale faces and `#53382e` appears on **zero** of them. **Front against side, looked at rather than inferred, on all three candidates:** the hotel/casino block (`shots/bk-hotel.png`, oblique so front and west flank are in one frame) now carries **brick masonry with courses and regular windows on the flank, matching its front** — not the flat brown of the complaint; and FIRST FEDERAL, the building actually called the bank (`bs-north.png`, front and north return in one frame), carries **the same pale stone and panel joints around the return as across the front**. Windowless on the return, which is what a party wall is, but the same material. **[One number I am NOT quoting]** my world-wide sweep also reported "143 flat-colour elevations". That figure is junk: the filter takes whole building MESHES, so a solid block box counts as one "elevation" and its roof tone reads as a flat face. It is not evidence of anything and must not be cited — the `#53382e` count and the three walked comparisons are what this confirmation rests on. scripts/flanks.mjs, bankflank.mjs, bankside.mjs. |
| CONFIRMED | G | "all buildings need to be much deeper otherwise it looks like a fake building" | Measured at HEAD: both vice shells are still BoxGeometry(w, h, 3.4). Same split as the row above; `depthOf(b.nm)` is exported for exactly this. Added by D — **RE-MEASURED at build `cbbabdd3e`: both are now 14 m deep** (GOLDEN ACES 12 × 15.4 × 14 at x 39.5, HOTEL ORPHEUS 11.55 × 13 × 14 at x 51.2). 3.4 → 14, so this row is landed in the world too and wants checking rather than building. (D)  **LANDED (G).** Both shells 3.4 -> 14 m, and the collider now ends where the building ends — it had claimed `zc + 1.7 + 8`, so 8 m of solid nothing stood behind a 3.4 m building. Re-measured at HEAD: 12x15.4x**14** and 11.55x13x**14**, front still on z -96. — **AUDITOR CONFIRMED, and checked against the word ALL rather than the two shells the row names (build 44bd1dfae).** Measured every building mass in the world (h ≥ 8 m): **22 of them, and ZERO have a footprint dimension under 5 m** — the complaint was about 3.4 m. Median shallowest dimension **12 m**, max 18. The two rebuilt shells measure **14 m deep** exactly as D re-measured. The four masses whose smallest dimension is ~6 m are narrow-FRONTED buildings with 14–24 m of depth behind them (the bodega's 6.05 m frontage is 19.7 m deep) plus the street's own north end cap — none is a shallow building. **Seen**, `shots/dp-deep.png` looking along the side street: both sides read as solid multi-storey masses with receding window lines and real returns, not flat cards. **[One correction to the evidence, so nobody is sent to the wrong building]** D's note reads *"GOLDEN ACES 12 × 15.4 × 14 at x 39.5, HOTEL ORPHEUS 11.55 × 13 × 14 at x 51.2"*. The **names are swapped** against the published doors: `doors()` puts GOLDEN ACES at x 51.29 and HOTEL ORPHEUS at x 39.5, and my own footprint measurements agree with the doors (11.55 wide at 51.2, 12.00 wide at 39.5). The dimensions are right; only the labels are crossed. scripts/depths.mjs. |
| CONFIRMED | H | tyres clip into the bed cavity | **AUDITOR CONFIRMED (audit/seams, build e2ead9895) — both halves.** The blocker in my earlier partial was wrong: **no new tag was needed.** `wheelZ` is unique per kind (sedan 1.45, hatch 1.2, pickup 1.65, van 1.5) and `cars.ts:813` already publishes `userData.wheelbase`, so **wheelbase 3.30 IS the pickup** — 4 of them, found without touching src. TYRE IN THE BED: **no.** H built real wheel housings. Rear tyre x 0.70..0.94, y 0.017..0.663, z 1.31..1.99; housing inner panel x **0.66..0.70** (outer face flush with the tyre's inner face), cap x 0.66..0.90 y **0.72..0.76** (clearing the tyre top by **0.057 m** — the same 0.057 as the confirmed wheel-arch row), housing z **1.22..2.08**, overrunning the tyre by 0.09 m at each end. Enclosed on every side, identical on all 4 pickups. Looked as well as measured: shots/bed4-{side,sideL,rear,q}.png from standing eye height, and bed4-qcrop.png brightened — the bed is a clean grey tub with no black bulge, and the faint step in it IS the housing. **I nearly filed the opposite finding:** I built the cavity box from source constants as x +-0.74, which is the bed's width BETWEEN the housings, so the tyre "intruded 4 cm" into a volume that does not exist over the axle. Measuring the real meshes killed it. BLOCK OUTSIDE THE SILHOUETTE: **none, 0 of 23 vehicles** — no part exceeds the body+tyre envelope (0.94 half-width) or drops below the road. One apparent outlier reaching 2.139 m resolves as a `1.7 x 0.1 x 1.5` slab rotated 0.950 rad at the hood position: **an open bonnet on the car lot's display truck** (20 parts against 16 on the street trucks, and visible in shots/af-apron.png). Deliberate, not a stray block. scripts/bedcavity.mjs, bedparts.mjs, bedlook.mjs. | superseded auditor partial: **not confirmed, and here is exactly how far I got.** Located the pickup at ≈(−4.5, −27), parked in front of BURGER BARN (`shots/bed-w1.png`). From the kerb the bed side and rail read clean, with no tyre bulging through the bed wall (`bed-in2.png`). **What I could not get is a view INTO the bed cavity from a standing player position**, which is where a 4 cm intrusion at the bed floor would show. Three approaches failed to find the truck by shape first — the fleet is DRAWN not placed, so the vehicle at a given coordinate changes between builds, and my bed-floor filter matched 0 of 8 clusters. **One line closes this permanently: `userData.variant = 'pickup'` on the body.** Until then, confirming it means finding the truck by eye every time. — auditor |
| CONFIRMED | H | block protruding from wheels on all vehicles | SEEN ON TWO VEHICLES from the kerb at standing height, `shots/f4-pickup.png` (green sedan, z −48) and `shots/f4-sedan.png` (maroon sedan, z −14). Both wheels on each sit **inside dark recessed arches**, the flank line runs unbroken front to rear, and **no rectangular block stands outside the silhouette of tyre and body**. Sample is 2 of the street fleet, not all 94 tyres — stated rather than implied. — auditor |
| OPEN | desk | PVBLIC vs PUBLIC on the library | user's call, awaiting answer |
| OPEN | AUDIT | verify the ledger | |
| OPEN | C | big sign should be simpler | |
| OPEN | C | the garlands are disconnected | |
| CONFIRMED | D | the atm is still not right | **AUDITOR CONFIRMED with the row above, same walk, same evidence (audit/seams, build e2ead9895).** This is the user's fresh wording of "ATM inlaid, slanted, lower, more detail"; both are settled by shots/aw-*.png and the measurements filed there — 0.170 m recess with a lit and a shadowed reveal, rakes 8.1/33.7/-21.3 deg, screen and keypad 1.04-1.58 m above the pavement, 8 parts. Kept as its own row rather than merged, because it records that the user said this TWICE and the second time is the one that got the cause fixed. Same caveat: the user has not seen attempt 4. | Same request as the REJECTED row above; kept separate because this is the user's fresh wording. MEASUREMENTS AND EYE-HEIGHT SHOTS ALREADY EXIST — read notes/D-atm-evidence.md before touching the object. Eight of the nine asked-for figures measure correct in the built world; the one miss is fascia HEIGHT 0.68 m against "about 1.0 m tall". The other candidate is not geometry: the machine body is within 4% luminance of the bank wall (#8d949b vs #9a9ca0), so a genuine 0.17 m recess has nothing to read against head-on. Three geometry attempts have missed; a fourth should not start until someone says which of those two is the fault. — RULING IMPLEMENTED, both halves, awaiting the auditor. Body #8d949b->#414a52 (146->74 against a 156 wall, 4%->53% separation) and the reveal recut from the wall's own tone with a lit side and a shadowed side. Fascia extended DOWNWARD 0.90->0.68 so height is 0.900 m with screen 1.370 and keypad 1.100 UNCHANGED. Recess 0.170 and both rakes unchanged. Shots shots/atm4-{front,left30,right30,walkup}.png at eye height. NOT self-confirmed. — **FASCIA BOTTOM MOVED 0.68 -> 0.75 after the user restated the number a third time.** I had shipped 0.68 because the same ruling asked for 0.9-1.0 m of fascia and the top is pinned at 1.58 by the screen, so only 0.68 reaches 0.90 while 0.75 yields 0.83. Raised twice, answered 0.75 twice: the number is a decision, not an oversight. Fascia is now 0.83 m; screen 1.370, keypad 1.100, both rakes and the 0.17 m recess all unchanged. **Re-measured on MY OWN build** — `scripts/atmmeasure.mjs` does a bare goto to :4184, the auditor's worktree, and reported the OLD 0.68 after the change had landed in my tree (GOTCHAS 26). `shots/D-atm-075-{front,left30}.png`. (D) |
| OPEN | E | make the library interior larger and more ambitious. more halls and st | |
| OPEN | F | make sure the people in the buildings are in the right orientation. (b | MEASURED by H, whose atlas it is — all ten keepers, decoded from EACH ROOM'S OWN customer spot (just inside its street door), because sector is relative to the viewer and "facing away" only means anything from where a customer can stand. Scale is 0 = looking at you, 4 = dead away. **bodega 4** — the only one actually facing away. **library 2** — profile, attending to something else. casino/hotel/thrift 1 — three-quarter, fine. **burger 0, church 0, diner 0, pawn 0, tax 0** — facing the customer. So THE BURGER KEEPER NAMED IN THE REPORT IS ALREADY CORRECT: he stands at the back wall z -3.7 facing the door at z +3.7 and the seats between. Fixing him would break him. The keeper matching the user's description is the bodega's, which authors facing -PI/2. The sprites themselves are sound — every keeper's sector changes with the bearing (s0/s2/s4/s6 around a circle), so this is authored facing, not the atlas. Decode table in notes/H-atlas-facing.md |
| OPEN | E | what is the shadow geometry here? did you end up answering what that w | |
| OPEN | F | why are these decorations simply floating in the air in the diner? / t | |
| OPEN | E | park bench looks bad and clips the drinking fountain | |
| OPEN | E | park bench looks awful and clips into fountain, also no shrubs like i  | |
| OPEN | G | make the library interior larger and more ambitious / why is church lo | **AUDITOR — measured mid-flight, build 4a311be0a, offered BEFORE this lands so it is not a post-mortem.** The library DID get larger: 163 -> **326 m², doubled**. But its **median clear aisle fell from 8.40 m to 2.10 m**, which is now **the narrowest of all ten rooms** — tighter than the bodega's 3.85 m, which this audit ranked severity 1. Free floor is a normal 81%, so the space was not consumed; it was **cut into strips** by the stacks that filled the new footprint. This is the bodega's fault reproduced: *"cramped" is a statement about shape, not area*, and doubling a room while quadrupling its subdivision leaves a reader with less continuous room than in any other interior in the world. Measured with scripts/roomaisle.mjs — largest continuous free run at each 0.25 m of depth, the same instrument used on the sidewalk. Not a verdict on the work, which is not finished; a number G can act on now. | **MOSTLY LANDED (G):** vestibule, 22 m depth, stacks, periodicals alcove, back-of-house doors, and the STAIR + gallery (climbed 2.65 m, balustrade you look down from). Aisle finding fixed too: median clear aisle 2.10 -> 10.4 m, measured on my own world. STILL OPEN: the high arched windows, which need numbers that agree with E's exterior — asked three times, see notes/BLOCKED-G.md.
| OPEN | E | look at the park field what is this? / shelter is ugly and the seating | |
| OPEN | F | bodega interior is very cramped and also doesnt match the exterior. so | |
| LANDED | G | hotel exterior looks nice / interior doesnt match the exterior however |  **LANDED (G).** Took ct/vice.ts's own constants inside — RED #8e1f2a, RED_D #5a1520, GOLD_D #8a6a22 — not lookalikes. Patterned carpet for plain tile, hanging glass pendants for flush discs, brass and mirror behind the desk, floor-length drapes. |
| OPEN | F | the interior door doesnt match the exterior doorway | |
| OPEN | G | casino interior is nice but i want more. bigger and more expansive / t | **AUDITOR — measured mid-flight, build 4a311be0a.** It did become more expansive: 95 -> **323 m²**, and its median clear aisle is now **17.0 m, the most open room in the world** — the request is delivered on its own terms. The problem is outside: **the interior is 1.96× the building it sits in.** Interior 17 × 19 = 323 m²; exterior footprint, taken from the collider the published door stands on, 11.55 × 14.30 = **165 m²**. For scale, church is 0.94× and hotel 0.58×. This is the bodega's *"wider than its own shopfront"* fault at a much larger size — the bodega overran by 2.75 m, this overruns by 158 m². Also the lowest free floor in the set (70%) and now the darkest room (frame luminance 57.8, was 76.4). scripts/bigtwo.mjs, facadewidth.mjs. Not a verdict on unfinished work; a number G can act on before it lands. | **ANSWERED (G):** expansion delivered, and the auditor's 1.96x overrun fixed the way the user's rule says — width back to the 11.55 m frontage (17.0 -> 11.0), depth kept. Interior 222 m² now, not 323.
| OPEN | AUDIT | verify the eight LANDED rows | |
| CONFIRMED | H | this guy is floating | **AUDITOR CONFIRMED (audit/seams, build e2ead9895).** I said last session that my instrument could not see this, because the float is in the FRAME and mesh-bottom-vs-groundAt can never reach it. So I built the one that can: scripts/footpaint.mjs reads each figure's own atlas frame, finds the **lowest OPAQUE pixel**, converts it to a world height, and compares THAT to the ground — it is the painted shoe that has to touch, not the quad. Over 18 citizens (64-row frames): **painted foot vs ground, median 0.000, MAX 0.000, zero figures off the ground.** The proof needs the contrasting number beside it: the QUAD bottom is off the ground on **20 of 20**, and the atlas padding is UNCHANGED at median 0.119 m (C measured 0.108-0.129), still 4 empty rows of 64. H did not repaint the atlas — they dropped the quad by exactly the padding so the painted shoe lands on the ground. **Either number alone fails this row wrongly:** my old quad check would have said 20/20 floating, and a padding check would have said nothing changed. Two figures did read 2.56 m up; they are 2-row-frame objects, not people — part of the park pergola (shots/pf-float.png), up where they belong. Splitting the sample by frame height separates them cleanly: 64-row n=18 off-ground 0, 2-row n=2 off-ground 2. | superseded routing note from 3f5a6b6f1: C measured it world-wide and proved it is the ATLAS, not any call site — seven figures, call-site gap 0.000 for every one, atlas padding 0.108-0.129, and that spread is only each figure's height scale. Cause: the shoe is painted on rows 57-59 of a 64-row frame, so FOUR rows below it are empty, and `geo.translate(0, 0.95, 0)` put the origin at the frame's BOTTOM EDGE rather than at the feet — 4 * 1.9/64 = 0.119 m of transparent padding holding every figure off the ground. Fixed in ONE place: citizens.ts now exports citizenPlane(), which both it and ct/crowd.ts call, because the two copies of those two lines are exactly how the street and the interiors would have ended up 12 cm apart. Measured after: 18 figures world-wide, painted feet to ground min 0.000 max 0.000 |
| OPEN | C | would like a view out of my window but the view it just a small gap an | |
| OPEN | C | just do what i want for this bespoke minor window ask | |
| OPEN | C | there should be a bit of a gap out of the window and then just a brick | |
| OPEN | C | gap in the door sucks. also i dont like that it says stand back when y | |
| CONFIRMED | B | a little too many grasses in the streets. like way too many. should be | **AUDITOR CONFIRMED (audit/seams, build 4a311be0a).** Counted with a signature derived from `weeds.ts:101` rather than guessed at: every tuft is a Group of exactly TWO PlaneGeometry quads at the 0.30/0.35 ratio with `position.y == height/2`. 273 tufts in the world, and the five on the street kerb seam are **exactly** the five B names — west z **−105.62, −92.78, −8.78**, east z **−91.88, −50.78**, all at x ±4.95, the road side of the kerb. Gaps between them are **12.8, 41.1 and 42.0 m** against the old 2.4 m placement: that is the difference between an exception and a line, which is the thing the user actually objected to. Park keeps 222 and the lot 39 (261, B said 268) — correctly untouched, they are meant to be weedier. Walked B's own check, `shots/wd-gutter.png` and `wd-gutter2.png`, east kerb z −30 to −60 both ways: **bare gutter, no tuft in the frame either direction.** | superseded builder note: 44 tufts down to 5 on the whole block. It was my overshoot: I placed them every 2.4 m along both kerbs and called it sporadic, which is a line, not an exception. Placement is now DERIVED from what shelters a weed — every third street lamp foot plus the two catch basin collars — so a post cannot move out from under a tuft and a removed lamp takes its weed with it. Fixed one pattern twice: every OTHER lamp put all four on the west kerb because the lamps alternate sides; every third lands on both (west -105.6/-92.8/-8.8, east -91.9/-50.8). C's lot and E's park keep their 268 and are meant to be weedier. builtlane and footprint pass; every tuft is in the kerb seam on the road side, clear of the 2 m walk. CHECK FROM: walk the east kerb from z -30 to -60 — bare gutter, no line. |
| CONFIRMED | D | cat is dead center in alley i need it right to the right of that news  | **AUDITOR CONFIRMED (audit/seams, build f4e54cbce)** — and this is the row the user has raised most, so it got the most care. **Right of the newspaper: yes.** `shots/cr-closer.png` shows the black cat standing immediately to the right of the newsprint sheet, ~0.6 m off it. I checked D's derivation independently rather than taking it: looking into the alley forward is (−1,0,0), so screen right is `cross(forward, up)` = (0,0,−1) = −z; the cat at z −42.05 against the paper at z −41.45 is 0.60 m in −z, i.e. to the right. **No longer dead centre:** from the alley mouth (`cr-mouth.png`, stood at (−6.2, −40.1)) the cat sits clearly right of frame centre, which is the fault this sixth brief names and which my own earlier CONFIRMED had praised as a virtue — **that verdict was against the fifth brief and is now correctly superseded.** **D identified the right object by looking, and that mattered:** the sheet beside the cat renders as newsprint with printed columns and a half-lifted leaf, while the plain tan slab further out is the cardboard — `props.ts` calls the first one `'flattened cardboard'`, so reading the label would have moved the cat to the wrong object. Both are visible in the same frame and the distinction is obvious on screen. | superseded builder note: Sixth position, and it REPLACES the fifth brief rather than refining it — the fifth move centred the cat in the mouth view deliberately, which is what this note calls the fault. "Right" derived, not guessed, because that is the half got wrong three times: forward here is `(sin yaw, 0, −cos yaw)`, so screen right from the mouth is `cross(forward, up)` = −z. WHICH newspaper answered by LOOKING and not by the label — `ct/props.ts` calls the decal beside the drain `'flattened cardboard'`, and rendered at (−10.60, −41.45) it is plainly newsprint with columns and a half-lifted leaf; the plain tan slab at (−9.40, −42.40) is the cardboard. Reading the label would have moved the cat to the wrong object. Now (−10.40, −42.05): shoulder ~3 cm off the paper's edge, 1.33 m clear of the grate casting, 1.45 m of open floor still to the south wall so it is not back in a corner, feet −0.011 on a floor at −0.031 — the same 20 mm decal lift as before, re-measured rather than assumed to travel. notes/D-crates-awning-cat.md  CHECK FROM: the alley mouth at (−6.2, −40.1) looking −x, the frame the request was made in — grate, newspaper, cat, left to right. `shots/D-cat-sixth.png`, closer in `shots/D-cat-beside-paper.png`. |
| CONFIRMED | H | pedestrians pile up and get stuck at the crossing / "the walk logic should allow people to walk around things" | **AUDITOR CONFIRMED (audit/seams, build cbbabdd3e).** Sampled the live crowd at 4 Hz for **240 s**, tracking every walker's position and act. **H CORRECTION, after this was written: the before-figure quoted next is withdrawn.** The old `jam` counter incremented on any frame with somebody inside the 0.7 m lookahead, including a healthy follow at matched pace, so 29.8 s measured PROXIMITY, not standing still, and there is no 60 s baseline to compare against. The auditor's own 240 s numbers below do not depend on it and stand. **CORRECTION, added on a later pass: the before-figure quoted next has been RETRACTED BY H.** `notes/H-crossing-pileup.md` establishes that the old `jam` counter incremented on any frame with somebody inside the 0.7 m lookahead — *including a healthy follow at matched pace* — so 29.8 s measured PROXIMITY, not standing still. **This confirmation still stands on my own after-measurements, which are unaffected and have since been repeated on the world the user plays; but the comparison below was against a baseline that does not mean what it says, and I should not have leaned on a builder's before-number without asking how it was counted.** Originally written as: Against H's (withdrawn) before-figure of *one walker stationary 29.8 s of a 60 s minute*: **worst stationary run now 16.0 s**, and it is a `window` act mid-block at (−6, −25.6), **not a crossing endpoint**. All six walkers moved 87–250 m; none was ever motionless for the sample. **Largest pile-up seen anywhere: 2 walkers within 1.5 m** — there is no queue. **POSITIVE CONTROL, and it caught my own blind spot:** my first two runs reported "no jam at the crossing" over 90 s in which **nobody crossed at all** — 0 of 2172 samples in the roadway — which is an empty set, not a pass. The crossings are both at the junction (`crowd-net.ts:152`), so with 6 walkers spread over ~110 m of block a short sample never reaches them. Extending to 240 s produced **3 kerb-to-kerb crossings and 222 samples of walker-in-roadway time**, with two walkers standing at (11.7, −96.7) and (11.58, −97) — the `n-bodega` / `n-corner` endpoints H identifies as the cause. So the crossing was exercised and did not jam. **One residual, flagged not buried:** a single uninterrupted `window` act measured 16–18 s across four runs against `WAIT.window = [5, 12]` in crowd.ts:257. My still-threshold is 0.08 m/s, so ~2 s of easing in and out at each end may account for it and I cannot separate the two with this instrument — worth a glance from H, and not a jam. scripts/crowdjam.mjs. **H ANSWERED IT: not a fault, and not the instrument's threshold either — it is the TIMESTEP CLAMP.** `src/main.ts:107` does `dt = Math.min(clock.getDelta(), 0.05)`, so below 20 fps sim time and wall time are different clocks. Measured headless with a probe reading walkers once a frame: **13.2 fps, mean frame 75.9 ms, 263 of 264 frames over the 50 ms clamp, sim time advancing at 0.659x wall.** A 12 s errand is therefore 18.2 s of wall clock, and the observed 18.6 s matches within the sampling interval. Traced it by splitting each stationary run into before/during/after the errand: **all of it was during** (before 0.0, after 0.2, jam 0.0, ghost 0.0) with `wait` never rising (0 rises in 12 runs), so it was one errand draining slowly, not two chained. `WAIT.window = [5,12]` is correct and crowd.ts is unchanged. Written up as **GOTCHAS 37** because it will bite any duration measured from outside the world. Separately worth someone's attention and NOT builder-owned: a player below 20 fps sees the world in slow motion, not merely at fewer frames. | superseded builder note: Root cause is NOT the crossing's width, it is its ENDS: both crossings begin and end on nodes where people deliberately stand still (`n-bodega` is a `door`, 4-8 s; `n-corner`/`w-corner` are `corner` acts, 1.5-4 s). Measured: walkers parked ON a crossing endpoint for 110 samples of one minute, 74 on n-bodega. A parked walker then read as a HEAD-ON MEETING (`theirs <= 1e-4`), so the arriver gave way for the whole errand and whoever came up behind them did too. And the existing "blocked too long, re-plan" escape sat inside `if (!placed)`, which a held walker never reaches — it sets step=0, so its own current position places fine. **[WITHDRAWN by H: this measured proximity, not stalling — see the correction at the head of this row.]** Fixed at the walker: a parked walker is an obstacle to go ROUND, not someone to yield to; jam counts frames that got nowhere rather than frames with anyone ahead; escalation 1.0 s yield -> step round -> 2.0 s reroute; `net.route(from,to,avoid)` strikes the blocked node out and re-routes the same destination round it; crossings given 1.3 m of lateral width vs a walk's 0.55 so people cross abreast in lanes. AFTER, 60 s: worst jam **29.8 s -> 0.0 s for all six**, 0 of 3090 samples off the walk. **NOT PROVED: I never reproduced a visible pile-up** — max 2 walkers at the junction, and street transits were 0 both before and after, so the throughput numbers are not evidence about the crossing. notes/H-crossing-pileup.md | |
| OPEN | H | textures on vehicles need a deep review and fix | PARTLY LANDED, and one item needs a ruling before I touch it. **Mirror (was my finding 1): FIXED** on both the flank and the pickup bed - see the doors row. **Blotch (finding 2): HALF FIXED.** The mottled edge was `dither()` running AFTER the arch, scattering 120 texels across it; the grain now goes down first and the arch is cut into it. The other half is that the arch and the sill are SEVEN LEVELS APART - rocker shadow 90,84,58 vs well 83,78,52 - so they merge into one dark mass over the bottom three quarters of the flank. **I have not changed that: the instruction was "the wells and the arch paint you already fixed are good - do not disturb them".** Darkening the well to about x0.18 (62,58,40) separates it from the sill, keeps the body's hue and stays well clear of the tyre's 16,17,20. One line, needs a yes. **Coachline (finding 3): RETRACTED - I was wrong.** I reported it HIGH and "confirmed visually"; a character dump of the flank shows the arch is filled AFTER the stripes and covers them. At the chrome row inside the arch's columns the texels read arch colour. I called it from a screenshot. **Density (finding 4): STILL OPEN** and still blocked on the masonry question - does A's helper round to whole texels per metre and accept a fractional canvas, or fix the canvas and accept a fractional density? Panels here run 0.34 m to 4.6 m so the rounding rule decides whether they can share a density at all. notes/H-per-side-pass.md |
| CONFIRMED | H | the doors are misaligned. i think the worker doesnt realize they need to confirm the logic independently per side of the car | **AUDITOR CONFIRMED (audit/seams, build 103766f18) — and I checked it the way the user asked, independently per side, because checking one side is how this survived.** The body is a single BoxGeometry with a 6-slot material array, which is exactly the shape of the original fault, so I worked at that level: for each material GROUP I derived `u → z` from **that group's own vertices** rather than assuming a convention, read **that group's own map**, and converted its dark columns to world z. **(1) MIRROR — fixed.** `material[0].map === material[1].map` is **false** on all four kinds; each flank carries its own paint. **(2) ALIGNMENT — fixed.** Sedan shuts read **−1.008 / 0.164 / 1.055** on +x and **−0.961 / 0.211 / 1.102** on −x, bracketing H's −0.98 / 0.19 / 1.08; the rear shut is at ~1.05–1.10, not the old 1.40, so it now sits under the pillar. **Both flanks agree on all four kinds:** sedan 9 features, pickup 7, hatch 10, van 7. **[My own threshold was wrong and I nearly filed a false fault]** my first pass reported "hatch: SIDES DISAGREE, 8 of 10". The per-feature differences are *perfectly uniform* — sedan **all 47 mm**, van **all 48**, pickup **all 31–32**, hatch **79** with two at half — and a real misalignment varies per feature while a constant offset is a sampling artifact: I read each column at its centre `(x+0.5)/W`, and the two faces' UVs run in opposite world directions, so the same painted edge is reported one column apart. The hatch only "failed" because 79 mm is one texel on a 3.8 m body and my tolerance was a flat 60 mm. **A fixed metric tolerance is wrong for a quantity quantised by texel size; the tolerance has to be the paint's own resolution.** scripts/sidefaces.mjs. | superseded builder note: TWO faults, and the user's diagnosis named the mechanism for both. (1) MIRROR: `[sideT, sideT]` handed ONE texture to both side faces of a box, whose opposite faces carry UVs running in opposite world directions (measured on the geometry: -x gives z = +len*u + faceZ0, +x gives z = -len*u - faceZ0). Each flank now gets its own paint, mirrored at the single place every feature's column is derived. **The same fault was on the pickup's BED** - the truck in the user's shot - where one `outM` served both walls; the rear arch was 0.40 m from its own wheel on the +x side. (2) ALIGNMENT: the sedan's panes put the rear pillar at 1.10 but the shut was painted at 1.40, so the pillar had no shut under it and the quarter light fell INSIDE the rear door. Shut moved to 1.10, which also puts the quarter window behind the back door as asked. CHECKED EACH SIDE SEPARATELY by reading each face's own texture through its own UVs - sedan +x/-x both read -0.98/0.19/1.08; hatch both -0.83/0.75; pickup both -1.01/0.46; van both -1.44/-0.14. Bed arch: -x wall z 1.621, +x wall z 1.652, wheel at 1.65, from texel columns 27-42 and 15-30. The greenhouse was never mirrored (loftCabin gives both side quads the same uOf(z)); my first probe said it was and was picking up the windshield/roof quads. notes/H-per-side-pass.md | |
| OPEN | E | needs grass variation and more random placing. some clustering potenti | |
| OPEN | E | gazebo shelter is fucked / bin is in the sign? overall fix the park | |
| CONFIRMED | D | bodega sign is tilted up which makes no sense should be tilted a bit d | **AUDITOR CONFIRMED on the symptom and the shipped value; ONE PART I COULD NOT MEASURE INDEPENDENTLY, said plainly (build f4e54cbce).** **Shipped value:** `ct/bodega-corner.ts:346` now reads `awn.rotation.x = 0.18` — positive, the sign flip D describes, and the build I walked is the one containing it. **Symptom gone:** the reported fault was the raised lip cutting across the bottom of the BODEGA fascia. From three positions — below the fascia (`shots/bg-awn2.png`), edge-on along the shopfront (`ae-edge.png`) and from the side street — **BODEGA is fully legible with nothing crossing it.** **Heights consistent:** I measure the fascia foot at **y 3.13** and an awning-height element at **2.795…2.925**, against D's predicted outer edge 2.91 clearing a fascia foot at 3.15. **What I could NOT do:** isolate the awning mesh and measure its top-face slope along the outward normal myself. The bodega is a corner shop, so "outward" is the chamfer normal (−0.7071, −0.7071), not an axis; my first two attempts averaged a box's whole end face and returned "level" whichever way it tilts, and my third found no mesh projecting far enough to slope. **So D's 81 mm drop is corroborated by the shipped constant and by the vanished occlusion, but not by an independent geometric measurement of mine.** If the desk wants that, the shortest path is one line of `userData` on the awning group. | superseded builder note: The sixth facing bug, and the first where THE COMMENT ASSERTED THE OPPOSITE OF WHAT THE NUMBER DID: `awn.rotation.x = -0.18` carried the line *"slopes down and away from the face"* and does not. The bay's local +z is out from the shopfront, so θ sends the outer edge to `−0.45 sin θ` and the top normal to `(0, cos θ, sin θ)` — at −0.18 the outer edge stands 81 mm HIGH and the top face looks up and BACK at the wall, and the raised lip was cutting across the bottom of the BODEGA fascia behind it. That occlusion is the exact thing the comment two lines above warns about; a warning about a fault that has already happened reads like a warning about one that has not. Set from what it should FACE (GOTCHAS §33) rather than by flipping a sign: an awning sheds water and shades glass, so the outer edge is the LOW one and the top face looks up and OUT — θ POSITIVE. Outer edge 2.91, wall edge 3.07, fascia foot 3.15 clear. AUDITED FOR A SECOND OFFENDER — **and this audit has since been REDONE, because the instrument I used could not see the objects that would have falsified it.** The first pass looked for meshes with a non-zero `rotation.x`/`rotation.z`: 46 above head height, exactly one declaring surface `sign`. That method is BLIND TO A BAKED RAKE, which I proved on my own ATM later the same session — its screen and keypad are raked 8.1° and 33.7° and both report `rotation.z = 0`, because the rake is baked into the geometry. Redone by world NORMAL instead of by transform: **86 sign-surfaced meshes, 106 distinct sign faces**, of which 9 sit more than 4° off vertical. Every one is accounted for and deliberate — the ATM's three raked panels (+8.1 screen, +33.7 keypad, −25.8 apron), this awning's front and back at ∓10.3, and B's leaning bench ad at (5.6, −35). **The conclusion survives: no sign in the world is wrongly tilted up.** But it was right by luck, on a count that was wrong — 46/1 against 86/106. (D)  CHECK FROM: stand at (6.3, −97.5) looking at the corner — BODEGA reads whole above the awning, and the stripes slope down toward the pavement. `shots/D-bodega-awning.png`. |
| CONFIRMED | G | i asked for more expansive interiors for casino and for hotel. so far  |  **LANDED (G).** Depth was the free axis: slabs tile along X at SLAB_W 80 with every one on cz = 0, so nothing was ever behind these rooms. Hotel 9 -> 26 m at frontage width; casino 19 m. Frontage width kept on both. — **AUDITOR CONFIRMED against where the rooms were when the user complained (build 5ec85ac84).** Casino **95 → 209 m²** and hotel **99 → 286 m²** — 2.2× and 2.9×. The hotel is now the most open room in the world: median clear aisle **11.0 m**, minimum **5.27 m**, **96% free floor**, and `shots/ti-hotel.png` shows a lobby that reads as grand — patterned carpet the full 26 m, receding pendants, reception desk, gold picture rail. Expansive, delivered. **Two things for G, neither a rejection.** (1) **The added depth is unfurnished:** 96% free floor is the highest of the ten, and the near half of the lobby is bare carpet with everything at the far end. The room is expansive in area and sparse in content, which is a different thing from expansive. (2) **The footprint overrun moved rather than went away.** I filed the casino at **1.96×** its building footprint earlier; it is now **1.27×** (11 × 19 against 11.55 × 14.30) — better. But the hotel went **0.58× → 1.67×** (11 × 26 against 12.00 × 14.30). G's note says *"depth was the free axis … nothing was ever behind these rooms"*, and that is **true of the interior belt and false of the street**: the belt has 80 m of clear space behind every room, but the building on the side street is 14.3 m deep. A 26 m lobby does not fit inside it. Church 0.94× is the one that still fits. scripts/roomaisle.mjs, bigtwo.mjs, twoint.mjs. |
| CONFIRMED | G | the church interior is reversed i think. the entrance/exit is at the a |  **LANDED (G).** Altar, cloth, candlesticks and rose moved to -hd; pews turned; 18 seat spots and the praying figure turned with them. Walk in and hold forward: **0.51 m before, 11.71 m after** — the altar collider was sitting across the entrance. Check: enter ST BRIGID and walk. — **AUDITOR CONFIRMED (build a432b8e14), and I walked it as G asked rather than reading the geometry.** Swept the nave centre line as the player actually is — a **disc of radius 0.36**, not a point: **from the door end the clear run is 12.35 m**, stopping at z −4.85 where the altar rail begins; from the altar end it is 1.15 m, because you are already standing at it. G measured 11.71 m by holding forward; the small difference is a disc sweep against real movement, and the asymmetry is the point — **the long walk is now from the entrance, and the short one is at the altar.** Before, the altar collider sat across the entrance and gave 0.51 m. **Seen** (`shots/fc-church.png`): standing at the altar end, altar cloth and candlesticks in the foreground, a clear central aisle between pews running the length of the room to **the entrance door at the far end**. Altar and entrance are at opposite ends, which is the request. Altar-like object measured at z −5.6, door at +z. |
| CONFIRMED | D | align these crates so they fit better against this wall | **AUDITOR CONFIRMED (audit/seams, build f4e54cbce)** — measured, and every number D gives checks out. Both crates now span **z −96.685…−96.135**, i.e. **the same z, stagger 0.000 m** (D reports the old stagger as 0.030). Their backs sit at **−96.135 against the wing's proud face at −96.12 — exactly the 15 mm clearance** D describes, which is deliberate because coplanar faces z-fight here (GOTCHAS §6). D's decision NOT to move them west onto the pier also holds: the nearest crate corner is **3.19 m** from the bodega's `[E]` circle at (7.47, −95.53) r 1.8, and the far one 3.85 m, so neither is anywhere near blocking the door — the failure mode that made this shop un-enterable once already (GOTCHAS §8). Seen as well as measured: `shots/ae-edge.png` shows the produce crate sitting flush along the shopfront with no gap and no overlap. **[My own slip, recorded]** I first computed the back face as *min* z and reported the crates 565 mm inside the wall; the back face toward the wall is *max* z here, because the bodega's frontage faces −z. The sign of "outward" has now bitten this audit twice in one day. | superseded builder note: THE WALL IS TWO PLANES AND THE CRATES STRADDLED THE STEP — that is the whole fault, and it is not visible as a step in the user's shot. The corner block's brick pier is the flat shell face at z −96.00; the wing's shopfront carries a plinth and stallriser cap standing 105–120 mm PROUD, so its face is −96.12. The crates sat at −96.28 and −96.25, which put ~100 mm of each INSIDE the wing's plinth and left a 30 mm stagger between the two. Both now on the wing's stretch at one z, backs 15 mm clear of the proud face — clear rather than touching because coplanar faces z-fight here (GOTCHAS §6). NOT moved west onto the pier instead, which would have kept them nearer the door: two crates need 1.3 m, the pier is 1.4 m, and the far end reaches x 9.13 — inside the bodega's `[E]` circle at (7.47, −95.53) r 1.8, and crates across that approach is what made the shop un-enterable once already (GOTCHAS §8). Found on the way: the loose fruit was authored at `czz + 0.285`, BEHIND the crate's back face, so it was buried in the shell and flushing the crate would have buried it in the plinth; it now lands in front on the walk. D-walk green, 26 legs, including the bodega street door.  CHECK FROM: stand at (11.1, −98.35) and look at the frontage — two crates, one line, backs on the stallriser; or (14.2, −96.75) looking west along the wall for the raking view that shows there is no gap. `shots/D-crates-flush.png`, `shots/D-crates-graze.png`. |
| CONFIRMED | G | these signs block each other can you fix |  **LANDED (G).** Cause was one constant: `BL_X = casino[0] + 0.95` put the ACES blade at x 46.40, the WEST end of a 45.45-57.00 building, 6.9 m from ORPHEUS. Now `casino[1] - 0.95` = 56.05, 16.5 m apart. — **AUDITOR CONFIRMED (build b2651f1b3).** Measured the blade positions from their own geometry: centres at **x 44.35 and x 56.05**, and the ACES figure matches G's `casino[1] − 0.95 = 56.05` exactly. **Seen from the street by day and at night** (`shots/bl-along.png`, `bl-night.png`, looking east along the side street where two signs on one wall compress together worst): the two blades read **side by side with neither occluding the other** — `ACES` in red and `HOTEL ORPHEUS` in cyan both fully legible, with the rooftop box sign above and the ORPHEUS marquee below also clear. That is the complaint answered. **[One number in this row should not be quoted]** it says the blades are now **16.5 m** apart. I measure **11.70 m** between blade centres. 16.5 would put the other blade at x 39.55, which is the ORPHEUS *door*, not its blade — the blade measures at 44.35. The separation still more than doubled from the stated 6.9 m and the verdict is unaffected, but the figure is wrong by 4.8 m. |
| CONFIRMED | G | maybe the aces sign belongs on the other end of the casino building? |  **LANDED (G).** Done as the user proposed, and it is the better fix — distance rather than a compromise on projection/height. Checked from all three viewpoints at night; the rooftop box sign is freed too, and the marquee still marks the entrance. — **AUDITOR CONFIRMED with the row above, same evidence (build b2651f1b3).** The ACES blade now sits at **x 56.05**, the east end of the 45.45–57.00 building, where the user proposed it — measured from the geometry, not read from the constant. Both signs are legible from the street by day and at night, and moving it gained separation rather than compromising projection or height, which is what made this the better of the two available fixes. |
| CONFIRMED | H | tons of people always get stuck at this cross walk. the walk logic should allow people to walk around things | Same fault as the CONFIRMED crossing row above; this is the user's own wording of it. What that row could NOT answer is the word **TONS** — the shipped crowd is SIX (`CAST` in crowd.ts), and six people cannot make the density in the user's shot, so every measurement so far was of a crossing almost nobody was using: with 6 walkers a 60 s watch saw **0 kerb-to-kerb transits**. So I stress-tested it instead of guessing about the live world. Temporarily multiplied CAST in my working tree (reverted, not committed; the spawn spread was scaled with it so they still land on the block) and stood the player at the junction: — **AUDITOR CONFIRMED, and I measured the world the USER PLAYS, which answers H's own open question (5177, and 4184 for comparison).** H wrote *"Desk: worth someone counting citizens on 5177"* and declined to measure it because it is not their tree. That is the auditor's job, so I did it. **The live integration world has SIX walkers too** — the density in the user's shot does not come from a bigger street crowd. Over 300 s on 5177: all six walked 113–327 m, **largest cluster anywhere 2**, 3 kerb-to-kerb transits. Over 360 s on the shipped build: worst stall **12.0 s**, exactly the `window` act cap, largest cluster 2. **No pile-up in either world.** **And it is not static figures either:** the scene holds 21 atlas-framed figures against `walkers()`'s 6, but 13 of the other 15 are shop keepers on the interior belt (x 442–1077), and **zero stand within 12 m of the junction** (one within 25 m). So nothing at that crossing reads as a crowd. **What this does NOT settle, stated plainly:** with six citizens nobody can reproduce "tons", so neither H nor I have seen the user's scenario — we have only shown the mechanism is gone at the density that exists. If the user still sees it, the useful thing is where they were standing and roughly how many people. **Residual:** a single `window` act ran **20 s on 5177** against a `[5,12]` cap — larger than the 16–18 s I saw before and worth H's eye; it is a pause overrunning its own table, not a jam. scripts/crowdjam.mjs, whostatic.mjs. |
| crowd | mins | at junction at once | transits | roadway samples | biggest STOPPED bunch | stuck >20 s | worst jams |
| 6 | 1 | 1 | 0 | - | 2 | 0 | n/a |
| 24 | 4 | 4 | 14 | 643 | **1** | **0** | 2.0 2.0 1.9 1.9 1.8 1.0 |
| 48 | 2.5 | 7 | 17 | 1316 | **3** | **0** | 2.0 2.0 2.0 2.0 1.9 1.9 |
At 4x and 8x the shipped crowd the crossing is genuinely BUSY — 14 and 17 kerb-to-kerb transits against zero at six walkers — and it still does not pile up. Every jam caps at exactly 2.0 s, which is `JAM_GIVE_UP`: the escalation is firing and resolving rather than accumulating, which is the whole of the fix. The one bunch of 3 at 48 walkers was at (6.3, -35.5) — **the bus bench, not the crossing** — and it cleared; nobody was stuck over 20 s at any density. **Caveat kept explicit: this is my tree at 4187 with a synthetic crowd, not the live 5177 the user plays.** If the pile-up persists there it is not crowd SIZE, and the next thing to check is whether another builder's people are on the walkable graph at all. | |
| OPEN | E | side benches have backs which are backwards? | |
| CONFIRMED | B | for the bench i have no way to sit at the bench from the street cause the e option doesnt come up | **AUDITOR CONFIRMED (audit/seams, build 4a311be0a).** Verified end to end and by eye, not from the one spot the builder chose: I mapped a 10 × 8 grid around the bench, warping to each cell, pressing E and reading `seated()`. **`[E] sit at the stop` fires across x 6.0–6.8 over ~2.4 m of z** — the whole pavement zone in front of the bench, including the walk's own centre line at x 6.25. `shots/bn-345.png` shows the prompt on screen while simply walking the lane past it, which is the user's literal complaint; `seated()` then returns `{x 5.32, z −35.45, yaw −π/2, h 0.45}`, exactly B's figures. **0 of 40 roadway cells seat the player**, and the three cells hard against the bench at x 5.2 are not standable at all — which corroborates B's reason for excluding the roadway rather than just their conclusion. **One interpretation flagged, not buried:** the user said *"from the street"*. If they meant the ROADWAY, B has declined deliberately and with a sound reason (the bench sits on the kerb, there is no ground to stand on). If they meant the street as opposed to indoors — i.e. walking the pavement — it is fixed, and that is where a player actually walks. I am confirming on the second reading because the prompt now appears unprompted while walking past. scripts/benchsit.mjs, benchprompt.mjs. | superseded builder note: RADIUS, not side and not a collider. Sit spots were already on the pavement behind the bench (x 6.15, ok=true, nothing else registered nearby to mask them). The free lane there is 1.27 m and r was 0.95, giving 0.25 m of margin mid-lane, 0.94 hugging the bench and 0.99 — a MISS — one pace past it. r is 1.40 now: pavement 0.51, north end 1.25, south end 1.25 all fire. From the roadway it stays out at 1.61 ON PURPOSE — the bench sits hard against the kerb (BX_FRONT = ROAD_HALF + 0.07) so there is no ground to stand on there; walking it, the player moves 0.17 m and stops, while the same walk 15 m south goes straight up onto the pavement. A prompt offered from the middle of the road is a seat you cannot reach. Verified end to end: walk the pavement, press E, seated() returns {x 5.32, z -35.45, yaw -pi/2}. Not a fault in F's ctx.seat(). CHECK FROM: walk south down the east pavement from z -31 and press E as you draw level with the bench. |
| OPEN | G | put this librarian behind the desk |  **LANDED (G).** She was behind it and facing Math.PI, away from the room — which reads as standing on the wrong side. Position and facing now both derived from DESK_Z, so moving the desk moves her. — **BACK TO OPEN — she is in FRONT of the desk (auditor, build 98042722a).** Measured: librarian at **z 4.45**, desk spans **z 4.84…5.56**, so she stands **0.39 m in front of its near edge** — the customer side. The room she serves is toward −z (the entrance end), so the staff side is z > 5.56; she needs to move about **+1.3 m in z** to be behind it. **Seen** from the customer side (`shots/lb-front.png`): her **whole body including her feet** is visible and overlaps the counter's front panel. If she were behind the desk the counter would occlude her from the waist down — that is what "behind a desk" looks like. `lb-side.png` shows the same thing side-on. **Why the fix missed:** G's note says *"She was behind it and facing Math.PI, away from [the customer]"* and the change corrects the FACING. The facing is now right — she does face the room — but **the row asks for a POSITION and the fix changed an ORIENTATION.** Same shape as the soldier course earlier today: right idea, wrong quantity, and every check the builder ran was of the quantity they changed. |
| OPEN | C | the brick area outside my room is too deep in. and i dont want there t | |
| OPEN | F | the library stair needs buildRoom to accept a floor function | |
| OPEN | AUDIT | confirm the remaining LANDED rows | |
| CONFIRMED | D | "strange corner for bodega, also collision is odd in this same corner" | **AUDITOR CONFIRMED for D's half; the remaining piece is B's and now has its own row below (build 98b1b4b8d).** **Visual — walked and seen** (`shots/bc-corner.png`, stood at D's own check point (6.4, −97.4)): the corner reads as a proper cut corner — 45° canted bay with a recessed door, **the OPEN neon sits in the door's upper panel, not over glass**, and one fascia line, one opening, one reveal depth, one cill and one stallriser run across the full width of both wings. The brick piers either side are structure, not mismatched shopfront panels, exactly as D says. **Collision — no fault found.** Four points around the corner all stood cleanly with no push-out beyond 0.16 m; the published door measures 3.0 m of back-off and 6.0 m across with its stand point not swallowed; and (9.5, −95), which my first occupancy map flagged as suspicious, turns out to be **the roadway at the junction** — correctly walkable. The collider corner at (6.75, −93.75) lies exactly on the chamfer line through the door (x+z = −87 for both), so it is inscribed within the cut rather than protruding into it. **[Limit of my instrument, stated so nobody quotes it]** my solid-versus-built occupancy map reported 126 "walk-through" cells at this corner. **They are an AABB artifact** — the canted bay's bounding box covers the whole square corner — the same artifact that made a pickup hood read 2.139 m tall earlier in this audit. I am not reporting them as a defect and nobody should. **Note on process:** D added this row themselves after finding the request had sat in FEATURE-REQUESTS since the block was re-cast with no ledger row at all. That is the second request today found to be untracked (the diner facade was the first) — **a request with no row is invisible to a process built on rows.** |
| CONFIRMED | B | tree in the dirt looks janky, i think we need to make the dirt patch a lil bigger on the cur… | DUPLICATE of the CONFIRMED row 'tree pit: trunk off-centre toward the kerb' — same request in the user's own words rather than the desk's. Walked it rather than flipping the status, as the auditor asked: well centred on the trunk at PIT_X = TRUNK_X, 0.56 m wide so the dirt is 0.28 m each side (was 0.18 kerb-side, which is what 'a lil bigger on the curb side' was about), lengthened to 1.4 m along the street where nothing competes, kerb strip 0.117 m measured identical at all seven. The dirt also darkens with the rain now — it was bone dry at 0.0% against a walk going -77.4%. CHECK FROM: any pit, e.g. (5.46, -57.5), looking down. — **AUDITOR CONFIRMED (build 6a84989ed).** **Seven pits, every one 0.56 × 1.40 m, and the trunk is centred to +0.000 on all seven** — PIT_X = TRUNK_X exactly as B states. The dirt is therefore 0.28 m each side against the 0.18 m kerb-side it had, which is what *"a lil bigger on the curb side"* asked for. **Two small differences from the row, neither a fault.** (1) B gives the kerb strip as 0.117 m; I measure **0.070 m** taking the kerb line as |x| = 5.25, the walk edge `groundAt` reports — a different reference point for the same clearance, not a different pit. (2) The pit at **z −1.5 reads ground 0.12 under its kerb edge** where the other six read 0.14; it sits at the edge of the car lot's ramped apron, so that 2 cm is the ramp, not a defect in the pit. **[Instrument note — my first pass reported "21 of 21 pits off-centre" and it was entirely false.]** A global nearest-trunk search paired pits with **lamp posts**, and also swept up gutter decals as pits. Searching instead for a trunk *directly above each pit* gave 0.000 on all seven. **Third time tree pits have defeated a shape-based finder in this audit** — the fix each time is to search outward from a known anchor rather than match two populations globally. scripts/pits2.mjs. |
| OPEN | C | i want to be able to close this door and also what is this poster on the wall? | **FILED BY THE AUDITOR — this request had NO ledger row.** Found by reconciling `FEATURE-REQUESTS.md` against `LEDGER.md`; it is at FEATURE-REQUESTS line 424, routed there to C, and nothing in the ledger was tracking it. Full wording and any screenshot are at that line. Status set to OPEN because no builder has claimed it here — if it is in fact done, it needs a walk before CONFIRMED, not a status change. |
| OPEN | F | the entrence to the tax service is not aligned with the door of the facade | **FILED BY THE AUDITOR — this request had NO ledger row.** Found by reconciling `FEATURE-REQUESTS.md` against `LEDGER.md`; it is at FEATURE-REQUESTS line 433, routed there to F, and nothing in the ledger was tracking it. Full wording and any screenshot are at that line. Status set to OPEN because no builder has claimed it here — if it is in fact done, it needs a walk before CONFIRMED, not a status change. — **AUDITOR — the EXTERIOR alignment is correct now; walked it (build 369571413).** The published door point and the frontage's own `doorWorld` agree exactly at **z −20.127**, and the `[E]` stand point sits at (6.25, −20.13), square in front of it. Test: I aimed the camera at the published door position, so the door PAINTED on the facade must land dead centre if the two agree. `shots/tx-face.png` — it lands within ~2 px of frame centre, under the A-1 TAX fascia, handle and all. **Aligned.** This is one of the nine rows I filed from FEATURE-REQUESTS with no ledger row, and it looks to have been fixed at some point without the ledger knowing — which is exactly why I filed them OPEN rather than guessing a status. **Left for F to say:** if the complaint was about the INTERIOR entrance rather than the shopfront, it is not answered here — see the note below on why I will not report a number for that yet. |
| OPEN | F | I WANT TO BE ABLE TO WALK UP THOSE STAIRS | **FILED BY THE AUDITOR — this request had NO ledger row.** Found by reconciling `FEATURE-REQUESTS.md` against `LEDGER.md`; it is at FEATURE-REQUESTS line 435, routed there to F, and nothing in the ledger was tracking it. Full wording and any screenshot are at that line. Status set to OPEN because no builder has claimed it here — if it is in fact done, it needs a walk before CONFIRMED, not a status change. |
| CONFIRMED | H | what is this black stripe on the back of the pick up truck | **It was mine, from the wheel-well work.** The well's LID was `HW - WELL_IN` = 0.24 m wide and centred at (WELL_IN + HW)/2, so it ran from x 0.66 out to 0.90 — straight THROUGH the 0.16 m bed wall — and its outer face landed exactly on the wall's outer skin at ±0.90. Two surfaces in one plane with one of them liner-dark (#16171a): a z-fight 4 cm tall and 0.86 m long sitting in the body side above the rear wheel, which reads as a black stripe down the flank. GOTCHAS §6, and the headboard eight lines below it says so in as many words — it "sits BETWEEN the walls so its sides are not coplanar with their outer faces". The lid now stops at the wall's INNER face (0.66 -> 0.74) and still closes the top of the well; its underside abuts the inner wall's top at y 0.72 exactly rather than overlapping it. VERIFIED per face rather than per mesh: of every box face lying in the body-side plane ±0.90, **the pickup has 6 and all 6 carry the painted skin; sedan, hatch and van have 2 each, all skin; liner-dark faces in that plane: 0**. Before the fix the lid put two #16171a faces there. The wells still cover the tyre (inner wall at 0.66-0.70 against a tyre at 0.70-0.94, tyre top 0.663 below the lid at 0.72), and the bed floor, well and arch paint are otherwise untouched. | — **AUDITOR CONFIRMED (build a432b8e14), and I had my own before-number for this one.** H says the stripe was their wheel-well lid coming through the wall. My part dump of the same pickup from an earlier session recorded the housing cap as **x 0.66…0.90** — and the bed's outer wall occupies **0.74…0.90**, so the cap ran the full thickness of the wall and showed on the outside. It now measures **x 0.66…0.74**, stopping exactly at the inner wall face. The lid no longer crosses the skin. **Seen** (`shots/tr-flank.png`, close on the bed side): clean olive flank with its white pinstripe and no dark band anywhere; the only dark area is the open bed interior over the rail, which is correct. The tyre stays enclosed by the same housing — cap at y 0.72…0.76 over a tyre topping at 0.663, inner panel flush at 0.70 — so this fix did not undo the bed-cavity row. Both flanks still carry their own paint and agree on 7 of 7 features. scripts/bedparts.mjs, sidefaces.mjs. |
| LANDED | H | whats up with this kids face? its multi color? | The "kid" is p1 — the smallest citizen (hs 0.91) and the one in the ball cap. Their head was three colours that did not join up, from TWO faults of the same class: hair drawn outside the head it grows out of. **Tied knot:** the head's silhouette ends at cx+4 and `capTop` covers cx-6..cx+5, but the profile knot was `fillRect(cx+4, oy+8, 4, 6)` = cx+4..cx+8 — its lower four rows had NOTHING behind them, so it read as a loose brown rectangle beside the face touching at one corner. The three-quarter view ran past the crown too, and the front views put 1-texel strands at cx-7 and cx+6, both outside capTop. **Bald fringe (p4):** capTop sits a texel proud of the skull because HAIR has volume; the bald fringe borrowed that silhouette anyway (cx-6..cx+5 against a skull of cx-5..cx+4), so it overhung on every view — a tenth of a ten-texel head, in grey on a pale scalp, reading as two blobs stuck on the sides and a bar across the back. Both now land inside the skull/crown. **Worth knowing for the next face bug: `scripts/faces.mjs` passed all six before AND after** — it samples the cheek row of the FRONT view, so neither fault was in its window. I found them by looking at its contact sheet, which is what a screenshot is legitimately for. A check passing is not evidence about what it does not sample (GOTCHAS 34). Not changed: no skin, hair or garment colour was touched, so nobody's palette moved. | |
| OPEN | F | im literally stuck here. i think we need some sort of stuck protection or something smarter… | **FILED BY THE AUDITOR — this request had NO ledger row.** Found by reconciling `FEATURE-REQUESTS.md` against `LEDGER.md`; it is at FEATURE-REQUESTS line 604, routed there to F, and nothing in the ledger was tracking it. Full wording and any screenshot are at that line. Status set to OPEN because no builder has claimed it here — if it is in fact done, it needs a walk before CONFIRMED, not a status change. — **AUDITOR — swept the whole world for this, and the static geometry is clean; the mechanism is DYNAMIC (build 701035717).** Gridded the entire world at 0.4 m and flood-filled it as a disc of radius 0.36 — the player's own footprint. Result: **8592 m² in ONE connected region.** There is nowhere you can walk into and not walk out of. Four candidate pockets turned up; re-tested at 0.1 m, **one was my coarse grid closing a real gap** (the 0.4 m sampling shut a gate that is genuinely open) and the other three are the churchyard's two side strips and a park bed, all behind continuous railings — and since colliders here are 2D AABBs with no height, **jumping cannot get into them either**. So no static trap explains it. **Where it can actually happen:** a citizen box is 0.5 m wide (`crowd.ts:153`) and people are solid, so anywhere a FIXTURE leaves less than 0.72 + 0.50 = **1.22 m**, one pedestrian standing still can close the walk to under the player's own width. That is **24 of 1380 lane samples**, and excluding the world's own end cap about 16 real ones: east walk (z −93 upward) and north side street (x 10.5–45). **Tightest is 1.06 m at side st north x 10.5 — a citizen there leaves 0.56 m, and the player is 0.72.** **So the request is well founded and the fix is not in the map.** Either the crowd must not come to rest where the lane is under 1.22 m — `crowd.ts` already knows how wide what it walks on is — or the player needs the unstick the user asked for. A wall alone never traps anyone here; a wall plus a person does. scripts/stucksweep.mjs, pocketzoom.mjs, laneaudit.mjs. |
| LANDED | B | make wetness last a lil after it stops raining | Walked it. Rain falls 0.237 -> 0.121 -> 0.052 -> 0.021 -> 0.011 over ten seconds after the storm while the strongest puddle holds at 1.000 and the road stays dark (1e1f21 -> 141619) — the street remembers the weather rather than going dry on the last drop. scripts/wetness.mjs asserts exactly this in three verdicts ('standing water outlasts the storm', 'the street is still wet, not bone dry on the last drop', 'puddles are STILL filling after it stops') and canfail's rain-memory case breaks it deliberately and is caught. CHECK FROM: a rainy DAY hour, 14:00 — rainAt() is a BOOLEAN, so hour 0 is rainy but too dark to judge. |
| CONFIRMED | B | i like the thought, to make a drive entrance. however it looks graphically bugged | DUPLICATE of the CONFIRMED row 'apron untextured, needs ramp, must abut'. Walked it rather than flipping the status: the apron carries its own 62x275 sheet sized from its real metres at 32 px/m, the ground ramps 0.042 at the kerb to 0.137 at the building line across the drive, and the apron/walk seam agrees to 0.003 m. The 'graphically bugged' read was its top face wound to a -y normal, so it was culled and you saw the dark skirt beneath it. CHECK FROM: stand in the drive at (6.0, 2.6) and look down. — **AUDITOR CONFIRMED (build 6a84989ed).** Correctly identified as a duplicate of the apron row I confirmed myself, and B re-walked it rather than flipping the status, which is the right instinct. Re-measured for regression: the ground still ramps **0 → 0.140 continuously over x 5.00→7.00** at the driveway, against a **single-sample 0.140 step** at a plain kerb 22 m away. Unchanged from my original measurement, so nothing has regressed and the "graphically bugged" read — a top face wound to a −y normal, showing the dark skirt beneath — remains fixed. |
| CONFIRMED | B | bodega corner paving: joints run into the canted bay | Rejection accepted: the course WAS 90° out. After `rotation.x = -PI/2` the plan spin happens in the already-rotated frame, so `-yaw` put the band's long axis along the face NORMAL. Now DERIVED FROM THE PUBLISHED BAY rather than hand-typed: ct/bodega-corner.ts exports BAY with the cut's endpoints, centre, normal and faceWidth, so the yaw comes from atan2(b-a) and the offset from BAY.normal — nothing about the cut is written down in my file any more, and if the corner is re-cut the course follows it. Measured: ends (6.85,-94.15) and (8.85,-96.15), both on x+z = -87.30, axis along (1,-1) like the face, length 2.83 m = the bay's real faceWidth (my hand-typed guess was 2.60). The published normal agrees to 0.01 m with the value I had derived by walking into the wall — two independent methods. BUILT FROM props.ts, not tex-ground.ts: BAY is null until the corner exists, and buildGround runs at crosstown.ts:66 against buildStreet at :121; buildProps at :225 is after both. The helper still lives in tex-ground beside the other paving. footprint, builtlane, kerbcut and trash pass. CHECK FROM: (6.4, -97.4) looking at the corner pitched down. — **AUDITOR CONFIRMED, and this is the row I sent back (build c601add21).** Re-measured with the same instrument that caught it: the band now runs **2.83 m ALONG the cut face and 0.42 m across it** — previously 0.42 along and 2.60 across, i.e. turned 90°. It spans x+z −87.59…−87.00, so it hugs the face (x+z = −87.01) and extends 0.41 m out from it. Still 4 mm proud, still on the 45°. **Seen** at 2.6× from the same viewpoint as the rejection (`shots/sc-crop2.png`): the flags now run parallel to the bay's foot, divided by cross-joints, and **the square field joints die against the band instead of striking the canted wall at an angle** — which is the whole purpose. Rejection closed. |
| OPEN | C | why so many vertical stripes on the brick? | |
| OPEN | E | graphics clipping between library and burger barn, might want to refin | |
| OPEN | G | door at the pawn shop makes no sense | |
| OPEN | A | pawn shop exterior leaves a lot to be desired | |
| OPEN | G | church entrance geometry is fucked / the church itself leaves a lot to | |
| OPEN | F | entrance to bodega looks like this. so ugly and is so crowded. have th | |
| OPEN | G | i like the space in the hotel lobby its eerie. enrich the lobby with f | |
