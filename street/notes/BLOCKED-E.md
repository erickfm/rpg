# BLOCKED — builder E · nothing assigned

Not blocked on a dependency. **Blocked on having no item to take**, which is
the one kind of blocker the queue cannot express, so it goes here — the README
says to write this file rather than rely on the desk reading a handoff, and
`scripts/desk.sh` surfaces it as an ACTION.

## State, measured not claimed

- `notes/queues/E-civic.md` last changed in `daa4c6c7`, which is the commit
  that ADDED my current top item. That item is done.
- All 11 `## Now` items: done, and verified by walking rather than looking.
- `## Next` is the GOLDEN ACES sign, which `OWNERSHIP.md` gives to **G**
  (`ct/vice.ts`).
- `node scripts/E-verify.mjs` — three areas walk, no unread exports.
- `npm run wiring` — 24 build exports, all constructed.
- `feat/civic` is 0 commits ahead of mainline. Everything is landed.

## Why I stopped instead of finding more

The last several rounds produced real work — the `alphaTest`/`transparent`
night-grade fix, retiring five spent patch files and three handoffs that had
started lying, `ct/park.ts` missing from the ownership registry, `E-verify`,
and the published-but-unread check that `npm run wiring` does not cover.

But all of it is tooling and hygiene around finished work. **The block itself
has not improved since the park's far half was furnished.** Another lap would
be me hunting for something to justify a commit in files three other builders
depend on, and churn there is exactly what the ownership rules exist to
prevent.

## Three things I will take the moment one is routed

1. **GOLDEN ACES roof sign** — needs a mandate, it is G's file. Diagnosis is
   done and free to whoever takes it: the board is **9.2 m wide in z on a
   building 3.4 m deep**, so its legs at z −91.8 and −98.2 land outside the
   roof they are supposed to stand on. It cannot be fixed by moving the legs.
   The board narrows to the roof, or it gains a deck to stand on.
2. **Library and church interiors** — the one part of my two buildings that
   does not exist. Both now have doors you can climb steps to and cannot open.
   `ct/interior.ts` already lists **E** among its consumers, so the kit
   expects them; nothing else does. This is real scope and wants routing
   rather than self-assignment.
3. **Anything from a fresh playtest** that lands in civic or the park. That is
   the best kind of item and I will take it immediately.

_Builder E, 2026-07-25. Delete when an item is routed._
