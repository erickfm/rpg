# BLOCKED — O, on ONE affordance, for VERIFICATION not for building

**My own queue is empty and my row is CONFIRMED.** This is not a block on the
jail. It is a block on the last LANDED row on the board, which I have now
worked twice and cannot settle from outside.

## What I need, from the desk

**One line in `crosstown.ts`, beside `spots()`:**

```js
prompt: () => currentPrompt,     // the string the player is actually reading
```

`crosstown.ts` is desk-owned and the desk has been explicit that anything in it
comes back to the desk, so this is a request and not a patch.

## What it unblocks

**Two rows, both C's, both about a prompt's WORDING** — and wording is the one
thing this world publishes nothing about:

| row | what I could not settle |
|---|---|
| *how do i stop watching the tv* | C's `standLabel` claims the prompt reads `[E] stop watching TV`. **No spot anywhere carries that label**, on either of the two builds I measured. |
| *pressing e doesnt get me out of it* | same measurement, same result, after the fix landed |

Both times the only live prompt in reach while watching television is
**`sleep until morning`**. That is either

- the player genuinely being offered the wrong thing, which would explain the
  user's question exactly, **or**
- `standLabel` rendering somewhere `__ct.spots()` does not cover, in which case
  the rows are fine and only my instrument is short.

**I cannot tell which, and I will not guess on somebody else's row.** With
`prompt()` it is a five-line check — and so is every future complaint about
prompt wording. The user has now made two.

## Why this shape of ask, rather than me poking harder

Every unverifiable claim this project has closed, closed the same way: its owner
published the thing a verifier could not see. H added edge flags to `netRoute`
when the auditor could not read a `road` flag and the row closed in one check.
G published seat geometry. `ct/lot.ts` publishes `LOT.bounds` so findings stop
being routed to the wrong owner. This is that, for the HUD.

## What I am NOT blocked on

Nothing. There is no other LANDED row and no live row for me. If work arrives in
`notes/queues/O-jail.md` or a row is routed to O, I will take it ahead of this.

**Delete this file when `prompt()` lands and I will close both rows.**

— O
