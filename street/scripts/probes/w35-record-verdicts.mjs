// w35 — ITEM 66: write the sample's verdicts back into notes/LEDGER.md.
//
// Appends one sentence to each sampled row's EVIDENCE cell and, for the single
// demotion, rewrites the STATUS cell. Row-structural edits only: the number of
// rows, their order, and every existing character of evidence are left alone —
// `scripts/ledger-intact.mjs` is run afterwards and enforces exactly that.
import { readFileSync, writeFileSync } from 'node:fs';

const PATH = 'notes/LEDGER.md';
const lines = readFileSync(PATH, 'utf8').split('\n');

// line number -> [newStatus|null, sentence to append]
const V = {
  80:  [null, '**RE-VERIFIED w35 2026-08-02 (item 66 random sample), fresh on my own build at :4191: HOLDS.** `scripts/footprint.mjs` reports **no standing puddles (0)**, and the guard is not asleep — canfail `footprint-water` re-adds one and is CAUGHT. — w35'],
  82:  [null, '**RE-VERIFIED w35 2026-08-02 (item 66 random sample): HOLDS, on the invariant rather than on the counts.** Across **2,719 distinct materials: 81 carry `selfLit`, 35 carry `printed`, and 0 carry BOTH** — no sheet claims to be a light, which is the whole point of the flag. The cell records 2,201/75/35/0; the first three have drifted upward as the world grew and only the **0** is load-bearing. — w35'],
  106: [null, '**RE-VERIFIED w35 2026-08-02 (item 66 random sample): HOLDS, with ONE STALE NUMBER IN THIS CELL.** All four named properties measured fresh: INLAID — the niche is built from five pieces with the back face set behind the flanks; SLANTED — **three distinct rakes, screen 8.1 deg and keypad 33.7 deg exactly as recorded, but the apron is -25.8 deg where this cell says -21.3**; LOWER — `D-rulings-hold` PASSES *the ATM fascia bottom is at 0.75 above the pavement (0.750)* and *the screen top has NOT moved with it (1.580)*; MORE DETAIL — 9 parts at the niche. canfail `rulings-atm` CAUGHT. **The apron figure is the only thing wrong here and it is the evidence, not the world** — the object is still slanted on three planes. Also worth the desk knowing: there are now **TWO** ATM installations (z 7.288 and z 8.238), where this cell describes one. — w35'],
  121: [null, '**RE-VERIFIED w35 2026-08-02 (item 66 random sample): HOLDS.** This row names its own predicate and I ran it: `scripts/facade-run.mjs` judges **56 facades — 0 with the run off centre, 0 with it off the wall.** canfail `facade-run` pushes every run off centre and is CAUGHT, so the green is a measurement and not a silence. — w35'],
  122: [null, '**RE-VERIFIED w35 2026-08-02 (item 66 random sample): HOLDS.** `scripts/check-seethrough.mjs` on my build: **16 shopfronts plus the bodega bay checked, 1,569 ground surfaces tinted, no pavement visible through any of them.** — w35'],
  125: [null, '**RE-VERIFIED w35 2026-08-02 (item 66 random sample): HOLDS.** `scripts/B-room-density.mjs`, one predicate across all 12 rooms: the thrift resolves to 11.3 x 9.4 m at x 1320 and **ranks 11th of 12 from the thin end at 1.09 props/m2** — "thinnest room in the world" would be rank 1. — w35'],
  163: [null, '**RE-VERIFIED w35 2026-08-02 (item 66 random sample): HOLDS.** The park site is published (x -39..-7, z -98..-68) and **the shelter is present** — a 4.2 x 4.2 m roof standing at y 2.47 at (-35.9, -83), which is the restored gazebo this cell\'s own correction describes rather than the deleted one its first paragraph does. — w35'],
  167: [null, '**RE-VERIFIED w35 2026-08-02 (item 66 random sample): HOLDS, to the millimetre.** Both crates span **z -96.685...-96.135 — the same z, stagger 0.000 m** — with their backs at -96.135 against the wing\'s proud face at -96.12, i.e. the 15 mm clearance this cell describes. Clear of the bodega `[E]` at (7.470, -95.530) r 1.8 by **3.03 m** to the nearest box corner (this cell says 3.19 m to the nearest CORNER; the difference is corner-vs-nearest-point, and nothing turns on it). `D-rulings-hold` PASSES both crate clauses. — w35'],
  187: [null, '**RE-VERIFIED w35 2026-08-02 (item 66 random sample): HOLDS — and the trap this row is famous for caught me on the first pass, which is worth recording.** I projected the cat and the NEAREST floor litter through the live camera from the user\'s own station (-8.5, -39.5) yaw -0.785 and got *"the cat is LEFT of it"* — a clean demotion, and wrong. **The nearest litter is not the paper.** `ct/props.ts:3142-3144` drops three pieces in this alley: flattened cardboard at (-10.60,-41.45) and (-9.40,-42.40), and the folded NEWSPAPER at **(-12.60,-42.05)**. Both pieces near the cat are cardboard. Projected properly: **paper at screen-x 377.5, cat at 546.3, cardboard at 622** — paper left, cat middle, cardboard right, which is exactly the ordering `shots/D-catsix-after.png` records. **2.617 m clear of the paper, 0.67 m clear of the nearest cardboard.** `D-rulings-hold` PASSES *"there IS paper to the cat\'s left in the user\'s frame — it is on the right of it"*, and canfail `rulings-cat` stands the cat back on the paper and is CAUGHT. **The row\'s own lesson — an offset is only right in the frame it was computed for — has a sibling: an object is only "the paper" if you checked which drop it came from.** — w35'],
  195: [null, '**RE-VERIFIED w35 2026-08-02 (item 66 random sample): HOLDS — and the auditor\'s stated limit is now closed.** The auditor could not prove the citizen it photographed was **p1**, because all six walker meshes share a 1.9 m geometry height. **They do not share `hs`:** `__ct.people()` publishes the per-person scale, the six read **[1.09, 0.91, 0.97, 1.05, 0.94, 1.02]**, and the row\'s "kid" is unambiguously **index 1 at hs 0.91**. On the contact sheet `shots/faces.png`, **p1\'s cap, hair and face join as one shape on all five painted views** — no loose brown rectangle touching at a corner — and **p4\'s bald fringe sits inside the skull** on every view, which was the second half of the fault. Source agrees: the old `fillRect(cx+4, oy+8, 4, 6)` is gone and the bald fringe now runs `cx-5..cx+4`, the skull\'s own extents. canfail `faces-bands` CAUGHT. **`faces.mjs` passing is still not evidence here** — it samples the cheek row of the FRONT view, exactly as this cell warns. — w35'],
  197: [null, '**RE-VERIFIED w35 2026-08-02 (item 66 random sample): HOLDS.** Ran both instruments this row names. `scripts/rain.mjs`: **wet road 0.2596 raining -> 0.5822 at +1.2 s -> 0.7332 at +13 s**, against the 0.2508/0.5540/0.7356 recorded here — the same curve. `scripts/wetness.mjs`: three verdicts green, wetness shedding 0.9677 -> 0.8563 over 420 frames with the gutter and the crown drying at different rates. canfail `wetness` and `rain-memory` both CAUGHT. **THE STALE LABEL THIS CELL FLAGGED IS STILL THERE:** `wetness.mjs` still prints *"puddles 2/2 showing"* during the storm, for objects the desk removed on 2026-07-25 and which `footprint.mjs` independently counts as **0**. The verdicts do not rest on it, but two instruments on one build disagree about whether a deleted feature exists. Not my item\'s file; flagged for the desk to queue. — w35'],
  198: [null, '**RE-VERIFIED w35 2026-08-02 (item 66 random sample): HOLDS, and the contrast is the proof.** `groundAt` sampled every 0.1 m across the drive at z 2.6: **0 at x 5.0 rising continuously to 0.140 at x 7.0 — 21 distinct heights.** The same sweep at a plain kerb 22 m away (z -20) gives **2 distinct heights**, a single step. So the apron ramps and a kerb does not, which is precisely what this cell claims. — w35'],
  204: [null, '**RE-VERIFIED w35 2026-08-02 (item 66 random sample): HOLDS, at A\'s re-pointed station.** The church resolves to 13 x 24 m centred x 760 — A\'s correction is right and x 680 is the casino. Inside it there is **exactly ONE object of crucifix proportions: 0.23 x 2.61 x 0.09 at (760, -11.84)**, the sanctuary end, matching this cell\'s figures to the centimetre. The only other tall narrow solids in the room are 24 window mullions repeating at x 753.5 and 766.5 up at y 5.13-7.45, i.e. the walls. **Not two superimposed crucifixes; one.** — w35'],
  215: [null, '**RE-VERIFIED w35 2026-08-02 (item 66 random sample): HOLDS, walked not looked at.** `scripts/D-pawnalley-walk.mjs`, the row\'s own instrument, is **5/5 PASS**: in from the street to x 22.59, along, the closed end stops you, turn round and back out to x 6.67, and 1.69 m of side-to-side room inside. — w35'],
  218: [null, '**RE-VERIFIED w35 2026-08-02 (item 66 random sample): HOLDS.** Took each seat board\'s local +y through its world matrix and asked how far it leans off vertical: **25 seat slats, worst tilt 0.000 deg** — dead level, not small tilts passing a loose threshold. The shelter is present as a 4.2 x 4.2 m roof at y 2.47 at (-35.9, -83). (My 25 slats against C\'s 24 is a filter difference, not a finding; C\'s "8 benches vs E\'s 9" discrepancy is still unresolved and still turns on nothing.) — w35'],
  226: [null, '**RE-VERIFIED w35 2026-08-02 (item 66 random sample): HOLDS.** Counted meshes whose centre falls inside each slot: **dumpster alley 42, pawn alley 120**, against H\'s 50 and 123 from its own boxes — the gap is where the box edges sit, not a loss of content. Both still read as places rather than gaps. — w35'],
  245: [null, '**RE-VERIFIED w35 2026-08-02 (item 66 random sample): HOLDS, structurally — which is the reading that survives, because z-fighting is view-dependent.** Of the 14 reveal solids at the 301 window wall, **0 pairs share a volume at all and 0 pairs present coplanar tops.** A z-fight needs faces that are both overlapping AND coplanar AND same-facing; with no overlapping pair there is nothing to fight, at any angle. A recorded a residual overlap on its own crude spatial filter and said so; on a filter scoped to the reveal bars there is none. — w35'],
  260: ['LANDED', '**DEMOTED TO LANDED BY w35, 2026-08-02 (item 66 random sample) — NOT because the world is wrong, but because THIS ROW STILL CANNOT BE VERIFIED FROM OUTSIDE, and the last verifier said so in as many words.** The auditor\'s own cell above reads *"CANNOT VERIFY with the instruments available"*, names the fix — publish the computed `sizeW` on the slot mesh\'s `userData` — and flags that `scripts/wallpool.mjs` **reimplements** the smoothstep instead of reading the world\'s value. **I checked whether any of that had changed. None of it has.** Traversed the live scene: **`userData.sizeW` is carried by 0 of 8,324 objects**, the string does not appear as a userData key anywhere, and no `__ct` affordance exposes the pooling registry — `props.ts:786` computes `sizeW = tw*tw*(3-2*tw)` and `props.ts:802` stores it on a registry internal to the module. Meanwhile `wallpool.mjs:78-80` carries its own hand-typed copy of that formula, so **it agrees with itself**: if `props.ts` changed the rule tomorrow, `wallpool` would keep printing the old answer and look right doing it. My run of it is green and that green means nothing about the world. **A row confirmed on an instrument that cannot distinguish the world from its own copy of the rule is not confirmed.** The world may well be correct — I am not claiming a defect, I am refusing to carry CONFIRMED on unfalsifiable evidence (BUILDER-BRIEF 7). **To re-confirm it, one line: publish `sizeW` on the slot mesh\'s userData the way `printed` and `payphone` already are, then the taper is a query instead of an inference.** Note also that this cell\'s SHA `5b1b8e0d4` is recorded UNRECOVERABLE, so the original measurement cannot be re-reached either. — w35'],
  274: [null, '**RE-VERIFIED w35 2026-08-02 (item 66 random sample): HOLDS.** `scripts/hermit.mjs`, the row\'s own instrument, is **12/12 ok** — shut and empty on load, the door reaches open BEFORE he steps out, he walks the 0.57 m in 0.93 s rather than teleporting, walks back, the door closes only after he is through, **"HE IS NEVER SEEN TO DISAPPEAR: visible->invisible happens only behind a shut door, every time"**, he waits rather than walking through you, and he is still rare at 4% of sampled hours. — w35'],
  320: [null, '**RE-VERIFIED w35 2026-08-02 (item 66 random sample), independently of w30: HOLDS.** Library resolves to 20 x 22 m at x 1080 with **27 colliders inside it and 0 pairs forming a corridor under PASSABLE** — asked of `__ct.corridor` and `__ct.gapRule` (ENTERABLE 0.40, PASSABLE 0.95), the world\'s own predicate rather than a retyped threshold, with moving actors excluded via `actorColliders()`. — w35'],
};

let touched = 0;
for (const [ln, [status, sentence]] of Object.entries(V)) {
  const i = Number(ln) - 1;
  const line = lines[i];
  if (!line || !line.startsWith('| ')) throw new Error(`line ${ln} is not a ledger row`);
  const cells = line.split('|');
  // cells[0]='' , cells[1]=STATUS, cells[2]=owner, cells[3]=request, cells[4..n-1]=evidence, last=''
  if (status) cells[1] = ` ${status} `;
  // APPEND TO THE LAST NON-EMPTY CELL, not to cells[length-2].
  // Most rows end with a trailing ` |`, so the final split element is ''; row
  // 245 does not, and assuming it did put my sentence into an INTERIOR EMPTY
  // CELL — which collapsed a literal `||`, the very sequence
  // `ledger-intact.mjs` counts as a contribution boundary. It reported
  // "2 -> 1 accounts" and it was right: I had silently eaten a segment marker
  // while the row got LONGER, which is the one damage a length check cannot see.
  let t = cells.length - 1;
  while (t > 3 && cells[t].trim() === '') t--;
  cells[t] = `${cells[t].replace(/\s+$/, '')} ${sentence} `;
  lines[i] = cells.join('|');
  touched++;
}
writeFileSync(PATH, lines.join('\n'));
console.log(`recorded verdicts on ${touched} rows`);
